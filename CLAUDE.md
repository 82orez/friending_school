# CLAUDE.md

Claude Code 작업 지침. 이 파일은 매 세션 로드되므로 **항상 압축적으로** 작성/갱신할 것(토큰 절감).

## 개요

**"청년을 세계로"** 워홀·해외진출 영어 통합 플랫폼(브랜드 "프렌딩 스쿨"). 구성: 5섹션 랜딩 + **과정 상세페이지 5종**(`/courses/[slug]`: workhol·kitchen·grammar1·grammar2·cosmetic — 회화 기초문법 1/2는 각각 독립 과정·독립 수강신청, 교재 키 basic1/basic2) + **전자책 뷰어 `/textbook/[course]`**(레지스트리 기반 교재 5종, 무료 미리보기 외 로그인) + **수강신청→마이페이지**(`/courses/[slug]/enroll` 강사 매칭형 수강신청 → `/mypage`) + **강사 지원**(`/teacher/apply`) + **강사 페이지**(`/teacher`: 프로필·사진·zoom·주간 가능시간·수강신청 승인/거절) + **admin 대시보드**(`/admin`: 수강신청·화상수업·회원·강사 관리·센터·유튜브). Next.js 16(App Router, Turbopack) + React 19.2 + Tailwind v4. shadcn/ui(`base-nova`/`neutral`), Supabase SSR 인증(이메일 + 카카오 OAuth), 알림 메일(Resend) + SMS(Solapi).

## 명령어

- `npm run dev` / `npm run build` / `npm start` (모두 Turbopack)
- shadcn 추가: `npx shadcn@latest add <component>` (v4.7.0, `components.json` 따름)
- DB: `db:new <name>`(마이그레이션 파일 생성) · `db:push`(원격 적용, **destructive**) · `db:list`(적용 이력 비교) · `db:diff`(`--linked`) · `db:types`(→ `src/types/database.types.ts`, 첫 실행 전 `mkdir -p src/types`)
- lint/test 스크립트 없음(테스트 프레임워크 미설치). Supabase CLI는 `devDependencies`에 있어 `npx supabase ...` 사용.

## 아키텍처

**랜딩 페이지** `src/app/page.tsx`(async server). **5섹션**: ①히어로(`hero-bg.jpg` 배경 + `text-brand-gradient` "세계로") ②셀프디벨롭(`<SelfDevelop/>` client, props `isLoggedIn`+`completedByCourse` — 교재 탭 5종/진행바/더보기 아코디언. **`LINKED_COURSE`(book.key→레지스트리 course)에 등록된 교재만 전자책 연동**: 현재 **5종 전부 연동**(workhol·kitchen·grammar1·grammar2(키 basic1/basic2)·cosmetic) — mock placeholder(`UnitCard`/비연동 분기)는 현재 미사용 dead. 연동 카드=`LinkedUnitCard`(유닛 `/textbook/<course>/<n>` 링크, 잠금=`!isLoggedIn && !freeUnits`(자물쇠→`/login?next=`), 로그인 시 우상단 ○/✓ **수동 완료 토글** `setUnitCompleted` 낙관적, 완료 카드 `bg-progress/5`). **부제/소개**: `renderUnit`이 레지스트리 `getTextbook(course)` 유닛에서 `subtitle=u.sub??titleKr`·`situation`(부제와 **다를 때만** 주입=사실상 workhol·kitchen·cosmetic)을 전달 — `situation` 있으면 카드 **좌측정렬 + 소개문단**(`line-clamp-3`), 없으면 가운데정렬(grammar). 완료 상태는 `BookPanel` 로컬 state로 상단 진행바와 공유. **연동 확장 시 2곳 동기**: `SelfDevelop`의 `LINKED_COURSE` + `page.tsx`의 진행률 쿼리 `reading_progress.in("course",[...])`) ③유튜브 생존기(`youtube_videos` 노출분 SSR 조회→비면 mock `VIDEOS` fallback, `getYoutubeId`로 썸네일) ④실전 스피킹(과정카드 5종 → `/courses/<slug>`, 앵커 `#courses`) ⑤액티비티. **데이터는 `src/data/landing.ts`**(`BOOKS`·`VIDEOS`·`COURSE_CARDS`·`ACTIVITIES` — 교재 유닛·과정 커리큘럼이 모두 `BOOKS`에서 파생되는 단일 소스). **수강료 단일 소스 `src/data/pricing.ts`**(`COURSE_PRICE_KRW`(numeric — 전역 고정가·PortOne 결제 금액·서버 검증 기준; **운영가 240,000원**)·`COURSE_PRICE_LABEL`(`formatPrice`)·`COURSE_PER_LABEL`(`/ ${TOTAL_SESSIONS}회`)) — `courses.ts`·`landing.ts`의 `price`/`per`가 참조(전 과정 공통, 차등 시 슬러그 맵으로 확장). **⚠️ per-건 수강료 오버라이드**: `enrollments.price_krw`(nullable, admin 테스트 신청만 세팅)가 있으면 **유효가격 = `price_krw ?? COURSE_PRICE_KRW`**가 표시·무통장 입금확인 캡/기본값·카드 검증(`settlePortonePayment`가 검증, `getVerifiedPortonePayment`는 통화만)·매출 전 경로에 authoritative(실 신청은 null→고정가 그대로). `EnrollmentsManager`·`StudentEnrollments`는 행별 `priceKrw` 숫자로 캡/결제창 금액. `BookUnit`={`n`/`t`/`sub?`/**`situation?`**/`s`} — **`situation?`(유닛 상황 소개)은 kitchen·cosmetic 24유닛에 채워짐**(각 `content/textbook/<course>/unit-NN.html`의 `.situation`에서 추출, 카드 소개문단용); workhol은 `WORKHOL_UNITS.situation` 별도, grammar는 없음. 카피 수정 시 이 파일 먼저 편집. `searchParams` `reset`/`verified`/`signup=success` 시 `SuccessBanner`. 시맨틱 `<h1>` 1개(히어로)·섹션 `<h2>`. 반응형 모바일 기본 + `md:`. 전체를 `bg-surface` 래퍼로 감싸 흰 카드 대비.

**공통 컴포넌트** `Navbar.tsx`(client, 슬라이드 메뉴)·`Footer.tsx`(server)는 `layout.tsx`에서 children 감쌈. **Footer엔 "Become a Teacher" 링크(`/teacher/apply`)** + 카피라이트.
- Navbar는 `user: { email? } | null` + `isAdmin` + `isTeacher` prop을 SSR로 받고(`layout.tsx`가 `getUserRole` 1회로 파생해 전달), `onAuthStateChange` 구독 + `pageshow`(bfcache) 리스너로 stale 방지. 로그인 시 admin→"관리자"·teacher→"Teacher"(`/teacher`) 링크 노출(모바일·데스크톱 2곳). **학생용 "마이페이지" 링크는 `!isTeacher`일 때만 노출**(강사는 숨김, 학생·admin은 노출). 로고는 `next/image`(`/images/logo.png`, `width=123 height=36`, `priority`).
- 슬라이드 메뉴: **커리큘럼 아코디언**(과정 5종 `/courses/<slug>`) + **교재 보기 아코디언**(`TEXTBOOKS` `src/data/textbook/index.ts`) + 인증 영역(로그인 시 **관리자**(admin만)·**마이페이지**·이메일·로그아웃 / 비로그인 시 로그인·회원가입). **이메일은 사이드 메뉴에만 표시**(데스크톱 상단 인라인에선 미표시). 두 아코디언 모두 `<button>` 토글(`curriculumOpen`/`textbookOpen` state, `aria-expanded/controls`, grid `0fr→1fr` 트릭), **메뉴 닫힐 때 둘 다 false 동기화**. 라우트는 `<Link>`+`onClick={closeMenu}`. 데스크톱 인라인에도 (관리자)·마이페이지·로그아웃/회원가입(**이메일은 미표시**). 새 교재는 `TEXTBOOKS`에 추가만 하면 자동 반영.
- a11y: 햄버거 `aria-expanded`·`aria-controls="mobile-menu"`, 메뉴 `role="dialog"`·`aria-modal`·`aria-hidden`·`inert={!menuOpen}`, `bg-black/40` 오버레이, 열림 시 닫기버튼 포커스·닫힘 시 햄버거 복귀(`triggerRef`/`closeButtonRef`/`prevMenuOpen`), body scroll lock, Esc 닫기. **수정 시 이 체인 유지.**
- 로그아웃은 `@/app/(auth)/logout/actions`의 `logout()`을 `<form action={logout}>`로 호출.

**루트 레이아웃** `src/app/layout.tsx`(async server): `getUser()` + `isAdmin(supabase,user.id)` → `<Navbar user isAdmin>`. `<AuthHashHandler/>`(`src/components/auth/`)를 마운트해 implicit hash(`#access_token=...`) 토큰을 client에서 `setSession` 후 `/?verified=success` 또는 `/reset-password`로 replace. `lang="ko"`, Pretendard(`next/font/local`, `--font-pretendard`) + Geist(`--font-sans`). **단, `globals.css`의 `@theme inline`이 `--font-sans`·`--font-heading` 모두 `→ "Twemoji Country Flags", var(--font-pretendard)` 매핑이라 `font-sans`는 Pretendard**(맨 앞 **Twemoji Country Flags** 웹폰트는 `@font-face` `unicode-range`로 국기 코드포인트에만 적용 — Windows/Linux 등 국기 글리프 없는 OS에서 `🇰🇷`가 "KR" 텍스트로 깨지는 것 방지, 폰트 `public/fonts/TwemojiCountryFlags.woff2` 자체호스팅, 일반 텍스트엔 영향 없음).

**스타일링** Tailwind v4: `globals.css`의 `@import` + `@theme inline`(별도 config 없음). `@custom-variant dark`. 토큰 세 그룹: (1) shadcn 색상/`--radius-*`(OKLch), (2) **마케팅 시맨틱 색**: `--color-brand`(#ff4757, 인증/강조 빨강), `--color-brand-blue`(#2563eb, **회원가입 동선 전용**), `--color-ink`(#1a1a1a), `--color-ink-soft`(#333), `--color-surface`(#f8f8f8), `--color-muted-fg`(#666), `--color-muted-fg-faint`(#999), `--color-rule`(#eee), `--color-rule-faint`(#ddd), (3) **project0607 플랫폼 팔레트(블루→핑크)**: `--color-accent-blue`(#6b8ff0)·`--color-accent-blue-soft`(#eef1fd)·`--color-accent-blue-ink`(#4a6bd4, 링크/탭 강조), `--color-cta`(#1a4fa0, **네이비 — 범용 CTA**: 수강신청·강사/센터/유튜브 admin 액션 버튼 등), `--color-progress`(#b22222). + `@layer utilities`의 **`.bg-brand-gradient`/`.text-brand-gradient`**(블루→핑크 135deg, 히어로 "세계로"·섹션 라벨·탭 active·로고). **새 코드는 하드코딩 hex 금지, 토큰 클래스 사용**(예외 단발성: #E05A6A 신청박스, #ffc107/#F5A623 별점, #aaa 등). 다크모드 미구현. `@layer base`: 활성 버튼 `cursor:pointer` 전역 + smooth 스크롤은 `html[data-scroll-behavior="smooth"]`에만 적용(layout.tsx `<html data-scroll-behavior="smooth">`). **⚠️ CSS로 `html{scroll-behavior:smooth}` 직접 선언 금지** — 라우트 네비게이션 시 Next가 끄지 못해 페이지 이동 후 최상단 점프 실패(교재 등 상단 가림). data 속성으로 선언해야 Next가 전환 중 일시 비활성화.

**shadcn/ui** `components.json`(`base-nova`/`neutral`/lucide/RSC). UI: `src/components/ui/{button,card,input,label,textarea,alert-dialog,calendar,popover,select,sonner}.tsx`. `select.tsx`는 미사용(tracked). **`sonner.tsx`=토스트**(라이브러리 `sonner`, next-themes 없이 `theme="light"` 고정 래퍼 — `position="top-center"` `richColors`; `layout.tsx`에 전역 `<Toaster/>` 마운트, 강사 폼 등에서 `toast.success/error`). `calendar.tsx`는 **수강신청 위저드 시작일 선택**(`EnrollWizard` step 2)에서 사용(`date-fns`(^4) + `react-day-picker`(^10), 한국어 `locale={ko}` 필수), `popover.tsx`는 미사용. `cn()`은 `src/lib/utils.ts`(clsx+tailwind-merge), 항상 사용. 아이콘 `lucide-react`, 프리미티브 `@base-ui/react`, 애니메이션 `tw-animate-css`.
- **Button variant**: shadcn 기본 + 추가 4종 — `brand`(빨강 primary), `brand-blue`(파랑, **회원가입 동선 전용**: SignupForm 제출·AlertDialogAction "보내기"), `brand-inverse`(흰, 빨강 배경 위), `kakao`(#FEE500/#191919, `KakaoButton` 전용, 고정 hex). `buttonVariants`로 `<a>`에도 적용. (네이비 CTA는 variant 아닌 `bg-cta` 클래스 — 수강신청·admin 액션 등.)
- ⚠️ **base-nova**: `AlertDialogAction`은 `Close`로 안 감싸져 **클릭해도 모달 자동 안 닫힘** → onClick에서 `setOpen(false)` 또는 controlled 사용. `AlertDialogCancel`은 정상.

**`SectionCard`** (`src/components/SectionCard.tsx`): 카드 div wrapper. variant 3종 — `accent-left`(`border-l-[6px] border-brand bg-surface`), `outline`(`border border-rule bg-surface`, 현재 전자책 Unit 목록에서 사용), `plain`(`bg-surface`). `rounded/p`는 호출 측 className. shadcn `Card`(슬롯)는 별개.

⚠️ **`ApplyForm.tsx`(`src/components/`)는 현재 orphan**(Phase 1 랜딩 리디자인에서 최종 CTA 섹션 제거 — 미사용 mock). 구 상담신청 동선(폼 `CourseApplyForm.tsx`·액션 `submitApplication`·메일러 `sendApplicationNotification`/`ApplicationEmailData`·admin `ApplicationsManager`·`applications` 테이블)은 **전부 제거됨**(`20260627224406_drop_applications.sql`로 테이블·`application_status` enum drop). 실제 신청 동선은 **강사 매칭형 수강신청**(`/courses/[slug]/enroll`, 아래).

**수강신청(강사 매칭형) + 마이페이지**: 과정 상세 CTA "수강 신청하기"→ **`/courses/[slug]/enroll`**(`page.tsx` server, **로그인 가드**`redirect("/login?next=…")` + **휴대폰 인증·영문 이름 가드**(`ready=phone_verified_at && english_name`; 폰 미인증=마이페이지 인증 안내 배너, 영문 이름 미등록=마이페이지 등록 안내 배너 — 결과 SMS 발송 위해 폰 필수, 영문 이름은 수강신청 필수)) (+ **page가 `loadEnrollTeachers()` 호출해 전체 강사 prop 전달** — `createAdminClient()`로 `profiles.role='teacher'`+`centers`+`teacher_availability` 일괄 조회·그룹핑, 공개안전필드(이름·아바타·국적·성별·센터·bio)+`slots`+**`heldSlots`** 반환 — **`slots`는 `teacher_availability` − 그 강사의 확정(`status='승인'`·`결제완료`) enrollment 슬롯(`subtractSlots`)**; **`결제대기`/`신청`은 더 이상 차감 안 함**(슬롯 노출, 다른 학생도 신청 가능) → 대신 **`heldSlots`=다른 학생의 진행중(`신청`/`결제대기`) 슬롯**(본인 제외, `loadEnrollTeachers(userId)`)로 노출해 **겹치면 확인 단계 경고**(하드 차단 아님). 폰 미인증 시 빈 배열) → **`<EnrollWizard teachers>`**(`src/components/course/`, client, **3스텝**): ①**일정·강사 선택**(통합 라이브 패널) = `<EnrollScheduleField>`(**admin `TeacherAvailabilityFinder` 입력 모델 도입**: 요일 다중 토글(한국어) + 요일당 수업 횟수(1~8회, 1회=30분) + 시작 시각 → 종료 자동 계산, 선택값에서 **요일×범위 30분 슬롯 배열 파생**해 `onChange` 통지; **옵셔널 `initialSlots?` prop**=마운트 1회 역산(`deriveInitial`)해 요일·시작시각·횟수 프리필+`초기화`가 그 초기값으로 복원 — 미전달 시 빈 기본값(위저드·테스트 생성은 미전달, reschedule-remaining만 `currentSlots` 전달)) + **그 아래 라이브 강사 목록**(`matches=useMemo(teachers.filter(teacherHasAllSlots(t.slots,slots)))` — **버튼 없이** 일정 바꾸면 즉시 갱신, admin finder와 동일 클라 필터·추가쿼리 0; 선택 강사가 매칭서 빠지면 `selectedTeacher=matches.find(...)` 자동 무효) → 강사 카드 택일 ②**시작일 선택** = 달력(`src/components/ui/calendar.tsx`=react-day-picker, `locale={ko}`) — **선택 범위 제한 3중**: (a)`before:minDate`(=today+3, **오늘+3일(D+3)부터 선택**) (b)`after:maxDate`(=today+14, **2주 이내만**) (c)`!allowedDays.has(d.getDay())`(**1단계에서 신청한 요일만 활성** — 주간 반복 수업 첫 수업일이라; 1단계로 돌아가 요일 바꿔 기존 선택일이 빠지면 `useEffect`로 `setDate(undefined)` 초기화) + **오늘 셀 강조 표시**(`classNames.today` accent-blue ring) ③**확인** = 요약(선택 시간이 `selectedTeacher.heldSlots`와 `slotsOverlap`이면 **앰버 경고 노티스 + `AlertDialog` 문구를 "다른 학생이 먼저 신청…확정 시 취소될 수 있음" 경고로 교체**) + `AlertDialog` → `submitEnrollment`(`useActionState`). 액션 `submitEnrollment`(`src/app/courses/enroll-actions.ts`) 가드 순서: (1)`getUser` 로그인 (2)`profiles.phone_verified_at`+`phone`+`english_name` 확인(미인증/영문 이름 미등록 차단, 서버 authoritative) (3)slots(`isValidSlot` day0-6·min30배수·중복제거·cap)·`start_date`(`YYYY-MM-DD`·**KST 오늘+3일~오늘+14일(2주) 이내**·**요일이 신청 slots 요일 중 하나**, 클라 달력 제약 서버 재검증)·teacherId 검증 (4)`rateLimit(enroll:IP,5/10분)` (5)**예약 무결성 재검증**(service_role): **유효가용=`teacher_availability`−강사 `승인`·`결제완료`(확정) 슬롯(`subtractSlots`)** → `teacherHasAllSlots(effective,slots)`(`승인`·`결제완료` 겹침만 하드 차단; `결제대기`/`신청` 겹침은 **허용**=경고 후 신청, 클라가 `heldSlots`로 경고) + **학생 본인 시간 충돌**(본인 `신청`/`승인`/`결제대기`/`결제완료` enrollment 슬롯과 `slotsOverlap`이면 차단) (6)본인 세션 client `enrollments` insert(`student_id`=uid, slots jsonb, `teacher_name`/`student_name`/`student_english_name`/`student_phone` 스냅샷) (7)**best-effort 강사 알림 메일**(`sendEnrollmentNotificationToTeacher`, 강사 이메일은 `admin.auth.admin.getUserById`, 영문). **슬롯 공유 모델 `src/lib/availability.ts`**(`Slot{day,min}`·`slotKey`·`SLOT_MIN`·`GRID_START/END_HOUR`·`DISPLAY_DAYS`·`DAY_LABELS`/`DAY_LABELS_KO`·`ROW_MINS`·`teacherHasAllSlots`(set⊇)·**`subtractSlots(base,remove)`**(예약 소비 차감)·**`slotsOverlap(a,b)`**(시간 충돌)·`isValidSlot`·`summarizeSlots(slots,ko)` 요일별 요약·**`enumerateLessonSessions(start,slots,total)`**(날짜별 수업 열거, 화상수업 클래스 생성용)·`lessonEndDate`·`TOTAL_SESSIONS`·`fmtTime`) — 그리드·매칭·요약·중복예약·클래스 단일 소스. **승인 시점 가드**: `approveEnrollment`(`src/app/teacher/actions.ts`)가 **승인 시 곧바로 `결제대기`로 전환**; update 직전 이 강사의 다른 `승인`·`결제대기`·`결제완료` 슬롯과 `slotsOverlap` 검사(race-safe, 겹치면 차단). **결제 1단계(무통장 입금 수동 확인)**: 회사 계좌 입금이라 확인 주체=admin → `confirmPayment(id)`(`src/app/admin/actions.ts`, `requireAdmin` 후 **공유 코어 `finalizeEnrollmentPayment`(`src/lib/payment.ts`, server-only — admin 입금확인·학생 테스트결제 공용, ⚠️ `"use server"` 아닌 모듈이라 클라 직접 호출 불가)** 위임: 상태 `결제대기` 가드→`결제완료`+**날짜별 클래스 자동 생성**(`generateClassesForEnrollment`, payment.ts로 이동)+학생 SMS+**강사 결제확정 알림 메일**(`sendEnrollmentPaymentConfirmedToTeacher`, 강사 이메일은 `admin.auth.admin.getUserById`, 영문, best-effort), `/admin/enrollments`·`/teacher`·`/mypage`·`/mypage/classroom` revalidate). `결제완료`는 `승인`과 함께 **하드 확정**(가용에서 차감·신규 신청 하드 차단) — **단, 과정 종료 시 자동 해제**(남은 `예정` 수업 없음=마지막 수업 다음날부터): `loadEndedEnrollmentIds`(`src/lib/booking.ts`, server-only)가 종료된 `결제완료` id를 산출해 **가용 차감(`loadEnrollmentSlotsByTeacher`)·승인 충돌(`approveEnrollment`)·그리드 표시(`deriveBookedSlots` 소비처)** 모든 지점에서 제외(클래스 없는 레거시는 `lessonEndDate` 폴백). 입금 계좌 단일 소스 `src/data/payment.ts`(`PAYMENT_BANK`). **2단계 PortOne V2 카드 결제 도입됨**(테스트 채널, 위 `confirmPortonePayment` — 클라 결제창 성공→서버 REST 검증→`finalizeEnrollmentPayment`). **PortOne 웹훅 라우트 `src/app/api/payments/portone/webhook/route.ts`(첫 route handler, `POST`)**: 원문 body+헤더로 `@portone/server-sdk` `Webhook.verify(PORTONE_V2_WEBHOOK_SECRET,…)` 서명 검증(실패=400)→ **`Transaction.Paid`→`settlePortonePayment(data.paymentId,{role:"system"})`**(기록+확정+과오납 자동환불) / **`Transaction.Cancelled`·`PartialCancelled`→`fetchPortonePaymentRaw`로 누적 취소금액 조회→`refundEnrollmentPayment`**(콘솔/외부 환불도 DB 동기화=수강 취소·미래 수업 취소). **멱등**: 이미 처리/취소는 200(클라 콜백·이중 도착 안전), 일시오류만 500(PortOne 재전송 유도), 무관/미기록 결제 200 무시. 인증 세션 불필요(서명 검증), `proxy.ts` `updateSession`은 차단 안 해 통과. **취소(학생/admin) 시 슬롯 자동 해제**. **`/mypage`(탭 구조)**: 공유 `src/app/mypage/layout.tsx`(server, 로그인 가드+라벨바+웰컴 배너+`<MyPageTabs>` `src/components/mypage/`, client `usePathname` 활성표시) 아래 **3개 하위 라우트** — `/mypage`(기본 정보=회원정보 dl+`StudentProfileForm`)·`/mypage/enrollments`(`<StudentEnrollments>`)·`/mypage/classroom`(`<ClassroomList isTeacher={false}>` via `loadClasses`). 각 page는 본인 데이터 조회(`enrollments` student_id / `profiles` / `classes`). **수강신청 내역**(`<StudentEnrollments>` `src/components/mypage/`, client) — 네이티브 `<details>` 아코디언(**확장 패널 dl=강사·주간 일정(`summarizeSlots(slots,true," / ")` 요일 `/` 구분)·수업 일정(시작~종료=`lessonEndDate`)·수업 횟수(`total_sessions`; page가 전달)·수강료(`priceLabel`)**, 헤더 강사명·시작일·상태 배지(`신청=승인대기`/`승인`/`결제대기=결제 대기`/`결제완료=결제 완료`/`거절`+사유/`취소`; **환불 건(`취소`+본인 payment cancelled/partial, page가 `payments_select_own`으로 조회한 `refunded`)은 "환불됨" 앰버 배지로 구분**)) + **`결제대기`면 확장 패널에 결제 수단 선택 UI**(`payMethod` state `null|bank|card` — **무통장/카드 중 택1 우선**, 선택 전엔 "🏦 무통장 입금 / 💳 카드 결제" 2토글만 노출·상호 전환 가능): **무통장 선택**→입금 안내 블록(과정 고정가 `priceLabel`(서버가 `getCourse(course).price` 해석)·`PAYMENT_BANK` 계좌·예금주) / **카드 선택**→결제 금액 요약 + **"카드로 결제하기" 버튼**→**PortOne V2 카드 결제**(`@portone/browser-sdk` `PortOne.requestPayment` 결제창=자체 확인 단계라 별도 다이얼로그 없음; `storeId`/`channelKey`=`NEXT_PUBLIC_PORTONE_*`, `totalAmount=row.priceKrw`(유효가격=`price_krw ?? COURSE_PRICE_KRW`, page가 전달)·`currency:"CURRENCY_KRW"`·`payMethod:"CARD"`·`customData:{enrollmentId}`·`redirectUrl`(모바일 대비))→성공(code null·paymentId) 시 `confirmPortonePayment(enrollmentId,paymentId)`(`src/app/mypage/actions.ts`) 서버 검증→낙관적 `결제완료`+toast. **`confirmPortonePayment`**: 로그인 가드→`GET api.portone.io/payments/{paymentId}`(`Authorization: PortOne {PORTONE_V2_API_SECRET}`(`.trim()` 없이), 서버 전용 시크릿; 401=시크릿 오류)→**`settlePortonePayment(paymentId,{role:"student"})`(`src/lib/payment.ts`)** 위임(검증(`getVerifiedPortonePayment`)→payments 기록(`recordPayment`)→`finalizeEnrollmentPayment` 확정→과오납 자동환불). 금액 클라 신뢰 금지. 실패 토스트에 `PortOne <status>·type` 노출(테스트 편의, ⚠️ 운영 시 일반 문구 복원). **웹훅 연동됨**(아래) — 창 닫힘/모바일 리다이렉트로 클라 콜백 유실돼도 서버가 확정 + **상태가 `신청`/`승인`/`결제대기`이면 "신청 취소" 버튼**(`결제완료`는 취소 불가=환불 별도)(`AlertDialog` 확인→`cancelEnrollment(id)` 낙관적 갱신+toast, 강사에 취소 이메일). `거절`/`취소` 행엔 미노출. `displayName` 우선순위 `성+이름(last_name+first_name 붙임) → email앞`. **회원정보 카드 필수화**: 이름(성·이름)·**영문 이름**·전화번호 모두 **필수**(전화번호는 **인증 완료=`phone_verified_at`**일 때만 충족) — `nameComplete`/`englishNameComplete`/`phoneComplete` 계산해 미완료 항목 있으면 카드 summary 아래 **안내 배너**(`bg-brand/5 border-brand/30 text-brand`, "회원 정보 완성을 위해 …항목을 완료해 주세요", 서버 렌더라 저장/인증 후 자동 갱신). **회원정보 아코디언 안에 `<StudentProfileForm>`**(`src/components/mypage/`, client, useActionState+controlled+toast) — 학생 본인 **성(last_name)·이름(first_name) 분리 입력**(성→이름 순, **둘 다 필수**: 라벨 빨강 `*`+native `required`, `<form id="student-profile-form" action={updateStudentProfile}>`) + **영문 이름(`english_name`, 수강신청 필수)**(`sanitizeEnglishName`로 onChange 시 비영문 즉시 제거, 영문자/공백/`.-'`만) + **전화번호 SMS 인증**(`<PhoneVerifyField>`, 이름 폼과 독립). **이름 폼은 강사 프로필과 동일한 보기/편집 모드**(`editing` state, 실수 저장 방지): 평소 이름 필드 `disabled`(`VIEW_TEXT="disabled:opacity-100 disabled:text-ink"`로 값 진하게)+버튼 **"수정"**(연필 아이콘)만, 클릭 시 필드 활성+**"취소"/"저장"** 노출. 진입 시 스냅샷(`snapshotRef`) → 취소는 이름 값 복원 후 보기 모드, 저장 성공(`state.ok`) 시 보기 모드 복귀. **`저장` 버튼은 폼 밖 카드 맨 아래 우측**(`form="student-profile-form"`로 이름 폼에 연결) — **전화 미인증이면 비활성**(`phoneVerified` state를 `PhoneVerifyField`의 `onVerifiedChange` 콜백으로 끌어올림, `disabled={pending || !phoneVerified}`). + **주소(선택 입력, `<AddressField>` `src/components/mypage/`, client)**: 이름 폼 안에 함께 배치돼 같은 "저장"으로 제출, 보기/편집 모드 연동(평소 비활성). **다음(카카오) 우편번호 검색 팝업**(`postcode.v2.js` on-demand 로드, `daum.Postcode` `oncomplete`→`zonecode`+`roadAddress||jibunAddress` 자동 채움→상세주소 focus) — 우편번호·기본주소 readOnly + 상세주소 수동, 필드 `name=postcode/address/address_detail`. **선택이라 `missing` 배너·필수 가드 제외**. 이름 액션 `updateStudentProfile`(`src/app/mypage/actions.ts`)은 **본인 세션 client로** `profiles` `first_name`·`last_name`·**`english_name`**·**주소 3필드(`postcode`·`address`·`address_detail`)**만 화이트리스트 update(RLS `profiles_update_own`+role/phone 트리거 보호라 service_role 불필요, `clean()`으로 trim/빈값 null/길이 제한, **영문 이름은 정규식 `/^[A-Za-z][A-Za-z .'-]*$/` 검증**) — **가드: 이름·영문 이름 누락 시 차단 + `phone_verified_at` 조회해 미인증 시 차단**(서버 authoritative, 클라 우회 방어)+`revalidatePath("/mypage")` — **phone은 미포함**(인증 플로우 전용). **전화번호 인증**(`<PhoneVerifyField>`, client, `src/lib/phone.ts` `normalizePhone`/`isValidKoreanMobile`/`formatPhone` 공유): 상태머신 (a)인증됨=`formatPhone`+✓배지+"번호 변경" (b)입력+"인증번호 받기" (c)6자리 코드+"확인", 60초 재전송 쿨다운. 액션 `sendPhoneOtp(phone)`(가드→번호검증→`rateLimit(otp-send:IP,5/10분)`+DB `last_sent_at` 60초 min-interval→6자리 코드 `sha256(uid:code)` 해시로 `phone_verifications` upsert(만료 3분)→`sendSms`(`src/lib/sms.ts`, Solapi)) / `verifyPhoneOtp(phone,code)`(만료·`attempts≥5`·번호·해시 검증, 실패 시 attempts++; 성공 시 **service_role로** `profiles` `phone`+`phone_verified_at` 기록+행 삭제). **인증 통과 시에만 phone 저장**(정책). `phone_verifications` 조회·쓰기는 `createAdminClient()`(service_role). **한국 관례라 표시는 `성+이름` 공백 없이 붙임**(강사 폼의 `[first,last].join(" ")` 영문 공백 방식과 의도적 차이). Navbar는 로그인 시 "마이페이지"(+admin이면 "관리자", +teacher면 "강사 페이지") 링크 노출(`layout.tsx`가 `isAdmin`/`isTeacher` prop 전달). **mypage는 강사 지원과 무관**(학생 신청내역·회원정보 전용, 전부 한국어).

**화상수업(zoom) — 클래스 + 내 강의실**: 결제 확정(`confirmPayment`→`결제완료`) 시 enrollment의 (start_date+slots+**`total_sessions ?? TOTAL_SESSIONS`**)에서 **날짜별 클래스 자동 생성**(`generateClassesForEnrollment`, service_role, **멱등** `upsert onConflict:"enrollment_id,session_no" ignoreDuplicates`; `total_sessions`는 admin 테스트 수강신청의 자유 횟수, 실 신청은 null→24). **클래스 1개=수업 1회(매칭 요일 하루)** — 같은 날 여러 슬롯이면 한 클래스(start=최소 min, end=최대 min+30). 열거는 `enumerateLessonSessions(start,slots,total)`(`src/lib/availability.ts`, `lessonEndDate` 미러·요일당 1회·TZ 비종속). **`classes` 테이블**(`20260626221643_add_classes.sql`): `enrollment_id`/`student_id`/`teacher_id` FK cascade·course/title·표시 스냅샷(teacher_name·student_name·student_english_name)·`session_no`·`session_date`·`start_min`/`end_min`·unique(enrollment_id,session_no); RLS `_select_own_student`/`_select_own_teacher`(enrollments 패턴), **사용자 INSERT/UPDATE/DELETE 없음**(생성=admin service_role, 정리=cascade); zoom_url 미저장(입장 시 강사 profiles 최신값 조회). **내 강의실 진입점**: 학생=`/mypage/classroom`(마이페이지 탭)·강사=`/teacher` 내 "My Classroom" 섹션(별도 `/classroom` 라우트 제거). 공통 로더 **`src/lib/classroom.ts`**(`loadClasses(supabase,userId,isTeacher)`·`mapClassRows`·`CLASS_SELECT`)로 `classes` 조회(RLS 본인 것; **환불/거절(enrollment.status∈{취소,거절}) 과정은 강의실에서 완전 제외** — loadClasses가 slots 조회 시 `status`도 함께 읽어 해당 enrollment 클래스를 필터, 학생·강사 공통. 개별 수업 연기(enrollment `결제완료` 유지)는 제외 대상 아님=취소선으로 계속 노출; **강사는 `.or(teacher_id.eq,original_teacher_id.eq)`로 현재 담당 + 1회성 대체로 넘어간 원 강사 회차까지**, 학생은 `student_id`만) — **과정 카드 대표 강사(학생 뷰)**: `loadClasses`가 enrollments select에 `teacher_name`도 읽어 `mapClassRows`가 `ClassItem.enrollmentTeacherName`(현재 담당, 강사 대체 반영)로 스탬프 → `ClassroomList`가 과정 그룹 카드 대표 강사를 `!isTeacher`면 이 값 우선(첫 과거 회차 스냅샷 대신, admin 목록과 동일 원칙). per-session 행·주간 뷰의 강사 표시는 실제 담당(과거=옛·미래=새) 유지. **1회성 강사 대체 read-only 표시**: `mapClassRows`가 `reassignedAway=isTeacher && original_teacher_id===uid && teacher_id!==uid`·`coveredByName`(=현재 대타명) 파생 → 원 강사 뷰에서 그 회차를 목록·주간 타임테이블에 **read-only**로 노출("Reassigned" 배지·"Covered by {대타}"·dim·**주간 그리드/아젠다·달력 취소선(line-through)**, 입장/연기/피드백작성/진행마커 없음, 대타 피드백 있으면 열람만=`ClassFeedbackModal readOnly`). **카운트**: 회차 수·진행률·`sessionNo/total`은 과정 전체 기준(대체됨 포함), **다음 수업·그룹 정렬·달력 진행마커만 대체됨 제외**(원 강사가 실제 담당할 회차 기준). 대타는 평소대로 정상(teacher_id=대타) + **대타 뷰 `ClassInfoModal`에 "Covering" 배지·"You're covering this class for another teacher." 안내**(`coveringForOther=isTeacher && teacher_id===uid && original_teacher_id && ≠uid` 파생, `mapClassRows`·`ClassItem`; 입장/피드백 등 액션은 정상=대타가 실제 담당), 학생은 기존 "강사 변경" 불변. 정산은 `teacher_id`(대타) 기준이라 자동 정합)→`<ClassroomList classes isTeacher>`(`src/components/classroom/`, client, **강사 화면 전체 영문·학생 한글**: 헤더·섹션·버튼·날짜포맷(`M월 D일 (요일)`↔`Jul 1 (Mon)`)·과정명(강사=`getCourse().englishTitle`)) — **진입 랜딩=카드/주간 뷰 토글**(`landingView` cards|weekly 세그먼트 pill, 기본 카드). **카드 뷰 2뷰**: ①기본=**과정(enrollment_id)별 카드 그리드**(`grid sm:grid-cols-2`, 카드=과정명·상대방·다음 수업 일시·**기간(첫~마지막 수업일, 보강 포함 `shortDate`)·주간 요일(`weekdaySummary`=**`enrollments.slots`(현재 템플릿) → `summarizeWeekdays`**, `/` 구분 예 "월/수/금"; `loadClasses`가 본인 enrollment `slots`를 조회해 `ClassItem.enrollmentSlots`로 스탬프, 비면 정규 수업 요일 폴백 — 일정 변경 반영)**·진행률 바(완료/전체)) ②카드 클릭→같은 페이지에서 **해당 과정 수업 목록**(상단 "← 목록으로/Back", **예정/지난 분리**, 지난은 흐림·입장 버튼 숨김, 행=날짜·시간(`fmtTime`)·회차(`sessionNo/총`, **총=과정 계획 회차=보강 제외 클래스 수**, `TOTAL_SESSIONS` 하드코딩 아님 — 테스트 자유 횟수 반영)). **주간 뷰**(`<WeekSchedule>`, 전체 과정·학생 통합 주간 스케줄, 주 이동(월요일 시작·`mondayOf`)·이번주 하이라이트): 데스크톱=월~일 타임그리드(06:00~24:00 `ROW_MINS`, 시간 거터 sticky, enrollment별 고유 배경색 `COURSE_PALETTE`, 진행중=`ring-cta`, **종료(레슨 종료 지남)=회색 음영**, 취소=흐림/line-through, 주말 헤더 일=`text-brand`/토=`text-accent-blue-ink`, **강사 뷰 블록 우상단 ○/△ 진행 마커**(`conductMark`, 목록·달력과 동일))·모바일=요일별 아젠다(`WeekAgendaRow`, 수업 있는 날만, 종료·취소 흐림, 행에 입장/연기 인라인). "이번 주" 버튼은 항상 노출(이번주면 `disabled`). **입장·연기·피드백 직접 실행**: 블록/행 클릭→`ClassInfoModal`(수업 요약 + **제목 옆 상태 배지 + 본문 `Info` 안내 문구**(`ClassRow`/`WeekAgendaRow`와 동일 문구·색 재사용: 취소=`취소/Cancelled`·연기=`연기/Postponed`(`cancelReason==='cancel'` 여부로 구분)·보강=`보강/Makeup`·학생 강사대체=`강사 변경`; 문구=연기 "맨 뒤 보강 편성"·취소 "취소됨"·보강 "연기 대신 보강" 안내, 강사대체는 배지만) + **입장**(`EnterClassButton` 라이브 시, 학생·강사 공통) + **연기**(`PostponeButton`, 학생·그 과정 다음 예정 1건만) + **피드백**(종료 수업만: 강사=작성/수정·학생=피드백 있을 때 보기 → `ClassFeedbackModal`을 launcher 패턴으로 승격=정보 모달 닫고 오픈, 중첩 Esc/z 충돌 방지) + **입장 시간창 전 미래 수업엔 "시작 15분 전 입장/Opens 15 min before" 안내 문구**(`showEntryHint`, ClassRow 힌트와 동일 미러) + "과정 상세로" `onGoToCourse`→그 과정 `CourseDetail`; 액션(입장·연기·피드백) 있으면 "과정 상세로"는 보조 버튼으로 강등, 안내 문구는 정보 텍스트라 강등 안 함). 모달·아젠다의 연기 노출 판정은 `WeekSchedule`가 `groups`에서 파생한 `ctxByEnrollment`(enrollment별 `{cancelAllowed(!isTeacher && count<MAX), cancelledCount, nextUpcomingId}`, `nextUpcomingIdOf`·1분 틱 `now`)로 공급. **`EnterClassButton`·`PostponeButton`은 ClassRow·모달·아젠다 공용 단일 소스**(입장=팝업차단 회피, 연기=확인 다이얼로그+`cancelClass`+남은 연기 토스트; `fullWidth`/`compact` variant). 연기 규칙(학생만·다음 예정 1건·1시간 컷오프·진행중이면 숨김)은 서버 `cancelClass` 가드와 동일, UI는 노출 제어만. 1분 틱 `now` state로 다음수업·진행률·입장창 갱신. **진입=과정 카드 → 과정 선택 시 그 과정 상세**(`CourseDetail`): **달력/목록 토글(달력 기본)**. 달력=그 과정 월간(`ui/calendar` 재사용·`date-fns/locale` ko/enUS, 예정=accent-blue·지난=rule 색 구분·오늘=노랑 배경(#FFF3CD)+앰버 링·주말 색(일=brand 빨강/토=accent-blue-ink, `dayOfWeek` 모디파이어+헤더 nth-child, `weekStartsOn=0`·outside days off), `mode=single required`, 기본 포커스=다음 예정 수업 달), 날짜 클릭 시 그 날 수업 목록·입장. 목록=예정/지난 섹션. **수업 1회=25분(+5분 휴식)**: 내 강의실 행 종료 시각은 `lessonEndMin(end_min)`=`end_min−(SLOT_MIN−LESSON_MIN)`=`end_min−5`로 `:25`/`:55` 표시(`LESSON_MIN=25`·`lessonEndMin` `availability.ts` 단일 소스, 위저드 `EnrollScheduleField`도 공유). 슬롯 점유·매칭은 30분(`end_min`) 그대로이나, **입장 시간창 종료·예정/지난 전환은 레슨 종료(`lessonEndMin`=`end_min−5`)=표시 종료와 일치**(클라 `mapClassRows`의 `endMs`·서버 `enterClass` 모두 `lessonEndMin` 적용 — 표시 종료 후 입장 가능했던 5분 간극 제거). **입장하기**: 시작 15분 전~레슨 종료 시각만 활성(`canEnterClass`/`kstDateMinToMs`/`ENTRY_LEAD_MS` `src/lib/classtime.ts`, KST=UTC+9 고정). 클릭→**팝업 차단 회피**(빈 탭 먼저 open 후 액션 URL로 이동)→`enterClass(classId)`(`src/app/classroom/actions.ts`): 로그인+**소유 검증**(student_id∥teacher_id)+**시간창 서버 강제**+강사 `zoom_url` 최신값(service_role)·`isValidZoomUrl` 검증→`{url}∥{error}`. 학생·강사 동일(15분 전). **개별 수업 연기(학생, 자동 보강)**: 학생 뷰 예정 수업에 **"수업 연기"** 버튼(영문 "Postpone"; **UI 문구만 "연기"(영문 버튼 "Postpone"/배지 "Postponed")로 통일**하고 내부 `classes.status`·코드 식별자는 `'취소'`/`cancelClass`/`cancellable`/`ClassCancellationEmailData`/`sendClassCancellationToTeacher` 그대로 — 실동작=이 회차 건너뛰고 맨 뒤 보강 1회, "취소" 어감이 부적절해 **학생 UI·학생 대상 서버 에러·강사 알림 메일까지 카피만 "연기/postpone"으로 변경**) → `cancelClass(classId)`(`src/app/classroom/actions.ts`): 소유(student_id)+상태 `예정`+**다음(가장 이른) 예정 수업 1건만**(같은 enrollment의 `예정` 중 미시작(`startMs>now`) 최소 시작시각 = `nextId`, 서버 가드 + 클라 `ClassRow`의 `cancellable`에 `item.id===nextUpcomingId` 조건 — `CourseDetail`이 `nextUpcomingId` 산출해 `SessionList`·`ClassroomCalendar`→`ClassRow` 전달, 취소 시 다음 수업이 새 대상)+**진행 중(입장 가능 창) 수업 있으면 연기 불가**(같은 enrollment에 `canEnterClass`(시작 15분 전~`lessonEndMin`) 참인 수업 하나라도 있으면 클라 `nextUpcomingId=null`로 버튼 숨김 + 서버 `cancelClass` `inProgress` 가드로 차단 — 진행 중 수업 입장 버튼과 다음 수업 연기 버튼 동시 노출 방지, 두 곳 동일 규칙)+**시작 1시간 전 컷오프**(`canCancelClass`/`CANCEL_CUTOFF_MS`)+**과정당 6회 한도**(`MAX_CANCELLATIONS`, **`status='취소' && cancel_reason='student'` 카운트** — 학생 연기만 차감, admin의 회사 사유 연기·취소는 미차감) 가드 → `status='취소'` + **자동 보강 생성**(과정 전체 `최대 session_date` 다음으로 **주간 스케줄 요일 집합 중 가장 빠른 날**=마지막 수업일 뒤 첫 스케줄 요일, 요일별 시각 맵 적용, `session_no=enrollment 최대+1`, `is_makeup=true`, best-effort; **공유 헬퍼 `createMakeupClass` `src/lib/makeup.ts` — 학생 `cancelClass`·admin `adminCancelClass` 공용**) + 강사 연기 알림 이메일(`sendClassCancellationToTeacher`, 영문 "A class has been postponed") + `/mypage`·`/teacher`·`/admin/classes` revalidate(클라 `router.refresh()`). 연기 행=연기 배지(한글 "연기"·영문 "Postponed")·흐림·입장 숨김, 보강 행=보강 배지. **강사 대체 시 학생 예정 수업 행에 "강사 변경" 배지+안내 문구**(`reassigned=!isTeacher && reassignedAt && !cancelled && !isPast`, `teacher_reassigned_at` 기반, 지나가면 자동 숨김; `CLASS_SELECT`/`mapClassRows`/`ClassItem`에 `reassignedAt` 포함), 달력 취소일=취소 모디파이어(line-through). 진행률·예정 분리에서 취소 회차 제외. **남은 연기 횟수 안내**: `cancelClass`가 `remaining`(=`MAX−연기수`) 반환 → 연기 확인 모달("이번에 연기하면 N회 남습니다", `cancelledCount` prop으로 산출)·완료 토스트("남은 연기 N회")에 표시. **Navbar에 내 강의실 직접 링크 없음**(학생=마이페이지 탭·강사=/teacher 섹션으로 접근). **admin은 `/admin/classes`에서 전체 수업 조회·개별 취소(보강 옵션)·일정 변경·강사 대체**(강사 가용+충돌 검증, 과정 종료 시 편집 차단, admin 대시보드 항목). 기존 `결제완료` 건은 클래스 백필 필요(신규 결제부터 자동). **수업 종료 후 강사 피드백(텍스트, 수강생 읽기 전용)**: `classes.feedback`/`feedback_at`(`20260629234513_add_class_feedback.sql`). `ClassRow`(`ClassroomList`)에서 **종료된 수업**(`ended=!cancelled && now≥endMs`, 레슨 종료 시각 이후)은 **행에 버튼만** 두고 내용/편집은 `ClassFeedbackModal`(같은 파일, 커스텀 오버레이 `z-[110]`/패널 `z-[120]`·Esc·scroll lock, 긴 피드백 UX)에서 처리 — 강사=버튼 "Write/Edit feedback"(`MessageSquare`)→모달 textarea 작성/수정/삭제·수강생=피드백 있으면 "피드백 보기"(`Eye`)→모달 읽기 전용(전문+`feedbackAt` 날짜). (인라인 전문 노출 폐지.) 저장 `saveClassFeedback(classId,text)`(`src/app/classroom/actions.ts`): 로그인+**강사 소유권**(`teacher_id`)+취소 아님+**종료 가드**(`Date.now()≥kstDateMinToMs(session_date,lessonEndMin(end_min))`)→service_role update(빈 문자열=null 삭제, ≤2000자), `/teacher`·`/mypage` revalidate. **새 RLS 없음**(쓰기=service_role 액션, 읽기=기존 student/teacher select). `CLASS_SELECT`/`mapClassRows`/`ClassItem`에 `feedback`·`feedbackAt` 포함. **수업 진행 인정**(`classes.teacher_entered_at`·`conducted_at`, `20260630021451`): **강사 Enter 클릭(`enterClass`가 강사 본인 첫 입장 시 `teacher_entered_at` 기록) AND 종료 후 비어있지 않은 피드백 저장**(`saveClassFeedback`가 `teacher_entered_at` 있고 미인정이면 `conducted_at` 박음) → **진행 인정**. **sticky**(이후 피드백 비워도 `conducted_at` 유지). 학생 노쇼라도 강사 입장+피드백이면 인정(학생 입장 미기록). **강사 뷰 진행 마커(강사 전용, `conductMark(item,now)`)**: `ClassItem.conductedAt`이면 **○ "Conducted"**(초록 `Circle` pill)·`teacherEnteredAt` 있고 미인정+종료(`now≥endMs`)면 **△ "Feedback pending"**(앰버 `Triangle` pill, 입장했으나 피드백 전) — 지난 수업 목록 행 + **달력 셀 우상단 코너 마커**(`modifiers` conducted=●/pending=▲ + `after:` 절대배치, 학생 뷰엔 빈 배열=미표시) + 달력 아래 강사 범례. `CLASS_SELECT`/`mapClassRows`/`ClassItem`에 `teacher_entered_at`(→`teacherEnteredAt`) 포함(마커 판정용). admin 상세=`ClassesManager` "진행" 컬럼(진행됨/미진행=종료됐는데 미인정/−)+모달 dl(진행 여부·강사 입장(입장함·시각/미입장)·**피드백 작성**(작성함·날짜/미작성))+**미진행 사유 노티스**(dl 아래, `완료 && !conducted_at`일 때만; `unconductedReason(r)`: `!teacher_entered_at`→"강사가 입장하지 않았습니다.", `!feedback`→"강사는 입장했으나 피드백을 작성하지 않았습니다."; brand 색 블록). ⚠️ 강사가 Enter 안 누르고 Zoom 직접 열면 미인정(정책). **admin `/admin/classes/[enrollmentId]`(`ClassesManager`)도 읽기 전용 열람**(행에 "피드백" 칩·`ClassDetailModal`에 강사 피드백 블록; 상세 페이지 select·`AdminClass` 타입에 `feedback`/`feedback_at` 포함, admin은 작성 불가).

**강사 지원** (`/teacher/apply`, `src/app/teacher/apply/page.tsx`, server, **UI 영문**, 과거 mypage 아코디언에서 분리): 진입점=Footer "Become a Teacher" 링크 + 거절 알림 메일의 `applyUrl`. 가드 `!user`→`/login?next=/teacher/apply`, role=teacher→`/teacher`, role=admin→`/`. `profiles`(first_name·last_name·phone·nationality·gender·avatar_url) + 본인 `teacher_applications` 최신 1건(`maybeSingle`) 조회 → `status='신청'`이면 "Under review"+제출정보 read-only, 그 외(미신청/거절)면 `<TeacherApplicationForm>`(`src/components/mypage/`, client — 컴포넌트 위치는 mypage 폴더 유지, useActionState+`submitTeacherApplication`+AlertDialog 확인+toast, **강사 프로필(`/teacher`)과 동일 필드세트**: **프로필사진(필수**, 선택 시엔 로컬 미리보기(objectURL)만·**실제 업로드는 최종 제출 시점**: `handleFinalConfirm`(async)이 `uploadAvatar`(브라우저)→publicUrl을 `FormData.set("avatar_url")` 후 **`startTransition(()=>formAction(fd))`** dispatch(⚠️ await 뒤 dispatch라 transition 필수) — 선택만 하고 이탈 시 고아 파일 없음, `cleanupOldAvatars`로 폴더당 1개 유지; state=`avatarFile`/`avatarPreview`(objectURL∥재신청 원격 URL)/`savedAvatarUrl`(재신청 시 사진 미변경이면 그대로 제출), `handleReview`에서 존재 수동 검증)·**first_name·last_name(필수)**·phone(선택)·**nationality(필수, 국기 이모지 `<select>` 9개국, `src/data/nationalities.ts` 단일 소스: `NATIONALITIES`{name(=저장 한국어 키)·en·flag}/`NATIONALITY_NAMES`(검증)/`nationalityLabel`(국기+한국어, admin)/`nationalityLabelEn`(국기+영문, 강사용 폼). 저장값은 한국어 국가명, 강사 폼은 영문·admin은 한국어 표시)**·**gender(필수, `<select>` 남성/여성, `src/data/genders.ts` 단일 소스: 코드 male/female 저장, `GENDER_VALUES`(검증)/`genderLabelEn`(폼)/`genderLabelKo`(admin))**·**center(필수, `<select>` 플레이스홀더(`value=""`)+**None sentinel(`value="none"`)**+centers, `center_id` FK 저장 — HTML `required`가 빈 `""`를 미선택 취급하므로 "소속 없음"은 별도 sentinel `"none"`으로 명시 선택; admin이 `/admin/centers`에서 관리하는 `centers` 테이블, 폼은 centers props로, 서버는 `"none"`→null·그 외 `resolveCenterId`(`src/lib/center.ts`)로 uuid+존재 검증, 빈 값 차단)**·**bio(필수, About/Bio)**·**experience(필수)**·**zoom_url(필수, http/https)** — action이 `name=[first,last].join(" ")` 조합 저장, props로 `userId`·`initialAvatarUrl`·`initialFirstName`·`initialLastName`·`initialPhone`·`initialNationality`·`initialGender` 받음), 거절이면 상단에 admin_note 사유 표시 후 재신청 허용 — **재신청 시 이전 지원서(`teacherApp`) 값으로 폼 프리필**(bio·experience·zoom_url 포함, `teacherApp?.x ?? profile?.x ?? ""`; 첫 지원은 프로필 값만), 폼이 `initialBio`/`initialExperience`/`initialZoomUrl` 옵셔널 props 수신. **제출은 2단계 확인 다이얼로그**(리뷰→최종확인): 리뷰 `AlertDialog`의 dl 최상단에 **"Profile image" 라벨 행**(썸네일 `avatarPreview`, blob 대응 `unoptimized`)으로 사진까지 검토 후, 최종 확인 시 `handleFinalConfirm`로 업로드+제출.

**강사 지원→승인 flow** (역할 부여): 직접전환 폐지. 학생이 `/teacher/apply`에서 지원 → `teacher_applications` 저장 → admin이 `/admin/teacher-requests`에서 승인(role=teacher)/거절. 액션 `submitTeacherApplication`(`src/app/teacher/apply-actions.ts`): 가드 (1)로그인 (2)role≠teacher/admin (3)필수값(first/last·nationality(화이트리스트)·gender(화이트리스트)·**center(필수: `"none"`=null 허용·빈값 차단·그 외 uuid 존재검증)**·bio≥10·experience·zoom_url·avatar) (4)`rateLimit(teacher-apply:IP)` (5)기존 '신청' 중복 차단 (6)본인 세션 client insert(`user_id`=getUser, RLS `_insert`=uid 일치) → best-effort `sendTeacherApplicationNotification` + `revalidatePath("/teacher/apply")`. 메일러는 `applications`용과 별개 함수(`src/lib/mailer.ts`).

**강사 페이지** (`/teacher`, `src/app/teacher/(dashboard)/`, **UI 영문**, **탭 구조**(마이페이지와 동일 패턴)): route group `(dashboard)`의 공유 `layout.tsx`가 **role 가드**(`!user`→`/login?next=/teacher`, role∉{teacher,admin}→`/` — admin 허용) + 라벨바·웰컴 배너 + `<TeacherTabs>`(`src/components/teacher/`, client `usePathname`: **Profile `/teacher` · Enrollment Requests `/teacher/requests` · My Classroom `/teacher/classroom`**). **`/teacher/apply`는 그룹 밖**(탭 레이아웃 미적용). **① Profile 탭**(`(dashboard)/page.tsx`)이 `profiles` 본인 row(**first_name·last_name**·avatar_url·zoom_url·bio·experience·phone·nationality·gender) 조회 → `<TeacherProfileForm>`(client) + 주간 가능시간(`AvailabilityModal`). **이름은 first/last 분리**(원어민 강사 위주 + admin 성 기준 정렬), 표시명=`[first,last].join(" ")`, 웰컴 호칭/마이페이지 등 친근한 호칭은 first 우선. 구 `full_name` 컬럼은 비파괴 보존(앱 미사용). **프로필 사진**: 브라우저 client로 `avatars/<uid>/avatar-<ts>.<ext>` 업로드(upsert)→publicUrl→`updateTeacherAvatar` 액션 저장(`useTransition` 낙관적, `next/image`로 미리보기). **변경 시 이전 파일 정리**: 공유 헬퍼 `src/lib/avatar.ts`(`uploadAvatar`→`{publicUrl,path}`, `cleanupOldAvatars(uid,keepPath)`=본인 폴더 list 후 keepPath 제외 remove, **best-effort**)를 강사 프로필·강사 신청폼 공용 사용 — 프로필은 **DB 반영 성공 후** 정리, 신청폼은 업로드 직후 정리(타임스탬프 파일명 유지=캐시 무효화, 누적만 제거). RLS: `avatars_public_read`(list)·`avatars_delete_own`(remove) 본인 폴더 허용. ⚠️ 승인 후 사진 변경 시 과거 `teacher_applications.avatar_url` 파일도 삭제돼 admin 신청목록 historical 썸네일은 깨질 수 있음(수용). **텍스트 폼**: `useActionState`+`updateTeacherProfile`, controlled inputs. **강사 편집 가능=bio·**experience**·phone(선택)·**zoom_url**만**; **first_name·last_name·nationality(국기 `<select>`)·gender(`<select>`)·center(`<select>` None+centers)는 강사 수정 불가**(승인 시 확정, 변경은 admin `TeacherRequestsManager`/승인 RPC로만) — **항상 `disabled`**(Edit 모드에서도 잠김, 표시 전용) + **hover 커서 규칙: 보기 모드=전부 일반 커서, 편집 모드에서만 잠금 필드(이름·국적·성별·센터·**이메일**)에 not-allowed**(보기 모드는 전체 수정 불가라 not-allowed가 중복 신호 → 제거). 구현: 잠금 필드 className `cn(VIEW_TEXT, editing && LOCKED)`(셀렉트는 인라인 cn 마지막 arg `editing && LOCKED`) — **`LOCKED="disabled:pointer-events-auto disabled:cursor-not-allowed"`**(Input 기본 `disabled:pointer-events-none`가 hover를 막으므로 auto로 되돌려 커서 노출), **`VIEW_TEXT`에 `disabled:cursor-default` 포함**(보기 모드·Textarea 기본 `disabled:cursor-not-allowed`를 덮어 일반 커서, 편집 모드엔 tailwind-merge로 LOCKED가 default를 덮음). **`VIEW_TEXT`에 `disabled:bg-transparent`도 포함** — shadcn `Input`/`Textarea` base의 `disabled:bg-input/50`(회색) 제거해 보기 모드 필드 배경을 투명 통일(native `<select>`는 원래 `bg-transparent`, Email도 `disabled:bg-transparent disabled:opacity-100`로 맞춤). 배경 회색 음영은 폼 필드에 안 씀(사진 썸네일 박스 `bg-surface`는 별개). **`*`(필수 표시)는 편집 모드(`editing`)에서만 라벨에 노출**(보기 모드는 어차피 수정 불가라 `*`가 무의미한 신호 → 제거, `LOCKED` 커서 규칙과 동일 철학). 대상=이름·국적·성별·**센터**·**프로필사진**·bio·experience·zoom_url(잠금 필드 포함, 전화만 optional). **프로필 사진도 동일**(폼 밖 상시-변경 섹션이나 필수 강제는 텍스트 폼 저장 시점=편집 모드라, 보기 모드 일관성+강제 시점 일치 위해 `editing` 게이팅). 구현=모든 필수 라벨 `{editing && <span className="text-brand">*</span>}`. + "Teacher Info" 헤딩 아래 admin-only 안내 문구. **보기/편집 모드**(`editing` state): 편집 가능 필드도 기본 `disabled`(보기) + "Edit" 버튼, Edit 클릭 시 그 4개만 편집 + Save/Cancel 노출. 진입 시 값 스냅샷(`snapshotRef`) → Cancel은 복원 후 보기 모드, Save 성공(`state.ok`) 시 보기 모드 복귀. 프로필 사진은 별도 섹션이라 편집 모드 무관(상시 변경). **필수(강사 편집분)**: 프로필사진·bio·experience·zoom_url(전화번호 선택; 이름·국적·성별은 강사 미편집이라 폼 필수 대상 아님): 클라 native `required`+라벨 빨강 `*`로 빈 제출 차단(**프로필사진은 폼 외부 업로드라 native 검증 불가 → 텍스트 폼 `onSubmit`에서 avatar 없으면 preventDefault+toast로 저장 차단**), 서버 액션이 `clean()` 후 누락 시 "Please fill in all required fields." 반환(공백-only/우회 차단). 액션(`src/app/teacher/actions.ts`)은 첫 줄 `requireTeacher()`(role teacher∥admin) 후 **service_role로 본인 id의 화이트리스트 컬럼만 update(=`bio`·`experience`·`phone`·`zoom_url`만; name·nationality·gender·center_id·role은 미포함 — 강사 폼 우회 방어, 이 4항목은 서버가 읽지도 않음)** + `revalidatePath("/teacher","layout")`. 아바타 액션은 URL이 본인 폴더(`/avatars/<uid>/`)인지 검증. **주간 가능시간**: 프로필 폼 아래 `<AvailabilityModal>`(`src/components/teacher/`, client) — **요약 카드**(제목·등록 슬롯 수 + "Edit availability" 버튼)를 두고, 클릭 시 **모달**(커스텀 오버레이+패널, Navbar 슬라이드 메뉴식 a11y: Esc·body scroll lock·focus 복귀·`inert`, 오버레이 `z-[110]`·패널 `z-[120]`) 안에서 `<AvailabilityGrid>`를 편집. 모달이 **열림/닫힘 + 미저장 경고**를 소유(닫기=X·오버레이·Esc → dirty면 "Close without saving?" `AlertDialog` 경유, **조건부 렌더라 discard=그리드 언마운트**로 폐기; Esc/오버레이는 확인 모달 열림 시 무시). `<AvailabilityGrid>`(client)는 부모 `<details>` 결합을 끊은 **순수 에디터**(닫기 책임 없음) — `onDirtyChange(dirty)` prop으로 dirty를 모달에 통지, 06:00~24:00 30분 슬롯, 요일 **월~일 표시**(`DISPLAY_DAYS=[1,2,3,4,5,6,0]`로 저장 day(0=일)↔표시 매핑, 라벨 Mon~Sun), 클릭·드래그 선택(pointer 이벤트+`elementFromPoint` 마우스/터치 통합, 셀 `touch-none`, 선택 셀 `bg-progress`), **frozen-pane**(외곽 `max-h-[65vh] overflow-auto` 양축 스크롤 박스 + 요일 헤더 행 `sticky top-0 z-20 bg-white` + 시간 열 `sticky left-0` → 세로 스크롤해도 요일 헤더 고정·가로 스크롤해도 시간 열 고정. 드래그 선택은 박스 가시범위로 제한), `useTransition`+**`updateTeacherAvailability(slots)`**(actions.ts)로 저장(영문 toast, dirty 시에만 Save 활성). 그리드 내 **확인 모달 3종**(Save/Revert/Clear all, controlled `AlertDialog`, **콘텐츠 `z-[130]`로 모달 위에 표시**) + 닫기 가드(Close without saving)는 모달 래퍼 소유. **Revert 버튼**(dirty일 때만 활성)은 "Discard unsaved changes?" 모달 거쳐 saved 스냅샷으로 복원. 그리드 표시범위는 컴포넌트 상수(`START_HOUR`/`END_HOUR`)에만 — DB 비종속. 액션은 `requireTeacher()` 후 service_role로 **본인 슬롯 전체 delete→bulk insert**(빈 선택=전부 비움), 검증(day 0-6·min 30배수·중복제거·cap 7×48)+`revalidatePath("/teacher","layout")`. `readOnly` prop으로 admin 뷰 재사용(편집/저장/모달 미부착, 가용 셀 `bg-accent-blue/40`). **② 수강신청 관리 탭**(`(dashboard)/requests/page.tsx`): `<TeacherEnrollments>`(`src/components/teacher/`, client, **UI 영문**) — page가 본인 `enrollments`(RLS `_select_own_teacher`, `student_english_name` 포함, `신청`(대기) 우선 정렬) 조회해 전달. **정렬 가능 테이블**(`SortHeader` Student/Start/Applied + Status/Course 컬럼) — 학생명은 `studentLabelOf`로 **`영문명(한글명)`**(영문명 없으면 한글명), 정렬키 `student`도 영문명 우선. **시간충돌 표시**(본인 진행중 신청끼리 슬롯 겹치면 `overlappingIds`로 행 좌측 앰버 액센트+"Time conflict" 칩, 거절/취소 제외). **행 클릭/Enter→`EnrollmentDetailModal`**(같은 파일 내 컴포넌트, 커스텀 오버레이 `z-[110]`+패널 `z-[120]`, a11y Esc·body scroll lock·닫기 버튼 포커스) — 상태 배지 Pending/Approved/Payment pending/Paid/Declined/Cancelled(`결제완료`=Paid, 강사는 표시만·확인 액션 없음; **환불 건은 "Refunded" 앰버 배지** — page가 `createAdminClient()`로 본인 enrollment의 환불 payment 조회해 `refunded` 전달) + `dl`(학생·과정·**Weekly schedule(`summarizeSlots(_,false," / ")` 영문·요일 `/` 구분)·Class period(시작~종료=`lessonEndDate`)·Sessions(`total_sessions`; page가 전달)**) + 충돌 시 경고 노티스. **승인/거절은 모달 안에서 `신청` 행만**(처리완료 read-only), 거절 사유 필수(`toast` 검증), controlled `AlertDialog` 확인 후 처리·낙관적 갱신·`onClose`. 액션 `approveEnrollment(id)`(**승인 시 `결제대기`로 전환**)/`rejectEnrollment(id,note)`(`actions.ts`, `requireTeacher()` → **소유권(`teacher_id=self`)+상태 가드(`.eq("status","신청")` 0행=이미 처리)** → service_role `enrollments` update → **best-effort 학생 SMS**(`student_phone` 스냅샷, `sendSms` 한국어 승인/거절 안내, 실패 로깅·롤백 없음) + **best-effort 관리자 이메일**(독립 try/catch, `getAdminEmails()`→승인=`sendEnrollmentApprovedToAdmin`(결제대기 전환→입금 확인 유도)·거절=`sendEnrollmentRejectedToAdmin`(사유 포함); 공용 헬퍼 `buildAdminEnrollmentEmail`이 `loadOwnPendingEnrollment` 확장 select(course·teacher_name·student_english_name·slots·total_sessions)로 `summarizeSlots`·`lessonEndDate`(승인만)·`getCourse().englishTitle` 조립) + `revalidatePath("/teacher","layout")`). **③ My Classroom 탭**(`(dashboard)/classroom/page.tsx`): `loadClasses(supabase,user.id,true)` → `<ClassroomList isTeacher>`(화상수업 항목 참조). ⚠️ 라우트 분리(마이페이지·강사 모두)로 enrollment/class 상태 변경 액션은 `revalidatePath("/teacher"|"/mypage", "layout")`(하위 탭 전부 갱신)을 사용.

**admin 대시보드** (Phase 5, `src/app/admin/`): `layout.tsx`(server)가 **role 가드**(`!user`→`/login?next=/admin`, `!isAdmin`→`/`) + `AdminNav`(client, **8탭**). 8 섹션(`/admin` 인덱스 `page.tsx`는 `redirect("/admin/enrollments")`) — **`enrollments/`(수강신청 관리)**, **`classes/`(화상수업 관리)**, `members/`, **`teacher-requests/`(강사 승인/삭제 + 강사 시간표 열람)**, **`centers/`(센터 CRUD)**, **`revenue/`(매출 현황)**, **`settlements/`(강사 정산 리포트)**, `youtube/`. **매출 현황**: `revenue/page.tsx`(server)가 `payments` 전량 + `enrollments` 스냅샷(course·teacher·student·is_test) 병합 + settings 환율 조회(service_role, payments RLS 우회)→`RevenueRow[]`(**`failed`·`is_test` 결제 제외**, `created_at`→KST `kstDate` 파생)+rates로 `<RevenueManager>`(client, `src/components/admin/`) — **읽기 전용 모니터링**. `SettlementsManager`의 기간 토글(일간/주간/월간·이전/다음/오늘)·`aggregate`·`SortHeader`·`ToggleChip` 패턴 재사용(TZ 날짜 헬퍼 인라인 복제, 기준 필드 `kstDate`). 구성: **KPI 카드**(총매출·환불·순매출(=amount−cancelled_amount, KRW 환산)·결제 건수) + **일자별 순매출 세로 막대 차트**(자체 SVG, 단일 시계열·per-bar 툴팁) + **분류별 집계 테이블**(과정별/**센터별**(강사 현재 소속 센터 역산 `enrollments.teacher_id→profiles.center_id→centers.name`, 미지정=미지정 센터)/결제수단별/학생별 `ToggleChip`, 순매출 내림 기본정렬) + **결제 트랜잭션 목록**(결제일·학생·과정·수단(`payMethodLabel`)·금액·순액·상태 배지(결제완료/부분환불/환불)·영수증 링크, 검색+상태 필터) + **CSV 내보내기**(클라 Blob, BOM으로 엑셀 한글 대응). 환불 등 결제 액션은 여기 없음(수강신청 탭에서 수행). 마이그레이션 불필요. **수강신청 관리**: `enrollments/page.tsx`(server)가 전체 `enrollments`(**`teacher_id` 포함**, `created_at desc`) service_role 조회→`<EnrollmentsManager>`(client, `src/components/admin/`) — 상태 필터 탭(전체/신청/결제대기/결제완료/승인/거절/취소/**환불**)(**환불=DB 상태 아님, 파생**: `status='취소' && payment.status∈{cancelled,partial_cancelled}` — 환불 건을 `취소`에서 분리해 앰버 "환불" 배지·별도 필터·카운트로 표시. 학생 마이페이지="환불됨"·강사="Refunded" 동일 파생, 공통 배지색 `bg-[#FFF4E5] text-[#B45309]`. 강사 화면은 payments RLS 미허용이라 page가 `createAdminClient()`로 환불 id 조회)+검색(학생·강사·과정)+**정렬 테이블**(`SortHeader` 학생/강사/**센터**(강사 현재 소속 센터 역산 `teacher_id→profiles.center_id→centers.name`, 미지정 폴백·검색 포함)/시작일/신청일; **시작일 열 아래 `summarizeSlots(slots)` 요일·시작시각 요약**) + **prop→state 동기화**(`useEffect(()=>setRows(enrollments),[enrollments])` — 테스트 신청 생성 등 `router.refresh()` 후 목록 즉시 반영) + **시간충돌 표시**(같은 `teacher_id` 진행중 신청끼리 슬롯 겹치면 `overlappingIds`로 "시간 겹침" 칩+좌측 앰버 액센트, 전체 rows 기준 계산이라 필터 무관). **행 클릭→`EnrollmentDetailModal`**(커스텀 오버레이 패널, **주간 일정**(`summarizeSlots`)·**수업 일정**(시작~종료=`lessonEndDate(start,slots,total_sessions??24)`)·**수업 횟수**(`total_sessions??24회`)·**센터**·전화·**수강료**·사유 표시 — `priceLabel`은 page가 `price_krw != null ? formatPrice(price_krw) : getCourse(course).price`로 파생(+`priceKrw` 숫자)해 `AdminEnrollment`에 실어 전달, 학생 결제대기 패널과 동일 유효가격). **입금 확인(결제 1단계)**: 모달에서 `결제대기` 행에 **"결제 확인"** 버튼+`AlertDialog`(입금 대조용 **"입금 확인 금액: {priceLabel}"** 표기)→`confirmPayment(id)`(`actions.ts`, 상태 가드→`결제완료`+학생 SMS+강사 결제확정 알림 메일). **개입=강제 취소**: 모달에서 `신청`/`승인`/`결제대기` 행에 사유 입력+`AlertDialog`→`adminCancelEnrollment(id,note)`(`requireAdmin()`→service_role, 상태 가드→`취소`+`teacher_note="[관리자] …"`, 학생 SMS best-effort, `/admin/enrollments`·`/teacher`·`/mypage` revalidate). **환불(카드 결제)**: page가 `payments`(enrollment_id별 최신) 병합해 `AdminEnrollment.payment{paymentId,status,amount,cancelledAmount}` 전달 → 모달에서 `결제완료`+`payment.status='paid'`면 **"환불" 버튼**(사유 필수+환불 금액(비우면 전액·부분 가능)+`AlertDialog`)→`refundPayment(id,{reason,amount?})`(`actions.ts`, `requireAdmin`→payments 조회(`method` 포함)→**무통장(`method='bank_transfer'`∥`payment_id`가 `bank-` 접두)이면 `cancelPortonePayment` 건너뜀**(PG 취소 불가=실제 환불은 계좌 수동 송금, 모달에 "계좌로 직접 송금" 안내)·카드면 PortOne 취소→`refundEnrollmentPayment`=수강 취소·미래 수업 취소·학생 SMS·강사 메일·`payment_refunded` 이벤트). 환불된 결제는 `환불됨: ₩N (부분)` 배지. `거절`/`취소`는 read-only. **[개발용] 테스트 수강신청 생성**: `/admin/enrollments` 상단 `<TestEnrollmentCreator>`(`src/components/admin/`, props teachers·courses·**students**(후보=가입 회원 중 강사·이메일 미인증 제외, `page`가 `listUsers`+`profiles` merge로 `{email,label}` 구성)·본인 이메일) — 모달에서 **학생 `<select>`**(가입 회원 드롭다운, 라벨 `이름 (이메일)`, 기본=본인 계정; 선택값 이메일을 액션에 전달, `createTestEnrollment`는 기존대로 `findUserIdByEmail` resolve)·**과정(등록 5종 `<select>` + "직접 입력…" 센티넬 `__custom__` 선택 시 한글·영문 과정명 2칸 입력, 커스텀은 `course="custom"` 슬러그+`course_title`/`course_english_title` 스냅샷)**·`EnrollScheduleField`로 요일·시간 선택→**그 시간에 가능한 강사 라이브 검색(`teacherHasAllSlots`, 실 위저드와 동일 패턴, `loadEnrollTeachers` 카드)·택일**·**시작일 자유**·**수업 횟수(1~60)**·**수강료(원, 기본 `COURSE_PRICE_KRW`)** 입력→`createTestEnrollment`(`actions.ts`, `requireAdmin`, **실 동선의 D+3/D+14·폰·영문 가드 우회**, service_role insert: status `신청`·`total_sessions`·**`course_english_title`·`price_krw`**·`is_test=true`, 강사/학생 스냅샷). 이후 승인→결제확인은 일반 흐름과 동일(결제확인 시 클래스 개수=`total_sessions ?? TOTAL_SESSIONS`). 생성분은 `EnrollmentsManager` 목록에 **"테스트" 칩** + 모달 **"테스트 삭제"**(`deleteTestEnrollment`, `is_test=true`만 하드삭제→classes FK cascade 동반 제거). **화상수업 관리(2단계)**: **①상위 `classes/page.tsx`**(server)가 전체 `classes`를 좁은 select로 조회→**`enrollment_id`별 JS 집계**(과정 인스턴스=수강신청 1건당 1행, 클래스 스냅샷으로 과정·학생·기간·진행 카운트 산출(**대표 강사는 `enrollments.teacher_name`(현재 담당, 강사 대체 반영)로 override** — 과거 회차 스냅샷은 옛 강사라 부적합); `upcoming`=**시간 기반** `예정 && kstDateMinToMs(session_date,lessonEndMin(end_min))≥now`(강사/수강생 강의실과 동일 — 수업 종료 시각 지나면 `done`, 날짜 단위 아님)/`done`/`cancelled`/`makeup`)→`<ClassEnrollmentsManager>`(client) — 검색(학생·강사·과정)+상태 필터(전체/진행중(`upcoming>0`)/완료/**환불**)+정렬(과정/학생/강사/시작일)(**환불**=page가 enrollments select에 `status` 추가해 `취소`/`거절`이면 `refunded` 플래그 → 배지 "환불"(앰버)·별도 필터, `진행중`/`완료`에서 제외 — 예정 수업 전무해 `upcoming=0`이면 무조건 "완료"로 오표시되던 문제 교정), 행=과정·학생·강사(**이름 아래 소속 센터 역산**)·기간(`firstDate~lastDate`+**아래 주간 요일·시작시각 요약** `schedule`=**`enrollments.slots`(현재 주간 템플릿, 일정 변경 시 갱신) → `summarizeSlots`** (page가 map 구성 후 enrollment id들의 `slots` 조회해 파생; slots 비면 클래스 session_date 파생 `slotSets` 폴백 — **과거 회차가 옛 스케줄로 오염시키던 문제 방지**)) + **지금 진행 중 라이브 표시**(집계가 `liveStartMs`(가장 이른 미종료 회차 입장 15분 전)·`liveEndMs`(레슨 종료) 파생 → 클라 **1분 틱** `isLive(now)`면 상태 셀 `bg-cta animate-pulse` "● 수업 중" 배지 + **라이브 과정 항상 최상단 안정 정렬**(사용자 컬럼 정렬 위에 고정); 상세 뷰 "진행중" 기준과 동일)·진행바(done/active)+예정·취소·보강 칩, **행 클릭→`/admin/classes/[enrollmentId]`**(router push). **목록/주간 뷰 토글**(`view` state, 세그먼트 pill 기본 목록): **주간 뷰=`<ClassWeekGrid>`**(`src/components/admin/`, client — page가 `classes` per-session 행에서 **진행 예정만**(취소·연기 제외, 보강 포함) `AdminSession[]`(enrollment·과정·강사·학생·`sessionDate`·`start/endMin`·`isMakeup`·**`weekdays`**=과정 주간 요일 요약 "월/수/금") 매핑해 prop 전달, 신규 쿼리 없음; **`weekdays`는 목록 뷰 `schedule`과 동일 소스(`enrollments.slots`)를 `summarizeWeekdays`로 파생**(slots 비면 `slotSets` 폴백)). 학생/강사 `WeekSchedule` 격자 스캐폴딩 차용(월~일 06:00~24:00 `ROW_MINS`·주 네비·주말색·오늘 강조·`mondayOf`)하되 **블록 대신 30분 슬롯 격자** — admin은 여러 수업이 겹쳐 수업당 블록이 안 맞음: `byDaySlot`(요일칼럼×`rowMin`→그 슬롯에 겹치는 세션들, 60분 수업=2칸 점유)로 각 칸에 **수업 개수** 표시(개수별 색 강도 1→`bg-accent-blue-soft`·2→`/30`·≥3→`/50` 흰글씨, **지나간 슬롯**(`now≥kstDateMinToMs(date,min+SLOT_MIN)`)은 배경 `bg-surface`·칸 `bg-rule/60` 회색 음영, 단 **미진행 종료 수업**(세션 종료 && `conductedOverride ?? !!conductedAt`=false)이 하나라도 있는 칸은 붉은색 `bg-brand/15 text-brand`로 강조) → 칸 클릭 **`SlotModal`**(커스텀 오버레이 `z-[110]`/패널 `z-[120]`·Esc·scroll lock, 그 시간 수업 목록=시간·과정(**과정명 옆 주간 요일 `weekdays` "월/수/금"** — 개별 회차 시각은 이미 노출돼 요일만, 시간 제외)·강사·학생·**센터**(강사 소속 센터 역산)·보강 배지·**대체 배지**(`teacher_reassigned_at` 있으면, `bg-cta/10 text-cta`, `AdminSession.teacherReassignedAt`) + **종료 수업엔 진행여부 배지**(`now≥lessonEnd`이면 유효 진행여부 `conductedOverride ?? !!conductedAt`로 초록 "진행됨"/brand "미진행", `ClassesManager` "진행" 컬럼·정산과 동일 기준 — `AdminSession`에 `conductedAt`/`conductedOverride` 포함), **행 클릭→`/admin/classes/[enrollmentId]`** 드릴인) + **수업 일정(`AdminSession.period`=`scheduleDateRange`)·수업 횟수(`totalSessions`)** 표시. **`ClassWeekGrid`는 `readOnly`·`onReassign?` prop으로 센터 매니저 재사용**: `readOnly`면 행 클릭 드릴인 대신(센터 매니저는 admin 상세 접근 불가) `onReassign`+**미래 회차에 "강사 대체" 버튼 인라인**(→`ReassignModal`); `AdminSession`에 `classId`·`teacherId`·`sessionNo`(대체 대상) 포함, admin은 `readOnly`/`onReassign` 미전달=기존 드릴인 유지. ⚠️ per-session 무제한 로드(기존 목록 쿼리 공유, PostgREST ~1000행 cap) — 대량 시 주 단위 날짜 필터가 scale path. ⚠️ JS 집계는 중규모까지 적정 — 대규모 시 **DB 집계 뷰/RPC**로 전환(상위 쿼리를 과정 수만큼만 로드)이 scale path. **②상세 `classes/[enrollmentId]/page.tsx`**(server)가 `eq("enrollment_id")`로 **scoped 조회**(0행=`notFound()`) + **이 enrollment의 `enrollment_events`(감사 로그) `created_at desc` 조회**→`<ClassesManager>`(아래에 읽기 전용 **`<EventTimeline events>`**(`src/components/admin/`, client)로 변경 이력 렌더=생성·승인·결제·연기·강사 대체·일정 변경 등 최신순, 행=KST 시각·이벤트 라벨·`detail` 요약(`summarize()` 이벤트별 매핑; **일정 변경=`oldSlots→newSlots`를 `summarizeSlots`로 "화 12:30 · 목 12:30 · 토 12:30 → 월 12:30 · 수 12:30 · 금 12:30" 표시**, 강사 대체=old→new 강사명 등)·액터 역할/이름; `EVENT_META` 타입→한국어 라벨/색 매핑)(client, `title`/`subtitle`/`backHref` props로 헤더+"← 목록으로"; **`subtitle`은 page가 `currentSlots`(=`enrollments.slots`)에서 파생한 주간 요일 요약(예 "월수금")을 강사~기간 사이에 포함**(slots 비면 정규 수업 날짜 `dowOf` 폴백 — 일정 변경 반영·과거 회차 오염 방지; **강사명·`teacherSlots`(남은 일정 일괄 변경 검증용 가용)는 `enrollments.teacher_id/teacher_name`(현재 담당) 기준** — `first.teacher_id`(과거 회차 스냅샷=옛 강사) 대신, 강사 대체 후 반영)=`강사 … · 센터 …(강사 현재 소속 센터 역산) · 요일 월/수/금 · 기간 … · 총 N회`) — 상태 필터(전체/예정/완료/취소)+검색+정렬 테이블(`SortHeader` 날짜/강사/회차 + 시간·**피드백** 컬럼, **과정·학생 컬럼은 제거**(한 enrollment 스코프라 중복, 헤더 title/subtitle에 표시), **날짜 셀·상세 모달 날짜 필드는 `formatDateKo`로 요일 병기**(`2026-07-04 (토)`, 정렬키는 문자열 날짜 그대로), 시간=`timeRange(start,end)`=시작~**레슨 종료**(`lessonEndMin`=end−5 → :25/:55, 내 강의실 등과 표시 통일; 슬롯 점유·편집 `<select>`는 30분(end) 그대로), `is_makeup`이면 "보강" 칩; **강사 열엔 `teacher_reassigned_at` 있으면 "대체" 칩(cta) — select·`AdminClass`에 `teacher_reassigned_at` 포함, in-modal 강사 대체(`doReassign`) 낙관 갱신도 스탬프**). **상태 배지·필터·카운트는 시간 기반 파생**(`displayStatus(r,now)`: 취소면 취소, `now≥kstDateMinToMs(session_date,lessonEndMin(end_min))`면 **완료**, `now≥`입장가능(`start_min−ENTRY_LEAD_MS`=시작 15분 전)이면 **진행중**(`bg-cta`+`animate-pulse` 필드 배지, 필터 탭 포함, 입장 시간창과 일치), 아니면 예정 — DB `status`엔 완료 없음, 1분 틱; 강의실·상위 목록과 동일 기준. 배지색 예정=파랑·완료=회색·취소=빨강). **피드백 컬럼="보기" 버튼**(`Eye`, 피드백 있으면 활성·없으면 disabled, `stopPropagation`으로 행 클릭과 분리)→`FeedbackModal`(피드백 전용 읽기 모달, 커스텀 오버레이 `z-[110]`/패널 `z-[120]`·Esc·scroll lock, 작성일+전문 `whitespace-pre-wrap`). 모달 편집(일정변경/취소/강사대체)은 DB `status='예정'` 기준이되 **회차 상태별로 노출을 좁힘**(`ClassDetailModal` 파생: `isEnded=displayStatus==="완료"`(레슨 종료 시각 지남)·**`hasStarted=now≥kstDateMinToMs(session_date,start_min)`(실제 시작 시각 지남)**·`isConducted=`conducted_override ?? !!conducted_at``(**유효 진행여부**=정산·마커와 동일 기준; 자동 진행됨이라도 admin이 미진행 override 시 미진행 취급)): **①미시작(미래 예정) = 강사 대체+연기/취소 2종 모두**, **②시작 후(진행중·종료 미진행) = 강사 대체 숨김+"이미 시작된 수업이라…" 안내문, 연기·취소만**(**강사 대체는 `!hasStarted` 게이팅** — 시작 시각 지나면 숨김; 노쇼 사후 처리 연기/취소는 진행중·종료 후에도 가능), **③진행됨(`isConducted`) = 완전 읽기전용**("진행 완료된 수업입니다"). + **과정 전체 종료 시 편집 자체 미노출**(`courseEnded`=ClassesManager가 `loadEndedEnrollmentIds` 기준을 클라 파생: 남은 미래 `예정` 수업(`now<kstDateMinToMs(date,lessonEndMin(end))`)이 하나도 없으면 종료, 단일 enrollment 스코프라 rows 전체가 한 과정). **행 클릭→`ClassDetailModal`**(커스텀 오버레이 `z-[110]`/패널 `z-[120]`): 위 노출 규칙에 따라 액션(미래 예정=2종) — ⓐ**수업 연기/취소**(라디오 3모드+"적용" 버튼, "남은 연기 N/6" 표시)→`adminCancelClass(id,reason)`(`reason`: **`student`**=수강생 요구 연기(보강 O·`cancel_reason='student'`로 남은 연기 차감)·**`company`**=강사/회사 사유 연기(보강 O·미차감)·**`cancel`**=수업 취소(보강 X); 상태 `예정` 가드+**유효 진행여부='진행됨'이면 거부**(`conducted_override ?? conducted_at`, 종료-미진행·미래 예정은 통과)→`취소`+`cancel_reason` 기록+연기 모드만 `createMakeupClass`+강사 이메일 best-effort; **6회 한도·1시간 컷오프 하드 미적용**=student 모드 남은 연기 0이어도 경고 후 진행, 차감은 카운트로 자연 반영. `postponedCount`(=학생 연기 수) prop으로 남은 연기 표시) ⓑ**강사 대체**(강사 사정으로 특정 회차만 다른 강사로 교체, 상세 page가 `loadEnrollTeachers()`로 전체 강사 카드 prop 전달→모달이 이 수업 요일·시간에 가용한 강사만 `teacherHasAllSlots` 클라 필터·원 강사 제외해 `<select>` 노출)→`adminReassignClass(id,newTeacherId)`(**공유 코어 `reassignClassCore` `src/lib/reassign.ts`에 위임** — 센터 매니저 `centerReassignClass`와 공용, admin은 센터 제약 없음; 상태 `예정` 가드+**시작 시각 전만**(`now≥kstDateMinToMs(session_date,start_min)`이면 차단 — 이미 시작/종료 수업 대체 방지, 클라 `!hasStarted`와 동일 기준)+새 강사 `role='teacher'`·**주간 가용 검증**+**같은 날짜 새 강사 시간 충돌 차단**→`teacher_id`/`teacher_name`+`teacher_reassigned_at`+**`original_teacher_id`(=덮어쓰기 전 원 강사, `cls.original_teacher_id ?? oldTeacherId` coalesce로 최초값 보존)** update; 시간·학생 불변이라 입장 시 zoom은 `teacher_id`로 실시간 조회돼 새 강사로 라우팅·새 강사 My Classroom 노출/원 강사 제거 — **마이그레이션 불필요**; best-effort 학생 SMS+기존/새 강사 메일 `sendClassReassignToOld/NewTeacher`). 셋 다 `/admin/classes`·`/admin/classes/[enrollmentId]`·`/teacher`·`/mypage` revalidate. `취소` 행은 read-only. **남은 일정 일괄 변경(과정 단위, 담당 강사 유지·요일/시간만)**: 상세 헤더에 **"남은 일정 일괄 변경"** 버튼(미래 `예정` 수업 있을 때만; 상세 page가 `enrollments.slots`(현재 패턴)+담당 강사 `teacher_availability`를 `currentSlots`/`teacherSlots`/`enrollmentId` prop으로 전달)→`RescheduleRemainingModal`(`EnrollScheduleField`로 새 요일·시각(**`initialSlots={currentSlots}`로 현재 주간 스케줄 프리필** — 진입 시 요일·시작시각이 현재값으로 선택돼 시작일만 바꿔 즉시 제출 가능) + 적용 시작일 date input(**KST 내일(D+1)~오늘+7일(D+7)** — 오늘 배치 시 과거 시각·같은 날 중복 혼선 방지 + 과도한 미래 이동 방지, `min`/`max`+`canSubmit` 범위 가드) + 강사 가용 밖이면 경고·제출 차단)→`adminRescheduleRemaining(enrollmentId,newSlots,effectiveDate)`(`actions.ts`, `requireAdmin()`→**적용 시작일 범위 재검증**(`effective <= todayKst()`∥`effective > addDaysStr(todayKst(),7)` 거절)→**미래 `예정` 클래스만** 대상(`session_no` 오름차순, 과거·완료·취소 유지)+강사 주간 가용 `teacherHasAllSlots` 검증→`enumerateLessonSessions(effectiveDate,newSlots,남은수)`로 **1:1 remap**(행 id·회차 유지=진행률 불변, session_date/start/end만 update)+**같은 강사·학생의 다른 enrollment `예정` 클래스와 시간 충돌 차단**+`enrollments.slots`=newSlots 갱신(향후 가용/요약 반영)). 성공 시 `router.refresh()`; ClassesManager는 `useEffect(()=>setRows(classes),[classes])`로 새 prop 동기화. `/admin/classes`·`/admin/classes/[id]`·`/admin/enrollments`·`/teacher`·`/mypage` revalidate. **남은 수업 전체 강사 대체(과정 단위, 강사 중도 하차 — enrollment 이관)**: 상세 헤더 "남은 일정 일괄 변경" 옆 **"남은 수업 전체 강사 대체"** 버튼(미래 `예정` 있을 때만)→`ReassignRemainingModal`(남은 클래스 요일·시각 union(rows 파생)에 가용한 강사만 `teacherHasAllSlots` 필터·원 강사 제외 `<select>` + **적용 시작일 필수(D+1 기본, D+1~D+7)**)→`adminReassignRemaining(enrollmentId,newTeacherId,effectiveDate)`(`actions.ts`, `requireAdmin()`→새 강사 `role='teacher'` 검증→남은 미래 `예정` 클래스(`session_no` asc)→**주간 패턴 `weekly`=`enrollments.slots` 정규화(비면 남은 클래스 union 폴백)**→새 강사 `teacherHasAllSlots(weekly)` 검증→`enumerateLessonSessions(effectiveDate,weekly,남은수)`로 재배치 타깃 산출→**새 강사 OR 학생의 다른 enrollment `예정`과 target 날짜·시간 충돌 차단**→남은 클래스 `teacher_id`/`teacher_name`+`session_date`/`start_min`/`end_min`+`teacher_reassigned_at` update(원자 가드)+**`enrollments.teacher_id`/`teacher_name` 이관(slots 불변)**; 과거·완료·취소는 원 강사 보존; best-effort 학생 SMS+새/기존 강사 메일 `sendCourseReassignTo*`; `/admin/classes`·`/admin/classes/[id]`·`/admin/enrollments`·`/teacher`·`/mypage` revalidate). **⚠️ 단일 대타 `adminReassignClass`(1회성, enrollment 유지·날짜 불변)와 구분**: 벌크는 enrollment 이관+시작일부터 재배치. **센터 관리**: `centers/page.tsx`(server)가 `from("centers")`(name·sort_order·**price_per_session·price_currency·manager_name**) + `settings` 외화 환율(`FOREIGN_CURRENCIES` 키 `.in()`→`ratesFromSettings`→`Rates`) 조회→`<CentersManager>`(client, `useRouter.refresh()`+낙관 토스트). **추가 폼**(센터명·**센터 매니저(선택)**·통화 `<select>`·단가)+**테이블 목록**(`#`·센터명·**매니저**(없으면 "미지정")·회당 단가·관리, `CurrentTeacherTable` 스타일)+**행 "수정"→`<CenterDetailModal>`**(`src/components/admin/`, 커스텀 오버레이 패널 `z-[110]`/`z-[120]`, Esc·scroll lock; 센터명·**매니저 표시이름**·**매니저 계정 `<select>`**(미지정 sentinel+후보(page가 listUsers+profiles로 조립)=지정 시 그 회원이 '센터 관리' 권한, `centers.manager_id`) 편집(`updateCenter(id,name,managerName,managerId)`는 이름·매니저명·매니저계정만·계정 존재검증; **통화·단가는 공유 `<RateHistoryEditor scope="center">` 적용일 이력 CRUD**), 인라인 편집 폐지)+**환율 설정 카드**(`FOREIGN_CURRENCIES` 반복=페소·달러 행, 통화별 `1{symbol}=N₩` 입력·"환율 적용"→`updateExchangeRate(code,value)`). **매니저 이름(`manager_name`)은 선택 입력**(nullable, 센터명만 필수), 센터 관리 CRUD로만 관리(타 소비처는 `id,name`만 select). **단가 통화 단일 소스 `src/data/currencies.ts`**: `CURRENCIES`(KRW `₩`/PHP `₱`/**USD `$`**)·`CURRENCY_VALUES`(검증)·`DEFAULT_CURRENCY`·`normalizeCurrency`·`formatPrice(amount,code)`(예 `₩30,000`/`₱1,500`/`$100`)·**`FOREIGN_CURRENCIES`**(원화 환산 가능 외화={code,settingKey,symbol,rateLabel}: PHP→`php_to_krw`·USD→`usd_to_krw`)·**`Rates`**(통화코드→1단위당 원 맵)·**`krwEquivalent(amount,code,rates)`**(외화면 `rates[code]>0`일 때 환산, KRW/미설정이면 null)·**`ratesFromSettings(rows)`**(settings key/value→rates). 외화 단가는 목록·모달·정산에서 `≈ ₩환산` 함께 표시. 액션 `addCenter(name,price,currency,managerName)`(초기 단가는 `rate_schedules`에 effective=오늘 1행 생성)/`updateCenter(id,name,managerName)`(이름·매니저만; 단가는 이력 CRUD)/`deleteCenter`/**`addRateSchedule`/`updateRateSchedule`/`deleteRateSchedule`**(정산 단가 적용일 이력 CRUD + `syncRateCache`)/**`updateExchangeRate(code,value)`**(통화별 `settings.<code>_to_krw` upsert, `FOREIGN_CURRENCIES` 검증+양수만)(`actions.ts`, `requireAdmin()`+service_role, `revalidateCenterConsumers()`로 `/admin/centers`·`/teacher/apply`·`/teacher`·`/admin/teacher-requests` 갱신). `normalizePrice(raw,currency)`(빈값/음수→null; **USD는 소수 첫째자리 반올림, 그 외 통화는 정수 내림** — 단가 입력 `step`도 USD=0.1·그외=1)·`normalizeText`(매니저명 trim 후 빈값→null). **`centers.price_per_session`은 `numeric(12,2)`**(20260703200612, USD 소수 지원) — numeric은 supabase-js가 문자열로 반환할 수 있어 두 소비처 page(`centers`·`settlements`)에서 `Number()` 강제(null 보존). 센터 삭제 시 FK `on delete set null`로 참조 강사는 자동 None. (구 `availability/` "강사 시간표" 탭은 강사 관리 `TeacherInfoModal`로 통합·제거됨.) **강사 정산 리포트(읽기 전용)**: `settlements/page.tsx`(server)가 **진행 수업만**(`classes` `.or("conducted_override.eq.true,and(conducted_override.is.null,conducted_at.not.is.null)")`=유효 진행여부 `conducted_override ?? conducted_at`)+`.neq("status","취소")` 조회(전용 select=`teacher_id`·`conducted_override` 포함) → 강사 **현재 소속 센터**(`profiles.center_id`→`centers` `manager_name`(표시용); **단가는 `rate_schedules` 이력에서 session_date 기준 역산**) 산정 + `settings` 외화 환율(`ratesFromSettings`→`Rates`, PHP·USD) → enriched row(`{teacherId,teacherName,centerId,centerName,centerManager,course,courseTitle,sessionDate,startMin,studentName,studentEnglishName,isMakeup,pricePerSession,currency}` — `start_min`·학생 스냅샷은 상세 세션 표기용, 정산 전용 select)로 `<SettlementsManager>`(client) 전달. **센터별 분류 행엔 센터명 아래 "매니저: {이름|미지정}" 보조 표시**(`Group.manager`, 센터별 모드에서만 세팅/노출; 강사별·과정별은 미노출). **센터별 행 클릭→`SettlementDetailModal`**(같은 파일, 커스텀 오버레이 `z-[110]`/패널 `z-[120]`·Esc·scroll lock): 그 센터 소속 **강사별 상세 정산**(강사·진행 수업·지급 예정액+합계) — **모달 자체 주간/월간 토글 + 이전/다음/오늘**(전체 `rows` prop을 자체 기간으로 필터, 메인 화면 기간과 독립). 집계는 메인·모달 공용 **`aggregate(rows,keyOf,rates)`** 헬퍼(수업 수·통화별 합·원화 합·단가 미설정 수). 분류를 강사별/과정별로 바꾸면 모달 닫힘. **모달 강사 행 클릭→같은 모달이 그 강사 상세 뷰로 전환**(`selectedTeacher` state, 상단 "← 뒤로"): **과정별 그룹(소계=`AmountCell`)+그 아래 날짜별 세션**(`fmtSessionDate`=`M/D (요일)`+**시작시각 `fmtTime(startMin)`**·`isMakeup`이면 "·보강"·**수강생명(`studentName (studentEnglishName)`)**·단건 금액 `SessionAmount`)+하단 강사 합계. 기간(주간/월간·이전/다음)은 목록/상세 두 뷰 공유, 상세는 `rows`를 센터·강사·기간으로 필터해 재구성(서버 무변경). 지급 대상=**conducted(강사 입장+피드백)**, 학생 출석 무관·취소 제외·보강 포함. **단가=`rate_schedules` 적용일 이력을 수업 날짜(session_date) 기준 역산**(`src/lib/rates.ts` `effectiveRate`: 강사 개별(scope=teacher, price!=null) > 소속 센터(scope=center); teacher `price=null` 행=그 날짜부터 센터 단가로 복귀. `centers.price_per_session`·`profiles.custom_price_*`는 오늘 기준 현재값 캐시라 정산은 미참조; 이력이 진실 소스. `SettlementRow.isCustomRate`로 강사별·상세 뷰에 "개별 단가" 배지; 외화 PHP·USD는 `krwEquivalent(_, _, rates)`로 `≈₩` 병기·합산). UI: **분류 토글(센터별(기본)/강사별/과정별)** × **기간 토글(일간/주간/월간, 주 시작 월요일)** + 이전/다음 이동·"오늘", conducted 수업 수·지급 예정액 집계 테이블(`SortHeader`)+합계 행. 단가 미설정(센터 미지정/단가 없음) 수업은 수업 수엔 포함·금액 제외+경고 배지. 날짜 계산은 클라 인라인 TZ-safe 헬퍼(`makeup.ts`/`booking.ts`는 server-only라 재사용 불가). `classes`엔 center/price 스냅샷 없어 **현재 소속 센터 기준**(강사 센터 변경 시 과거 정산 소급 변동 수용). 마이그레이션 없음(단 `conducted_override` 컬럼 참조). **conducted 자동판정 보정=admin 수동 override(`classes.conducted_override` boolean, 3-state: null=자동(`conducted_at` 기준)/true=강제 진행됨/false=강제 미진행)**: `ClassesManager` 상세 모달 "진행 여부 수정" 세그먼트(자동/진행됨/미진행, **종료된 수업만**=`displayStatus==="완료" && status!=="취소"`, 취소/대체 읽기전용 여부와 독립)→`adminSetClassConducted(id,override)`(`actions.ts`, `requireAdmin`→취소 아님·**종료 후**(`now≥lessonEnd`) 가드→`conducted_override` update, `/admin/classes`·`/admin/settlements`·`/teacher`·`/mypage` revalidate). **유효 진행여부 `override ?? !!conducted_at`가 정산·admin `conductInfo`·강사 `conductMark`(○/△) 3곳 공통** — override=true면 정산 포함·강사 ○, false면 제외·마커 없음. `conducted_at`(sticky 자동 신호)은 불변, override만 위에 얹음. 각 server page가 **`createAdminClient()`(service_role, RLS 우회)**로 데이터 조회 → client 매니저(`src/components/admin/{EnrollmentsManager,ClassEnrollmentsManager,ClassesManager,MembersManager,TeacherRequestsManager,CentersManager,SettlementsManager,YoutubeManager}`)가 검색·필터·CRUD. 회원은 `admin.auth.admin.listUsers` + `profiles.role` merge. 쓰기는 `src/app/admin/actions.ts`(`"use server"`) — **모든 액션 첫 줄 `requireAdmin()`(세션 클라로 isAdmin 재확인, **반환=admin user id∥null**=감사 로그 actor_id용, `requireTeacher`와 동일 규약; `if (!(await requireAdmin()))` 불리언 문맥 불변) 후 service_role 쓰기**. 유튜브 `add/update/setVisibility/delete`(`revalidatePath("/admin/youtube")`+`"/"`로 랜딩 갱신). **회원 role 직접전환은 폐지** — `MembersManager`는 **role 읽기 전용 배지** + **이메일 미인증 시 "미인증" 칩**(`page`가 `listUsers`의 `email_confirmed_at`→`email_confirmed:boolean`로 파생 전달, 미인증 행만 `bg-brand/10 text-brand` 칩)만 표시. **role 변경은 강사 신청 flow로만**: `approveTeacherApplication`(**Postgres RPC `approve_teacher_application(p_app_id)`(security definer) 호출로 원자 처리** — 상태가 '신청'일 때만, role=teacher(`profiles.role`+`auth.users.app_metadata` 동기)+신청서 내용으로 profiles 채움(필수 first/last/bio 항상, 선택 experience·phone·zoom_url·avatar_url·nationality·gender·center_id은 `coalesce(신청서,기존)`)+status '승인'을 **단일 트랜잭션**으로. RPC 반환코드 `ok`/`not_found`/`not_pending`(재처리 차단=프로필 덮어쓰기 사고 방지)/`is_admin` 매핑. 성공 시 **지원자에 승인 알림 메일** best-effort, `revalidatePath("/teacher","layout")`)·`rejectTeacherApplication(id,note)`(**상태 가드: '신청'만**, `.eq("status","신청").select()` 0행=이미 처리, 성공 시 **지원자에 거절 알림 메일**(사유 포함) best-effort)·**`deleteTeacher(uid)`**(회수(→student)는 orphan/불일치 문제로 **폐지** → **계정 전체 삭제**: admin 보호 후 `auth.admin.deleteUser`→FK cascade로 profiles·teacher_applications·teacher_availability·reading_progress 제거, 아바타 Storage 파일은 cascade 비대상이라 `storage.from("avatars").list/remove`로 명시 정리, best-effort). `teacher-requests/page.tsx`는 `teacher_applications`(신청 우선 정렬)+`listUsers` 이메일 보강+현재 강사(`profiles.role='teacher'`, **프로필 필드 first/last_name·avatar_url·zoom_url·bio·experience·phone·nationality·gender·center_id 함께 select**(center_id는 `centers` 조회한 `centerNameById`로 이름 resolve해 `centerName` 전달) + 해당 강사들 `teacher_availability` 일괄 조회·`{day,min}[]` 그룹핑) 조회→`TeacherRequestsManager`(지원 목록=상태 탭·확장 카드, **승인/거절 버튼은 '신청' 행만**(처리완료는 read-only)·거절 사유 필수·승인 시 "현재 강사" 낙관적 추가(슬롯=[])·**toast**(alert 아님). **"현재 강사" 목록은 `<CurrentTeacherTable>`**(`src/components/admin/`, client, 정렬 가능 테이블 — 컬럼 이름·센터·국적·성별, `SortHeader` 토글 asc/desc, 빈 값 후순위·`localeCompare("ko")`, 정렬값은 원본 raw 기준이라 국기 이모지·라벨 미영향) — 행마다 **수업 보기**(→`<TeacherClassesModal>`, "수업 목록" 열)·**정보 보기**(→`<TeacherInfoModal>`)·**강사 삭제**(비가역 경고 AlertDialog, 삭제 시 강사·지원행 낙관적 제거)). **강사별 진행 중인 수업(과정) 열람 `<TeacherClassesModal>`**(`src/components/admin/`, client, `TeacherInfoModal` 스켈레톤 복사=오버레이 `z-[110]`/패널 `z-[120]`·Esc·scroll lock·닫기 포커스, `teacher:CurrentTeacher|null` null이면 미렌더): 헤더=강사명+**"진행 중인 수업" 배지**(accent-blue), 본문=`teacher.classes` 과정 카드 목록(과정명·학생명(`한글명 (영문명)`)·상태 배지(승인/결제 대기/결제 완료)·**주간 일정(`summarizeSlots(_,true," / ")` 요일 `/` 구분)·수업 일정(`scheduleDateRange` 시작~종료)·수업 횟수(`TeacherClassItem.totalSessions`=enrollment `total_sessions ?? 24`)**·**결제완료**면 `done/total회 · 다음 {formatDateKo(nextDate)}`(nextDate 없으면 "예정 수업 없음")·**승인/결제대기**(아직 classes 없음)면 `시작 예정 {startDate}`), 비면 "진행 중인 수업이 없습니다." **`CurrentTeacher.classes`(`TeacherClassItem[]`)는 page가 조립**: `teacher-requests/page.tsx`가 이미 로드하는 enrollment(`.in("status",["승인","결제대기","결제완료"])`−`loadEndedEnrollmentIds` 종료분 제외, `bookedSlots`와 동일 `rowsByTeacher` 재사용, select에 `course/course_title/start_date` 추가) + **classes 집계 쿼리**(`.in("teacher_id",teacherIds)`, enrollment_id별 `total`(취소·보강 제외 회차)·`done`(`kstDateMinToMs(session_date,lessonEndMin(end_min))<now`)·`nextDate`/`nextMin`(미종료 중 최이른))로 강사별 `TeacherClassItem[]`(nextDate→startDate 정렬) 파생. 범위=승인+결제대기+결제완료(미종료)만(신청·거절·취소·종료분 제외). 진행 판정은 서버 렌더 `Date.now()` 스냅샷(라이브 틱 없음). **"현재 강사" 섹션 위에 `<TeacherAvailabilityFinder>`**(`src/components/admin/`, client, props `teachers`+`onView=setInfoTarget`): 원하는 요일(다중 토글)+**수업 횟수(1~8회, 1회=30분)**+시작 시각(`<select>`, 06:00~24:00 30분)으로 **종료=시작+횟수×30분 자동 계산**(읽기전용 표시, 종료≤24:00 되게 시작 옵션 클램프) → **해당 범위 전체가 비어 있는 강사** 필터(이미 로드된 `teacher.slots` 클라 필터, 추가 쿼리 0 — 매칭=**선택 모든 요일 AND** × **범위 내 모든 30분 슬롯 존재**). 결과 행 "정보 보기"는 동일 `setInfoTarget`로 `<TeacherInfoModal>` 재사용. **`<TeacherInfoModal>`**(`src/components/admin/`, client): base-nova `AlertDialog`는 폭이 `sm:max-w-sm`로 고정돼 넓은 콘텐츠에 부적합 → **`AvailabilityModal` 식 커스텀 패널**(오버레이 `z-[110]`+패널 `z-[120]` `w-[min(92vw,720px)]` `max-h-[90vh]` flex-col, 본문만 `overflow-auto`, a11y: Esc·body scroll lock·닫기 버튼 포커스·오버레이 클릭 닫기, 하단 고정 "닫기" 버튼). 내용: **프로필 사진**(avatar_url 있으면 80px `next/image`, 없으면 이름/이메일 **이니셜 폴백** 박스)+이름·이메일 → `dl`(이메일·전화·국적·성별·**센터(편집: `<select>`+dirty 시 저장→`updateTeacherCenter`, 낙관 `onCenterUpdated`)·적용 단가(개별 단가 적용일 이력=공유 `<RateHistoryEditor scope="teacher" allowRevert>` — 현재 적용 요약+이력 목록(수정/삭제)+추가 폼(적용일 date+통화+금액, "센터 단가 사용"=price=null 행); 없으면 센터 단가 폴백(`fallbackLabel`=선택 센터 현재 단가); page가 `schedulesByTeacher`+`rates` 전달)**·Bio·경력·Zoom URL) → "주간 가능 시간" `<AvailabilityGrid readOnly>`(가용 셀 `bg-accent-blue/40`) + 헤더 우측 **"PDF로 내보내기"** 버튼(`openTimetablePrint` — 강사 가용을 A4 세로 인쇄창으로 열어 `window.print()`). `CurrentTeacher` 타입에 phone·nationality·gender·centerName·bio·experience·zoomUrl·avatarUrl·slots 포함. **승인 알림/거절 알림 메일은 `src/lib/mailer.ts`의 `sendTeacherApprovalNotification`/`sendTeacherRejectionNotification`(지원자 대상, 링크는 액션에서 `getOrigin` 생성).** **랜딩 유튜브 섹션은 `youtube_videos`(is_visible=true) 조회 → 비면 mock `VIDEOS` fallback.** **강사 시간표 열람**은 강사 관리 `TeacherInfoModal`(현재 강사 행 "정보 보기")의 `<AvailabilityGrid readOnly>`로 제공(별도 admin 페이지 없음).

**센터 매니저** (`/center`, **UI 한국어**, 탭 구조): 센터 담당자에게 **소속 센터 강사 조회 + 개별 회차 강사 대체** 권한 부여. **식별=`centers.manager_id`(FK→auth.users, `20260713003440`)** — role enum 미변경, 한 명이 여러 센터 담당 가능. admin이 `/admin/centers` 상세 모달에서 **매니저 계정 지정**(`CenterDetailModal` `<select>`, 후보=listUsers+profiles; `updateCenter(id,name,managerName,managerId)`가 `manager_id` set·계정 존재검증). **가드 `src/lib/center-manager.ts`**: `requireCenterManager()`(세션→`loadManagedCenters`(service_role, `manager_id=uid`)→담당 센터 없으면 null, 액션·page 첫 줄 가드)·`isCenterManager`. `layout.tsx`가 `centers.manager_id=user.id` 존재로 `isCenterManager` 파생→Navbar **"센터 관리"** 링크. 조회는 전부 **가드 후 service_role**(매니저는 admin 아님, profiles RLS는 본인만) — 새 RLS 불필요. **`/center/(layout)` 가드+웰컴+`<CenterTabs>`(강사 관리 `/center`·주간 일정 `/center/schedule`)**. **① 강사 관리**(`page.tsx`→`loadCenterTeachers(admin,centerIds)` `src/lib/teacher-directory.ts`=admin `teacher-requests`의 `CurrentTeacher[]` 조립 센터 스코프 이식)→`<CenterTeachers>`가 admin `CurrentTeacherTable`(목록)+`TeacherClassesModal`(진행 중 강좌)+`TeacherInfoModal` **`readOnly`**(센터 변경·단가(급여) 숨김, 프로필·가용·PDF만) 재사용. **② 주간 일정**(`schedule/page.tsx`→`loadCenterSessions`+`loadCenterTeacherCards`)→`<CenterWeekSchedule>`(1분 틱)→admin **`<ClassWeekGrid readOnly onReassign>`** 재사용. **강사 대체는 주간 SlotModal 인라인**: `readOnly && onReassign && 미시작`(`now<시작시각`) 회차 행에 **"강사 대체"** 버튼→SlotModal 닫고 `<ReassignModal>`(`src/components/center/`, 같은 센터·그 시간 가용 강사만 `teacherHasAllSlots` 후보)→**`centerReassignClass(classId,newTeacherId)`**(`src/app/center/actions.ts`, `requireCenterManager`→**공유 코어 `reassignClassCore`**(`src/lib/reassign.ts`)에 `actor.role='center_manager'`·`constrainCenterIds` 전달=현재·새 강사 모두 담당 센터 소속이어야 통과). **`ClassWeekGrid`의 `AdminSession`에 `classId`·`teacherId`·`sessionNo`(대체 대상)·`period`(수업 일정 `scheduleDateRange`)·`totalSessions`(수업 횟수) 추가** — admin `/admin/classes`·center 로더 두 파이프라인이 공급, admin은 `onReassign` 미전달=버튼 없음·행 클릭 드릴인 유지(무변경).

### 인증 라우트 (Supabase Server Actions)

폼 5종 + OAuth는 `src/app/(auth)/` route group(괄호는 URL 미노출). `/auth/confirm`만 `src/app/auth/confirm/`로 분리(메일/OAuth가 URL 직접 참조).
- **`/login`** — page는 로그인 상태면 `/`로 redirect. `searchParams`: `error=auth-code-error`(빨강+회원가입 링크), `verified=pending`(녹색). `login()`은 `signInWithPassword` 실패 시 분기: `email_not_confirmed`→재발송(`canResend:true`); `invalid_credentials`→`getUserIdentitySummary()`로(`!found`→미가입 / `hasPassword`→비번 불일치 / `!hasPassword`→카카오 계정 안내), `null`→일시 오류. `resendConfirmation(email)`도 export(`resend({type:"signup"})`, Login/Signup 공유). rate limit + 이메일 검증. 배너는 `AuthBannerSlot`.
- **`/signup`** — **회원가입 동선 brand-blue 통일**(제출버튼·AlertDialogAction·"로그인" 링크·Eye ring·Navbar "회원가입"). 색 되돌리려면 이 네 곳 함께. page는 로그인 시 `/` redirect. 이메일/비번(**8자+**)/확인 검증 후 **`signUp()` 전 `getUserIdentitySummary(email)` 사전 검사**: `!found`→가입 진행 / `found&&!confirmed`→"이미 가입 시도"(`canResend`) / `found&&hasPassword`→로그인 안내 / `found&&!hasPassword`→카카오 안내. **⚠️ 핵심 보안: Supabase는 미확인 사용자 재가입 시 비번을 덮어쓰므로 이 사전 검사로 계정 탈취 차단. `!found`일 때만 `signUp({options:{emailRedirectTo:${origin}/auth/confirm}})` — 순서 절대 유지.** `null`→fail closed. signUp 후 `user_already_exists`/빈 `identities` 분기는 race condition 안전망. rate limit(IP 5분/5회).
- **`/logout`** — `logout()`: `signOut()` → `revalidatePath("/","layout")` → `redirect("/")`. Navbar 폼에서만 호출.
- **`/forgot-password`** — page 로그인 시 `/` redirect. `error=link-expired`→빨강 배너. `emailExists()` 확인 후 `resetPasswordForEmail(email,{redirectTo:${origin}/auth/confirm?next=/reset-password})`. rate limit(이메일 1분/1회).
- **`/reset-password`** — **세션 필수**(page에서 `getUser()`, 미인증 시 `/forgot-password`). `resetPassword()`: (1) 비번 검증(8자+, 확인), (2) **새 비번≠기존**(임시 `persistSession:false` client로 `signInWithPassword` 시도), (3) `updateUser({password})`, (4) **`signOut({scope:"others"})`**(타 기기 무효화), (5) `redirect("/?reset=success")`.
- **`/auth/confirm`** — GET route handler. 세 형식: `token_hash`+`type`→`verifyOtp()`; `code`→`exchangeCodeForSession()`(PKCE/OAuth 공용); `flow=oauth`→OAuth 식별. **`next`는 open redirect 방지로 `/`시작 & `//`아닌 경로만 허용**, 아니면 `/`. recovery 식별(`type==="recovery"` 또는 `next.startsWith("/reset-password")`). 성공: 비-recovery는 `verified=success`(이메일)/`login=success`(OAuth) 부착, recovery는 `next`로. 실패: recovery→`/forgot-password?error=link-expired`, 그 외→`/login?error=auth-code-error` 또는 `?verified=pending`. **두 분기 모두 유지.**
- **카카오 OAuth** — `src/app/(auth)/oauth/actions.ts`의 `signInWithKakao()`: `signInWithOAuth({provider:"kakao",options:{redirectTo:${origin}/auth/confirm?next=/&flow=oauth}})` → `data.url`로 외부 redirect. 실패 시 `/login?error=auth-code-error`. **rate limit 없음**(카카오 throttling 의존). UI: `KakaoButton.tsx`(client, `useTransition`+SVG+Loader2)가 Login/Signup 양쪽 "또는" 구분선 아래.
- **폼 컴포넌트** `src/components/auth/{LoginForm,SignupForm,ForgotPasswordForm,ResetPasswordForm}.tsx`: 모두 **`useActionState` + `<form action={formAction}>`**, **입력은 controlled(`useState`+`value`+`onChange`)**(React 19 auto-reset & Base UI `defaultValue` 경고 회피). 비번 필드: `Eye`/`EyeOff` 토글(`type="button"`, ring) + `useCapsLockWarning`(`src/hooks/use-caps-lock.ts`). a11y: 에러 시 `aria-invalid` + `aria-describedby`(id: `login-error`/`signup-error`/`forgot-error`/`reset-error` + caps/hint), `role="alert"`, 한국어 존댓말. query 배너는 `AuthBannerSlot`.
- **재발송 UX**(Login/Signup 공통): (1) `<AlertDialog>` 확인 모달, (2) success 시 **60초 클라 쿨다운**(`cooldownSec` + `setInterval`, 버튼 disabled). 서버 rate limit(1분/1회)과 이중 보호. **SignupForm은 success/error 양쪽에 트리거** — 한쪽 수정 시 다른 쪽도 검토(조건부 숨김 시 카운트다운 회귀 주의).
- **로딩 스피너**: `Loader2 animate-spin` + 컨텍스트 텍스트("로그인 중" 등), **trailing `...` 금지**. shadcn Button 내부는 `<Loader2 className="animate-spin"/>` 한 줄, raw button은 `<span className="inline-flex items-center gap-1.5">`로 감쌈.

### 전자책 `/textbook/[course]` (레지스트리 기반, 교재 5종)

- **레지스트리** `src/data/textbook/index.ts` — `TEXTBOOK_REGISTRY: Textbook[]`(5종: `workhol`·`kitchen`·`grammar1`·`grammar2`·`cosmetic`). 각 `{course,title,subtitle,eyebrow,freeUnits[],units[]}`. **course는 DB `reading_progress.course` & URL segment & `content/textbook/<course>/` 폴더와 1:1.** `units`는 `unitsFromBook(bookKey)`로 `landing.ts` `BOOKS`(셀프디벨롭)에서 파생(중복 방지, `titleKr=sub`·`situation=u.situation??sub??""`) — `workhol`만 `workhol.ts`의 `WORKHOL_UNITS`(실 situation 포함) 직접 사용. kitchen·cosmetic은 `BOOKS`에 `situation` 채워져 실 소개, grammar는 situation 없어 `sub` 폴백(=titleKr). `grammar2`는 unit 번호 **25–48**(`freeUnits:[25]`). export: `getTextbook`/`getTextbookUnit`/`getAdjacentUnits`(유닛 배열 순서 기준 prev/next)/`TEXTBOOKS`(`{course,title,href}`, Navbar "교재 보기"가 `.map()`).
- `src/data/textbook/workhol.ts` — `WORKHOL_COURSE="workhol"`, `WORKHOL_UNITS`(5필드×24). 워홀 콘텐츠(폴더 `content/textbook/workhol`). ⚠️ 과거 course 키는 `cooking`이었고 마이그레이션 `20260609013944_rename_cooking_course_to_workhol.sql`로 `reading_progress.course`를 일괄 갱신.
- `[course]/page.tsx`(server) — `generateStaticParams`=레지스트리, `getTextbook`→`notFound()`. Unit 목록(카드=제목+부제+**소개문단**, 단 소개문단은 `situation!==titleKr`일 때만 렌더 — grammar는 sub 중복이라 숨김=랜딩과 동일), 로그인 시 `reading_progress`(course별, `completed`만) 1회 조회→"학습 완료됨"/"시작 전" 텍스트·`CheckCircle2`·`Lock`·**완료 카드 배경 `bg-progress/5 border-progress/40`**(랜딩 완료 카드와 동일). 잠금=`!user && !freeUnits.includes(unit)`, 자물쇠는 `/login?next=...`.
- `[course]/[unit]/page.tsx`(server) — `generateStaticParams`=레지스트리 flatMap, `notFound()`(course/unit 무효), **무료 외 미로그인 `redirect("/login?next=/textbook/<course>/<unit>")`**. HTML `content/textbook/<course>/<htmlFile>` `fs.readFile`→`TextbookViewer`. **읽은 HTML의 `__AUDIO_BASE__` 토큰을 `${NEXT_PUBLIC_SUPABASE_URL}/storage/v1/object/public/textbook-audio/`로 `replaceAll`**(문장별 음성용 절대 URL; 토큰 미포함 교재엔 무영향). `prev/nextHref`는 `getAdjacentUnits`, `totalUnits`=최대 unit 번호(grammar2 라벨 "Unit 25/48"용).
- `src/app/textbook/actions.ts`(공유 `"use server"`) — **`setUnitCompleted(course,unit,completed)`**(수동 완료 정책 — 스크롤 자동 판정 없음). **진입 가드 순서 고정: (1) `getTextbook(course)` 화이트리스트, (2) unit이 해당 교재 `units`에 존재, (3) `getUser()` 없으면 silent.** `upsert({onConflict:"user_id,course,unit"})` 후 `revalidatePath("/")`+`"/textbook/<course>"`(랜딩·목록 완료 동기화). ⚠️ 과거 `saveScrollProgress`(스크롤 자동 진행률)는 제거됨.
- `src/components/textbook/TextbookViewer.tsx`(client) — **iframe `srcDoc`** 격리, `scrolling="no"` + `ResizeObserver` + 0.5/1.5s 재측정으로 높이 확장→부모 스크롤. 상·하단 `NavBar`(상단 `sticky top-[72px]`), prev/next는 **`prevHref`/`nextHref` 유무로 렌더**. 하단 **수동 완료 토글**(`CompletionToggle`, 로그인만): 미완료=`학습 완료로 표시` 버튼 / 완료=`학습 완료됨` 배지 + `완료 취소하기`(X) 버튼 분리. `initialCompleted` prop, `useTransition` 낙관적+실패 롤백. 액션 import는 `@/app/textbook/actions`.
- **HTML 본문**: 루트 `content/textbook/<course>/unit-XX.html`(완전한 HTML 문서, 2자리 zero-pad). **`src/` 아닌 `content/`**, `fs.readFile`(번들 미포함·public 아님). **새 교재 추가: (1) `content/textbook/<course>/unit-NN.html`, (2) `TEXTBOOK_REGISTRY`에 항목 추가(`units`는 `unitsFromBook` 또는 직접 작성), (3) 랜딩 연동하려면 `LINKED_COURSE`+`page.tsx` 쿼리 2곳.** 라우트·Navbar·진행률이 모두 레지스트리 기반이라 마이그레이션 불필요. ⚠️ 뷰어 iframe `srcDoc`은 sandbox 없어 **본문 내 `<script>` 실행됨**(모바일 가독성 검증은 헤드리스 Chrome으로 iframe 폭 렌더).
- **문장별 원어민 음성(workhol·kitchen)**: 유닛 HTML 문장 옆 `.audio-btn`(▶/🔊) + 인라인 `<script>`(`new Audio(AUDIO_BASE+data-audio)`, 단일재생·토글·pulse). **AUDIO_BASE=`__AUDIO_BASE__<course>/`**(page.tsx `replaceAll("__AUDIO_BASE__", <storage base>)` 범용 치환), mp3는 **Supabase Storage 공개 버킷 `textbook-audio`**(키 `<course>/unitNN/...`). **workhol**: TALK·EXTENSION·ASK 옆, `data-audio="unitNN/{talk,ext,ask}_N.mp3"`(~649개), 스크립트 `scripts/add-workhol-audio-buttons.mjs`(버튼 주입) + `scripts/sync-workhol-audio.mjs`(라이브→버킷, `survival-english-au.netlify.app`). **kitchen**(워홀 주방영어 현장형): Key Words·Key Lines·Live Dialogue 옆, `data-audio="unitNN/{word_N,line_N,dialogue}.mp3"`(유닛당 18개=24유닛×18=432), 소스 HTML에 버튼 내장(별도 주입 불필요) — 동기화 `scripts/sync-kitchen-audio.mjs`(`sync:kitchen:audio`, 라이브 `friending-kitchen-ver01.netlify.app/audio/`→버킷 키 `kitchen/…`). 두 동기화 모두 **service_role → 사용자 직접 실행**(Claude 403), 멱등(upsert). 타 교재 확장 시 동일 패턴(버튼+`__AUDIO_BASE__<course>/` 토큰+버킷 `<course>/…`) 재사용.
- **grammar1·grammar2 콘텐츠**(`content/textbook/grammar{1,2}/`)는 타 교재와 달리 **desktop/mobile 디자인 분리**: 데스크톱=`.page` 인쇄형(182mm), 모바일=`body.mobile` 규칙(테이블→카드화 등, specificity 높아 자체 `@media`보다 우선). `<body>` 직후 인라인 스크립트가 `matchMedia("(max-width:768px)")`로 `.mobile` 클래스 자동 토글(FOUC 방지 동기 실행+리사이즈 추종). **color 모드 고정**(`:root` 기본값), 과거 Color/B&W·Desktop/Mobile **toolbar는 제거됨**. workhol/kitchen은 단순 `@media` 반응형(class 없음).

### 헬퍼

- `src/lib/utils.ts` — `cn()`.
- `src/lib/auth.ts` — `getUserRole`/`isAdmin`/`isTeacher`(supabase,userId)(`profiles.role`, app_metadata 대신 profiles 우선).
- `src/lib/mailer.ts` — `sendTeacherApplicationNotification`(강사지원, 관리자 대상) + **`sendEnrollmentNotificationToTeacher(to[],data)`(수강신청, 강사 대상·영문)** + **`sendEnrollmentPaymentConfirmedToTeacher(to[],data)`(결제 확정→수업 확정 알림, 강사 대상·영문, `EnrollmentEmailData` 재사용)** + **`sendEnrollmentCancellationToTeacher(to[],data)`(학생 취소 알림, 강사 대상·영문, `EnrollmentEmailData` 재사용)** + **`sendClassReassignToNewTeacher`/`sendClassReassignToOldTeacher(to[],data)`(개별 수업 강사 대체 알림, 강사 대상·영문, `ClassReassignEmailData`: 새 강사=배정·기존 강사=이관)** + **`sendCourseReassignToNewTeacher`/`sendCourseReassignToOldTeacher(to[],data)`(강사 중도 하차 시 과정 전체 이관 알림, 강사 대상·영문, `CourseReassignEmailData`: schedule·remainingCount·nextDate·old/newTeacherName)** + **`sendEnrollmentNotificationToAdmin`(신규 신청 접수)/`sendEnrollmentApprovedToAdmin`(강사 승인→결제대기)/`sendEnrollmentRejectedToAdmin`(강사 거절, 사유 포함)/**`sendEnrollmentPaidToAdmin`(카드 결제 완료→수업 확정, `finalizeEnrollmentPayment`가 `actor.role!=="admin"`=카드(student/system)만 발송·무통장 admin 확인은 제외)**(관리자 대상·영문, `EnrollmentAdminEmailData`(+`reason?`) 재사용, `getAdminEmails()` 수신)** + **`sendTeacherApprovalNotification`/`sendTeacherRejectionNotification`(강사 심사 결과, 지원자 대상)** — 모두 Resend, best-effort no-op, `import "server-only"`.
- `src/lib/origin.ts` — `getOrigin(headers)`: `NEXT_PUBLIC_SITE_URL`→`origin`→`x-forwarded-host`+proto→`host`.
- `src/lib/email.ts` — `isValidEmail()`(서버측 검증).
- `src/lib/url.ts` — `isValidZoomUrl()`(http/https 검증, 강사 프로필·강사 지원 폼 공유).
- `src/lib/phone.ts` — `normalizePhone`(숫자만)/`isValidKoreanMobile`(`/^01[016789]\d{7,8}$/`)/`formatPhone`(하이픈 표시). 마이페이지 전화번호 인증 클라·서버 공유.
- `src/lib/sms.ts` — `sendSms(to,text):Promise<boolean>`(Solapi `SolapiMessageService.send`, `import "server-only"`). env 미설정/예외 시 false. ⚠️ **`send()`는 "접수 성공"이지 "발송 성공"이 아님** — 발신번호 미등록·잔액부족 등은 예외가 아니라 응답 `groupInfo.count.registeredFailed`/`failedMessageList`에 담기므로, `registeredSuccess<1 ∥ registeredFailed>0 ∥ failedList`면 false 반환 + 사유 콘솔 로깅(OTP는 성공 여부 필요). 전화번호 인증 OTP + **수강신청 승인/거절 결과 통보**에 사용.
- `src/lib/availability.ts` — 주간 슬롯 공유 모델(`Slot{day,min}`·`slotKey`·`SLOT_MIN`·`GRID_START/END_HOUR`·`DISPLAY_DAYS`·`DAY_LABELS`/`DAY_LABELS_KO`·`ROW_MINS`·`teacherHasAllSlots`(강사 슬롯⊇요청)·`subtractSlots`·`slotsOverlap`·**`overlappingIds(items{id,group,slots})`**(같은 group=강사 내 슬롯 겹치는 id 집합 — 수강신청 목록 시간충돌 시각표시용, 활성 행만 걸러 호출)·`isValidSlot`·`summarizeSlots(slots,ko,sep=" · ")`(요일+시각 "월 14:00 · 수 14:00", **`sep`으로 구분자 교체** — 학생 마이페이지·강사 모달·강사/관리자 이메일은 `" / "` 전달)·**`summarizeWeekdays(slots,ko)`**(요일-only "월/수/금" — 시각 제외, admin 주간 뷰 `SlotModal` 과정 행용)·**`lessonEndMin(endMin)`**(슬롯 종료−5분=레슨/입장 종료, 표시·입장 시간창 단일 소스)·**`dowOf(YYYY-MM-DD)`**(UTC 파싱 요일 인덱스 0=일~6=토, TZ 비종속)·**`formatDateKo(YYYY-MM-DD)`**(→`"2026-07-04 (토)"`, 날짜 원문+요일 접미)·**`scheduleDateRange(startDate,slots,totalSessions)`**(수업 일정 "시작일 ~ 종료일", `lessonEndDate` 기반 — 학생/강사 패널·주간 SlotModal 공용)). 강사 그리드·수강신청 그리드·매칭·요약·충돌표시 단일 소스(클라/서버 공용, server-only 아님).
- `src/lib/booking.ts`(server-only) — `loadEndedEnrollmentIds(admin,teacherIds)`(종료된 `결제완료` enrollment id 집합 — 남은 `예정` 수업(`classes.session_date≥오늘KST`) 없으면 종료, 클래스 없는 레거시는 `lessonEndDate` 폴백) + `todayKst()`. 슬롯 점유로 취급하는 모든 지점(가용 차감·승인 충돌·그리드 표시)이 공용해 **종료 과정의 슬롯 자동 해제**. service_role/세션 client 양쪽 허용.
- `src/lib/avatar.ts` — `uploadAvatar(file,uid)`→`{publicUrl,path}` + `cleanupOldAvatars(uid,keepPath)`(본인 폴더 누적 파일 정리, best-effort). 강사 프로필·강사 신청폼 공유(브라우저 client 전용).
- `src/lib/timetable-print.ts`(브라우저 client 전용) — `buildTimetablePrintHtml({teacherName,slots,bookedSlots})`(강사 주간 가용을 A4 세로 자립형 HTML로 렌더, `@page A4 portrait`+`print-color-adjust:exact`, `@/lib/availability` 상수 재사용) + `openTimetablePrint(...)`(전용 창 열어 `window.print()`=PDF 저장). admin `TeacherInfoModal` "PDF로 내보내기" 버튼 전용, 새 의존성 없음.
- `src/lib/makeup.ts` — `createMakeupClass(admin,cls)`→보강 날짜∥undefined(취소 1회당 자동 보강 생성, 마지막 수업일 뒤 첫 스케줄 요일, `is_makeup=true`, best-effort) + `weekdayOf`/`addDaysStr`(TZ 비종속 날짜 헬퍼). 학생 `cancelClass`·admin `adminCancelClass` 공용, `import "server-only"`.
- `src/lib/reassign.ts`(server-only) — **개별 회차 강사 대체 공유 코어 `reassignClassCore(admin,{classId,newTeacherId,actor,constrainCenterIds?})`**: `예정`·시작 전·새 강사 가용·같은 날 충돌 검증→update(teacher_id/name·`teacher_reassigned_at`·`original_teacher_id` coalesce)→학생 SMS·강사 메일·`class_reassigned` 이벤트·revalidate. `constrainCenterIds`(센터 매니저)면 현재·새 강사 모두 그 센터 소속이어야 통과. admin `adminReassignClass`(제약 없음)·센터 `centerReassignClass` 공용.
- `src/lib/center-manager.ts`(server-only) — `requireCenterManager()`(세션→담당 센터 없으면 null, 가드)·`loadManagedCenters(admin,uid)`(centers.manager_id=uid)·`isCenterManager`. 센터 매니저 페이지·액션 진입 가드.
- `src/lib/teacher-directory.ts`(server-only) — 센터 스코프 강사 데이터: `loadCenterTeachers`(→`CurrentTeacher[]`, 강사 관리 탭)·`loadCenterSessions`(→`AdminSession[]`, 주간 일정)·`loadCenterTeacherCards`(→`CenterTeacher[]`, 대체 후보). admin teacher-requests·classes 조립을 센터 소속으로 이식.
- `src/lib/payment.ts`(`import "server-only"`) — 결제 확정 공유 코어. `finalizeEnrollmentPayment(enrollmentId,{id,role:"admin"|"student"|"system"})`(상태 `결제대기` 게이트+role='student'면 소유권 재검증(admin·system은 skip=이미 authoritative)→`결제완료` race-safe update→**무통장(role='admin')이면 `payments` 기록**(`recordPayment`, `payment_id='bank-{enrollmentId}'`·`method='bank_transfer'`·`amount=opts.amount ?? (enr.price_krw ?? COURSE_PRICE_KRW)`·`note=opts.note`; **`finalizeEnrollmentPayment(_, _, opts?:{amount?,note?})` 3번째 인자로 admin 무통장 확정 시 실입금액(정가 이하 할인)·메모 전달** — `confirmPayment(id,{amount,note})`가 enrollment `price_krw` 조회해 `1~유효가격` 검증 후 위임, `payment_confirmed` 이벤트 detail에 `bankAmount`·`discounted`·`note` 기록; 카드/웹훅은 opts 미전달; 상류 `settlePortonePayment`가 이미 기록해 이중 방지=매출·환불 단일 소스화). **확정 후 재조정**: 무통장 결제는 `EnrollmentsManager` 상세 모달의 "실입금액·메모 수정" 버튼(`adjustable=isBankPay && payment.status='paid'`)→`adjustBankPayment(paymentId,{amount,note})`(`src/app/admin/actions.ts`, `requireAdmin`→무통장·`paid` 가드(카드/환불 거부)→enrollment `price_krw` 조회해 `1~유효가격` 검증→`payments.amount`/`note` update→`payment_adjusted` 감사 이벤트(detail old/newAmount·old/newNote)→`/admin/enrollments`·`/admin/revenue` revalidate)로 사후 정정, **매출 대시보드 자동 반영**+`generateClassesForEnrollment`(멱등 클래스 생성, 여기로 이동)+학생 SMS+강사 결제확정 메일+`payment_confirmed` 이벤트(actorId: system이면 null, `detail.via`: student=`portone_card`·system=`portone_webhook`)+revalidate 4곳) + `generateClassesForEnrollment`/`EnrollmentForClasses`. finalize 호출부는 상위 파이프라인. ⚠️ `"use server"` 아닌 모듈이라 클라 직접 호출 불가(우회 방지). **결제 기록·정산·환불 코어(같은 파일)**: `recordPayment(admin,{...})`(payments 멱등 insert-once, **카드·무통장 공용** — 무통장은 `finalizeEnrollmentPayment` admin 경로가 호출), **`settlePortonePayment(paymentId,{role:"student"|"system"})`**(검증(`getVerifiedPortonePayment`)→**enrollment 조회(`student_id, price_krw`)해 `expected=price_krw ?? COURSE_PRICE_KRW`와 `v.amount` 대조**(불일치=`recordPayment` 전에 거부, 기존 "불일치=미기록" 보존)→기록→finalize→**확정 불가 시 자동 환불**: finalize 실패 후 상태 재조회 — (a)`결제완료`+타 `paid` payment=**과오납(중복결제)** 자동취소(`overpaid:true`) / (b)enrollment `취소`·`거절`·삭제(`!cur`)=**취소된 신청에 캡처된 결제** 자동취소(금전 손실 방지) / (c)`결제완료`+타 결제 없음=멱등 성공 / (d)그 외(결제대기 등)=확정 실패 반환. **공유 헬퍼 `attemptAutoRefund`**: `cancelPortonePayment` 성공 시 payments `cancelled`+`payment_refunded` 이벤트, **실패 시 `payments.note="[자동환불실패]…"` 표식+`payment_refund_failed` 이벤트**(admin 결제목록·매출·감사로그에 노출→수동 환불 유도, 돈 방치 방지) — 학생 confirm·웹훅 Paid 공용), **`refundEnrollmentPayment(admin,{payment,totalCancelledAmount,reason,actor})`**(환불 후 DB 동기화 — payments 상태(`cancelled`/`partial_cancelled`)·`cancelled_amount`(절대값=멱등) + enrollment `결제완료`→`취소` CAS + **미래 `예정` 수업 일괄 `취소`**(`cancel_reason='cancel'`, 과거 유지) + 학생 SMS·강사 메일(`sendEnrollmentRefundToTeacher`)·`payment_refunded` 이벤트; admin 환불·취소 웹훅 공용, PortOne 취소는 호출측이 이미 수행).
- `src/lib/portone.ts`(`import "server-only"`) — `getVerifiedPortonePayment(paymentId)`: PortOne V2 REST(`GET /payments/{id}`, `Authorization: PortOne {PORTONE_V2_API_SECRET}`) 조회+검증(status=PAID·**통화 KRW만** 검증, ⚠️ **금액 상등은 여기서 안 함** — enrollment 유효가격(`price_krw ?? COURSE_PRICE_KRW`)을 아직 모르므로 `amount`만 반환하고 호출자 `settlePortonePayment`가 대조)→`customData.enrollmentId` 반환. 반환 `{ok, enrollmentId?, error?, transient?, amount?, currency?, pgTxId?, method?, receiptUrl?, raw?}`(strictNullChecks off라 평탄 타입; `transient`=일시오류=웹훅 500 재시도; 성공 시 payments 기록용 결제상세 포함). 학생 액션·웹훅 공용(결제 검증 단일 소스, 금액 서버 authoritative). + **`fetchPortonePaymentRaw(paymentId)`**(검증 없는 raw 조회 — 취소 웹훅의 누적 취소금액 `amount.cancelled` 읽기용) + **`cancelPortonePayment(paymentId,{reason,amount?})`**(`POST /payments/{id}/cancel`, 환불; amount 미지정=잔액 전액).
- `src/lib/events.ts`(server-only) — **과정 라이프사이클 감사 로그**. `logEnrollmentEvent(admin,{enrollmentId?,classId?,eventType,actorId?,actorRole?,actorName?,course?,courseTitle?,studentName?,teacherName?,detail?})`(service_role insert into `enrollment_events`, **best-effort**=자체 try/catch·절대 throw 안 함=로그 실패가 본 뮤테이션 차단 안 함, `createMakeupClass`처럼 호출측 admin 클라 재사용) + `EnrollmentEventType` union(14종: `enrollment_created`(실/테스트 `detail.isTest`)·`enrollment_approved`·`enrollment_rejected`·`payment_confirmed`·**`payment_adjusted`(무통장 실입금액·메모 재조정, detail old/newAmount·old/newNote)**·`payment_refunded`(환불→수강취소, admin/자동/취소웹훅)·**`payment_refund_failed`(자동환불 시도 실패=수동 환불 필요, `attemptAutoRefund`가 기록)**·`enrollment_cancelled`(학생/admin, actor_role 구분)·`class_postponed`·`class_cancelled`·`class_reassigned`·`remaining_rescheduled`·`remaining_reassigned`·`conducted_overridden`). **DB enum 아닌 앱 union**(확장 시 마이그레이션 불필요). 각 뮤테이션 액션이 성공 직후 revalidate 앞에서 1줄 호출(계측 지점=`submitEnrollment`·`createTestEnrollment`·`approveEnrollment`·`rejectEnrollment`·`confirmPayment`·`cancelEnrollment`·`adminCancelEnrollment`·`cancelClass`·`adminCancelClass`·`adminReassignClass`·`adminRescheduleRemaining`·`adminReassignRemaining`·`adminSetClassConducted`). `detail` jsonb에 before/after(강사 대체=old/newTeacher, 일정 변경=old/newSlots·effectiveDate·affectedCount 등) 담아 덮어쓰기 복원성 확보. 조회는 **admin 전용**(RLS 정책 없음=service_role만). **저빈도 상세(피드백·conducted 자동 인정)·출결(입장)은 미계측**(향후 tier 확장 후보).
- `src/lib/rate-limit.ts` — in-memory 토큰 버킷 `rateLimit/getClientIp/formatRetryAfter`. **⚠️ 프로세스 메모리라 멀티 인스턴스(Vercel)에선 효과 제한** → 필요 시 Upstash Redis로 함수 내부만 교체.
- `src/hooks/use-caps-lock.ts` — `useCapsLockWarning()`, `capsLockHandlers` spread.

### Supabase SSR

- **경로 `src/utils/supabase/`** — `client.ts`(`createBrowserClient`) · `server.ts`(`createServerClient`+cookies) · `middleware.ts`(`updateSession()` 매 요청 `getUser()`로 세션 갱신 — **`createServerClient`와 `getUser()` 사이 로직 금지**) · `admin.ts`(`service_role`, 첫 줄 `import "server-only"`).
- **`admin.ts` export**: `createAdminClient()` + `emailExists()`(@deprecated) + `getUserStatus()`(@deprecated) + **`getUserIdentitySummary(email)`**(권장: `{found:false}` | `{found:true,confirmed,hasPassword,oauthProviders[]}` | `null`) + **`getAdminEmails()`**(`profiles.role='admin'`→`getUserById`로 이메일, 신청 알림 수신자, 실패 시 `[]`). 내부: `listUsers`로 id 찾고 `getUserById(id)` 재호출해 `identities` 안전 확보(listUsers만으론 빈 배열로 옴). 모두 페이지네이션(perPage 1000, ≤50p) — **같은 액션에서 2번 호출 금지**. 에러 시 `null`(호출 측 "일시적 오류" 분기).
- `src/proxy.ts` — Next 16 `proxy` 컨벤션(v15 `middleware` 리네임), `updateSession` 호출, `matcher`로 정적 자산 제외. 헬퍼 `middleware.ts` 파일명은 유지.
- **환경 변수**: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`(필수), `SUPABASE_SERVICE_ROLE_KEY`(admin용, **`NEXT_PUBLIC_` 절대 금지**), `NEXT_PUBLIC_SITE_URL`(운영 권장). **`RESEND_API_KEY`**(신청 알림 메일, 미설정 시 발송 no-op) + **`APPLICATION_NOTIFY_FROM`**(선택, 발신 주소 — Resend 인증 도메인; 미설정 시 `onboarding@resend.dev` 테스트 발신) + **`SOLAPI_API_KEY`/`SOLAPI_API_SECRET`/`SOLAPI_SENDER`**(전화번호 SMS 인증, Solapi/CoolSMS — 발신번호는 사전등록 필수; 미설정 시 OTP 발송 실패 = 인증 불가) + **`NEXT_PUBLIC_PORTONE_STORE_ID`/`NEXT_PUBLIC_PORTONE_CHANNEL_KEY`**(PortOne V2 결제창, 클라 공개; 테스트 채널)·**`PORTONE_V2_API_SECRET`**(PortOne V2 REST 결제 검증, **서버 전용 — `NEXT_PUBLIC_` 절대 금지**; 콘솔 API Keys의 **V2 API Secret**이어야 함, V1 키·웹훅 시크릿·오타·공백이면 REST 401)·**`PORTONE_V2_WEBHOOK_SECRET`**(웹훅 서명 검증 `Webhook.verify`, **서버 전용**; 콘솔 웹훅 설정 시크릿, 미설정 시 웹훅 500; 콘솔에 웹훅 URL `https://<도메인>/api/payments/portone/webhook` 등록 필요, 로컬은 터널). PortOne 결제 env 미설정 시 결제 버튼이 "결제 설정이 필요합니다" 안내.
- **Dashboard 설정**: Site URL = 운영 도메인, Redirect URLs에 `https://<도메인>/auth/confirm` + `http://localhost:3000/**`. 권장 메일 템플릿: `{{ .SiteURL }}/auth/confirm?token_hash={{ .TokenHash }}&type=email&next=/`. **카카오**: Kakao Developers에서 앱 생성 → REST 키+Secret → Redirect URI `https://<ref>.supabase.co/auth/v1/callback` → **이메일 "필수동의"**. Supabase Providers>Kakao 활성화 + **"Allow account linking" 활성화**(같은 이메일 이메일/카카오 자동 연결, 비활성 시 콜백 실패).

### DB 스키마 (`supabase/`)

마이그레이션은 `db:new`로만 생성(CLI는 timestamp 형식만 인식), Dashboard SQL 직접 실행 금지. link 정보는 `supabase/.temp/`(gitignore, 새 머신은 `npx supabase login` + `link --project-ref`). **⚠️ Claude는 CLI 원격 접근 불가(403) → `db:push`/`db:types`는 사용자가 직접 실행.** 핵심 마이그레이션 4개(+ `20260609013944` cooking→workhol 리네임, `20260609230959` `reading_progress.scroll_percent` 제거, **`20260610232255` profiles 강사 컬럼·role 자가변경 차단 트리거 / `20260610232258` `avatars` Storage 버킷 / `20260612052457` `teacher_availability` 테이블** — **모두 db:push·db:types 적용 완료**, + `20260612014004` 미구현 LMS 화상수업 객체 롤백(`classes`/`class_sessions`/`academic_events` `drop if exists`, 공유 `tg_set_updated_at` 보존 — 순net 무변경, CLAUDE.md 미문서화 기능):
- **`20260522065400_create_profiles_and_role_trigger.sql`**: `user_role` ENUM(`admin|teacher|student`); `profiles`(`id` PK FK→auth.users CASCADE, `role` DEFAULT 'student', `created_at`/`updated_at`); RLS + `profiles_select_own`/`_update_own`(`auth.uid()=id`, **INSERT/DELETE 정책 없음**); `handle_new_user()`(SECURITY DEFINER) + `on_auth_user_created` trigger(모든 가입 경로에서 `profiles` insert + `raw_app_meta_data`에 `{role:'student'}` merge); `tg_set_updated_at()` trigger. **backfill 포함**. **role 활용: admin 대시보드(`/admin`)·강사 페이지(`/teacher`)에서 사용** — `src/lib/auth.ts`의 `isAdmin`/`isTeacher`/`getUserRole`(`profiles.role`, app_metadata는 stale 가능해 profiles 우선). ⚠️ `_update_own`엔 WITH CHECK 없어 role 자가변경 가능했으나 **`20260610232255`의 `prevent_role_self_change` 트리거**(BEFORE UPDATE, `current_user not in (service_role,postgres,supabase_admin)`이면 role을 OLD로 되돌림)로 차단 — 일반 사용자 escalation 불가, admin 액션(service_role)만 변경 가능.
- **`20260610232255_extend_profiles_teacher.sql`**: `profiles`에 `full_name`/`avatar_url`/`zoom_url`/`bio`/`headline`/`phone` 컬럼(강사 프로필, 향후 학생 공용) + `prevent_role_self_change` 트리거(위). ⚠️ `headline`은 **`20260617004232_profiles_drop_headline.sql`로 제거됨**(강사 프로필 한 줄 소개 항목 폐지). + **`experience`는 `20260617011522`로 추가**(강사 신청폼↔프로필 일치). db:push·db:types 적용 완료.
- **`20260612234016_split_profiles_name.sql`**: `profiles`에 `first_name`/`last_name` 추가 + 구 `full_name`에서 백필(마지막 공백 split: 앞=first, 끝 토큰=last, 공백 없으면 전체=first). `full_name`은 **drop 안 함**(비파괴 보존, 앱은 first/last만 사용). db:push·db:types 적용 완료.
- **`20260623054826_add_address_to_profiles.sql`**: `profiles`에 `postcode`/`address`/`address_detail` 추가(학생 마이페이지 주소, **선택 입력 nullable**). 트리거/RLS 변경 없음(`profiles_update_own`로 본인 갱신). ⚠️ db:push·db:types 적용 완료.
- **`20260624225034_add_english_name_to_profiles.sql`**: `profiles`에 `english_name text` 추가(학생/공용 영문 이름, **수강신청 필수**·마이페이지 입력). 민감 컬럼 아니라 트리거/RLS 변경 없음(`profiles_update_own`). 강사 표시용 스냅샷은 `enrollments.student_english_name`(`20260624233056`). ⚠️ db:push·db:types 적용 완료.
- **`20260623075341_add_enrollments.sql`**: `enrollment_status` ENUM(`신청|승인|결제대기|결제완료|거절|취소` — `결제대기`는 `20260625001341`로 `after '승인'`, **`결제완료`는 `20260626015712`로 `after '결제대기'`**(admin 입금 확인 시 전환, 결제 1단계); 슬롯 점유: `승인`·`결제완료`=하드 확정(가용 차감, **단 `결제완료`는 과정 종료 후 자동 해제** — `loadEndedEnrollmentIds`), `신청`·`결제대기`=소프트 보류); `enrollments`(`id` uuid PK, `student_id`/`teacher_id` FK→auth.users **ON DELETE CASCADE**(로그인 필수), `course`/`course_title`, `start_date date`, `slots jsonb`(`[{day,min}]` 주간 30분 슬롯 스냅샷), `teacher_name`/`student_name`/`student_english_name`/`student_phone`(표시·SMS 스냅샷; `student_english_name`은 **`20260624233056`로 추가**+백필 — 강사 세션은 RLS로 학생 profiles 직접 조회 불가라 스냅샷), `status` DEFAULT '신청', `teacher_note`, `created_at`/`updated_at`); 인덱스 `(student_id)`/`(teacher_id,status)`/`(created_at desc)`; RLS + `_insert`(`to authenticated` `student_id=auth.uid()`) + `_select_own_student`(본인 학생, mypage) + `_select_own_teacher`(본인 강사, 대시보드). **UPDATE/DELETE 사용자 정책 없음**(승인/거절/**학생 자가 취소**는 강사·학생 액션 service_role: `cancelEnrollment`(`src/app/mypage/actions.ts`)=본인 소유+상태 `신청`/`승인`/`결제대기` 가드→`취소`, 강사에 취소 이메일 `sendEnrollmentCancellationToTeacher` best-effort). `tg_set_updated_at` 재사용. 강사 매칭형 수강신청의 저장소(구 상담신청 `applications` 폐지·대체). ⚠️ db:push·db:types 적용 완료. + **`20260628014316_add_enrollment_test_fields.sql`**: `enrollments`에 `total_sessions int`(null=기본 24, admin 테스트 수강신청의 자유 횟수→클래스 생성 개수)·`is_test boolean default false`(admin 테스트 생성분 식별/정리) 추가(additive, 기존 무영향). ⚠️ **db:push·db:types는 사용자가 직접 실행 필요**. + **`20260712190316_add_course_english_title.sql`**: `enrollments`·`classes`에 `course_english_title text`(nullable) 추가 — admin 테스트 신청의 커스텀 과정 영문명 스냅샷(실 신청·등록 과정은 null→`getCourse` 해석). 강사 표시/이메일 폴백 `getCourse(course)?.englishTitle ?? course_english_title ?? course_title`(classroom·teacher requests·강사/관리자 이메일 6곳). db:push·db:types 적용 완료. + **`20260712211454_add_enrollment_price.sql`**: `enrollments.price_krw integer`(nullable) 추가 — per-건 수강료(admin 테스트 신청만, null→`COURSE_PRICE_KRW` 폴백). 유효가격이 표시·무통장·카드·매출 authoritative(위 pricing 항목). db:push·db:types 적용 완료.
- **`20260626221643_add_classes.sql`**: `classes`(화상수업 클래스 — 결제 확정 시 `confirmPayment`가 날짜별 자동 생성, 위 화상수업 항목): `id` uuid PK, `enrollment_id`/`student_id`/`teacher_id` FK→auth.users·enrollments **cascade**, `course`/`course_title`, 표시 스냅샷 `teacher_name`/`student_name`/`student_english_name`, `session_no smallint`(회차), `session_date date`(KST), `start_min`/`end_min smallint`(30배수 CHECK), `unique(enrollment_id,session_no)`(멱등 생성); 인덱스 `(student_id,session_date)`/`(teacher_id,session_date)`/`(enrollment_id)`; RLS + `_select_own_student`/`_select_own_teacher`만(**INSERT/UPDATE/DELETE 사용자 정책 없음** — 생성 service_role·정리 cascade); `tg_set_updated_at` 재사용. **db:push·db:types 적용 완료.**
- **`20260627011057_add_class_cancellation.sql`**: `classes`에 `status text default '예정' check in('예정','취소')`(개별 수업 취소)·`is_makeup boolean default false`(자동 보강 회차 식별) 추가. 취소 시 보강은 `session_no=enrollment 최대+1`로 부여(기존 unique 충돌 없음). **db:push·db:types 적용 완료.**
- **`20260629234513_add_class_feedback.sql`**: `classes`에 `feedback text`·`feedback_at timestamptz` 추가(수업 종료 후 강사 피드백, 수강생 읽기 전용). 트리거/RLS 변경 없음(추가 컬럼만 — 쓰기는 `saveClassFeedback` service_role 액션이 강사 소유권+종료 가드로 처리, 읽기는 기존 `classes_select_own_student`/`_teacher`). ⚠️ **db:push·db:types는 사용자가 직접 실행 필요**.
- **`20260630021451_add_class_conducted.sql`**: `classes`에 `teacher_entered_at timestamptz`(강사 Enter 첫 입장 시각)·`conducted_at timestamptz`(수업 진행 인정, sticky) 추가. 트리거/RLS 변경 없음(쓰기는 `enterClass`/`saveClassFeedback` service_role 액션). 진행 인정 규칙=강사 입장 AND 피드백 저장. ⚠️ **db:push·db:types는 사용자가 직접 실행 필요**.
- **`20260702020332_add_class_teacher_reassigned.sql`**: `classes`에 `teacher_reassigned_at timestamptz` 추가(admin 강사 대체 시각). 트리거/RLS 변경 없음(쓰기=`adminReassignClass`/`adminReassignRemaining` service_role, 읽기=기존 student/teacher select). 학생 강의실 예정 수업 "강사 변경" 표시용. ⚠️ **db:push·db:types는 사용자가 직접 실행 필요**.
- **`20260703082705_add_class_conducted_override.sql`**: `classes`에 `conducted_override boolean`(nullable, null=자동/true=강제 진행됨/false=강제 미진행) 추가=admin 수동 진행여부 보정. 트리거/RLS 변경 없음(쓰기=`adminSetClassConducted` service_role, 읽기=기존 student/teacher select). 정산·admin·강사 마커가 유효값 `override ?? !!conducted_at` 공통 사용. ⚠️ **db:push·db:types는 사용자가 직접 실행 필요**.
- **`20260704010328_add_class_cancel_reason.sql`**: `classes`에 `cancel_reason text check in('student','company','cancel')` 추가(연기/취소 사유·남은 연기 차감 구분: `student`=수강생 요구 연기(보강 O·차감)/`company`=회사 사유 연기(보강 O·미차감)/`cancel`=취소(보강 X)). 기존 `status='취소'` 행은 `'student'`로 백필(변경 전 카운트 보존). **"남은 연기"=`cancel_reason='student'`만 집계**(학생 `cancelClass`·admin 목록 `postponed`·학생 강의실 `cancelledCount`·admin 모달 `postponedCount` 4곳 정합; 학생 강의실 배지도 `cancel`=취소/그 외=연기로 구분). 트리거/RLS 변경 없음(쓰기=`cancelClass`/`adminCancelClass` service_role). ⚠️ **db:push·db:types는 사용자가 직접 실행 필요**.
- **`20260707030036_add_enrollment_events.sql`**: `enrollment_events`(과정 라이프사이클 감사 로그, **추가 전용·불변**): `id` uuid PK, `enrollment_id` FK→enrollments **on delete set null**·`class_id` FK→classes **set null**(도메인 행 삭제=테스트 신청·계정 삭제 후에도 로그 보존)·`event_type text`(앱 union 검증)·`actor_id` FK→auth.users **set null**·`actor_role`·표시 스냅샷(`actor_name`·`course`·`course_title`·`student_name`·`teacher_name`)·`detail jsonb`·`created_at`(**`updated_at`·트리거 없음**=불변). 인덱스 `(enrollment_id,created_at desc)`/`(created_at desc)`; RLS enable + **정책 없음=service_role만 읽기/쓰기**(사용자 노출 없음). 쓰기=`logEnrollmentEvent`(`src/lib/events.ts`), 조회=`/admin/classes/[enrollmentId]` page가 service_role로. **db:push·db:types 적용 완료.**
- **`20260707061111_add_class_original_teacher.sql`**: `classes`에 `original_teacher_id uuid` FK→auth.users **on delete set null** 추가 + **RLS select 정책 `classes_select_original_teacher`(`using(auth.uid()=original_teacher_id)`)**. **1회성 강사 대체(`adminReassignClass`)로 대타에게 넘어간 회차를 원 강사가 read-only로 계속 열람**하기 위함(대체 시 덮어쓰기 전 원 강사 id를 최초 1회 coalesce 저장). 기존 두 select 정책과 OR 결합. 쓰기 정책 없음(service_role만). ⚠️ **db:push·db:types는 사용자가 직접 실행 필요**.
- **`20260708092653_add_payments.sql`**: `payments`(결제/환불 트랜잭션 기록 — **PortOne 카드 + 무통장 입금 공용**): `id` uuid PK, `payment_id text unique`(카드=PortOne paymentId, **무통장=합성 `bank-{enrollmentId}`**), `enrollment_id` FK→enrollments **set null**·`student_id` FK→auth.users **set null**, `amount`·`currency`·`status`(check `paid`/`cancelled`/`partial_cancelled`/`failed`)·`cancelled_amount`·`pg_tx_id`·`method`·`receipt_url`·`raw jsonb`·`created_at`/`updated_at`; 인덱스 `(enrollment_id)`/`(student_id,created_at desc)`; RLS + `payments_select_own`(본인 select) — **INSERT/UPDATE/DELETE 사용자 정책 없음**(쓰기=service_role: `recordPayment`/`refundEnrollmentPayment`); `tg_set_updated_at` 재사용. 결제 확정 시 `settlePortonePayment`가 upsert(멱등), 환불 시 상태·`cancelled_amount` 갱신. ⚠️ **db:push·db:types는 사용자가 직접 실행 필요**. + **`20260710035628_add_payments_note.sql`**: `payments.note text` 추가(무통장 입금 확정 시 실입금액이 정가보다 적은 경우(할인 등) 사유 코멘트; 쓰기=`recordPayment` service_role, admin 상세·매출 대시보드 목록/CSV에 노출). 트리거/RLS 무변경. ⚠️ **db:push·db:types는 사용자가 직접 실행 필요**.
- **`20260610232258_create_avatars_bucket.sql`**: Storage 버킷 `avatars`(public read) + `storage.objects` 정책(공개 select + authenticated insert/update/delete는 본인 폴더 `(storage.foldername(name))[1]=auth.uid()`). 프로필 사진 업로드 경로 `avatars/<uid>/...`.
- **`20260612052457_add_teacher_availability.sql`**: `teacher_availability`(복합 PK `(teacher_id,day_of_week,start_min)`, `teacher_id` FK→auth.users CASCADE, `day_of_week smallint [0,6]`(0=일, JS getDay 동일), `start_min smallint`(자정 기준 분, 30배수·[0,1439] CHECK — UI 06:00~24:00 범위는 비종속), `created_at`/`updated_at`); 인덱스 `(teacher_id)`; RLS + `_select/_insert/_delete_own`(`auth.uid()=teacher_id`, **UPDATE 정책 없음**=슬롯 boolean이라 add/delete로 표현); `tg_set_updated_at` 트리거. admin 정책 없음(service_role 우회). 가용 슬롯 1개=row 1개, 강사 액션은 delete→bulk insert로 전체 교체.
- **`20260523231214_add_reading_progress.sql`**: `reading_progress`(복합 PK `(user_id,course,unit)`, `user_id` FK CASCADE, `course text`, `unit int [1,100]`, `completed bool DEFAULT false`, `last_viewed_at`); 인덱스 `(user_id,course)`; RLS + `_select/_insert/_update_own`(`auth.uid()=user_id`, **DELETE 정책 없음**). ⚠️ 최초엔 `scroll_percent int [0,100]` 컬럼도 있었으나 `20260609230959`에서 제거(수동 완료 정책).
- ~~**`20260608233330_add_applications.sql`**(상담신청 `applications` 테이블 + `application_status` enum)~~ → **`20260627224406_drop_applications.sql`로 폐지**(`drop table … cascade` + `drop type application_status`). 상담신청 동선이 수강신청 `enrollments`로 완전 대체되어 admin '신청 관리' UI·메일러·테이블 일괄 제거. 공유 `tg_set_updated_at`는 보존(트리거만 cascade 제거). ⚠️ **db:push·db:types는 사용자가 직접 실행 필요**.
- **`20260616013935_add_teacher_applications.sql`**: `teacher_application_status` ENUM(`신청|승인|거절`); `teacher_applications`(`id` uuid PK, `user_id` FK→auth.users **ON DELETE CASCADE**(로그인 필수, 익명 불가), `name`/`first_name`/`last_name`/`phone`/`bio`(필수)/`experience`/`zoom_url`/`avatar_url`, `status` DEFAULT '신청', `admin_note`, `created_at`/`updated_at`); 인덱스 `(user_id)`/`(status)`/`(created_at desc)` + **부분 유니크 `(user_id) where status='신청'`**(중복 대기 신청 차단, 거절/승인 후 재신청 허용); RLS + `teacher_applications_insert`(`to authenticated` `with check (user_id=auth.uid())` — 익명 제외) + `_select_own`(본인, mypage 상태표시). **UPDATE/DELETE 사용자 정책 없음**(승인/거절/role은 admin service_role). `tg_set_updated_at` 재사용. db:push·db:types 적용 완료. **role 부여는 이 flow로만**(Members 직접전환 폐지). + **`20260616234643_..._phone_nullable.sql`**(phone `drop not null`+빈문자열→null) + **`20260617000547_..._split_name.sql`**(`first_name`/`last_name` 추가·name에서 백필, 구 `name`은 표시/검색/메일 호환 위해 보존하고 조합값으로 계속 채움) + **`20260617003445_..._drop_headline.sql`**(headline 컬럼 제거) + **`20260617011522_align_teacher_application_profile.sql`**(강사 신청폼↔프로필 필드 일치: `teacher_applications` `intro`→`bio` 리네임 + `zoom_url`/`avatar_url` 추가, `profiles`에 `experience` 추가) + **`20260617072141_approve_teacher_application_fn.sql`**(승인 원자 처리 RPC `approve_teacher_application(uuid) returns text`, security definer, service_role grant — 상태 '신청' 가드 + role/profile/status 단일 트랜잭션) + **`20260620000000_add_nationality_teacher.sql`**(`teacher_applications`·`profiles`에 `nationality text` 추가(국가명 문자열 저장, 9개국 `src/data/nationalities.ts`) + 승인 RPC `create or replace`로 `nationality=coalesce(신청서,기존)` 한 줄 추가) + **`20260620010000_add_gender_teacher.sql`**(`teacher_applications`·`profiles`에 `gender text` 추가(코드 male/female 저장, `src/data/genders.ts`) + 승인 RPC `create or replace`(nationality·gender coalesce 모두 포함한 최신 정의)) + **`20260620020000_add_centers.sql`**(`centers` 테이블(id·name·sort_order, RLS 공개 select·쓰기 admin service_role만, `tg_set_updated_at`, 시드 Lani·Sebu) + `teacher_applications`·`profiles`에 `center_id uuid` FK→centers `on delete set null` + 승인 RPC `create or replace`(nationality·gender·center_id coalesce 포함 최신 정의)) + **`20260621233848_add_center_price.sql`**(`centers.price_per_session integer` nullable=센터 1회당 단가; **`20260703200612_center_price_decimal.sql`로 `numeric(12,2)`로 확장**=USD 소수 첫째자리 지원, ⚠️ **db:push·db:types는 사용자가 직접 실행 필요**) + **`20260710231928_custom_teacher_rate.sql`**(`profiles.custom_price_per_session numeric(12,2)`·`custom_price_currency text` 추가, 둘 다 nullable·기본값 없음=미설정 시 소속 센터 단가 사용 — 강사 개별 정산 단가 오버라이드, 트리거/RLS 무변경(가격 컬럼은 `prevent_role/phone_self_change` 무관), **db:push·db:types 적용 완료**; ⚠️ 이 두 컬럼은 이후 **`20260711020100_rate_schedules.sql`로 "현재값 캐시"로 격하**) + **`20260711020100_rate_schedules.sql`**(`rate_schedules`(id·scope('center'|'teacher')·scope_id·`price_per_session numeric(12,2)`(null=teacher 센터복귀/center 미설정)·currency·`effective_from date`·note·updated_at) — **정산 단가 적용일 이력=단일 진실 소스**: 정산(`settlements/page.tsx`)이 수업 `session_date` 기준 `effectiveRate`(`src/lib/rates.ts`)로 역산(강사 개별>센터), `centers`/`profiles` 단가 컬럼은 오늘 기준 캐시(이력 CRUD `addRateSchedule`/`updateRateSchedule`/`deleteRateSchedule`가 `syncRateCache`로 동기화). 인덱스 `(scope,scope_id,effective_from desc)`, RLS enable+정책 없음=service_role만, `tg_set_updated_at`. 백필=기존 현재값→`effective_from='2000-01-01'` 1행씩(과거 수업 커버, 회귀 안전). `updateTeacherRate` 폐지·`updateCenter` 단가 분리. UI=공유 `RateHistoryEditor`(센터=`CenterDetailModal`, 강사=`TeacherInfoModal`). ⚠️ **db:push·db:types 사용자 직접 실행 필요**) + **`20260622013926_add_center_currency.sql`**(`centers.price_currency text not null default 'KRW'`=단가 통화 KRW/PHP, `src/data/currencies.ts`) + **`20260703022012_add_center_manager.sql`**(`centers.manager_name text` nullable=센터 담당 매니저 이름, 선택 입력, 트리거/RLS 무변경 — ⚠️ **db:push·db:types는 사용자가 직접 실행 필요**) + **`20260713003440_add_center_manager_id.sql`**(`centers.manager_id uuid` FK→auth.users `on delete set null`+인덱스=센터 매니저 계정, 지정 시 '센터 관리' 권한. 트리거/RLS 무변경 — 위 센터 매니저 항목. ⚠️ **db:push·db:types 사용자 직접 실행 필요**) + **`20260622014843_add_settings_table.sql`**(`settings` key-value 테이블(key PK·value·updated_at, RLS 공개 select·쓰기 service_role만, `tg_set_updated_at`, 시드 `php_to_krw='25'`)=앱 전역 설정, 현재 페소 환율 1건) + **`20260622230424_add_phone_verification.sql`**(`profiles.phone_verified_at timestamptz` + `phone_verifications` 테이블(user_id PK FK→auth.users cascade·phone·code_hash·expires_at·attempts·last_sent_at, RLS enabled+정책 없음=service_role만)=SMS OTP 챌린지 + **트리거 `prevent_phone_self_change`**(BEFORE UPDATE profiles, `prevent_role_self_change` 미러: service_role 외엔 `phone`/`phone_verified_at`를 OLD로 되돌림 → 인증 우회 차단)) — **모두 db:push·db:types 적용 완료**.
- **`20260608235010_add_youtube_and_admin.sql`**: `youtube_videos`(`id` uuid PK, `tag`/`url`/`title`/`description`, `is_visible` bool DEFAULT true, `sort_order` int, `created_at`/`updated_at`); 인덱스 `(is_visible,sort_order)`; RLS + **`youtube_videos_select_visible`(`to anon,authenticated` `using (is_visible=true)`)**, **쓰기 정책 없음**(admin service_role만); `tg_set_updated_at` 트리거; **시드 3건**. + **admin 부여 DO 블록**: `82orez@gmail.com`의 `profiles.role='admin'` & `auth.users.raw_app_meta_data.role='admin'`(계정 미존재 시 no-op).
- **타입** `src/types/database.types.ts`: `db:types`로 생성. **`db:push` 후 항상 재생성**(drift 방지). `Database["public"]["Tables"][...]["Row"|"Insert"|"Update"]`. (현재 SSR client는 `<Database>` 미적용 → `.from("enrollments")`·`.from("youtube_videos")` 등 untyped.)

## 주의사항

- **TS 느슨함**: `strict:false`, `noImplicitAny:false` — 타입 명시 확인 권장.
- **Tailwind v4 `@apply` 제한**: `@layer components` 안에서 다른 커스텀 컴포넌트 클래스 `@apply` 시 빌드 실패 → 유틸 추출 또는 JSX className 조합.
- **shadcn + `cn()`**: 신규 UI는 `src/components/ui/` 재사용 우선, 추가는 CLI. `base-nova`/`neutral` 변경 금지. className은 항상 `cn()`(직접 결합 시 tailwind-merge 누락).
- **Server Action + `useActionState` 유지**: `fetch` 없이 `<form action>`. 성공 시 `revalidatePath`→`redirect`, 실패 시 `{error}`(/`{success}`). 인증 액션 import는 `@/app/(auth)/<route>/actions`.
- **Controlled input 필수**: Base UI `defaultValue` 경고 + React 19 auto-reset → 항상 `useState`+`value`+`onChange`, round-trip 금지.
- **인증 액션 순서**: (1) 필수값 검증 (2) `rateLimit()` (3) `isValidEmail()` (4) Supabase 호출. rate limit 키는 `email.toLowerCase()`. redirect는 `getOrigin()`.
- **사용자 열거 노출은 의도된 정책**: login/signup/forgot 모두 가입/미가입 구분 — 변경 시 세 곳 함께.
- **⚠️ 미확인 사용자 비번 덮어쓰기 방지(절대 우회 금지)**: signup은 `signUp()` 전 `getUserIdentitySummary()`로 확인, `found&&!confirmed` 차단. `!summary.found`일 때만 `signUp()` 호출 순서 유지.
- **Prettier**: `printWidth:150`, `endOfLine:"crlf"`, `singleQuote:false`, `trailingComma:"all"`, `semi:true`, `tabWidth:2`, `prettier-plugin-tailwindcss`.
- **버튼 커서 금지**: 전역 규칙 있음 → `cursor-pointer` 추가 금지(`disabled:cursor-not-allowed`만 허용).
- **색상 토큰 우선**: hex arbitrary value 피하고 시맨틱 토큰 사용.
- **CTA/anchor**: 랜딩 `#courses`, 과정 상세 `#apply-form` 등 anchor + `scroll-behavior:smooth`. `<a>` 버튼화는 `buttonVariants()`+`cn()`.
- **모바일 메뉴 a11y 회귀 주의**(위 Navbar 항목 체인 유지).
- **DB 변경은 항상 마이그레이션**: `db:new`→검토→`db:push`. **`db:push`는 destructive** — 직후 `/signup` 회귀 테스트(가입 성공 + `profiles` row + `raw_app_meta_data.role='student'`). 파일명 수동 명명 금지.
- **⚠️ role은 `app_metadata`에만, `user_metadata` 절대 금지**(후자는 클라가 `updateUser({data})`로 수정 가능 → 권한 우회). role 읽기는 JWT `app_metadata.role` 또는 `profiles.role`만. role 부여 시 `admin.auth.admin.updateUserById(id,{app_metadata:{role}})` + `profiles` update 함께.
- **profiles RLS 최소(본인 row만)**: admin 전체 조회 정책 미구현 → 필요 시 `createAdminClient()` 우회. 향후 `profiles_admin_all` 정책 추가.
- **교재 완료 액션 가드 유지**(위 `src/app/textbook/actions.ts` 항목): `setUnitCompleted`은 레지스트리 화이트리스트+unit 존재 검증 후 upsert. 완료는 **사용자 수동 토글만**(스크롤 자동 판정 부활 금지).

## 별칭

`@/*` → `./src/*`. 이미지는 `next/image`만(`public/images/`). **강사 아바타는 Supabase Storage 공개 URL** → `next.config.ts` `images.remotePatterns`에 `*.supabase.co` `/storage/v1/object/public/**` 등록됨. Favicon은 `src/app/icon.svg`(파일 컨벤션).

## 배포

Vercel 배포. `vercel.json`은 `regions:["icn1"]`(서울 리전 고정)만 설정 — 한국 사용자 대상 레이턴시 최적화. 환경 변수는 위 Supabase SSR 항목 참조.
