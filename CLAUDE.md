# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트 개요

"프렌딩 스쿨" 워홀 영어 교육 과정의 랜딩 페이지입니다. Next.js 15(App Router) + React 19 + Tailwind CSS v4 기반의 단일 페이지 마케팅 사이트이며, Supabase SSR 인증을 위한 기반 코드와 함께 shadcn/ui(`base-nova` 스타일, `neutral` baseColor) 컴포넌트, Supabase Server Actions 기반 이메일 인증 화면(`/login`, `/signup`, `/forgot-password`, `/reset-password`, `/auth/confirm`)이 함께 구성되어 있습니다.

## 자주 사용하는 명령어

- 개발 서버 실행: `npm run dev` (Turbopack, 기본 포트 3000)
- 프로덕션 빌드: `npm run build` (Turbopack)
- 프로덕션 서버 실행: `npm start`
- shadcn 컴포넌트 추가: `npx shadcn@latest add <component>` (현재 `shadcn` v4.7.0 사용, `components.json` 별칭/스타일을 따라 생성됨)

별도의 lint/test 스크립트는 정의되어 있지 않습니다(테스트 프레임워크 미설치).

## 코드 구조 및 아키텍처

- **App Router 단일 페이지 구조**: 마케팅 콘텐츠 본문은 `src/app/page.tsx`(서버 컴포넌트) 한 파일에 모두 들어 있습니다. 섹션(히어로, 후회 3가지, 차별점, 과정 정보, 후기, 커리큘럼, CTA 등)은 별도 컴포넌트로 분리되어 있지 않고 파일 상단의 데이터 배열(`reasons`, `lastReason`, `opportunityCards`, `infoCards`, `reviewsTop`, `reviewBottom`, `units`)을 `.map()`으로 렌더링합니다. 섹션 카피/항목을 수정할 때는 해당 데이터 배열을 먼저 찾아 편집하는 것이 빠릅니다.
- **공통 컴포넌트**: 네비게이션 바(슬라이드 메뉴 포함)와 푸터는 `src/components/Navbar.tsx`, `src/components/Footer.tsx`로 분리되어 `src/app/layout.tsx`에서 `{children}`을 감싸도록 배치됩니다. `Navbar`는 슬라이드 메뉴 상태(`useState`) 때문에 `"use client"` 클라이언트 컴포넌트이며, **로그인 사용자 정보를 `user: { email?: string | null } | null` prop으로 받습니다**(layout이 SSR 단계에서 Supabase로 조회 후 전달). 또한 `supabase.auth.onAuthStateChange` 구독 + `pageshow` 이벤트(bfcache 복원) 리스너로 다른 탭의 로그인/로그아웃, 토큰 갱신, 브라우저 뒤로/앞으로 시에도 stale 없이 즉시 갱신됩니다. 데스크톱(`md+`)에서는 햄버거 옆 인라인으로, 모바일에서는 슬라이드 메뉴 내부에 로그인 상태별 인증 메뉴("로그인/회원가입" 또는 "이메일/로그아웃")가 노출되며 가운데 컬럼이 항상 뷰포트 정중앙에 위치하도록 좌/중/우 3개 `flex-1` 컬럼 구조를 유지합니다. 로그아웃은 `src/app/logout/actions.ts`의 Server Action을 `<form action={logout}>`로 호출하므로 클라이언트 컴포넌트에서도 직접 import해 사용합니다. `Footer`는 서버 컴포넌트입니다. 전역 색상/폰트(`bg-white font-sans text-[#1a1a1a]`)는 `layout.tsx`의 `<body>` 클래스에 적용되어 있고, `<html>` 클래스는 `cn("font-sans", geist.variable)`로 합성됩니다.
- **루트 레이아웃**: `src/app/layout.tsx`는 **`async` 서버 컴포넌트**로, `createClient(await cookies()).auth.getUser()`를 호출해 현재 사용자를 조회한 뒤 `<Navbar user={user ? { email } : null} />`로 전달합니다(미들웨어가 매 요청마다 세션을 갱신하므로 여기서 다시 호출해도 안전). 또한 `<AuthHashHandler />`(`src/components/auth/AuthHashHandler.tsx`)를 모든 페이지에 마운트해, Supabase implicit 플로우(`#access_token=...&type=signup`)로 도착한 토큰을 클라이언트에서 감지·`setSession` 처리하고 적절한 페이지(`/?verified=success` 또는 `/reset-password`)로 `window.location.replace` 합니다(처리 중 풀스크린 로딩 오버레이 표시). `lang="ko"`, 한글 메타데이터, Pretendard 가변 폰트(`next/font/local`로 `node_modules/pretendard/dist/web/variable/woff2/PretendardVariable.woff2` 로드, `--font-pretendard` CSS 변수)를 설정하고, `next/font/google`의 `Geist`를 추가로 로드해 `<html>`에 `geist.variable`(`--font-sans`)을 적용합니다. 다만 `globals.css`의 `@theme inline { --font-sans: var(--font-pretendard); }` 매핑 때문에 **`font-sans` 유틸리티의 실제 글꼴은 Pretendard**이며, Geist 변수는 현재 본문에 직접 사용되지는 않습니다(향후 영문 전용 컴포넌트용). 페이지 제목/설명은 한국어 마케팅 카피이므로 변경 시 SEO 영향을 고려해야 합니다.
- **스타일링**: Tailwind CSS v4를 `src/app/globals.css`의 `@import "tailwindcss"` + `@import "tw-animate-css"` + `@import "shadcn/tailwind.css"` + `@theme inline` 방식으로 사용합니다(별도 `tailwind.config` 파일 없음). `@custom-variant dark (&:is(.dark *))`로 다크 모드 변형을 등록하며, `@theme inline`에 shadcn 색상 토큰(`--color-background`, `--color-primary`, `--color-card`, `--color-muted`, `--color-destructive`, `--color-sidebar-*`, `--color-chart-1~5`)과 `--radius-{sm,md,lg,xl,2xl,3xl,4xl}` 스케일을 매핑합니다. `:root`/`.dark` 블록에 OKLch 기반 라이트·다크 값이 정의되어 있으며, 다크 모드는 `<html class="dark">` 토글 방식으로 CSS만 준비된 상태입니다(토글러 미구현). `--font-sans`는 `--font-pretendard`에 매핑되어 있고, 페이지 내 강조색은 shadcn `--primary`와 별개로 **브랜드 레드 `#ff4757`**(CTA/포인트 색상)이 인라인 클래스에서 일관되게 사용됩니다. PostCSS는 `@tailwindcss/postcss` 플러그인 하나만 사용합니다(`postcss.config.mjs`). **`@layer base`에 전역 커서 규칙** `button:not(:disabled), [role="button"]:not([aria-disabled="true"]) { cursor: pointer; }` 이 있어 모든 활성 버튼이 자동으로 pointer 커서를 갖습니다(disabled는 브라우저 기본).
- **shadcn/ui 통합**: 설정은 `components.json`(`style: "base-nova"`, `baseColor: "neutral"`, `cssVariables: true`, `iconLibrary: "lucide"`, RSC 활성화)에 있고, 별칭은 `@/components`, `@/components/ui`, `@/lib`, `@/lib/utils`, `@/hooks`로 등록되어 있습니다. 현재 추가된 UI 컴포넌트는 `src/components/ui/{button,card,input,label,alert-dialog}.tsx`입니다. className 병합용 `cn()` 유틸은 `src/lib/utils.ts`에 `clsx` + `tailwind-merge` 조합으로 구현되어 있고, 모든 shadcn 컴포넌트가 이를 사용하므로 새 컴포넌트 작성 시에도 동일하게 사용해야 합니다. 아이콘은 `lucide-react`, 헤드리스 프리미티브는 `@base-ui/react`, 애니메이션 보조 클래스는 `tw-animate-css`를 사용합니다. **`base-nova` 변형 주의사항**: `AlertDialogAction`은 `@base-ui/react`의 `Close`로 감싸지지 않고 그냥 `<Button>`만 렌더하므로 **클릭해도 모달이 자동으로 닫히지 않습니다.** 액션 onClick 핸들러에서 `setOpen(false)`를 직접 호출하거나 dialog를 controlled(`open`/`onOpenChange`)로 사용해 명시적으로 닫아야 합니다. `AlertDialogCancel`은 `Close`로 감싸져 있어 자동 닫힘 정상 동작.
- **인증 라우트(Supabase Server Actions)**: 이메일/비밀번호 로그인·회원가입·로그아웃과 이메일 OTP 확인 플로우가 다음과 같이 구성되어 있습니다.
  - `/login` — `src/app/login/page.tsx`(서버 컴포넌트) + `src/app/login/actions.ts`의 `login()`. **로그인 상태면 페이지에서 `auth.getUser()` 확인 후 `/`로 redirect**(이미 인증된 사용자에게 폼을 노출하지 않음). `searchParams`로 `error=auth-code-error`(빨강 배너 + "회원가입" 다음 액션 링크) 또는 `verified=pending`(녹색 배너, PKCE 교환 실패 시 안내) 표시. 액션은 `signInWithPassword` 호출 후 실패 시 **세 가지 케이스로 분기**: (1) `email_not_confirmed` → "이메일 인증이 완료되지 않았습니다…" + `canResend: true` 플래그 반환(폼에서 재발송 버튼 노출), (2) `invalid_credentials` → `emailExists()`로 분기 ("가입되지 않은 이메일" / "비밀번호 불일치"), `null` 반환 시 일시적 오류 메시지, (3) 그 외 → 일반 오류. 같은 파일에 `resendConfirmation(email)` 서버 액션도 export — `supabase.auth.resend({ type: "signup" })` 호출용으로 LoginForm/SignupForm에서 `useTransition`으로 공유. rate limit + 이메일 형식 검증 적용. 배너는 `AuthBannerSlot`으로 감싸 implicit hash flow 도착 시 깜빡임 방지.
  - `/signup` — `src/app/signup/page.tsx` + `src/app/signup/actions.ts`의 `signup()`. **로그인 상태면 페이지에서 `auth.getUser()` 확인 후 `/`로 redirect**. 이메일/비밀번호(**8자 이상**)/비밀번호 확인 검증 후 **`signUp()` 호출 전에 `getUserStatus(email)` 사전 검사로 분기**: (1) `"confirmed"` → "이미 가입된 이메일입니다…"(로그인 안내), (2) `"unconfirmed"` → "이미 가입 시도된 이메일입니다…" + `canResend: true` 플래그 반환(SignupForm 에러 박스 내부에 "다시 보내기" 버튼 노출, `resendConfirmation` 공유 호출). **이 사전 검사가 핵심 보안 장치** — Supabase는 미확인 사용자가 재가입을 시도하면 저장된 비밀번호를 새 값으로 **덮어쓰므로**, admin API로 가입 상태를 먼저 확인하지 않으면 공격자가 피해자 계정의 비밀번호를 탈취할 수 있음. `null`(admin API 실패) 시 fail closed("일시적 오류" 메시지). `"not_found"`일 때만 `supabase.auth.signUp({ options: { emailRedirectTo: ${origin}/auth/confirm } })` 진행하고 성공 시 `{ success }` 메시지 + 성공 박스에도 동일한 "다시 보내기" 버튼 노출. signUp 이후의 `error.code === "user_already_exists"` / `data.user.identities` 빈 배열 분기는 사전 검사 직후 인증 완료된 race condition 안전망으로 유지. rate limit(IP당 5분에 5회) + 이메일 형식 검증 적용.
  - `/logout` — `src/app/logout/actions.ts`의 `logout()`. `supabase.auth.signOut()` → `revalidatePath("/", "layout")` → `redirect("/")`. 별도 페이지 없이 Navbar의 `<form action={logout}>` 버튼에서 직접 호출.
  - `/forgot-password` — `src/app/forgot-password/page.tsx` + `actions.ts`의 `forgotPassword()`. **로그인 상태면 페이지에서 `auth.getUser()` 확인 후 `/`로 redirect**. `searchParams.error === "link-expired"`이면 빨강 배너(+ "로그인" 다음 액션 링크) 표시. 이메일 입력 후 `emailExists()`로 등록 여부 확인 → 미가입이면 에러, 가입된 경우 `supabase.auth.resetPasswordForEmail(email, { redirectTo: ${origin}/auth/confirm?next=/reset-password })`. 로그인/회원가입과 일관된 사용자 열거 노출 정책. rate limit(이메일당 1분에 1회) + 이메일 형식 검증.
  - `/reset-password` — `src/app/reset-password/page.tsx` + `actions.ts`의 `resetPassword()`. **세션 필수**: 페이지가 서버 컴포넌트로 `auth.getUser()`를 확인하고 미인증 시 `/forgot-password`로 redirect. 액션은 (1) 비밀번호 검증(**8자 이상**, 확인 일치), (2) **새 비밀번호 ≠ 기존 비밀번호 확인** (`persistSession: false` 임시 client로 `signInWithPassword` 시도해 성공하면 동일로 판정 — 메인 recovery 세션은 영향받지 않음), (3) `supabase.auth.updateUser({ password })`, (4) **`supabase.auth.signOut({ scope: "others" })`** 로 다른 기기 세션 강제 무효화(공격자 세션 차단), (5) `revalidatePath` 후 `redirect("/?reset=success")` (홈 상단에 녹색 배너). 이메일 링크 → `/auth/confirm`(recovery 분기) → `/reset-password` 경로 외에는 직접 도달 불가.
  - `/auth/confirm` — `src/app/auth/confirm/route.ts`의 GET 라우트 핸들러. 두 가지 이메일 인증 형식을 모두 처리: (1) `token_hash` + `type`이 있으면 `supabase.auth.verifyOtp()`(Supabase SSR 권장 템플릿), (2) `code`가 있으면 `supabase.auth.exchangeCodeForSession()`(Supabase 기본 PKCE 템플릿 `{{ .ConfirmationURL }}`). **`next` 파라미터는 open redirect 방지를 위해 `/`로 시작하고 `//`로 시작하지 않는 경로만 허용**, 그 외는 `/`로 폴백. **recovery 흐름 식별**(`type === "recovery"` 또는 `next.startsWith("/reset-password")`)로 분기. **성공 시 동작**: recovery가 아니면 redirect 대상 URL에 `verified=success` 쿼리를 추가(홈에서 녹색 배너 노출). recovery는 그대로 `next`(보통 `/reset-password`)로 보냄. **실패 시 동작**: recovery면 `/forgot-password?error=link-expired`, 그 외는 `/login?error=auth-code-error` 또는 `/login?verified=pending`(PKCE 교환 실패 — 이메일은 인증됐을 가능성). 두 분기(token_hash, code) 모두 유지할 것. URL hash로만 오는 implicit 플로우(`#access_token=...`)는 서버에서 못 보므로 layout의 `AuthHashHandler`가 클라이언트에서 처리(동일하게 `/?verified=success`로 보냄).
  - 폼 컴포넌트: `src/components/auth/{LoginForm,SignupForm,ForgotPasswordForm,ResetPasswordForm}.tsx`. 모두 클라이언트 컴포넌트로 **React 19 `useActionState` + `<form action={formAction}>` 패턴** 사용. **모든 입력 필드는 `useState` 기반 controlled input(`value` + `onChange`)**으로 작성 — React 19는 server action 완료 후 form을 자동 reset하고, Base UI `InputPrimitive`도 `defaultValue` 변경 시 콘솔 경고를 띄움. 비밀번호 필드에는 `lucide-react`의 `Eye`/`EyeOff` 아이콘 토글(각 필드 독립 state, 버튼은 반드시 `type="button"`)과 `useCapsLockWarning` 훅(`src/hooks/use-caps-lock.ts`) 기반 Caps Lock 경고가 적용됩니다. 모든 메시지는 한국어 존댓말(마침표 포함). 인증 페이지의 query string 기반 배너(`error=...`, `verified=...`)는 `src/components/auth/AuthBannerSlot.tsx`로 감싸 implicit hash flow로 도착했을 때 깜빡임 없이 자동 숨김 처리.
  - **인증 메일 재발송 UX(LoginForm/SignupForm 공통 패턴)**: `resendConfirmation` 호출 트리거에는 두 단계 보호가 적용됩니다. (1) **확인 모달**: 트리거 버튼 클릭 시 즉시 호출하지 않고 `<AlertDialog>`("인증 메일을 다시 보내시겠습니까?" / [취소] [보내기])를 띄워 사용자 의도를 재확인. (2) **60초 클라이언트 쿨다운**: `resendConfirmation`이 `success`를 반환하면 `setCooldownSec(60)`으로 카운트다운 시작 → 매초 `setInterval`로 감소, 0이 될 때까지 트리거 버튼 disabled + 라벨이 `"${cooldownSec}초 후 다시 시도 가능"`으로 표시. 서버 측 rate limit(이메일당 1분/1회)과 일치시킨 값. 페이지 리로드 시 클라이언트 쿨다운은 리셋되지만 서버 rate limit가 여전히 차단하므로 이중 보호. 두 폼이 동일한 인라인 패턴(`confirmOpen`/`cooldownSec` state + `useEffect` 타이머 + controlled `<AlertDialog>`)을 사용하며, 호출처가 늘면 `useResendConfirmation(email)` 훅으로 추출 고려. **SignupForm은 success 박스와 error+canResend 박스 양쪽에 트리거가 있으니** 한쪽을 수정할 때 다른 쪽도 함께 검토할 것 — 특히 버튼을 조건부(`!resendResult?.success` 등)로 숨기면 카운트다운이 보이지 않는 회귀가 생길 수 있음.
- **재사용 헬퍼 (`src/lib/`, `src/hooks/`)**: 인증 액션과 폼에서 공유하는 작은 유틸들.
  - `src/lib/utils.ts` — `cn()` (clsx + tailwind-merge).
  - `src/lib/origin.ts` — `getOrigin(headers)` — `NEXT_PUBLIC_SITE_URL` → `origin` 헤더 → `x-forwarded-host` + 프로토콜 → `host` 순으로 절대 origin 결정. login(`resendConfirmation`), signup, forgot-password의 redirect URL 구성에 사용.
  - `src/lib/email.ts` — `isValidEmail(email)` — 모든 인증 액션의 서버측 이메일 형식 검증. HTML `type="email"` 우회 요청 방어.
  - `src/lib/rate-limit.ts` — in-memory 토큰 버킷. `rateLimit(key, max, windowMs)` / `getClientIp(headers)` / `formatRetryAfter(sec)`. login(이메일당 1분/10회), signup(IP당 5분/5회), forgot-password·resend(이메일당 1분/1회)에 적용. **주의: 프로세스 메모리라 Vercel 등 멀티 인스턴스에서는 효과 제한** — 트래픽이 늘면 Upstash Redis 등 외부 스토어로 같은 시그니처 유지하며 교체 가능.
  - `src/hooks/use-caps-lock.ts` — `useCapsLockWarning()` — 비밀번호 필드용. `capsLockHandlers`를 input에 spread, `capsLockOn`이 true면 amber 색 경고 노출. 모든 비밀번호 입력 필드(LoginForm 1, SignupForm 2, ResetPasswordForm 2)에 적용.
- **홈 페이지 query string 배너**: `src/app/page.tsx`는 `async` 서버 컴포넌트로 `searchParams`를 받아 `reset=success`(비밀번호 변경 완료) / `verified=success`(이메일 인증 자동 로그인 완료) 일 때 hero 위에 녹색 안내 배너를 노출합니다. 인증 액션의 redirect 목적지가 이 query를 동반합니다.
- **자산**: 이미지/로고는 `public/images/`에 위치하며 `<img src="/images/...">`로 직접 참조됩니다 (`next/image` 미사용). Favicon은 Next.js App Router 파일 컨벤션을 따라 `src/app/icon.svg`로 두면 자동으로 `<head>`에 `<link rel="icon" type="image/svg+xml">`이 주입됩니다(`layout.tsx`에 별도 `metadata.icons` 설정 없음).
- **경로 별칭**: `@/*` → `./src/*` (tsconfig.json).
- **Supabase 인증 SSR 구조**: `@supabase/ssr` + `@supabase/supabase-js`를 사용해 컨텍스트별 클라이언트를 분리해 둡니다.
  - `src/utils/supabase/client.ts` — 클라이언트 컴포넌트용 `createBrowserClient`.
  - `src/utils/supabase/server.ts` — 서버 컴포넌트/Route Handler용 `createServerClient` (Next의 `cookies()` 스토어 주입).
  - `src/utils/supabase/middleware.ts` — `updateSession()`이 매 요청마다 `supabase.auth.getUser()`를 호출해 쿠키 기반 세션을 갱신합니다. **`createServerClient`와 `getUser()` 사이에 다른 로직을 두지 말 것** (세션 갱신 누락으로 사용자가 임의로 로그아웃될 수 있음).
  - `src/utils/supabase/admin.ts` — `service_role` 키로 `@supabase/supabase-js`의 `createClient` 직접 호출(쿠키/세션 무관). 파일 첫 줄의 `import "server-only"`로 **클라이언트 번들 유입 시 빌드 실패**하도록 보호합니다. `createAdminClient()` 외에 두 헬퍼를 export: (1) `emailExists(email): Promise<boolean | null>` — 등록 여부만 boolean으로(login/forgot-password에서 사용), (2) `getUserStatus(email): Promise<"confirmed" | "unconfirmed" | "not_found" | null>` — 이메일 인증 완료 여부까지 구분(`email_confirmed_at` 필드 검사, signup의 비밀번호 덮어쓰기 방지에 사용). 두 함수 모두 `auth.admin.listUsers`를 페이지네이션(perPage 1000, 최대 50페이지) 순회해 매칭. **에러 시 `null` 반환**(서비스 키 누락, 네트워크 등) → 호출 측에서 모호한 케이스로 처리(login/signup/forgot-password 모두 "일시적인 오류가 발생했습니다" 메시지로 분기). 실패는 `console.error`로 서버 로그에 기록되어 운영자 진단 가능.
  - `src/middleware.ts`가 위 `updateSession`을 호출하며, `matcher`로 정적 자산(`_next/static`, `_next/image`, 이미지 확장자, `favicon.ico`)을 제외한 모든 경로에서 동작합니다.
  - 필요한 환경 변수:
    - `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` — 미설정 시 미들웨어 단계에서 런타임 오류.
    - `SUPABASE_SERVICE_ROLE_KEY` — admin 클라이언트 사용(로그인 에러 세분화, 회원가입 미확인 사용자 차단, 비밀번호 찾기 사전 확인) 시 필요. **절대 `NEXT_PUBLIC_` 접두사 금지**(DB 전체 권한 노출).
    - `NEXT_PUBLIC_SITE_URL` (선택, 운영 환경 권장) — `getOrigin()`이 헤더 자동 감지 대신 이 값을 우선 사용. Vercel 등 프록시 환경에서 결정론적 redirect URL 보장.
  - **Supabase Dashboard 설정 요구사항**: Authentication > URL Configuration에서 (1) **Site URL**을 운영 도메인으로 설정(기본 `http://localhost:3000`이면 운영 환경에서 메일 링크가 localhost를 가리켜 실패), (2) **Redirect URLs** 화이트리스트에 `https://<운영도메인>/auth/confirm` 및 (개발용) `http://localhost:3000/**` 추가. 미설정 시 우리가 보낸 `redirectTo`가 무시되고 Site URL로 폴백됨. Email Templates의 기본 `{{ .ConfirmationURL }}`(PKCE 플로우)은 cross-device/메일 prefetch 시 verifier 쿠키 부재로 자동 로그인이 실패할 수 있는데, `AuthHashHandler`가 implicit hash로 폴백 처리하므로 동작은 보장됨. 더 깔끔한 경험을 원하면 템플릿을 `{{ .SiteURL }}/auth/confirm?token_hash={{ .TokenHash }}&type=email&next=/` 형태(token_hash 플로우)로 변경 권장.

## 작업 시 주의사항

- **TypeScript 설정이 느슨함**: `tsconfig.json`에서 `strict: false`, `noImplicitAny: false`로 설정되어 있습니다. 빌드 시 타입 오류가 잡히지 않을 수 있으므로 명시적으로 타입을 확인하는 것이 안전합니다.
- **Tailwind v4 `@apply` 제한**: `@layer components` 안에서 `@apply`로 다른 커스텀 컴포넌트 클래스를 참조하면 빌드가 실패합니다. 컴포넌트 클래스 간 재사용이 필요하면 별도 유틸리티 클래스로 추출하거나 JSX 측에서 className을 직접 조합하세요.
- **shadcn 컴포넌트와 `cn()` 사용 규칙**: 신규 UI 요소는 우선 `src/components/ui/`의 shadcn 컴포넌트(`Button`, `Card`, `Input`, `Label` 등) 재사용을 고려하고, 추가가 필요하면 shadcn CLI(`npx shadcn@latest add ...`)로 추가합니다. `components.json`의 `style: "base-nova"`/`baseColor: "neutral"`을 변경하지 마세요. className 조합은 항상 `@/lib/utils`의 `cn()`을 통해 처리해야 합니다(문자열을 직접 결합하면 `tailwind-merge`가 누락되어 충돌하는 Tailwind 클래스가 중복으로 출력됩니다).
- **Server Action + `useActionState` 패턴 유지**: 인증 폼은 클라이언트에서 `fetch` 호출 없이 `<form action={formAction}>` + `useActionState`로 통신합니다. 새 폼을 추가할 때도 동일한 패턴(`"use server"` 액션 파일 + 클라이언트 폼 컴포넌트 + 한국어 검증 메시지)을 따르고, 액션은 성공 시 `revalidatePath` 후 `redirect`, 실패 시 `{ error }`(필요하면 `{ success }`)를 반환하도록 작성하세요.
- **Controlled input 필수 (Base UI + React 19 form action)**: `@base-ui/react`의 `InputPrimitive`는 uncontrolled 상태에서 `defaultValue`가 변경되면 콘솔 경고를 띄웁니다. 또한 React 19의 `<form action={serverAction}>`은 액션 완료 후 form을 자동 reset하므로 uncontrolled 입력은 에러 시 값이 모두 사라집니다. 따라서 폼 입력은 항상 `useState` + `value` + `onChange`로 controlled 작성하고, server action에서 입력값을 다시 돌려보내는 round-trip은 만들지 마세요(state로 자연스럽게 보존됨).
- **인증 액션 공통 시퀀스**: 새 인증 액션 추가 시 (1) 필수값 비어있음 검증, (2) `rateLimit()` 호출, (3) `isValidEmail()` 검증, (4) 실제 Supabase 호출 순서로 작성하세요. rate limit 키는 케이스 무관 비교를 위해 `email.toLowerCase()`로. redirect URL은 `getOrigin(headerList)`로 결정해 운영 환경의 `NEXT_PUBLIC_SITE_URL` 우선 사용을 보장.
- **사용자 열거(user enumeration) 노출은 의도된 정책**: login/signup/forgot-password 모두 "가입된/미가입 이메일"을 구분해 안내합니다. 일관성과 UX를 위해 채택한 결정이며, 더 엄격한 anti-enumeration이 필요해지면 세 곳을 함께 수정해야 합니다(한쪽만 막으면 다른 페이지로 우회 가능).
- **미확인 사용자 비밀번호 덮어쓰기 방지(절대 우회 금지)**: signup 액션은 `supabase.auth.signUp()` 호출 전에 반드시 `getUserStatus()`로 가입 상태를 확인하고 `"unconfirmed"`이면 차단해야 합니다. 이를 건너뛰면 공격자가 피해자의 미확인 이메일로 임의 비밀번호 재가입을 보내 **저장된 비밀번호를 덮어쓰는 계정 탈취 시나리오**가 가능해집니다. signup 액션을 수정할 때 이 순서(사전 검사 → signUp)를 반드시 유지하세요.
- **Rate limit의 운영 한계**: `src/lib/rate-limit.ts`는 프로세스 메모리 기반이라 Vercel 같은 멀티 인스턴스 serverless 환경에서는 인스턴스 간 카운트가 분리되어 효과가 절반 이하로 떨어집니다. 단일 노드 호스팅에서는 잘 동작하지만, 본격적인 봇/무차별 대입 방어가 필요하면 Upstash Redis 등 외부 스토어로 함수 내부만 교체하세요(호출부 시그니처는 그대로).
- **Prettier 규칙**: `printWidth: 150`, `endOfLine: "crlf"`, `singleQuote: false`(큰따옴표), `trailingComma: "all"`, `semi: true`, `tabWidth: 2`, `prettier-plugin-tailwindcss` 사용. Tailwind 클래스 정렬은 플러그인에 맡깁니다.
- **버튼 커서 클래스 명시 금지**: `globals.css`의 전역 규칙으로 모든 `<button>`/`[role="button"]`이 자동 pointer 처리되므로 className에 `cursor-pointer`를 추가하지 마세요(중복). disabled 상태 명시 피드백이 필요한 경우만 `disabled:cursor-not-allowed`를 유지합니다.
