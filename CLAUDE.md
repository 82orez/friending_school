# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트 개요

"프렌딩 스쿨" 워홀 영어 교육 과정의 랜딩 페이지입니다. Next.js 15(App Router) + React 19 + Tailwind CSS v4 기반의 단일 페이지 마케팅 사이트이며, Supabase SSR 인증을 위한 기반 코드와 함께 shadcn/ui(`base-nova` 스타일, `neutral` baseColor) 컴포넌트, Supabase Server Actions 기반 이메일 인증 화면(`/login`, `/signup`, `/forgot-password`, `/reset-password`, `/auth/confirm`) 및 카카오 OAuth 소셜 로그인이 함께 구성되어 있습니다. 랜딩 페이지 외에 **전자책(교재) 뷰어 `/textbook/cooking`** (24개 Unit, Unit 1은 무료 미리보기·나머지는 로그인 필수)도 함께 제공됩니다.

## 자주 사용하는 명령어

- 개발 서버 실행: `npm run dev` (Turbopack, 기본 포트 3000)
- 프로덕션 빌드: `npm run build` (Turbopack)
- 프로덕션 서버 실행: `npm start`
- shadcn 컴포넌트 추가: `npx shadcn@latest add <component>` (현재 `shadcn` v4.7.0 사용, `components.json` 별칭/스타일을 따라 생성됨)
- DB 마이그레이션 새 파일 생성: `npm run db:new <name>` (`supabase/migrations/<timestamp>_<name>.sql` 빈 파일 생성)
- DB 마이그레이션 원격 적용: `npm run db:push` (연결된 운영 DB에 즉시 반영. **destructive**)
- DB 마이그레이션 상태 비교: `npm run db:list` (Local/Remote 양쪽 적용 이력 비교)
- DB 스키마 diff: `npm run db:diff` (현재 `--linked` 모드 — 운영 DB와 로컬 마이그레이션 파일들 사이의 차이)
- DB 타입 자동 생성: `npm run db:types` (`src/types/database.types.ts`로 출력. 첫 실행 전 `mkdir -p src/types` 필요)

별도의 lint/test 스크립트는 정의되어 있지 않습니다(테스트 프레임워크 미설치). Supabase CLI는 `devDependencies`에 `supabase` 패키지로 설치되어 있어 별도 brew/curl 설치 없이 `npx supabase ...` 또는 위 npm 스크립트로 호출 가능합니다(팀원/CI 간 버전 고정 효과).

## 코드 구조 및 아키텍처

- **App Router 단일 페이지 구조**: 마케팅 콘텐츠 본문은 `src/app/page.tsx`(서버 컴포넌트) 한 파일에 모두 들어 있습니다. 섹션(히어로, 후회 3가지, 차별점, 과정 정보, 후기, 커리큘럼, CTA 등)은 별도 컴포넌트로 분리되어 있지 않고 파일 상단의 데이터 배열(`reasons`, `lastReason`, `opportunityCards`, `infoCards`, `reviewsTop`, `reviewBottom`, `units`)을 `.map()`으로 렌더링합니다. 섹션 카피/항목을 수정할 때는 해당 데이터 배열을 먼저 찾아 편집하는 것이 빠릅니다. **시맨틱 구조**: 페이지에는 정확히 한 개의 `<h1>`(히어로 메인 헤딩, "초보 워홀 영어 / 2주만에 판바꾸기")이 있고, 각 섹션 제목은 `<h2>`, 카드 제목은 `<h3>`로 계층 유지. 후기 카드는 `<blockquote>` + `<footer><cite>...</cite>...</footer>` 구조(`<cite>`는 `not-italic` 클래스로 이탤릭 차단), 정보 카드는 `<dl>/<dt>/<dd>`, 커리큘럼 카드 내부 항목은 `<ul className="list-none">/<li>`, 별점은 `<div role="img" aria-label="별점 5점 만점에 5점">`. 카드 컨테이너 div는 모두 `SectionCard` wrapper(아래 항목 참고)로 통일됨. **반응형 타이포·패딩**: 모바일 기본값에 `md:` 분기를 더하는 패턴(예: `text-[24px] md:text-3xl`, `p-6 md:p-8`, `py-10 md:py-14`). 표준 매핑 가능한 값은 `text-4xl/3xl/2xl/xl/lg`, `leading-snug/normal`로 토큰화되어 있고, 비표준 px(`text-[17px]`, `text-[19px]`, `text-[26px]`, `text-[28px]` 등)는 디자인 의도 보존을 위해 그대로 유지. **CTA 두 곳**: 히어로 CTA는 `<a href="#apply" className={cn(buttonVariants({ variant: "brand" }), "... md:max-w-xl ...")}>`로 anchor 스크롤(Navbar "신청방법" 메뉴와 동일 anchor 공유, `md:max-w-xl`로 데스크톱 폭 제한). 최종 CTA 섹션(`<section id="apply">`, 빨강 그라데이션 배경)은 단순 버튼이 아니라 **`<ApplyForm />` 컴포넌트가 본문으로 들어갑니다**(아래 ApplyForm 항목 참조). 히어로 CTA 라벨("지금 신청하기")과 ApplyForm 제출 버튼 라벨("지금 신청하기" / pending 시 "신청 중")이 통일되어 있고, 폼 제출 성공 시 감사 메시지로 교체되므로 두 CTA의 종착점이 동일합니다.
- **공통 컴포넌트**: 네비게이션 바(슬라이드 메뉴 포함)와 푸터는 `src/components/Navbar.tsx`, `src/components/Footer.tsx`로 분리되어 `src/app/layout.tsx`에서 `{children}`을 감싸도록 배치됩니다. `Navbar`는 슬라이드 메뉴 상태(`useState`) 때문에 `"use client"` 클라이언트 컴포넌트이며, **로그인 사용자 정보를 `user: { email?: string | null } | null` prop으로 받습니다**(layout이 SSR 단계에서 Supabase로 조회 후 전달). 또한 `supabase.auth.onAuthStateChange` 구독 + `pageshow` 이벤트(bfcache 복원) 리스너로 다른 탭의 로그인/로그아웃, 토큰 갱신, 브라우저 뒤로/앞으로 시에도 stale 없이 즉시 갱신됩니다. **모바일 슬라이드 메뉴 항목 3종 (위→아래 순서)**: `#curriculum`(anchor, "커리큘럼"), `#apply`(anchor, "신청방법"), **"교재 보기"(확장형 토글, 항상 마지막)** — 새 anchor/페이지 메뉴를 추가할 때는 이 패턴(anchor는 `<a href>`, 별도 라우트는 `<Link href>`)을 따르고 `onClick={closeMenu}`를 반드시 함께 걸어야 메뉴가 자동으로 닫힙니다. **"교재 보기"는 단일 Link가 아닌 `<button>` 토글**로, 클릭 시 하위 `<div id="mobile-textbook-submenu">` 안의 `TEXTBOOKS` 목록(`src/data/textbook/index.ts`)이 펼쳐집니다(`textbookOpen` state + `aria-expanded`/`aria-controls`, `ChevronDown` 아이콘 180도 회전, CSS grid `grid-template-rows: 0fr → 1fr` 트릭으로 높이 부드럽게 전환). 교재 보기를 마지막에 둔 이유는 펼쳐졌을 때 하위 목록이 아래로 늘어나도 다른 메뉴 항목을 가리지 않기 때문 — 새 메뉴 추가 시에도 교재 보기는 항상 맨 아래에 유지하세요. **슬라이드 메뉴 전체가 닫힐 때 `useEffect`로 `textbookOpen`도 false로 동기화**해 다시 메뉴를 열었을 때 항상 접힌 상태로 시작합니다. 새 교재 추가 시 Navbar는 건드릴 필요 없이 `TEXTBOOKS` 배열에 한 줄만 push하면 자동 반영. 데스크톱(`md+`)에서는 햄버거 옆 인라인으로, 모바일에서는 슬라이드 메뉴 내부에 로그인 상태별 인증 메뉴("로그인/회원가입" 또는 "이메일/로그아웃")가 노출되며 가운데 컬럼이 항상 뷰포트 정중앙에 위치하도록 좌/중/우 3개 `flex-1` 컬럼 구조를 유지합니다. 로그아웃은 `src/app/(auth)/logout/actions.ts`의 Server Action을 `<form action={logout}>`로 호출하므로 클라이언트 컴포넌트에서도 직접 import해 사용합니다. Navbar 로고는 `next/image`의 `<Image>` 컴포넌트로 렌더(`width={107} height={40}`로 원본 비율 1664:624 명시, `priority`로 LCP 최적화). 슬라이드 메뉴는 a11y가 보강되어 있습니다 — 햄버거 버튼에 `aria-expanded`·`aria-controls="mobile-menu"`, 메뉴 컨테이너에 `role="dialog"`·`aria-modal`·`aria-label`·`aria-hidden={!menuOpen}`·`inert={!menuOpen}`(닫혔을 때 Tab/SR 차단), 메뉴 뒤에 `bg-black/40` 클릭 가능 오버레이(z-150). 메뉴 열림 시 닫기 버튼으로 자동 포커스, 닫힘 시 햄버거로 포커스 복귀(`triggerRef`/`closeButtonRef`/`prevMenuOpen` 사용). 메뉴 열려 있는 동안 `document.body.style.overflow = "hidden"`로 배경 스크롤 잠금, Esc로 닫기. `Footer`는 서버 컴포넌트입니다. 전역 색상/폰트(`bg-white font-sans text-[#1a1a1a]`)는 `layout.tsx`의 `<body>` 클래스에 적용되어 있고, `<html>` 클래스는 `cn("font-sans", geist.variable)`로 합성됩니다(layout 본문은 아직 토큰화 전 — `globals.css`에 정의된 `text-ink` 토큰으로 옮기면 향후 다크 모드 대응이 더 쉬워집니다).
- **루트 레이아웃**: `src/app/layout.tsx`는 **`async` 서버 컴포넌트**로, `createClient(await cookies()).auth.getUser()`를 호출해 현재 사용자를 조회한 뒤 `<Navbar user={user ? { email } : null} />`로 전달합니다(미들웨어가 매 요청마다 세션을 갱신하므로 여기서 다시 호출해도 안전). 또한 `<AuthHashHandler />`(`src/components/auth/AuthHashHandler.tsx`)를 모든 페이지에 마운트해, Supabase implicit 플로우(`#access_token=...&type=signup`)로 도착한 토큰을 클라이언트에서 감지·`setSession` 처리하고 적절한 페이지(`/?verified=success` 또는 `/reset-password`)로 `window.location.replace` 합니다(처리 중 풀스크린 로딩 오버레이 표시). `lang="ko"`, 한글 메타데이터, Pretendard 가변 폰트(`next/font/local`로 `node_modules/pretendard/dist/web/variable/woff2/PretendardVariable.woff2` 로드, `--font-pretendard` CSS 변수)를 설정하고, `next/font/google`의 `Geist`를 추가로 로드해 `<html>`에 `geist.variable`(`--font-sans`)을 적용합니다. 다만 `globals.css`의 `@theme inline { --font-sans: var(--font-pretendard); }` 매핑 때문에 **`font-sans` 유틸리티의 실제 글꼴은 Pretendard**이며, Geist 변수는 현재 본문에 직접 사용되지는 않습니다(향후 영문 전용 컴포넌트용). 페이지 제목/설명은 한국어 마케팅 카피이므로 변경 시 SEO 영향을 고려해야 합니다.
- **스타일링**: Tailwind CSS v4를 `src/app/globals.css`의 `@import "tailwindcss"` + `@import "tw-animate-css"` + `@import "shadcn/tailwind.css"` + `@theme inline` 방식으로 사용합니다(별도 `tailwind.config` 파일 없음). `@custom-variant dark (&:is(.dark *))`로 다크 모드 변형을 등록하며, `@theme inline`에 두 그룹의 토큰을 매핑합니다. (1) shadcn 색상 토큰(`--color-background`, `--color-primary`, `--color-card`, `--color-muted`, `--color-destructive`, `--color-sidebar-*`, `--color-chart-1~5`)과 `--radius-{sm,md,lg,xl,2xl,3xl,4xl}` 스케일 — `:root`/`.dark` 블록에 OKLch 기반 값. (2) **랜딩/마케팅 전용 시맨틱 색상 토큰** 9종: `--color-brand`(#ff4757, 메인 CTA·강조), `--color-brand-blue`(#2563eb, **회원가입 동선 전용 CTA·강조** — 같은 페이지에 두 종류의 primary 액션이 공존할 때 회원가입/신규 가입을 시각적으로 분리), `--color-ink`(#1a1a1a, 메인 텍스트·다크 배경), `--color-ink-soft`(#333, Navbar 등), `--color-surface`(#f8f8f8, 카드 배경), `--color-muted-fg`(#666, 본문 회색), `--color-muted-fg-faint`(#999, 라벨 회색), `--color-rule`(#eee, 보더), `--color-rule-faint`(#ddd, 옅은 보더). 모두 `:root`의 hex 변수(`--brand`, `--brand-blue` 등)에서 매핑되어 향후 `.dark`에 한 줄씩 추가하면 다크 모드 대응이 가능합니다. 클래스 사용: `bg-brand`, `text-brand`, `border-brand`, `bg-brand/90`(알파 슬래시 정상 동작), `bg-brand-blue`, `text-brand-blue`, `bg-brand-blue/90`, `bg-surface`, `text-muted-fg`, `border-rule` 등. **하드코딩 hex(`bg-[#ff4757]`, `text-[#666]` 등)는 새 코드에 쓰지 말고 토큰 클래스를 사용**하세요(예외: 단일 사용처의 #2d2d2d 그래디언트 끝점, #ff6b7a, #ffc107 별점, #555, #888은 그대로 유지). 다크 모드 자체는 토글러/`.dark` 매핑 미구현 상태로 라이트 전용입니다. `--font-sans`는 `--font-pretendard`에 매핑되어 있어 `font-sans` 유틸리티는 Pretendard 글꼴을 사용합니다. PostCSS는 `@tailwindcss/postcss` 플러그인 하나만 사용합니다(`postcss.config.mjs`). **`@layer base`에 두 가지 규칙**: (1) 전역 커서 — `button:not(:disabled), [role="button"]:not([aria-disabled="true"]) { cursor: pointer; }` 모든 활성 버튼이 자동으로 pointer 커서, (2) `html { scroll-behavior: smooth; }` — anchor 클릭(`#apply`, `#curriculum` 등) 시 부드러운 스크롤.
- **shadcn/ui 통합**: 설정은 `components.json`(`style: "base-nova"`, `baseColor: "neutral"`, `cssVariables: true`, `iconLibrary: "lucide"`, RSC 활성화)에 있고, 별칭은 `@/components`, `@/components/ui`, `@/lib`, `@/lib/utils`, `@/hooks`로 등록되어 있습니다. 현재 추가된 UI 컴포넌트는 `src/components/ui/{button,card,input,label,textarea,alert-dialog,calendar,popover,select}.tsx`입니다. `calendar.tsx`는 `react-day-picker` 기반(내부 `formatters.formatMonthDropdown`이 `locale.code`를 사용하므로 한국어 표시를 원하면 `locale={ko}`(date-fns) 전달 필수), `popover.tsx`는 `@base-ui/react`의 `Popover` 래퍼(`PopoverTrigger`/`PopoverContent` + `align`/`side`/`sideOffset` prop). **`select.tsx`는 shadcn CLI로 추가만 되어 있고 아직 어디서도 import되지 않으며 untracked 상태**(향후 select가 필요한 폼이 생기면 추가 작업 없이 바로 호출 가능). **`textarea.tsx`는 ApplyForm의 "희망 날짜/시간" 필드에서 사용**(기본 `field-sizing-content min-h-16`, `Input`과 동일한 border/focus 스타일). **`Button` 컴포넌트의 variant**는 shadcn 기본(`default`/`outline`/`secondary`/`ghost`/`destructive`/`link`)에 더해 **랜딩 페이지용 네 가지가 추가**되어 있습니다: `brand`(빨간 배경의 primary CTA — 라이트 배경 위), `brand-blue`(파란 배경의 CTA — **회원가입 동선 전용**, SignupForm 제출 버튼·AlertDialogAction "보내기"·ApplyForm 제출 버튼에 사용해 brand(빨강)와 분리), `brand-inverse`(흰 배경의 inverse CTA — 빨간 배경 섹션 위, focus ring offset이 brand 토큰으로 매핑됨), `kakao`(카카오 공식 가이드 색상 #FEE500 노랑 배경 + #191919 텍스트 — `KakaoButton` 컴포넌트 전용. 카카오 브랜드 색은 고정값이므로 토큰화하지 않고 hex 직접 사용). 세 variant 모두 `buttonVariants`(같은 파일에서 export)로 추출 가능해 `<a>` 등 비-button 요소에도 적용할 수 있습니다(예: 히어로 CTA는 `<a href="#apply" className={cn(buttonVariants({ variant: "brand" }), ...)}>`). className 병합용 `cn()` 유틸은 `src/lib/utils.ts`에 `clsx` + `tailwind-merge` 조합으로 구현되어 있고, 모든 shadcn 컴포넌트가 이를 사용하므로 새 컴포넌트 작성 시에도 동일하게 사용해야 합니다. 아이콘은 `lucide-react`, 헤드리스 프리미티브는 `@base-ui/react`, 애니메이션 보조 클래스는 `tw-animate-css`를 사용합니다. **날짜 처리는 `date-fns`(^4.2.1) + `react-day-picker`(^10.0.1)** — `calendar.tsx`/`popover.tsx`는 현재 어디서도 import되지 않음(ApplyForm이 textarea 기반으로 단순화되면서 분리됨). 새 폼에서 날짜 선택 UI가 필요해지면 두 컴포넌트와 `date-fns`(`format`/`parseISO`, 한국어 로케일은 `date-fns/locale`의 `ko`)를 그대로 재사용 가능. **`base-nova` 변형 주의사항**: `AlertDialogAction`은 `@base-ui/react`의 `Close`로 감싸지지 않고 그냥 `<Button>`만 렌더하므로 **클릭해도 모달이 자동으로 닫히지 않습니다.** 액션 onClick 핸들러에서 `setOpen(false)`를 직접 호출하거나 dialog를 controlled(`open`/`onOpenChange`)로 사용해 명시적으로 닫아야 합니다. `AlertDialogCancel`은 `Close`로 감싸져 있어 자동 닫힘 정상 동작.
- **마케팅 카드 wrapper `SectionCard`** (`src/components/SectionCard.tsx`): 랜딩 페이지의 5종 카드(후회/기회/정보/리뷰/커리큘럼) + 소개 박스가 모두 사용하는 작은 div wrapper입니다. variant 3종으로 보더/배경 차이만 캡슐화하고, `rounded-*`·`p-*`·`text-center` 등은 호출 측에서 className으로 지정합니다(카드별 정체성 유지). `variant`: `accent-left`(`border-l-[6px] border-brand bg-surface` — 후회·커리큘럼), `outline`(`border border-rule bg-surface` — 기회·리뷰), `plain`(`bg-surface` — 정보·소개). shadcn의 `Card` 컴포넌트(헤더/푸터/타이틀 슬롯 포함)는 인증 폼처럼 콘텐츠 컨테이너 용도이고, `SectionCard`는 마케팅 섹션 박스 용도라 분리되어 있습니다.
- **신청 폼 `ApplyForm`** (`src/components/ApplyForm.tsx`): 최종 CTA 섹션(`<section id="apply">`, 빨강 그라데이션 배경)의 본문으로 들어가는 클라이언트 컴포넌트입니다. 입력 필드는 useState 기반 controlled 입력 4종(이름·전화번호·이메일(선택)·희망 날짜/시간)으로 구성됩니다. **희망 날짜/시간은 단일 `<Textarea>`**(`rows={4}`, `className="min-h-[100px]"`, placeholder `"희망 날짜/시간 등을 입력해 주세요."`, `required`)로 자유 형식 여러 줄 입력을 받습니다 — "주말 오전 가능 / 평일은 저녁 7시 이후만 가능" 같은 줄바꿈 포함 희망 사항을 자연스럽게 적도록 허용. **레이아웃**: 1열 grid(`grid-cols-1 gap-4 md:grid-cols-2`)에 이름·전화는 모바일 1열·md 2열, 이메일과 희망 날짜/시간은 `md:col-span-2`로 카드 전체 폭. 제출 버튼은 `<Button variant="brand-blue">`이며, 빨강 CTA 섹션 위에 흰색 카드(`bg-white text-ink`)로 떠 있어 brand(빨강)와의 시각적 충돌을 피합니다. 제출 성공 시 폼이 사라지고 ✓ 아이콘 + "신청해 주셔서 감사합니다." 박스로 교체(`role="status"`, `submitted` state 분기). **a11y**: 폼은 `aria-labelledby="apply-heading"`으로 섹션 헤딩(`<h2 id="apply-heading">`)과 연결.
  - **현재 mock 상태(중요)**: `handleSubmit`은 서버 액션을 호출하지 않고 `console.log("[ApplyForm] submission:", { name, phone, email, schedule })` + `setTimeout(() => setSubmitted(true), 400)`으로 성공만 흉내냅니다. **실제 제출 로직(이메일/Slack/DB 등) 연결이 미구현**이라 사용자 입력은 어디에도 저장되지 않습니다. 운영 전에 (1) `"use server"` 액션 추가, (2) 인증 폼과 일관된 `useActionState` 패턴으로 전환, (3) 서버측 검증(`isValidEmail`, 전화번호 형식) + `getOrigin()` 기반 알림/저장 흐름을 마련하세요. 액션을 추가하면 form은 `<form action={formAction}>`로 바꾸고 controlled state는 그대로 유지(React 19 auto-reset 회피).
- **인증 라우트(Supabase Server Actions)**: 이메일/비밀번호 로그인·회원가입·로그아웃, 이메일 OTP 확인, 카카오 OAuth 소셜 로그인 플로우가 다음과 같이 구성되어 있습니다. **폼 라우트 5종(`/login`, `/signup`, `/logout`, `/forgot-password`, `/reset-password`)과 OAuth 액션(`src/app/(auth)/oauth/actions.ts`)은 `src/app/(auth)/` route group 하위로 묶여 있으며, route group 괄호는 URL 세그먼트로 노출되지 않으므로 외부 URL은 그대로 유지됩니다.** `/auth/confirm`은 Supabase 메일 템플릿/OAuth 콜백이 URL을 직접 참조하므로 `src/app/auth/confirm/`로 별도 유지.
  - `/login` — `src/app/(auth)/login/page.tsx`(서버 컴포넌트) + `src/app/(auth)/login/actions.ts`의 `login()`. **로그인 상태면 페이지에서 `auth.getUser()` 확인 후 `/`로 redirect**(이미 인증된 사용자에게 폼을 노출하지 않음). `searchParams`로 `error=auth-code-error`(빨강 배너 + "회원가입" 다음 액션 링크) 또는 `verified=pending`(녹색 배너, PKCE 교환 실패 시 안내) 표시. 액션은 `signInWithPassword` 호출 후 실패 시 **세 가지 케이스로 분기**: (1) `email_not_confirmed` → "이메일 인증이 완료되지 않았습니다…" + `canResend: true` 플래그 반환(폼에서 재발송 버튼 노출), (2) `invalid_credentials` → `getUserIdentitySummary()`로 분기 (`!found` → "가입되지 않은 이메일" / `hasPassword` → "비밀번호가 일치하지 않습니다" / `!hasPassword` → "이 이메일은 카카오로 가입된 계정입니다. 아래 '카카오로 시작하기'를 이용해 주세요"), `null` 반환 시 일시적 오류 메시지, (3) 그 외 → 일반 오류. 같은 파일에 `resendConfirmation(email)` 서버 액션도 export — `supabase.auth.resend({ type: "signup" })` 호출용으로 LoginForm/SignupForm에서 `useTransition`으로 공유. rate limit + 이메일 형식 검증 적용. 배너는 `AuthBannerSlot`으로 감싸 implicit hash flow 도착 시 깜빡임 방지.
  - `/signup` — `src/app/(auth)/signup/page.tsx` + `src/app/(auth)/signup/actions.ts`의 `signup()`. **회원가입 동선은 brand-blue(파랑)로 통일**: SignupForm 제출 버튼·AlertDialogAction "보내기"는 `variant="brand-blue"`, 하단 "로그인" 보조 링크는 `text-brand-blue`, 비밀번호 Eye 토글의 focus ring도 `focus-visible:ring-brand-blue/50`. Navbar의 데스크톱·모바일 "회원가입" 링크(`bg-brand-blue`)도 동일 — 다른 인증 페이지(login/forgot/reset)의 빨강 brand와 의도적으로 구분해 신규 가입 액션을 시각적으로 분리합니다. **수정 시 주의**: 색상을 brand로 되돌리려면 Navbar(데스크톱+모바일) / SignupForm(제출 버튼 + AlertDialogAction + "로그인" 링크 + Eye 토글 ring 두 곳) 네 곳을 함께 바꿔야 동선 안에서 색상이 깜빡이지 않습니다. **로그인 상태면 페이지에서 `auth.getUser()` 확인 후 `/`로 redirect**. 이메일/비밀번호(**8자 이상**)/비밀번호 확인 검증 후 **`signUp()` 호출 전에 `getUserIdentitySummary(email)` 사전 검사로 분기**: (1) `!found` → 신규 가입 진행, (2) `found && !confirmed` → "이미 가입 시도된 이메일입니다…" + `canResend: true` 플래그(SignupForm 에러 박스 내부에 "다시 보내기" 버튼 노출, `resendConfirmation` 공유 호출), (3) `found && hasPassword` → "이미 가입된 이메일입니다. 로그인 페이지에서 로그인해 주세요.", (4) `found && !hasPassword` → "이 이메일은 카카오로 가입된 계정입니다. 로그인 페이지에서 '카카오로 시작하기'를 이용해 주세요." (현재 OAuth provider는 카카오뿐이므로 `oauthProviders`가 비어 있는 비정상 케이스에도 동일 메시지로 통일 — provider 확장 시 분기 정밀화 필요). **이 사전 검사가 핵심 보안 장치** — Supabase는 미확인 사용자가 재가입을 시도하면 저장된 비밀번호를 새 값으로 **덮어쓰므로**, admin API로 가입 상태를 먼저 확인하지 않으면 공격자가 피해자 계정의 비밀번호를 탈취할 수 있음. `null`(admin API 실패) 시 fail closed("일시적 오류" 메시지). `!found`일 때만 `supabase.auth.signUp({ options: { emailRedirectTo: ${origin}/auth/confirm } })` 진행하고 성공 시 `{ success }` 메시지 + 성공 박스에도 동일한 "다시 보내기" 버튼 노출. signUp 이후의 `error.code === "user_already_exists"` / `data.user.identities` 빈 배열 분기는 사전 검사 직후 인증 완료된 race condition 안전망으로 유지. rate limit(IP당 5분에 5회) + 이메일 형식 검증 적용.
  - `/logout` — `src/app/(auth)/logout/actions.ts`의 `logout()`. `supabase.auth.signOut()` → `revalidatePath("/", "layout")` → `redirect("/")`. 별도 페이지 없이 Navbar의 `<form action={logout}>` 버튼에서 직접 호출.
  - `/forgot-password` — `src/app/(auth)/forgot-password/page.tsx` + `actions.ts`의 `forgotPassword()`. **로그인 상태면 페이지에서 `auth.getUser()` 확인 후 `/`로 redirect**. `searchParams.error === "link-expired"`이면 빨강 배너(+ "로그인" 다음 액션 링크) 표시. 이메일 입력 후 `emailExists()`로 등록 여부 확인 → 미가입이면 에러, 가입된 경우 `supabase.auth.resetPasswordForEmail(email, { redirectTo: ${origin}/auth/confirm?next=/reset-password })`. 로그인/회원가입과 일관된 사용자 열거 노출 정책. rate limit(이메일당 1분에 1회) + 이메일 형식 검증.
  - `/reset-password` — `src/app/(auth)/reset-password/page.tsx` + `actions.ts`의 `resetPassword()`. **세션 필수**: 페이지가 서버 컴포넌트로 `auth.getUser()`를 확인하고 미인증 시 `/forgot-password`로 redirect. 액션은 (1) 비밀번호 검증(**8자 이상**, 확인 일치), (2) **새 비밀번호 ≠ 기존 비밀번호 확인** (`persistSession: false` 임시 client로 `signInWithPassword` 시도해 성공하면 동일로 판정 — 메인 recovery 세션은 영향받지 않음), (3) `supabase.auth.updateUser({ password })`, (4) **`supabase.auth.signOut({ scope: "others" })`** 로 다른 기기 세션 강제 무효화(공격자 세션 차단), (5) `revalidatePath` 후 `redirect("/?reset=success")` (홈 상단에 녹색 배너). 이메일 링크 → `/auth/confirm`(recovery 분기) → `/reset-password` 경로 외에는 직접 도달 불가.
  - `/auth/confirm` — `src/app/auth/confirm/route.ts`의 GET 라우트 핸들러. 세 가지 인증 형식을 모두 처리: (1) `token_hash` + `type`이 있으면 `supabase.auth.verifyOtp()`(Supabase SSR 권장 템플릿), (2) `code`가 있으면 `supabase.auth.exchangeCodeForSession()`(Supabase 기본 PKCE 템플릿 `{{ .ConfirmationURL }}` + OAuth 콜백 공용), (3) `flow=oauth` 쿼리가 함께 오면 OAuth 흐름으로 식별해 성공 배너 텍스트를 분기. **`next` 파라미터는 open redirect 방지를 위해 `/`로 시작하고 `//`로 시작하지 않는 경로만 허용**, 그 외는 `/`로 폴백. **recovery 흐름 식별**(`type === "recovery"` 또는 `next.startsWith("/reset-password")`)로 분기. **성공 시 동작**: recovery가 아니면 redirect 대상 URL에 `verified=success`(이메일) 또는 `login=success`(OAuth) 쿼리를 추가(홈에서 각각 다른 녹색 배너). recovery는 그대로 `next`(보통 `/reset-password`)로 보냄. **실패 시 동작**: recovery면 `/forgot-password?error=link-expired`, OAuth면 `/login?error=auth-code-error`(cross-device prefetch 시나리오 없음), 그 외는 `/login?error=auth-code-error` 또는 `/login?verified=pending`(PKCE 교환 실패 — 이메일은 인증됐을 가능성). 두 분기(token_hash, code) 모두 유지할 것. URL hash로만 오는 implicit 플로우(`#access_token=...`)는 서버에서 못 보므로 layout의 `AuthHashHandler`가 클라이언트에서 처리(동일하게 `/?verified=success`로 보냄).
  - **카카오 OAuth 소셜 로그인** — `src/app/(auth)/oauth/actions.ts`의 `signInWithKakao()` 서버 액션. `supabase.auth.signInWithOAuth({ provider: "kakao", options: { redirectTo: ${origin}/auth/confirm?next=/&flow=oauth } })`를 호출하고, 반환된 `data.url`(카카오 인증 페이지)로 외부 `redirect()`. 실패 시 `/login?error=auth-code-error`로 폴백. `getOrigin()` 재사용으로 운영 환경의 `NEXT_PUBLIC_SITE_URL` 우선. **rate limit은 적용하지 않음**(카카오 자체 throttling에 의존, 추후 봇 공격 발견 시 IP 기반 제한 추가). 콜백은 `/auth/confirm`의 기존 PKCE `code` 분기를 재사용하고, `flow=oauth` 쿼리로 OAuth 흐름임을 식별해 성공 시 `login=success`, 실패 시 `/login?error=auth-code-error`로 분기. **UI 진입점**: `src/components/auth/KakaoButton.tsx`(클라이언트 컴포넌트, `useTransition` + 인라인 카카오 SVG 아이콘 + Loader2)가 LoginForm/SignupForm 양쪽의 submit 버튼 아래 "또는" 구분선 다음에 동일하게 배치됨. 버튼 색상은 `Button` variant `kakao`(#FEE500 노랑 + #191919 텍스트, 카카오 공식 가이드).
  - 폼 컴포넌트: `src/components/auth/{LoginForm,SignupForm,ForgotPasswordForm,ResetPasswordForm}.tsx`. 모두 클라이언트 컴포넌트로 **React 19 `useActionState` + `<form action={formAction}>` 패턴** 사용. **모든 입력 필드는 `useState` 기반 controlled input(`value` + `onChange`)**으로 작성 — React 19는 server action 완료 후 form을 자동 reset하고, Base UI `InputPrimitive`도 `defaultValue` 변경 시 콘솔 경고를 띄움. 비밀번호 필드에는 `lucide-react`의 `Eye`/`EyeOff` 아이콘 토글(각 필드 독립 state, 버튼은 반드시 `type="button"`, `focus-visible:ring-2 focus-visible:ring-brand/50`)과 `useCapsLockWarning` 훅(`src/hooks/use-caps-lock.ts`) 기반 Caps Lock 경고가 적용됩니다. 모든 submit 버튼은 `<Button variant="brand" ...>`로 통일(이전 인라인 `bg-[#ff4757]` 스타일 제거). **a11y**: 모든 입력 필드에 에러 발생 시 `aria-invalid={!!state?.error}` 와 `aria-describedby={state?.error ? "<form>-error" : ...}`(폼별 id: `login-error` / `signup-error` / `forgot-error` / `reset-error`, 비밀번호 필드는 추가로 `<form>-password-caps`·`signup-password-hint`·`reset-password-hint` 등을 조합). 에러/힌트/Caps Lock 경고 요소에 대응 `id` 부여, 에러 단락에 `role="alert"`. 모든 메시지는 한국어 존댓말(마침표 포함). 인증 페이지의 query string 기반 배너(`error=...`, `verified=...`)는 `src/components/auth/AuthBannerSlot.tsx`로 감싸 implicit hash flow로 도착했을 때 깜빡임 없이 자동 숨김 처리.
  - **인증 메일 재발송 UX(LoginForm/SignupForm 공통 패턴)**: `resendConfirmation` 호출 트리거에는 두 단계 보호가 적용됩니다. (1) **확인 모달**: 트리거 버튼 클릭 시 즉시 호출하지 않고 `<AlertDialog>`("인증 메일을 다시 보내시겠습니까?" / [취소] [보내기])를 띄워 사용자 의도를 재확인. (2) **60초 클라이언트 쿨다운**: `resendConfirmation`이 `success`를 반환하면 `setCooldownSec(60)`으로 카운트다운 시작 → 매초 `setInterval`로 감소, 0이 될 때까지 트리거 버튼 disabled + 라벨이 `"${cooldownSec}초 후 다시 시도 가능"`으로 표시. 서버 측 rate limit(이메일당 1분/1회)과 일치시킨 값. 페이지 리로드 시 클라이언트 쿨다운은 리셋되지만 서버 rate limit가 여전히 차단하므로 이중 보호. 두 폼이 동일한 인라인 패턴(`confirmOpen`/`cooldownSec` state + `useEffect` 타이머 + controlled `<AlertDialog>` + `resendContent` JSX 변수)을 사용하며, 호출처가 늘면 `useResendConfirmation(email)` 훅으로 추출 고려. **SignupForm은 success 박스와 error+canResend 박스 양쪽에 트리거가 있으니** 한쪽을 수정할 때 다른 쪽도 함께 검토할 것 — 특히 버튼을 조건부(`!resendResult?.success` 등)로 숨기면 카운트다운이 보이지 않는 회귀가 생길 수 있음.
  - **로딩 버튼 스피너 패턴**: 모든 인증 폼의 제출/재발송 버튼은 로딩 상태에서 `lucide-react`의 `Loader2`에 `animate-spin`을 적용한 스피너 + 컨텍스트 텍스트("로그인 중", "발송 중", "재발송 중" 등)를 함께 표시합니다. trailing `...` dots는 스피너가 진행 표시를 대신하므로 **사용하지 않습니다.** 두 가지 마크업이 있습니다: (1) shadcn `<Button>` 내부 — `[&_svg:not([class*='size-'])]:size-4` + 사이즈 variant의 `gap-1.5` 덕분에 `<Loader2 className="animate-spin" />` 한 줄만 자식으로 두면 자동 정렬, (2) raw `<button>`(재발송 텍스트 링크 등) — Button의 자동 처리가 없으므로 `<span className="inline-flex items-center gap-1.5"><Loader2 className="h-3.5 w-3.5 animate-spin" />재발송 중</span>` 형태로 명시. 새 로딩 버튼 추가 시 동일 패턴을 따르세요.
- **전자책(교재) 시스템 `/textbook/cooking`**: "워홀 실전영어 요리편" 24개 Unit 뷰어. 데이터·라우트·뷰어·진행률 저장이 다음 5개 파일에 분리되어 있습니다.
  - `src/data/textbook/cooking.ts` — `COOKING_COURSE = "cooking"`, `COOKING_TOTAL_UNITS = 24`, `COOKING_UNITS: CookingUnit[]`(unit/title/titleKr/situation/htmlFile 5필드 × 24개), `getCookingUnit(unit)` 헬퍼. **course 식별자는 DB `reading_progress.course` 컬럼 값과 1:1 — 새 시리즈를 추가할 때는 데이터 파일 + course 상수 + 라우트 디렉터리(`/textbook/<course>/`)를 함께 추가**하고, 액션 가드(`course !== COOKING_COURSE`)도 화이트리스트에 새 값을 더해야 안전.
  - `src/data/textbook/index.ts` — **교재 레지스트리** `TEXTBOOKS: TextbookEntry[]`. `{ course, title, href }` 메타데이터만 보관하며 Navbar의 "교재 보기" 확장형 하위 메뉴가 이 배열을 `.map()`해 렌더합니다. `course` 값은 drift 방지를 위해 시리즈별 상수(`COOKING_COURSE` 등)를 import해서 사용. 새 교재 추가 시 시리즈 데이터 파일 작성 후 이 배열에 한 줄(`{ course: NEW_COURSE, title: "...", href: "/textbook/..." }`) 추가하면 Navbar에 자동 노출.
  - `src/app/textbook/cooking/page.tsx` — Unit 목록 (서버 컴포넌트). 로그인 사용자면 `reading_progress`를 한 번 조회해 unit별 진행률 바·완료 체크(`CheckCircle2`)·자물쇠(`Lock` — Unit 2~24, 미로그인 시)를 렌더. 미로그인 자물쇠 카드는 `/login?next=/textbook/cooking/<unit>`으로 보냄.
  - `src/app/textbook/cooking/[unit]/page.tsx` — 단일 Unit (서버 컴포넌트). `generateStaticParams`로 1~24 모두 prerender, `notFound()`로 범위 외 차단, **Unit 2~24는 미로그인이면 `redirect("/login?next=...")`**. HTML 본문은 `content/textbook/cooking/unit-XX.html` 파일을 `fs.readFile`로 읽어 `TextbookViewer`에 prop으로 전달, `initialScrollPercent`도 SSR 단계에서 `reading_progress` 조회 후 함께 전달(서버에서 첫 페인트 직전에 위치 복원이 가능하도록).
  - `src/app/textbook/cooking/actions.ts` — `saveScrollProgress(course, unit, percent, completed)` 서버 액션. **course 화이트리스트 검증 + unit 범위 검증 + percent 0~100 clamp + 로그인 체크**가 필수 진입 가드(클라이언트에서 임의 호출되어도 안전). `reading_progress`에 `upsert({ onConflict: "user_id,course,unit" })`로 저장하되, **한 번 `completed = true`였던 row는 false로 되돌아가지 않도록 기존 row의 `completed`를 먼저 조회해 OR 결합**(사용자가 완독 후 다시 위로 스크롤해도 완료 마크 유지).
  - `src/components/textbook/TextbookViewer.tsx` — 클라이언트 컴포넌트. **iframe `srcDoc={html}`**로 본문을 격리(교재 HTML이 자체 `<style>`·script를 가져도 우리 페이지에 영향 안 미침), `scrolling="no"` + `ResizeObserver` + 0.5s/1.5s 지연 재측정으로 iframe 높이를 본문 내용에 맞춰 동적 확장(이미지 늦은 로드 대응) → 부모 문서가 자연 스크롤을 담당. 상·하단에 **`NavBar` 컴포넌트(목록/Unit n/N/이전·다음)** 두 번 렌더, 상단은 `sticky top-[72px]`로 Navbar 아래 고정. **초기 위치 복원**(`initialScrollPercent`)은 iframe load 후 `requestAnimationFrame`으로 한 번만 적용(`scrollRestored` 가드). **스크롤 진행률 저장**은 로그인 사용자만 활성화되며 `SAVE_IDLE_MS = 1000` 디바운스 + 마지막 전송값(`lastSentPercentRef`)과 동일하면 skip하여 트래픽 절약, `COMPLETED_THRESHOLD = 95` 이상 스크롤 시 완료 처리. 네트워크 오류는 silent (다음 스크롤에서 재시도). 본문 영역 배경은 `bg-[#f5f5f5]`(교재 디자인이 흰 카드 위에 떠 있도록).
  - **HTML 본문 파일**: 프로젝트 루트 `content/textbook/cooking/unit-01.html` ~ `unit-24.html`(각 파일은 자체 `<head>`/`<style>`/`<body>`를 포함한 완전한 HTML 문서). **`src/`가 아닌 프로젝트 루트의 `content/` 디렉터리**에 위치해 fs.readFile로 직접 읽으며, Next.js 번들에는 포함되지 않습니다. 새 unit/시리즈를 추가할 때는 (1) `content/textbook/<course>/unit-XX.html` 파일 생성, (2) `COOKING_UNITS` 같은 데이터 배열에 항목 추가, (3) `COOKING_TOTAL_UNITS` 증가 순으로 진행. 본문 HTML을 수정해도 마이그레이션은 불필요.
- **재사용 헬퍼 (`src/lib/`, `src/hooks/`)**: 인증 액션과 폼에서 공유하는 작은 유틸들.
  - `src/lib/utils.ts` — `cn()` (clsx + tailwind-merge).
  - `src/lib/origin.ts` — `getOrigin(headers)` — `NEXT_PUBLIC_SITE_URL` → `origin` 헤더 → `x-forwarded-host` + 프로토콜 → `host` 순으로 절대 origin 결정. login(`resendConfirmation`), signup, forgot-password의 redirect URL 구성에 사용.
  - `src/lib/email.ts` — `isValidEmail(email)` — 모든 인증 액션의 서버측 이메일 형식 검증. HTML `type="email"` 우회 요청 방어.
  - `src/lib/rate-limit.ts` — in-memory 토큰 버킷. `rateLimit(key, max, windowMs)` / `getClientIp(headers)` / `formatRetryAfter(sec)`. login(이메일당 1분/10회), signup(IP당 5분/5회), forgot-password·resend(이메일당 1분/1회)에 적용. **주의: 프로세스 메모리라 Vercel 등 멀티 인스턴스에서는 효과 제한** — 트래픽이 늘면 Upstash Redis 등 외부 스토어로 같은 시그니처 유지하며 교체 가능.
  - `src/hooks/use-caps-lock.ts` — `useCapsLockWarning()` — 비밀번호 필드용. `capsLockHandlers`를 input에 spread, `capsLockOn`이 true면 amber 색 경고 노출. 모든 비밀번호 입력 필드(LoginForm 1, SignupForm 2, ResetPasswordForm 2)에 적용.
- **홈 페이지 query string 배너**: `src/app/page.tsx`는 `async` 서버 컴포넌트로 `searchParams`를 받아 `reset=success`(비밀번호 변경 완료) / `verified=success`(이메일 인증 자동 로그인 완료) / `login=success`(카카오 등 OAuth 로그인 완료) 일 때 hero 위에 녹색 안내 배너를 노출합니다. 인증 액션의 redirect 목적지가 이 query를 동반합니다.
- **자산**: 이미지/로고는 `public/images/`에 위치합니다. 현재 사용처는 Navbar 로고 한 곳뿐이고 **`next/image`의 `<Image>` 컴포넌트로 렌더**되어 WebP/AVIF 자동 변환·다운스케일·CLS 방어가 적용되어 있습니다(`width`/`height`는 원본 비율과 일치하도록 명시, `priority`로 LCP 최적화). 새 이미지를 추가할 때도 `<img>`가 아닌 `next/image`를 사용하세요. Favicon은 Next.js App Router 파일 컨벤션을 따라 `src/app/icon.svg`로 두면 자동으로 `<head>`에 `<link rel="icon" type="image/svg+xml">`이 주입됩니다(`layout.tsx`에 별도 `metadata.icons` 설정 없음). **교재 본문 HTML(`content/textbook/<course>/unit-XX.html`)은 `public/`이 아닌 프로젝트 루트 `content/` 디렉터리**에 두며, 서버 컴포넌트에서 `fs.readFile`로 읽어 `iframe srcDoc`으로 격리 렌더(`/textbook/cooking/[unit]/page.tsx` 참고). public/이 아니므로 직접 URL 접근은 불가하며, 페이지 라우트의 인증/리다이렉트 로직을 거쳐야 노출됩니다.
- **경로 별칭**: `@/*` → `./src/*` (tsconfig.json).
- **Supabase 인증 SSR 구조**: `@supabase/ssr` + `@supabase/supabase-js`를 사용해 컨텍스트별 클라이언트를 분리해 둡니다.
  - `src/utils/supabase/client.ts` — 클라이언트 컴포넌트용 `createBrowserClient`.
  - `src/utils/supabase/server.ts` — 서버 컴포넌트/Route Handler용 `createServerClient` (Next의 `cookies()` 스토어 주입).
  - `src/utils/supabase/middleware.ts` — `updateSession()`이 매 요청마다 `supabase.auth.getUser()`를 호출해 쿠키 기반 세션을 갱신합니다. **`createServerClient`와 `getUser()` 사이에 다른 로직을 두지 말 것** (세션 갱신 누락으로 사용자가 임의로 로그아웃될 수 있음).
  - `src/utils/supabase/admin.ts` — `service_role` 키로 `@supabase/supabase-js`의 `createClient` 직접 호출(쿠키/세션 무관). 파일 첫 줄의 `import "server-only"`로 **클라이언트 번들 유입 시 빌드 실패**하도록 보호합니다. `createAdminClient()` 외에 세 헬퍼를 export: (1) `emailExists(email): Promise<boolean | null>` — 등록 여부만 boolean으로(forgot-password에서 사용), (2) `getUserStatus(email): Promise<"confirmed" | "unconfirmed" | "not_found" | null>` — 이메일 인증 완료 여부까지 구분(`email_confirmed_at` 필드 검사). **(1)(2)는 @deprecated** — provider 분기가 필요한 새 코드에서는 (3) 사용 권장. (3) `getUserIdentitySummary(email): Promise<{ found: false } | { found: true, confirmed: boolean, hasPassword: boolean, oauthProviders: string[] } | null>` — 사용자 identities 배열을 검사해 비밀번호 가입(`provider === "email"`)/OAuth 공급자 목록까지 분리. signup/login의 provider 분기 메시지에 사용. **내부 동작**: listUsers로 이메일 매칭 user.id를 찾은 뒤 `admin.auth.admin.getUserById(id)`를 한 번 더 호출해 identities를 안전하게 채움 — `listUsers` 응답만으로는 `identities`가 빈 배열/undefined로 와서 provider 분기가 항상 fallback으로 빠지는 Supabase 동작(사용자 열거 방지 부수 효과)을 회피하기 위함. 세 함수 모두 `auth.admin.listUsers`를 페이지네이션(perPage 1000, 최대 50페이지) 순회해 매칭하므로 **같은 액션에서 두 번 호출하지 말 것**. **에러 시 `null` 반환**(서비스 키 누락, 네트워크 등) → 호출 측에서 모호한 케이스로 처리(login/signup/forgot-password 모두 "일시적인 오류가 발생했습니다" 메시지로 분기). 실패는 `console.error`로 서버 로그에 기록되어 운영자 진단 가능.
  - `src/proxy.ts`(Next.js 16의 `proxy` 컨벤션 — v15의 `middleware`에서 리네임됨. `export async function proxy(request)`)가 위 `updateSession`을 호출하며, `matcher`로 정적 자산(`_next/static`, `_next/image`, 이미지 확장자, `favicon.ico`)을 제외한 모든 경로에서 동작합니다. 헬퍼 모듈 `src/utils/supabase/middleware.ts`는 Next 특수 컨벤션 파일이 아니므로 파일명·`updateSession` 이름을 그대로 유지합니다.
  - 필요한 환경 변수:
    - `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` — 미설정 시 미들웨어 단계에서 런타임 오류.
    - `SUPABASE_SERVICE_ROLE_KEY` — admin 클라이언트 사용(로그인 에러 세분화, 회원가입 미확인 사용자 차단, 비밀번호 찾기 사전 확인) 시 필요. **절대 `NEXT_PUBLIC_` 접두사 금지**(DB 전체 권한 노출).
    - `NEXT_PUBLIC_SITE_URL` (선택, 운영 환경 권장) — `getOrigin()`이 헤더 자동 감지 대신 이 값을 우선 사용. Vercel 등 프록시 환경에서 결정론적 redirect URL 보장.
  - **Supabase Dashboard 설정 요구사항**: Authentication > URL Configuration에서 (1) **Site URL**을 운영 도메인으로 설정(기본 `http://localhost:3000`이면 운영 환경에서 메일 링크가 localhost를 가리켜 실패), (2) **Redirect URLs** 화이트리스트에 `https://<운영도메인>/auth/confirm` 및 (개발용) `http://localhost:3000/**` 추가. 미설정 시 우리가 보낸 `redirectTo`가 무시되고 Site URL로 폴백됨. Email Templates의 기본 `{{ .ConfirmationURL }}`(PKCE 플로우)은 cross-device/메일 prefetch 시 verifier 쿠키 부재로 자동 로그인이 실패할 수 있는데, `AuthHashHandler`가 implicit hash로 폴백 처리하므로 동작은 보장됨. 더 깔끔한 경험을 원하면 템플릿을 `{{ .SiteURL }}/auth/confirm?token_hash={{ .TokenHash }}&type=email&next=/` 형태(token_hash 플로우)로 변경 권장. **카카오 OAuth 활성화 요구사항**: (1) Kakao Developers 콘솔(https://developers.kakao.com)에서 애플리케이션 생성 → 카카오 로그인 활성화 → REST API 키 + Client Secret 발급 → Redirect URI에 `https://<Supabase 프로젝트>.supabase.co/auth/v1/callback` 등록 → 동의항목에서 **카카오계정(이메일)을 "필수동의"로 설정** 권장(선택동의면 사용자가 거부 시 user.email이 빈 값으로 들어와 provider 분기 메시지·account linking 모두 무력화). (2) Supabase Dashboard > Authentication > Providers > Kakao 활성화 후 위에서 받은 키 입력. (3) **Authentication > Sign In / Up에서 "Allow account linking" / "Manual linking" 활성화** — 같은 이메일로 이메일/비밀번호 + 카카오 두 경로가 들어와도 동일 user에 identity가 자동 연결됨. 비활성화 상태면 카카오 콜백이 실패해 사용자가 영문 없이 `/login?error=auth-code-error`로 폴백됨. Redirect URLs 화이트리스트는 이메일 플로우용이 이미 있으면 그대로 사용 가능(`/auth/confirm`을 OAuth 콜백으로 공유).
- **Supabase DB 스키마 및 마이그레이션 (`supabase/`)**: `supabase init`으로 생성된 디렉터리에 `config.toml`(CLI 설정, git 커밋 OK), `.gitignore`(`.temp/`/`.branches/` 등 자동 제외), `migrations/<YYYYMMDDHHmmss>_<name>.sql` 마이그레이션 파일이 보관됩니다. Supabase CLI는 **timestamp 형식 파일명만 마이그레이션으로 인식**하므로 새 파일은 반드시 `npm run db:new <name>`으로 생성(자동 timestamp 부여)하세요. Dashboard SQL Editor에서 직접 실행하지 말고 항상 마이그레이션 파일로 관리(소스 코드와 함께 버전 추적). `npm run db:push`로 원격 운영 DB에 적용하면 `npm run db:list` 출력의 Remote 컬럼에 timestamp가 추가되어 적용 이력이 일치하는지 확인 가능. CLI는 `devDependencies`의 `supabase` 패키지로 설치되어 있고 프로젝트 link 정보는 `supabase/.temp/`에 저장됩니다(gitignore 처리됨, 새 머신에서는 `npx supabase login` + `npx supabase link --project-ref <ref>` 재실행 필요). **현재 적용된 스키마(`public` 스키마)** — 마이그레이션 2개가 적용되어 있습니다.
  - **`20260522065400_create_profiles_and_role_trigger.sql`**: (1) `public.user_role` ENUM 타입 — `'admin' | 'teacher' | 'student'` 세 값. (2) `public.profiles` 테이블 — `id uuid PK FK → auth.users(id) ON DELETE CASCADE`, `role public.user_role NOT NULL DEFAULT 'student'`, `created_at`/`updated_at timestamptz NOT NULL DEFAULT now()`. (3) RLS 활성화 + 정책 2개 — `profiles_select_own`·`profiles_update_own`은 `auth.uid() = id`로 본인 row만 조회·수정 허용. **INSERT/DELETE 정책 없음** — 신규 가입은 trigger가, 관리자 작업은 `service_role` 키(`admin.ts`)가 우회. (4) `public.handle_new_user()` 함수(`SECURITY DEFINER`, `search_path = public, auth`) + `on_auth_user_created` after-insert trigger on `auth.users` — 모든 가입 경로(이메일/카카오)에서 자동 실행해 `profiles`에 `{id, role:'student'}` insert(`on conflict do nothing`) + `auth.users.raw_app_meta_data`에 `{role:'student'}` JSON merge(`coalesce` 방어). (5) `public.tg_set_updated_at()` + `profiles_set_updated_at` before-update trigger — UPDATE 시 `updated_at = now()` 자동 갱신. **마이그레이션 적용 시 backfill 포함** — 트리거 등록 시점에 이미 가입돼 있던 사용자 전원에게 같은 SQL로 `student` role을 부여(profiles insert + raw_app_meta_data merge)했으므로 추가 작업 불필요. **현재 role 활용은 미구현** — 저장 인프라만 완성된 상태이고, 페이지 가드/UI 분기/admin RLS 정책 세분화는 추후 작업으로 분리.
  - **`20260523231214_add_reading_progress.sql`**: (1) `public.reading_progress` 테이블 — **복합 PK `(user_id, course, unit)`**, `user_id uuid FK → auth.users(id) ON DELETE CASCADE`, `course text`(현재 `'cooking'`만 사용, 향후 시리즈 확장 대비), `unit int CHECK (between 1 and 100)`, `scroll_percent int DEFAULT 0 CHECK (between 0 and 100)`, `completed boolean DEFAULT false`, `last_viewed_at timestamptz DEFAULT now()`. (2) 인덱스 `reading_progress_user_course_idx (user_id, course)` — 목록 페이지의 unit별 진행률 한 번에 조회 시 사용. (3) RLS 활성화 + 정책 3개 — `_select_own`/`_insert_own`/`_update_own` 모두 `auth.uid() = user_id`. **DELETE 정책 없음** (사용자가 직접 삭제할 일 없음, 필요 시 `service_role`로 우회). 저장은 `src/app/textbook/cooking/actions.ts`의 `saveScrollProgress` 서버 액션이 담당하며, 조회는 목록 페이지(unit별 진행률 일괄)·단일 unit 페이지(`initialScrollPercent` SSR 복원)에서 직접 `from("reading_progress")` 호출.
- **DB 타입 자동 생성 (`src/types/database.types.ts`)**: `npm run db:types`로 운영 DB의 `public` 스키마 전체(테이블/뷰/함수/ENUM)를 TypeScript 타입으로 export. 현재 `profiles`/`reading_progress` 테이블과 `user_role` ENUM이 포함되어 있고, `Database["public"]["Tables"]["..."]["Row"|"Insert"|"Update"]` 형태로 참조 가능. **DB 마이그레이션 적용(`db:push`) 후 잊지 말고 함께 재생성**해야 type drift가 안 생깁니다. 현재 코드는 `createClient<Database>` 제네릭은 아직 사용하지 않지만(supabase client wrappers가 untyped), 새 쿼리에서 row 타입을 명시할 때 import해서 쓰면 자동 보강됩니다.

## 작업 시 주의사항

- **TypeScript 설정이 느슨함**: `tsconfig.json`에서 `strict: false`, `noImplicitAny: false`로 설정되어 있습니다. 빌드 시 타입 오류가 잡히지 않을 수 있으므로 명시적으로 타입을 확인하는 것이 안전합니다.
- **Tailwind v4 `@apply` 제한**: `@layer components` 안에서 `@apply`로 다른 커스텀 컴포넌트 클래스를 참조하면 빌드가 실패합니다. 컴포넌트 클래스 간 재사용이 필요하면 별도 유틸리티 클래스로 추출하거나 JSX 측에서 className을 직접 조합하세요.
- **shadcn 컴포넌트와 `cn()` 사용 규칙**: 신규 UI 요소는 우선 `src/components/ui/`의 shadcn 컴포넌트(`Button`, `Card`, `Input`, `Label` 등) 재사용을 고려하고, 추가가 필요하면 shadcn CLI(`npx shadcn@latest add ...`)로 추가합니다. `components.json`의 `style: "base-nova"`/`baseColor: "neutral"`을 변경하지 마세요. className 조합은 항상 `@/lib/utils`의 `cn()`을 통해 처리해야 합니다(문자열을 직접 결합하면 `tailwind-merge`가 누락되어 충돌하는 Tailwind 클래스가 중복으로 출력됩니다).
- **Server Action + `useActionState` 패턴 유지**: 인증 폼은 클라이언트에서 `fetch` 호출 없이 `<form action={formAction}>` + `useActionState`로 통신합니다. 새 폼을 추가할 때도 동일한 패턴(`"use server"` 액션 파일 + 클라이언트 폼 컴포넌트 + 한국어 검증 메시지)을 따르고, 액션은 성공 시 `revalidatePath` 후 `redirect`, 실패 시 `{ error }`(필요하면 `{ success }`)를 반환하도록 작성하세요. **인증 폼의 액션 import는 모두 `@/app/(auth)/<route>/actions` 경로 사용**(LoginForm/SignupForm/ForgotPasswordForm/ResetPasswordForm 일괄 적용). 새 인증 폼을 추가할 때도 동일 경로 컨벤션(괄호 포함, route group 디렉터리와 일치)을 따르세요.
- **Controlled input 필수 (Base UI + React 19 form action)**: `@base-ui/react`의 `InputPrimitive`는 uncontrolled 상태에서 `defaultValue`가 변경되면 콘솔 경고를 띄웁니다. 또한 React 19의 `<form action={serverAction}>`은 액션 완료 후 form을 자동 reset하므로 uncontrolled 입력은 에러 시 값이 모두 사라집니다. 따라서 폼 입력은 항상 `useState` + `value` + `onChange`로 controlled 작성하고, server action에서 입력값을 다시 돌려보내는 round-trip은 만들지 마세요(state로 자연스럽게 보존됨).
- **인증 액션 공통 시퀀스**: 새 인증 액션 추가 시 (1) 필수값 비어있음 검증, (2) `rateLimit()` 호출, (3) `isValidEmail()` 검증, (4) 실제 Supabase 호출 순서로 작성하세요. rate limit 키는 케이스 무관 비교를 위해 `email.toLowerCase()`로. redirect URL은 `getOrigin(headerList)`로 결정해 운영 환경의 `NEXT_PUBLIC_SITE_URL` 우선 사용을 보장.
- **사용자 열거(user enumeration) 노출은 의도된 정책**: login/signup/forgot-password 모두 "가입된/미가입 이메일"을 구분해 안내합니다. 일관성과 UX를 위해 채택한 결정이며, 더 엄격한 anti-enumeration이 필요해지면 세 곳을 함께 수정해야 합니다(한쪽만 막으면 다른 페이지로 우회 가능).
- **미확인 사용자 비밀번호 덮어쓰기 방지(절대 우회 금지)**: signup 액션은 `supabase.auth.signUp()` 호출 전에 반드시 `getUserIdentitySummary()`로 가입 상태를 확인하고 `found && !confirmed`이면 차단해야 합니다. 이를 건너뛰면 공격자가 피해자의 미확인 이메일로 임의 비밀번호 재가입을 보내 **저장된 비밀번호를 덮어쓰는 계정 탈취 시나리오**가 가능해집니다. provider 분기가 추가된 후에도 `!summary.found`일 때만 `signUp()` 호출하는 순서를 반드시 유지하세요.
- **Rate limit의 운영 한계**: `src/lib/rate-limit.ts`는 프로세스 메모리 기반이라 Vercel 같은 멀티 인스턴스 serverless 환경에서는 인스턴스 간 카운트가 분리되어 효과가 절반 이하로 떨어집니다. 단일 노드 호스팅에서는 잘 동작하지만, 본격적인 봇/무차별 대입 방어가 필요하면 Upstash Redis 등 외부 스토어로 함수 내부만 교체하세요(호출부 시그니처는 그대로).
- **Prettier 규칙**: `printWidth: 150`, `endOfLine: "crlf"`, `singleQuote: false`(큰따옴표), `trailingComma: "all"`, `semi: true`, `tabWidth: 2`, `prettier-plugin-tailwindcss` 사용. Tailwind 클래스 정렬은 플러그인에 맡깁니다.
- **버튼 커서 클래스 명시 금지**: `globals.css`의 전역 규칙으로 모든 `<button>`/`[role="button"]`이 자동 pointer 처리되므로 className에 `cursor-pointer`를 추가하지 마세요(중복). disabled 상태 명시 피드백이 필요한 경우만 `disabled:cursor-not-allowed`를 유지합니다.
- **색상은 토큰 클래스 우선**: 새 마크업에 색상을 적용할 때는 `globals.css`의 시맨틱 토큰(`bg-brand`, `text-brand`, `text-ink`, `text-ink-soft`, `bg-surface`, `text-muted-fg`, `text-muted-fg-faint`, `border-rule`, `border-rule-faint`)을 우선 사용하고, 인라인 `bg-[#ff4757]` / `text-[#666]` 같은 hex arbitrary value는 피하세요(`bg-brand/90` 같은 알파 슬래시도 정상 동작). 토큰에 없는 단발성 색(그래디언트 끝점, 노란 별점 등)만 hex 그대로 유지. 향후 `.dark` 블록에 토큰 다크 값을 매핑하면 페이지 전체가 자동 다크 대응됩니다.
- **CTA·anchor 네비게이션**: 페이지 상단 히어로 CTA와 Navbar "신청방법" 메뉴는 모두 `#apply` anchor를 공유하며 `html { scroll-behavior: smooth }`로 부드럽게 스크롤됩니다. 새 섹션 anchor를 추가할 때 같은 패턴(섹션 `<section id="...">` + Navbar/CTA에서 동일 anchor 참조)을 따르세요. `<Button>` 대신 `<a>`에 버튼 스타일을 입혀야 할 때는 `buttonVariants({ variant })` + `cn()` 조합을 사용해 디자인 일관성을 유지합니다.
- **모바일 슬라이드 메뉴 a11y 회귀 주의**: `Navbar`의 모바일 메뉴는 `inert={!menuOpen}`(닫혔을 때 Tab/스크린리더 차단), `role="dialog"`/`aria-modal`, `aria-expanded`/`aria-controls`, Escape 키 닫기, body scroll lock, 메뉴 열림 시 닫기 버튼으로 자동 포커스·닫힘 시 햄버거로 포커스 복귀(`triggerRef`/`closeButtonRef`/`prevMenuOpen`)가 모두 연결되어 있습니다. 메뉴 마크업/effect를 수정할 때 이 a11y 체인을 깨뜨리지 않도록 유의하세요.
- **DB 변경은 항상 마이그레이션 파일로**: 운영 DB 스키마를 변경할 때 Supabase Dashboard SQL Editor에서 직접 실행하지 말고 `npm run db:new <name>`으로 마이그레이션 파일을 만들고 검토 후 `npm run db:push`로 적용하세요. Dashboard 직접 실행은 git 이력에 남지 않아 추후 재현/롤백/팀 공유가 어렵습니다. **`db:push`는 destructive 작업** — 트리거/RLS 정책에 버그가 있으면 적용 직후부터 신규 가입 흐름 전체가 실패할 수 있으므로, push 직후 반드시 `/signup`에서 새 이메일로 가입 회귀 테스트를 한 번 수행해 (1) 가입 자체 성공, (2) `public.profiles`에 row 생성, (3) `auth.users.raw_app_meta_data.role`이 `'student'`로 박혔는지 확인하세요. 파일명은 CLI가 timestamp 형식(`YYYYMMDDHHmmss_<name>.sql`)만 인식 — `npm run db:new`가 자동 부여하므로 수동 명명 금지.
- **회원 role은 `app_metadata`에만, 절대 `user_metadata` 금지**: `auth.users.raw_app_meta_data.role`은 `service_role` 키(=`admin.ts`)로만 수정 가능하지만 `raw_user_meta_data`는 클라이언트에서 `supabase.auth.updateUser({ data: {...} })`로 사용자가 직접 수정할 수 있어 권한 우회가 가능합니다. 우리 trigger는 `raw_app_meta_data`만 건드리며, 코드에서 role을 읽을 때도 JWT의 `app_metadata.role` 또는 `public.profiles.role` 두 출처에서만 가져오세요. 새 role을 부여·변경하는 코드를 추가할 때도 admin 클라이언트로 `auth.admin.updateUserById(id, { app_metadata: { role } })` + `profiles` 테이블 update를 함께 수행해야 두 출처가 어긋나지 않습니다(추후 `setUserRole(id, role)` 헬퍼로 추출 후보).
- **profiles RLS는 현재 최소 수준(본인 row만)**: `profiles_select_own`/`profiles_update_own` 두 정책만 있어 로그인 사용자는 자기 profile만 조회·수정 가능하며, **admin이 모든 사용자 profile을 조회하는 등의 권한 시나리오는 정책 미구현 상태**입니다. 클라이언트에서 `.from("profiles").select()`해도 RLS 때문에 다른 user의 row가 안 보이는 점을 미리 알아두세요(필요 시 server-side에서 `createAdminClient()`로 우회). 향후 admin 대시보드가 추가될 때 `create policy "profiles_admin_all" on profiles for all using ((auth.jwt() -> 'app_metadata' ->> 'role') = 'admin')` 형태로 추가 마이그레이션 작성 필요.
- **교재 진행률 액션은 항상 화이트리스트 + 범위 검증부터**: `saveScrollProgress`는 클라이언트가 임의의 `course`/`unit`/`percent`를 보낼 수 있으므로 (1) `course`를 알려진 시리즈만 통과시키는 화이트리스트(현재 `=== COOKING_COURSE` 단일 비교), (2) `unit`을 `Number.isInteger` + `[1, COOKING_TOTAL_UNITS]` 범위로 검증, (3) `percent`를 `Number.isFinite` + 0~100 clamp, (4) `auth.getUser()` 결과 없으면 silent return — 이 네 가드를 **반드시 함수 진입부에 순서대로 유지**하세요. RLS도 user_id를 보호하지만, 화이트리스트가 빠지면 임의 course 문자열로 row가 누적될 수 있어 정합성 깨짐. 새 시리즈 추가 시 화이트리스트를 `course === A || course === B` 또는 `Set` 비교로 확장.
- **교재 "한 번 완료 시 유지" 불변량 유지**: `saveScrollProgress`가 upsert 전에 기존 row의 `completed`를 읽어 OR 결합하는 이유는, 사용자가 완독 후 다시 위로 스크롤하면 percent < 95가 되어 다음 저장이 `completed = false`로 덮어쓰는 회귀를 막기 위함입니다. 새로운 진행률 컬럼을 추가하거나 저장 로직을 리팩터링할 때 이 read-then-merge 패턴을 깨뜨리지 마세요(필요 시 단일 SQL `update set completed = completed or $newValue`로 대체 가능).
