# CLAUDE.md

Claude Code 작업 지침. 이 파일은 매 세션 로드되므로 **항상 압축적으로** 작성/갱신할 것(토큰 절감).

## 개요

**"청년을 세계로"** 워홀·해외진출 영어 통합 플랫폼(브랜드 "프렌딩 스쿨"). 구성: 5섹션 랜딩 + **과정 상세페이지 5종**(`/courses/[slug]`: workhol·kitchen·grammar1·grammar2·cosmetic — 회화 기초문법 1/2는 각각 독립 과정·독립 수강신청, 교재 키 basic1/basic2) + **전자책 뷰어 `/textbook/[course]`**(레지스트리 기반 교재 5종, 무료 미리보기 외 로그인) + **수강신청→마이페이지**(`/courses/[slug]/enroll` 강사 매칭형 수강신청 → `/mypage`) + **강사 지원**(`/teacher/apply`) + **강사 페이지**(`/teacher`: 프로필·사진·zoom·주간 가능시간·수강신청 승인/거절) + **admin 대시보드**(`/admin`: 수강신청·화상수업·회원·강사 관리·센터·유튜브). Next.js 16(App Router, Turbopack) + React 19.2 + Tailwind v4. shadcn/ui(`base-nova`/`neutral`), Supabase SSR 인증(이메일 + 카카오 OAuth), 알림 메일(Resend) + SMS(Solapi).

## 명령어

- `npm run dev` / `npm run build` / `npm start` (모두 Turbopack)
- shadcn 추가: `npx shadcn@latest add <component>` (v4.7.0, `components.json` 따름)
- DB: `db:new <name>`(마이그레이션 파일 생성) · `db:push`(원격 적용, **destructive**) · `db:list`(적용 이력 비교) · `db:diff`(`--linked`) · `db:types`(→ `src/types/database.types.ts`, 첫 실행 전 `mkdir -p src/types`)
- lint/test 스크립트 없음(테스트 프레임워크 미설치). Supabase CLI는 `devDependencies`에 있어 `npx supabase ...` 사용.
- **dev 시드**: `seed:teachers`(`scripts/seed-teachers.mjs`, service_role) — fake 강사 12명 생성(`auth.admin.createUser` email_confirm→`profiles` role=teacher 승격+필드(first/last·bio·experience·zoom·phone·**nationality(한국어 키)·gender(male/female)·center_id(centers에서 이름→id resolve, Lani/Sebu/null)** — 9개국·양 성별·센터 분산)+`app_metadata.role` 동기→`teacher_availability` 강사별 무작위·결정적 슬롯, avatar는 null). 이메일 `teacher-NN@seed.friendingschool.test`(공통 비번 `SeedTeacher!123`)로 식별, **멱등**(재실행=재사용+슬롯 교체). `seed:teachers:clean`=해당 도메인 계정 일괄 `deleteUser`(cascade). ⚠️ 원격/service_role이라 **사용자가 직접 실행**(Claude 403).

## 아키텍처

**랜딩 페이지** `src/app/page.tsx`(async server). **5섹션**: ①히어로(`hero-bg.jpg` 배경 + `text-brand-gradient` "세계로") ②셀프디벨롭(`<SelfDevelop/>` client, props `isLoggedIn`+`completedByCourse` — 교재 탭 5종/진행바/더보기 아코디언. **`LINKED_COURSE`(book.key→레지스트리 course)에 등록된 교재만 전자책 연동**: 현재 **5종 전부 연동**(workhol·kitchen·grammar1·grammar2(키 basic1/basic2)·cosmetic) — mock placeholder(`UnitCard`/비연동 분기)는 현재 미사용 dead. 연동 카드=`LinkedUnitCard`(유닛 `/textbook/<course>/<n>` 링크, 잠금=`!isLoggedIn && !freeUnits`(자물쇠→`/login?next=`), 로그인 시 우상단 ○/✓ **수동 완료 토글** `setUnitCompleted` 낙관적, 완료 카드 `bg-progress/5`). **부제/소개**: `renderUnit`이 레지스트리 `getTextbook(course)` 유닛에서 `subtitle=u.sub??titleKr`·`situation`(부제와 **다를 때만** 주입=사실상 workhol·kitchen·cosmetic)을 전달 — `situation` 있으면 카드 **좌측정렬 + 소개문단**(`line-clamp-3`), 없으면 가운데정렬(grammar). 완료 상태는 `BookPanel` 로컬 state로 상단 진행바와 공유. **연동 확장 시 2곳 동기**: `SelfDevelop`의 `LINKED_COURSE` + `page.tsx`의 진행률 쿼리 `reading_progress.in("course",[...])`) ③유튜브 생존기(`youtube_videos` 노출분 SSR 조회→비면 mock `VIDEOS` fallback, `getYoutubeId`로 썸네일) ④실전 스피킹(과정카드 5종 → `/courses/<slug>`, 앵커 `#courses`) ⑤액티비티. **데이터는 `src/data/landing.ts`**(`BOOKS`·`VIDEOS`·`COURSE_CARDS`·`ACTIVITIES` — 교재 유닛·과정 커리큘럼이 모두 `BOOKS`에서 파생되는 단일 소스). **수강료 단일 소스 `src/data/pricing.ts`**(`COURSE_PRICE_KRW`(numeric, PG용)·`COURSE_PRICE_LABEL`(`formatPrice`로 "₩240,000")·`COURSE_PER_LABEL`(`/ ${TOTAL_SESSIONS}회`)) — `courses.ts`·`landing.ts`의 `price`/`per`가 참조(전 과정 공통, 차등 시 슬러그 맵으로 확장). `BookUnit`={`n`/`t`/`sub?`/**`situation?`**/`s`} — **`situation?`(유닛 상황 소개)은 kitchen·cosmetic 24유닛에 채워짐**(각 `content/textbook/<course>/unit-NN.html`의 `.situation`에서 추출, 카드 소개문단용); workhol은 `WORKHOL_UNITS.situation` 별도, grammar는 없음. 카피 수정 시 이 파일 먼저 편집. `searchParams` `reset`/`verified`/`signup=success` 시 `SuccessBanner`. 시맨틱 `<h1>` 1개(히어로)·섹션 `<h2>`. 반응형 모바일 기본 + `md:`. 전체를 `bg-surface` 래퍼로 감싸 흰 카드 대비.

**공통 컴포넌트** `Navbar.tsx`(client, 슬라이드 메뉴)·`Footer.tsx`(server)는 `layout.tsx`에서 children 감쌈. **Footer엔 "Become a Teacher" 링크(`/teacher/apply`)** + 카피라이트.
- Navbar는 `user: { email? } | null` + `isAdmin` + `isTeacher` prop을 SSR로 받고(`layout.tsx`가 `getUserRole` 1회로 파생해 전달), `onAuthStateChange` 구독 + `pageshow`(bfcache) 리스너로 stale 방지. 로그인 시 admin→"관리자"·teacher→"Teacher"(`/teacher`) 링크 노출(모바일·데스크톱 2곳). **학생용 "마이페이지" 링크는 `!isTeacher`일 때만 노출**(강사는 숨김, 학생·admin은 노출). 로고는 `next/image`(`/images/logo.png`, `width=123 height=36`, `priority`).
- 슬라이드 메뉴: **커리큘럼 아코디언**(과정 5종 `/courses/<slug>`) + **교재 보기 아코디언**(`TEXTBOOKS` `src/data/textbook/index.ts`) + 인증 영역(로그인 시 **관리자**(admin만)·**마이페이지**·이메일·로그아웃 / 비로그인 시 로그인·회원가입). **이메일은 사이드 메뉴에만 표시**(데스크톱 상단 인라인에선 미표시). 두 아코디언 모두 `<button>` 토글(`curriculumOpen`/`textbookOpen` state, `aria-expanded/controls`, grid `0fr→1fr` 트릭), **메뉴 닫힐 때 둘 다 false 동기화**. 라우트는 `<Link>`+`onClick={closeMenu}`. 데스크톱 인라인에도 (관리자)·마이페이지·로그아웃/회원가입(**이메일은 미표시**). 새 교재는 `TEXTBOOKS`에 추가만 하면 자동 반영.
- a11y: 햄버거 `aria-expanded`·`aria-controls="mobile-menu"`, 메뉴 `role="dialog"`·`aria-modal`·`aria-hidden`·`inert={!menuOpen}`, `bg-black/40` 오버레이, 열림 시 닫기버튼 포커스·닫힘 시 햄버거 복귀(`triggerRef`/`closeButtonRef`/`prevMenuOpen`), body scroll lock, Esc 닫기. **수정 시 이 체인 유지.**
- 로그아웃은 `@/app/(auth)/logout/actions`의 `logout()`을 `<form action={logout}>`로 호출.

**루트 레이아웃** `src/app/layout.tsx`(async server): `getUser()` + `isAdmin(supabase,user.id)` → `<Navbar user isAdmin>`. `<AuthHashHandler/>`(`src/components/auth/`)를 마운트해 implicit hash(`#access_token=...`) 토큰을 client에서 `setSession` 후 `/?verified=success` 또는 `/reset-password`로 replace. `lang="ko"`, Pretendard(`next/font/local`, `--font-pretendard`) + Geist(`--font-sans`). **단, `globals.css`의 `@theme inline`이 `--font-sans`·`--font-heading` 모두 `→ "Twemoji Country Flags", var(--font-pretendard)` 매핑이라 `font-sans`는 Pretendard**(맨 앞 **Twemoji Country Flags** 웹폰트는 `@font-face` `unicode-range`로 국기 코드포인트에만 적용 — Windows/Linux 등 국기 글리프 없는 OS에서 `🇰🇷`가 "KR" 텍스트로 깨지는 것 방지, 폰트 `public/fonts/TwemojiCountryFlags.woff2` 자체호스팅, 일반 텍스트엔 영향 없음).

**스타일링** Tailwind v4: `globals.css`의 `@import` + `@theme inline`(별도 config 없음). `@custom-variant dark`. 토큰 세 그룹: (1) shadcn 색상/`--radius-*`(OKLch), (2) **마케팅 시맨틱 색**: `--color-brand`(#ff4757, 인증/강조 빨강), `--color-brand-blue`(#2563eb, **회원가입 동선 전용**), `--color-ink`(#1a1a1a), `--color-ink-soft`(#333), `--color-surface`(#f8f8f8), `--color-muted-fg`(#666), `--color-muted-fg-faint`(#999), `--color-rule`(#eee), `--color-rule-faint`(#ddd), (3) **project0607 플랫폼 팔레트(블루→핑크)**: `--color-accent-blue`(#6b8ff0)·`--color-accent-blue-soft`(#eef1fd)·`--color-accent-blue-ink`(#4a6bd4, 링크/탭 강조), `--color-cta`(#1a4fa0, **네이비 — 범용 CTA**: 수강신청·강사/센터/유튜브 admin 액션 버튼 등), `--color-progress`(#b22222). + `@layer utilities`의 **`.bg-brand-gradient`/`.text-brand-gradient`**(블루→핑크 135deg, 히어로 "세계로"·섹션 라벨·탭 active·로고). **새 코드는 하드코딩 hex 금지, 토큰 클래스 사용**(예외 단발성: #E05A6A 신청박스, #ffc107/#F5A623 별점, #aaa 등). 다크모드 미구현. `@layer base`: 활성 버튼 `cursor:pointer` 전역 + smooth 스크롤은 `html[data-scroll-behavior="smooth"]`에만 적용(layout.tsx `<html data-scroll-behavior="smooth">`). **⚠️ CSS로 `html{scroll-behavior:smooth}` 직접 선언 금지** — 라우트 네비게이션 시 Next가 끄지 못해 페이지 이동 후 최상단 점프 실패(교재 등 상단 가림). data 속성으로 선언해야 Next가 전환 중 일시 비활성화.

**shadcn/ui** `components.json`(`base-nova`/`neutral`/lucide/RSC). UI: `src/components/ui/{button,card,input,label,textarea,alert-dialog,calendar,popover,select,sonner}.tsx`. `select.tsx`는 미사용(tracked). **`sonner.tsx`=토스트**(라이브러리 `sonner`, next-themes 없이 `theme="light"` 고정 래퍼 — `position="top-center"` `richColors`; `layout.tsx`에 전역 `<Toaster/>` 마운트, 강사 폼 등에서 `toast.success/error`). `calendar.tsx`는 **수강신청 위저드 시작일 선택**(`EnrollWizard` step 2)에서 사용(`date-fns`(^4) + `react-day-picker`(^10), 한국어 `locale={ko}` 필수), `popover.tsx`는 미사용. `cn()`은 `src/lib/utils.ts`(clsx+tailwind-merge), 항상 사용. 아이콘 `lucide-react`, 프리미티브 `@base-ui/react`, 애니메이션 `tw-animate-css`.
- **Button variant**: shadcn 기본 + 추가 4종 — `brand`(빨강 primary), `brand-blue`(파랑, **회원가입 동선 전용**: SignupForm 제출·AlertDialogAction "보내기"), `brand-inverse`(흰, 빨강 배경 위), `kakao`(#FEE500/#191919, `KakaoButton` 전용, 고정 hex). `buttonVariants`로 `<a>`에도 적용. (네이비 CTA는 variant 아닌 `bg-cta` 클래스 — 수강신청·admin 액션 등.)
- ⚠️ **base-nova**: `AlertDialogAction`은 `Close`로 안 감싸져 **클릭해도 모달 자동 안 닫힘** → onClick에서 `setOpen(false)` 또는 controlled 사용. `AlertDialogCancel`은 정상.

**`SectionCard`** (`src/components/SectionCard.tsx`): 카드 div wrapper. variant 3종 — `accent-left`(`border-l-[6px] border-brand bg-surface`), `outline`(`border border-rule bg-surface`, 현재 전자책 Unit 목록에서 사용), `plain`(`bg-surface`). `rounded/p`는 호출 측 className. shadcn `Card`(슬롯)는 별개.

⚠️ **`ApplyForm.tsx`(`src/components/`)는 현재 orphan**(Phase 1 랜딩 리디자인에서 최종 CTA 섹션 제거 — 미사용 mock). 구 상담신청 동선(폼 `CourseApplyForm.tsx`·액션 `submitApplication`·메일러 `sendApplicationNotification`/`ApplicationEmailData`·admin `ApplicationsManager`·`applications` 테이블)은 **전부 제거됨**(`20260627224406_drop_applications.sql`로 테이블·`application_status` enum drop). 실제 신청 동선은 **강사 매칭형 수강신청**(`/courses/[slug]/enroll`, 아래).

**수강신청(강사 매칭형) + 마이페이지**: 과정 상세 CTA "수강 신청하기"→ **`/courses/[slug]/enroll`**(`page.tsx` server, **로그인 가드**`redirect("/login?next=…")` + **휴대폰 인증·영문 이름 가드**(`ready=phone_verified_at && english_name`; 폰 미인증=마이페이지 인증 안내 배너, 영문 이름 미등록=마이페이지 등록 안내 배너 — 결과 SMS 발송 위해 폰 필수, 영문 이름은 수강신청 필수)) (+ **page가 `loadEnrollTeachers()` 호출해 전체 강사 prop 전달** — `createAdminClient()`로 `profiles.role='teacher'`+`centers`+`teacher_availability` 일괄 조회·그룹핑, 공개안전필드(이름·아바타·국적·성별·센터·bio)+`slots`+**`heldSlots`** 반환 — **`slots`는 `teacher_availability` − 그 강사의 확정(`status='승인'`·`결제완료`) enrollment 슬롯(`subtractSlots`)**; **`결제대기`/`신청`은 더 이상 차감 안 함**(슬롯 노출, 다른 학생도 신청 가능) → 대신 **`heldSlots`=다른 학생의 진행중(`신청`/`결제대기`) 슬롯**(본인 제외, `loadEnrollTeachers(userId)`)로 노출해 **겹치면 확인 단계 경고**(하드 차단 아님). 폰 미인증 시 빈 배열) → **`<EnrollWizard teachers>`**(`src/components/course/`, client, **3스텝**): ①**일정·강사 선택**(통합 라이브 패널) = `<EnrollScheduleField>`(**admin `TeacherAvailabilityFinder` 입력 모델 도입**: 요일 다중 토글(한국어) + 요일당 수업 횟수(1~8회, 1회=30분) + 시작 시각 → 종료 자동 계산, 선택값에서 **요일×범위 30분 슬롯 배열 파생**해 `onChange` 통지) + **그 아래 라이브 강사 목록**(`matches=useMemo(teachers.filter(teacherHasAllSlots(t.slots,slots)))` — **버튼 없이** 일정 바꾸면 즉시 갱신, admin finder와 동일 클라 필터·추가쿼리 0; 선택 강사가 매칭서 빠지면 `selectedTeacher=matches.find(...)` 자동 무효) → 강사 카드 택일 ②**시작일 선택** = 달력(`src/components/ui/calendar.tsx`=react-day-picker, `locale={ko}`) — **선택 범위 제한 3중**: (a)`before:minDate`(=today+3, **오늘+3일(D+3)부터 선택**) (b)`after:maxDate`(=today+14, **2주 이내만**) (c)`!allowedDays.has(d.getDay())`(**1단계에서 신청한 요일만 활성** — 주간 반복 수업 첫 수업일이라; 1단계로 돌아가 요일 바꿔 기존 선택일이 빠지면 `useEffect`로 `setDate(undefined)` 초기화) + **오늘 셀 강조 표시**(`classNames.today` accent-blue ring) ③**확인** = 요약(선택 시간이 `selectedTeacher.heldSlots`와 `slotsOverlap`이면 **앰버 경고 노티스 + `AlertDialog` 문구를 "다른 학생이 먼저 신청…확정 시 취소될 수 있음" 경고로 교체**) + `AlertDialog` → `submitEnrollment`(`useActionState`). 액션 `submitEnrollment`(`src/app/courses/enroll-actions.ts`) 가드 순서: (1)`getUser` 로그인 (2)`profiles.phone_verified_at`+`phone`+`english_name` 확인(미인증/영문 이름 미등록 차단, 서버 authoritative) (3)slots(`isValidSlot` day0-6·min30배수·중복제거·cap)·`start_date`(`YYYY-MM-DD`·**KST 오늘+3일~오늘+14일(2주) 이내**·**요일이 신청 slots 요일 중 하나**, 클라 달력 제약 서버 재검증)·teacherId 검증 (4)`rateLimit(enroll:IP,5/10분)` (5)**예약 무결성 재검증**(service_role): **유효가용=`teacher_availability`−강사 `승인`·`결제완료`(확정) 슬롯(`subtractSlots`)** → `teacherHasAllSlots(effective,slots)`(`승인`·`결제완료` 겹침만 하드 차단; `결제대기`/`신청` 겹침은 **허용**=경고 후 신청, 클라가 `heldSlots`로 경고) + **학생 본인 시간 충돌**(본인 `신청`/`승인`/`결제대기`/`결제완료` enrollment 슬롯과 `slotsOverlap`이면 차단) (6)본인 세션 client `enrollments` insert(`student_id`=uid, slots jsonb, `teacher_name`/`student_name`/`student_english_name`/`student_phone` 스냅샷) (7)**best-effort 강사 알림 메일**(`sendEnrollmentNotificationToTeacher`, 강사 이메일은 `admin.auth.admin.getUserById`, 영문). **슬롯 공유 모델 `src/lib/availability.ts`**(`Slot{day,min}`·`slotKey`·`SLOT_MIN`·`GRID_START/END_HOUR`·`DISPLAY_DAYS`·`DAY_LABELS`/`DAY_LABELS_KO`·`ROW_MINS`·`teacherHasAllSlots`(set⊇)·**`subtractSlots(base,remove)`**(예약 소비 차감)·**`slotsOverlap(a,b)`**(시간 충돌)·`isValidSlot`·`summarizeSlots(slots,ko)` 요일별 요약·**`enumerateLessonSessions(start,slots,total)`**(날짜별 수업 열거, 화상수업 클래스 생성용)·`lessonEndDate`·`TOTAL_SESSIONS`·`fmtTime`) — 그리드·매칭·요약·중복예약·클래스 단일 소스. **승인 시점 가드**: `approveEnrollment`(`src/app/teacher/actions.ts`)가 **승인 시 곧바로 `결제대기`로 전환**; update 직전 이 강사의 다른 `승인`·`결제대기`·`결제완료` 슬롯과 `slotsOverlap` 검사(race-safe, 겹치면 차단). **결제 1단계(무통장 입금 수동 확인)**: 회사 계좌 입금이라 확인 주체=admin → `confirmPayment(id)`(`src/app/admin/actions.ts`, `requireAdmin`→상태 `결제대기` 가드→`결제완료`+**날짜별 클래스 자동 생성**(`generateClassesForEnrollment`, 아래 화상수업)+학생 SMS+**강사 결제확정 알림 메일**(`sendEnrollmentPaymentConfirmedToTeacher`, 강사 이메일은 `admin.auth.admin.getUserById`, 영문, best-effort), `/admin/enrollments`·`/teacher`·`/mypage`·`/mypage/classroom` revalidate). `결제완료`는 `승인`과 함께 **하드 확정**(가용에서 차감·신규 신청 하드 차단) — **단, 과정 종료 시 자동 해제**(남은 `예정` 수업 없음=마지막 수업 다음날부터): `loadEndedEnrollmentIds`(`src/lib/booking.ts`, server-only)가 종료된 `결제완료` id를 산출해 **가용 차감(`loadEnrollmentSlotsByTeacher`)·승인 충돌(`approveEnrollment`)·그리드 표시(`deriveBookedSlots` 소비처)** 모든 지점에서 제외(클래스 없는 레거시는 `lessonEndDate` 폴백). 입금 계좌 단일 소스 `src/data/payment.ts`(`PAYMENT_BANK`). **2단계 PortOne(아임포트) PG는 추후**(payments 테이블·numeric 금액·웹훅이 동일 `결제완료` 전환 재사용). **취소(학생/admin) 시 슬롯 자동 해제**. **`/mypage`(탭 구조)**: 공유 `src/app/mypage/layout.tsx`(server, 로그인 가드+라벨바+웰컴 배너+`<MyPageTabs>` `src/components/mypage/`, client `usePathname` 활성표시) 아래 **3개 하위 라우트** — `/mypage`(기본 정보=회원정보 dl+`StudentProfileForm`)·`/mypage/enrollments`(`<StudentEnrollments>`)·`/mypage/classroom`(`<ClassroomList isTeacher={false}>` via `loadClasses`). 각 page는 본인 데이터 조회(`enrollments` student_id / `profiles` / `classes`). **수강신청 내역**(`<StudentEnrollments>` `src/components/mypage/`, client) — 네이티브 `<details>` 아코디언(강사명·일정(`summarizeSlots`)·시작일·상태 배지(`신청=승인대기`/`승인`/`결제대기=결제 대기`/`결제완료=결제 완료`/`거절`+사유/`취소`)) + **`결제대기`면 확장 패널에 입금 안내 블록**(과정 고정가 `priceLabel`(서버가 `getCourse(course).price` 해석)·`PAYMENT_BANK` 계좌·예금주) + **상태가 `신청`/`승인`/`결제대기`이면 "신청 취소" 버튼**(`결제완료`는 취소 불가=환불 별도)(`AlertDialog` 확인→`cancelEnrollment(id)` 낙관적 갱신+toast, 강사에 취소 이메일). `거절`/`취소` 행엔 미노출. `displayName` 우선순위 `성+이름(last_name+first_name 붙임) → email앞`. **회원정보 카드 필수화**: 이름(성·이름)·**영문 이름**·전화번호 모두 **필수**(전화번호는 **인증 완료=`phone_verified_at`**일 때만 충족) — `nameComplete`/`englishNameComplete`/`phoneComplete` 계산해 미완료 항목 있으면 카드 summary 아래 **안내 배너**(`bg-brand/5 border-brand/30 text-brand`, "회원 정보 완성을 위해 …항목을 완료해 주세요", 서버 렌더라 저장/인증 후 자동 갱신). **회원정보 아코디언 안에 `<StudentProfileForm>`**(`src/components/mypage/`, client, useActionState+controlled+toast) — 학생 본인 **성(last_name)·이름(first_name) 분리 입력**(성→이름 순, **둘 다 필수**: 라벨 빨강 `*`+native `required`, `<form id="student-profile-form" action={updateStudentProfile}>`) + **영문 이름(`english_name`, 수강신청 필수)**(`sanitizeEnglishName`로 onChange 시 비영문 즉시 제거, 영문자/공백/`.-'`만) + **전화번호 SMS 인증**(`<PhoneVerifyField>`, 이름 폼과 독립). **이름 폼은 강사 프로필과 동일한 보기/편집 모드**(`editing` state, 실수 저장 방지): 평소 이름 필드 `disabled`(`VIEW_TEXT="disabled:opacity-100 disabled:text-ink"`로 값 진하게)+버튼 **"수정"**(연필 아이콘)만, 클릭 시 필드 활성+**"취소"/"저장"** 노출. 진입 시 스냅샷(`snapshotRef`) → 취소는 이름 값 복원 후 보기 모드, 저장 성공(`state.ok`) 시 보기 모드 복귀. **`저장` 버튼은 폼 밖 카드 맨 아래 우측**(`form="student-profile-form"`로 이름 폼에 연결) — **전화 미인증이면 비활성**(`phoneVerified` state를 `PhoneVerifyField`의 `onVerifiedChange` 콜백으로 끌어올림, `disabled={pending || !phoneVerified}`). + **주소(선택 입력, `<AddressField>` `src/components/mypage/`, client)**: 이름 폼 안에 함께 배치돼 같은 "저장"으로 제출, 보기/편집 모드 연동(평소 비활성). **다음(카카오) 우편번호 검색 팝업**(`postcode.v2.js` on-demand 로드, `daum.Postcode` `oncomplete`→`zonecode`+`roadAddress||jibunAddress` 자동 채움→상세주소 focus) — 우편번호·기본주소 readOnly + 상세주소 수동, 필드 `name=postcode/address/address_detail`. **선택이라 `missing` 배너·필수 가드 제외**. 이름 액션 `updateStudentProfile`(`src/app/mypage/actions.ts`)은 **본인 세션 client로** `profiles` `first_name`·`last_name`·**`english_name`**·**주소 3필드(`postcode`·`address`·`address_detail`)**만 화이트리스트 update(RLS `profiles_update_own`+role/phone 트리거 보호라 service_role 불필요, `clean()`으로 trim/빈값 null/길이 제한, **영문 이름은 정규식 `/^[A-Za-z][A-Za-z .'-]*$/` 검증**) — **가드: 이름·영문 이름 누락 시 차단 + `phone_verified_at` 조회해 미인증 시 차단**(서버 authoritative, 클라 우회 방어)+`revalidatePath("/mypage")` — **phone은 미포함**(인증 플로우 전용). **전화번호 인증**(`<PhoneVerifyField>`, client, `src/lib/phone.ts` `normalizePhone`/`isValidKoreanMobile`/`formatPhone` 공유): 상태머신 (a)인증됨=`formatPhone`+✓배지+"번호 변경" (b)입력+"인증번호 받기" (c)6자리 코드+"확인", 60초 재전송 쿨다운. 액션 `sendPhoneOtp(phone)`(가드→번호검증→`rateLimit(otp-send:IP,5/10분)`+DB `last_sent_at` 60초 min-interval→6자리 코드 `sha256(uid:code)` 해시로 `phone_verifications` upsert(만료 3분)→`sendSms`(`src/lib/sms.ts`, Solapi)) / `verifyPhoneOtp(phone,code)`(만료·`attempts≥5`·번호·해시 검증, 실패 시 attempts++; 성공 시 **service_role로** `profiles` `phone`+`phone_verified_at` 기록+행 삭제). **인증 통과 시에만 phone 저장**(정책). `phone_verifications` 조회·쓰기는 `createAdminClient()`(service_role). **한국 관례라 표시는 `성+이름` 공백 없이 붙임**(강사 폼의 `[first,last].join(" ")` 영문 공백 방식과 의도적 차이). Navbar는 로그인 시 "마이페이지"(+admin이면 "관리자", +teacher면 "강사 페이지") 링크 노출(`layout.tsx`가 `isAdmin`/`isTeacher` prop 전달). **mypage는 강사 지원과 무관**(학생 신청내역·회원정보 전용, 전부 한국어).

**화상수업(zoom) — 클래스 + 내 강의실**: 결제 확정(`confirmPayment`→`결제완료`) 시 enrollment의 (start_date+slots+**`total_sessions ?? TOTAL_SESSIONS`**)에서 **날짜별 클래스 자동 생성**(`generateClassesForEnrollment`, service_role, **멱등** `upsert onConflict:"enrollment_id,session_no" ignoreDuplicates`; `total_sessions`는 admin 테스트 수강신청의 자유 횟수, 실 신청은 null→24). **클래스 1개=수업 1회(매칭 요일 하루)** — 같은 날 여러 슬롯이면 한 클래스(start=최소 min, end=최대 min+30). 열거는 `enumerateLessonSessions(start,slots,total)`(`src/lib/availability.ts`, `lessonEndDate` 미러·요일당 1회·TZ 비종속). **`classes` 테이블**(`20260626221643_add_classes.sql`): `enrollment_id`/`student_id`/`teacher_id` FK cascade·course/title·표시 스냅샷(teacher_name·student_name·student_english_name)·`session_no`·`session_date`·`start_min`/`end_min`·unique(enrollment_id,session_no); RLS `_select_own_student`/`_select_own_teacher`(enrollments 패턴), **사용자 INSERT/UPDATE/DELETE 없음**(생성=admin service_role, 정리=cascade); zoom_url 미저장(입장 시 강사 profiles 최신값 조회). **내 강의실 진입점**: 학생=`/mypage/classroom`(마이페이지 탭)·강사=`/teacher` 내 "My Classroom" 섹션(별도 `/classroom` 라우트 제거). 공통 로더 **`src/lib/classroom.ts`**(`loadClasses(supabase,userId,isTeacher)`·`mapClassRows`·`CLASS_SELECT`)로 `classes` 조회(RLS 본인 것)→`<ClassroomList classes isTeacher>`(`src/components/classroom/`, client, **강사 화면 전체 영문·학생 한글**: 헤더·섹션·버튼·날짜포맷(`M월 D일 (요일)`↔`Jul 1 (Mon)`)·과정명(강사=`getCourse().englishTitle`)) — **2뷰**: ①기본=**과정(enrollment_id)별 카드 그리드**(`grid sm:grid-cols-2`, 카드=과정명·상대방·다음 수업 일시·진행률 바(완료/전체)) ②카드 클릭→같은 페이지에서 **해당 과정 수업 목록**(상단 "← 목록으로/Back", **예정/지난 분리**, 지난은 흐림·입장 버튼 숨김, 행=날짜·시간(`fmtTime`)·회차(`sessionNo/총`, **총=과정 계획 회차=보강 제외 클래스 수**, `TOTAL_SESSIONS` 하드코딩 아님 — 테스트 자유 횟수 반영)). 1분 틱 `now` state로 다음수업·진행률·입장창 갱신. **진입=과정 카드 → 과정 선택 시 그 과정 상세**(`CourseDetail`): **달력/목록 토글(달력 기본)**. 달력=그 과정 월간(`ui/calendar` 재사용·`date-fns/locale` ko/enUS, 예정=accent-blue·지난=rule 색 구분·오늘=노랑 배경(#FFF3CD)+앰버 링·주말 색(일=brand 빨강/토=accent-blue-ink, `dayOfWeek` 모디파이어+헤더 nth-child, `weekStartsOn=0`·outside days off), `mode=single required`, 기본 포커스=다음 예정 수업 달), 날짜 클릭 시 그 날 수업 목록·입장. 목록=예정/지난 섹션. **수업 1회=25분(+5분 휴식)**: 내 강의실 행 종료 시각은 `lessonEndMin(end_min)`=`end_min−(SLOT_MIN−LESSON_MIN)`=`end_min−5`로 `:25`/`:55` 표시(`LESSON_MIN=25`·`lessonEndMin` `availability.ts` 단일 소스, 위저드 `EnrollScheduleField`도 공유). 슬롯 점유·매칭은 30분(`end_min`) 그대로이나, **입장 시간창 종료·예정/지난 전환은 레슨 종료(`lessonEndMin`=`end_min−5`)=표시 종료와 일치**(클라 `mapClassRows`의 `endMs`·서버 `enterClass` 모두 `lessonEndMin` 적용 — 표시 종료 후 입장 가능했던 5분 간극 제거). **입장하기**: 시작 15분 전~레슨 종료 시각만 활성(`canEnterClass`/`kstDateMinToMs`/`ENTRY_LEAD_MS` `src/lib/classtime.ts`, KST=UTC+9 고정). 클릭→**팝업 차단 회피**(빈 탭 먼저 open 후 액션 URL로 이동)→`enterClass(classId)`(`src/app/classroom/actions.ts`): 로그인+**소유 검증**(student_id∥teacher_id)+**시간창 서버 강제**+강사 `zoom_url` 최신값(service_role)·`isValidZoomUrl` 검증→`{url}∥{error}`. 학생·강사 동일(15분 전). **개별 수업 취소(학생, 자동 보강)**: 학생 뷰 예정 수업에 "취소" 버튼 → `cancelClass(classId)`(`src/app/classroom/actions.ts`): 소유(student_id)+상태 `예정`+**시작 1시간 전 컷오프**(`canCancelClass`/`CANCEL_CUTOFF_MS`)+**과정당 6회 한도**(`MAX_CANCELLATIONS`, `status='취소'` 카운트) 가드 → `status='취소'` + **자동 보강 생성**(과정 전체 `최대 session_date` 다음으로 **주간 스케줄 요일 집합 중 가장 빠른 날**=마지막 수업일 뒤 첫 스케줄 요일, 요일별 시각 맵 적용, `session_no=enrollment 최대+1`, `is_makeup=true`, best-effort; **공유 헬퍼 `createMakeupClass` `src/lib/makeup.ts` — 학생 `cancelClass`·admin `adminCancelClass` 공용**) + 강사 이메일(`sendClassCancellationToTeacher`) + `/mypage`·`/teacher`·`/admin/classes` revalidate(클라 `router.refresh()`). 취소 행=취소 배지·흐림·입장 숨김, 보강 행=보강 배지, 달력 취소일=취소 모디파이어(line-through). 진행률·예정 분리에서 취소 회차 제외. **남은 취소 횟수 안내**: `cancelClass`가 `remaining`(=`MAX−취소수`) 반환 → 취소 확인 모달("이번에 취소하면 N회 남습니다", `cancelledCount` prop으로 산출)·완료 토스트("남은 취소 N회")에 표시. **Navbar에 내 강의실 직접 링크 없음**(학생=마이페이지 탭·강사=/teacher 섹션으로 접근). **admin은 `/admin/classes`에서 전체 수업 조회·개별 취소(보강 옵션)·일정 변경·강사 대체**(강사 가용+충돌 검증, 과정 종료 시 편집 차단, admin 대시보드 항목). 기존 `결제완료` 건은 클래스 백필 필요(신규 결제부터 자동). **수업 종료 후 강사 피드백(텍스트, 수강생 읽기 전용)**: `classes.feedback`/`feedback_at`(`20260629234513_add_class_feedback.sql`). `ClassRow`(`ClassroomList`)에서 **종료된 수업**(`ended=!cancelled && now≥endMs`, 레슨 종료 시각 이후)은 **행에 버튼만** 두고 내용/편집은 `ClassFeedbackModal`(같은 파일, 커스텀 오버레이 `z-[110]`/패널 `z-[120]`·Esc·scroll lock, 긴 피드백 UX)에서 처리 — 강사=버튼 "Write/Edit feedback"(`MessageSquare`)→모달 textarea 작성/수정/삭제·수강생=피드백 있으면 "피드백 보기"(`Eye`)→모달 읽기 전용(전문+`feedbackAt` 날짜). (인라인 전문 노출 폐지.) 저장 `saveClassFeedback(classId,text)`(`src/app/classroom/actions.ts`): 로그인+**강사 소유권**(`teacher_id`)+취소 아님+**종료 가드**(`Date.now()≥kstDateMinToMs(session_date,lessonEndMin(end_min))`)→service_role update(빈 문자열=null 삭제, ≤2000자), `/teacher`·`/mypage` revalidate. **새 RLS 없음**(쓰기=service_role 액션, 읽기=기존 student/teacher select). `CLASS_SELECT`/`mapClassRows`/`ClassItem`에 `feedback`·`feedbackAt` 포함. **수업 진행 인정**(`classes.teacher_entered_at`·`conducted_at`, `20260630021451`): **강사 Enter 클릭(`enterClass`가 강사 본인 첫 입장 시 `teacher_entered_at` 기록) AND 종료 후 비어있지 않은 피드백 저장**(`saveClassFeedback`가 `teacher_entered_at` 있고 미인정이면 `conducted_at` 박음) → **진행 인정**. **sticky**(이후 피드백 비워도 `conducted_at` 유지). 학생 노쇼라도 강사 입장+피드백이면 인정(학생 입장 미기록). **강사 뷰 진행 마커(강사 전용, `conductMark(item,now)`)**: `ClassItem.conductedAt`이면 **○ "Conducted"**(초록 `Circle` pill)·`teacherEnteredAt` 있고 미인정+종료(`now≥endMs`)면 **△ "Feedback pending"**(앰버 `Triangle` pill, 입장했으나 피드백 전) — 지난 수업 목록 행 + **달력 셀 우상단 코너 마커**(`modifiers` conducted=●/pending=▲ + `after:` 절대배치, 학생 뷰엔 빈 배열=미표시) + 달력 아래 강사 범례. `CLASS_SELECT`/`mapClassRows`/`ClassItem`에 `teacher_entered_at`(→`teacherEnteredAt`) 포함(마커 판정용). admin 상세=`ClassesManager` "진행" 컬럼(진행됨/미진행=종료됐는데 미인정/−)+모달 dl(진행 여부·강사 입장 시각). ⚠️ 강사가 Enter 안 누르고 Zoom 직접 열면 미인정(정책). **admin `/admin/classes/[enrollmentId]`(`ClassesManager`)도 읽기 전용 열람**(행에 "피드백" 칩·`ClassDetailModal`에 강사 피드백 블록; 상세 페이지 select·`AdminClass` 타입에 `feedback`/`feedback_at` 포함, admin은 작성 불가).

**강사 지원** (`/teacher/apply`, `src/app/teacher/apply/page.tsx`, server, **UI 영문**, 과거 mypage 아코디언에서 분리): 진입점=Footer "Become a Teacher" 링크 + 거절 알림 메일의 `applyUrl`. 가드 `!user`→`/login?next=/teacher/apply`, role=teacher→`/teacher`, role=admin→`/`. `profiles`(first_name·last_name·phone·nationality·gender·avatar_url) + 본인 `teacher_applications` 최신 1건(`maybeSingle`) 조회 → `status='신청'`이면 "Under review"+제출정보 read-only, 그 외(미신청/거절)면 `<TeacherApplicationForm>`(`src/components/mypage/`, client — 컴포넌트 위치는 mypage 폴더 유지, useActionState+`submitTeacherApplication`+AlertDialog 확인+toast, **강사 프로필(`/teacher`)과 동일 필드세트**: **프로필사진(필수**, 브라우저 client로 `avatars/<uid>/` 업로드→publicUrl을 hidden `avatar_url`로 제출, hidden이라 `handleReview`에서 수동 검증)·**first_name·last_name(필수)**·phone(선택)·**nationality(필수, 국기 이모지 `<select>` 9개국, `src/data/nationalities.ts` 단일 소스: `NATIONALITIES`{name(=저장 한국어 키)·en·flag}/`NATIONALITY_NAMES`(검증)/`nationalityLabel`(국기+한국어, admin)/`nationalityLabelEn`(국기+영문, 강사용 폼). 저장값은 한국어 국가명, 강사 폼은 영문·admin은 한국어 표시)**·**gender(필수, `<select>` 남성/여성, `src/data/genders.ts` 단일 소스: 코드 male/female 저장, `GENDER_VALUES`(검증)/`genderLabelEn`(폼)/`genderLabelKo`(admin))**·**center(선택, `<select>` None+centers, `center_id` FK 저장 — admin이 `/admin/centers`에서 관리하는 `centers` 테이블, 폼은 centers props로 받음, 서버는 `resolveCenterId`(`src/lib/center.ts`)로 uuid+존재 검증)**·**bio(필수, About/Bio)**·**experience(필수)**·**zoom_url(필수, http/https)** — action이 `name=[first,last].join(" ")` 조합 저장, props로 `userId`·`initialAvatarUrl`·`initialFirstName`·`initialLastName`·`initialPhone`·`initialNationality`·`initialGender` 받음), 거절이면 상단에 admin_note 사유 표시 후 재신청 허용 — **재신청 시 이전 지원서(`teacherApp`) 값으로 폼 프리필**(bio·experience·zoom_url 포함, `teacherApp?.x ?? profile?.x ?? ""`; 첫 지원은 프로필 값만), 폼이 `initialBio`/`initialExperience`/`initialZoomUrl` 옵셔널 props 수신.

**강사 지원→승인 flow** (역할 부여): 직접전환 폐지. 학생이 `/teacher/apply`에서 지원 → `teacher_applications` 저장 → admin이 `/admin/teacher-requests`에서 승인(role=teacher)/거절. 액션 `submitTeacherApplication`(`src/app/teacher/apply-actions.ts`): 가드 (1)로그인 (2)role≠teacher/admin (3)필수값(first/last·nationality(화이트리스트)·gender(화이트리스트)·bio≥10·experience·zoom_url·avatar) (4)`rateLimit(teacher-apply:IP)` (5)기존 '신청' 중복 차단 (6)본인 세션 client insert(`user_id`=getUser, RLS `_insert`=uid 일치) → best-effort `sendTeacherApplicationNotification` + `revalidatePath("/teacher/apply")`. 메일러는 `applications`용과 별개 함수(`src/lib/mailer.ts`).

**강사 페이지** (`/teacher`, `src/app/teacher/(dashboard)/`, **UI 영문**, **탭 구조**(마이페이지와 동일 패턴)): route group `(dashboard)`의 공유 `layout.tsx`가 **role 가드**(`!user`→`/login?next=/teacher`, role∉{teacher,admin}→`/` — admin 허용) + 라벨바·웰컴 배너 + `<TeacherTabs>`(`src/components/teacher/`, client `usePathname`: **Profile `/teacher` · Enrollment Requests `/teacher/requests` · My Classroom `/teacher/classroom`**). **`/teacher/apply`는 그룹 밖**(탭 레이아웃 미적용). **① Profile 탭**(`(dashboard)/page.tsx`)이 `profiles` 본인 row(**first_name·last_name**·avatar_url·zoom_url·bio·experience·phone·nationality·gender) 조회 → `<TeacherProfileForm>`(client) + 주간 가능시간(`AvailabilityModal`). **이름은 first/last 분리**(원어민 강사 위주 + admin 성 기준 정렬), 표시명=`[first,last].join(" ")`, 웰컴 호칭/마이페이지 등 친근한 호칭은 first 우선. 구 `full_name` 컬럼은 비파괴 보존(앱 미사용). **프로필 사진**: 브라우저 client로 `avatars/<uid>/avatar-<ts>.<ext>` 업로드(upsert)→publicUrl→`updateTeacherAvatar` 액션 저장(`useTransition` 낙관적, `next/image`로 미리보기). **변경 시 이전 파일 정리**: 공유 헬퍼 `src/lib/avatar.ts`(`uploadAvatar`→`{publicUrl,path}`, `cleanupOldAvatars(uid,keepPath)`=본인 폴더 list 후 keepPath 제외 remove, **best-effort**)를 강사 프로필·강사 신청폼 공용 사용 — 프로필은 **DB 반영 성공 후** 정리, 신청폼은 업로드 직후 정리(타임스탬프 파일명 유지=캐시 무효화, 누적만 제거). RLS: `avatars_public_read`(list)·`avatars_delete_own`(remove) 본인 폴더 허용. ⚠️ 승인 후 사진 변경 시 과거 `teacher_applications.avatar_url` 파일도 삭제돼 admin 신청목록 historical 썸네일은 깨질 수 있음(수용). **텍스트 폼**: `useActionState`+`updateTeacherProfile`, controlled inputs(first_name·last_name·bio·**experience**·phone(선택)·**nationality**(국기 `<select>`, 필수)·**gender**(`<select>` 남성/여성, 필수)·**center**(`<select>` None+centers, 선택, centers props)·**zoom_url**(화상수업 연동은 추후, URL만 저장)). **보기/편집 모드**(`editing` state): 기본은 모든 필드 `disabled`(보기) + "Edit" 버튼, Edit 클릭 시 편집 가능 + Save/Cancel 노출. 진입 시 값 스냅샷(`snapshotRef`) → Cancel은 복원 후 보기 모드, Save 성공(`state.ok`) 시 보기 모드 복귀. 프로필 사진은 별도 섹션이라 편집 모드 무관(상시 변경). **전화번호 제외 전 항목 필수**(프로필사진·first/last name·bio·experience·zoom_url): 클라 native `required`+라벨 빨강 `*`로 빈 제출 차단(**프로필사진은 폼 외부 업로드라 native 검증 불가 → 텍스트 폼 `onSubmit`에서 avatar 없으면 preventDefault+toast로 저장 차단**), 서버 액션이 `clean()` 후 누락 시 "Please fill in all required fields." 반환(공백-only/우회 차단). 액션(`src/app/teacher/actions.ts`)은 첫 줄 `requireTeacher()`(role teacher∥admin) 후 **service_role로 본인 id의 화이트리스트 컬럼만 update(role 절대 미포함)** + `revalidatePath("/teacher","layout")`. 아바타 액션은 URL이 본인 폴더(`/avatars/<uid>/`)인지 검증. **주간 가능시간**: 프로필 폼 아래 `<AvailabilityModal>`(`src/components/teacher/`, client) — **요약 카드**(제목·등록 슬롯 수 + "Edit availability" 버튼)를 두고, 클릭 시 **모달**(커스텀 오버레이+패널, Navbar 슬라이드 메뉴식 a11y: Esc·body scroll lock·focus 복귀·`inert`, 오버레이 `z-[110]`·패널 `z-[120]`) 안에서 `<AvailabilityGrid>`를 편집. 모달이 **열림/닫힘 + 미저장 경고**를 소유(닫기=X·오버레이·Esc → dirty면 "Close without saving?" `AlertDialog` 경유, **조건부 렌더라 discard=그리드 언마운트**로 폐기; Esc/오버레이는 확인 모달 열림 시 무시). `<AvailabilityGrid>`(client)는 부모 `<details>` 결합을 끊은 **순수 에디터**(닫기 책임 없음) — `onDirtyChange(dirty)` prop으로 dirty를 모달에 통지, 06:00~24:00 30분 슬롯, 요일 **월~일 표시**(`DISPLAY_DAYS=[1,2,3,4,5,6,0]`로 저장 day(0=일)↔표시 매핑, 라벨 Mon~Sun), 클릭·드래그 선택(pointer 이벤트+`elementFromPoint` 마우스/터치 통합, 셀 `touch-none`, 선택 셀 `bg-progress`), **frozen-pane**(외곽 `max-h-[65vh] overflow-auto` 양축 스크롤 박스 + 요일 헤더 행 `sticky top-0 z-20 bg-white` + 시간 열 `sticky left-0` → 세로 스크롤해도 요일 헤더 고정·가로 스크롤해도 시간 열 고정. 드래그 선택은 박스 가시범위로 제한), `useTransition`+**`updateTeacherAvailability(slots)`**(actions.ts)로 저장(영문 toast, dirty 시에만 Save 활성). 그리드 내 **확인 모달 3종**(Save/Revert/Clear all, controlled `AlertDialog`, **콘텐츠 `z-[130]`로 모달 위에 표시**) + 닫기 가드(Close without saving)는 모달 래퍼 소유. **Revert 버튼**(dirty일 때만 활성)은 "Discard unsaved changes?" 모달 거쳐 saved 스냅샷으로 복원. 그리드 표시범위는 컴포넌트 상수(`START_HOUR`/`END_HOUR`)에만 — DB 비종속. 액션은 `requireTeacher()` 후 service_role로 **본인 슬롯 전체 delete→bulk insert**(빈 선택=전부 비움), 검증(day 0-6·min 30배수·중복제거·cap 7×48)+`revalidatePath("/teacher","layout")`. `readOnly` prop으로 admin 뷰 재사용(편집/저장/모달 미부착, 가용 셀 `bg-accent-blue/40`). **② 수강신청 관리 탭**(`(dashboard)/requests/page.tsx`): `<TeacherEnrollments>`(`src/components/teacher/`, client, **UI 영문**) — page가 본인 `enrollments`(RLS `_select_own_teacher`, `student_english_name` 포함, `신청`(대기) 우선 정렬) 조회해 전달. **정렬 가능 테이블**(`SortHeader` Student/Start/Applied + Status/Course 컬럼) — 학생명은 `studentLabelOf`로 **`영문명(한글명)`**(영문명 없으면 한글명), 정렬키 `student`도 영문명 우선. **시간충돌 표시**(본인 진행중 신청끼리 슬롯 겹치면 `overlappingIds`로 행 좌측 앰버 액센트+"Time conflict" 칩, 거절/취소 제외). **행 클릭/Enter→`EnrollmentDetailModal`**(같은 파일 내 컴포넌트, 커스텀 오버레이 `z-[110]`+패널 `z-[120]`, a11y Esc·body scroll lock·닫기 버튼 포커스) — 상태 배지 Pending/Approved/Payment pending/Paid/Declined/Cancelled(`결제완료`=Paid, 강사는 표시만·확인 액션 없음) + `dl`(학생·과정·일정(`summarizeSlots(_,false)` 영문)·시작일) + 충돌 시 경고 노티스. **승인/거절은 모달 안에서 `신청` 행만**(처리완료 read-only), 거절 사유 필수(`toast` 검증), controlled `AlertDialog` 확인 후 처리·낙관적 갱신·`onClose`. 액션 `approveEnrollment(id)`(**승인 시 `결제대기`로 전환**)/`rejectEnrollment(id,note)`(`actions.ts`, `requireTeacher()` → **소유권(`teacher_id=self`)+상태 가드(`.eq("status","신청")` 0행=이미 처리)** → service_role `enrollments` update → **best-effort 학생 SMS**(`student_phone` 스냅샷, `sendSms` 한국어 승인/거절 안내, 실패 로깅·롤백 없음) + **best-effort 관리자 이메일**(독립 try/catch, `getAdminEmails()`→승인=`sendEnrollmentApprovedToAdmin`(결제대기 전환→입금 확인 유도)·거절=`sendEnrollmentRejectedToAdmin`(사유 포함); 공용 헬퍼 `buildAdminEnrollmentEmail`이 `loadOwnPendingEnrollment` 확장 select(course·teacher_name·student_english_name·slots·total_sessions)로 `summarizeSlots`·`lessonEndDate`(승인만)·`getCourse().englishTitle` 조립) + `revalidatePath("/teacher","layout")`). **③ My Classroom 탭**(`(dashboard)/classroom/page.tsx`): `loadClasses(supabase,user.id,true)` → `<ClassroomList isTeacher>`(화상수업 항목 참조). ⚠️ 라우트 분리(마이페이지·강사 모두)로 enrollment/class 상태 변경 액션은 `revalidatePath("/teacher"|"/mypage", "layout")`(하위 탭 전부 갱신)을 사용.

**admin 대시보드** (Phase 5, `src/app/admin/`): `layout.tsx`(server)가 **role 가드**(`!user`→`/login?next=/admin`, `!isAdmin`→`/`) + `AdminNav`(client, **6탭**). 6 섹션(`/admin` 인덱스 `page.tsx`는 `redirect("/admin/enrollments")`) — **`enrollments/`(수강신청 관리)**, **`classes/`(화상수업 관리)**, `members/`, **`teacher-requests/`(강사 승인/삭제 + 강사 시간표 열람)**, **`centers/`(센터 CRUD)**, `youtube/`. **수강신청 관리**: `enrollments/page.tsx`(server)가 전체 `enrollments`(**`teacher_id` 포함**, `created_at desc`) service_role 조회→`<EnrollmentsManager>`(client, `src/components/admin/`) — 상태 필터 탭(전체/신청/결제대기/결제완료/승인/거절/취소)+검색(학생·강사·과정)+**정렬 테이블**(`SortHeader` 학생/강사/시작일/신청일) + **시간충돌 표시**(같은 `teacher_id` 진행중 신청끼리 슬롯 겹치면 `overlappingIds`로 "시간 겹침" 칩+좌측 앰버 액센트, 전체 rows 기준 계산이라 필터 무관). **행 클릭→`EnrollmentDetailModal`**(커스텀 오버레이 패널, 일정 `summarizeSlots`·전화·**수강료**·사유 표시 — `priceLabel`은 page가 `enrollments.course`(슬러그) select 후 `getCourse(course).price`로 파생해 `AdminEnrollment`에 실어 전달, 학생 결제대기 패널과 동일 고정가). **입금 확인(결제 1단계)**: 모달에서 `결제대기` 행에 **"결제 확인"** 버튼+`AlertDialog`(입금 대조용 **"입금 확인 금액: {priceLabel}"** 표기)→`confirmPayment(id)`(`actions.ts`, 상태 가드→`결제완료`+학생 SMS+강사 결제확정 알림 메일). **개입=강제 취소**: 모달에서 `신청`/`승인`/`결제대기` 행에 사유 입력+`AlertDialog`→`adminCancelEnrollment(id,note)`(`requireAdmin()`→service_role, 상태 가드→`취소`+`teacher_note="[관리자] …"`, 학생 SMS best-effort, `/admin/enrollments`·`/teacher`·`/mypage` revalidate). `결제완료`/`거절`/`취소`는 read-only. **[개발용] 테스트 수강신청 생성**: `/admin/enrollments` 상단 `<TestEnrollmentCreator>`(`src/components/admin/`, props teachers·courses·본인 이메일) — 모달에서 학생 이메일(기본 본인)·과정·`EnrollScheduleField`로 요일·시간 선택→**그 시간에 가능한 강사 라이브 검색(`teacherHasAllSlots`, 실 위저드와 동일 패턴, `loadEnrollTeachers` 카드)·택일**·**시작일 자유**·**수업 횟수(1~60)** 입력→`createTestEnrollment`(`actions.ts`, `requireAdmin`, **실 동선의 D+3/D+14·폰·영문 가드 우회**, service_role insert: status `신청`·`total_sessions`·`is_test=true`, 강사/학생 스냅샷). 이후 승인→결제확인은 일반 흐름과 동일(결제확인 시 클래스 개수=`total_sessions ?? TOTAL_SESSIONS`). 생성분은 `EnrollmentsManager` 목록에 **"테스트" 칩** + 모달 **"테스트 삭제"**(`deleteTestEnrollment`, `is_test=true`만 하드삭제→classes FK cascade 동반 제거). **화상수업 관리(2단계)**: **①상위 `classes/page.tsx`**(server)가 전체 `classes`를 좁은 select로 조회→**`enrollment_id`별 JS 집계**(과정 인스턴스=수강신청 1건당 1행, 클래스 스냅샷으로 과정·학생·강사·기간·진행 카운트 산출; `upcoming`=**시간 기반** `예정 && kstDateMinToMs(session_date,lessonEndMin(end_min))≥now`(강사/수강생 강의실과 동일 — 수업 종료 시각 지나면 `done`, 날짜 단위 아님)/`done`/`cancelled`/`makeup`)→`<ClassEnrollmentsManager>`(client) — 검색(학생·강사·과정)+상태 필터(전체/진행중(`upcoming>0`)/완료)+정렬(과정/학생/강사/시작일), 행=과정·학생·강사·기간(`firstDate~lastDate`)·진행바(done/active)+예정·취소·보강 칩, **행 클릭→`/admin/classes/[enrollmentId]`**(router push). ⚠️ JS 집계는 중규모까지 적정 — 대규모 시 **DB 집계 뷰/RPC**로 전환(상위 쿼리를 과정 수만큼만 로드)이 scale path. **②상세 `classes/[enrollmentId]/page.tsx`**(server)가 `eq("enrollment_id")`로 **scoped 조회**(0행=`notFound()`)→`<ClassesManager>`(client, `title`/`subtitle`/`backHref` props로 헤더+"← 목록으로") — 상태 필터(전체/예정/완료/취소)+검색+정렬 테이블(`SortHeader` 날짜/강사/회차 + 시간·**피드백** 컬럼, **과정·학생 컬럼은 제거**(한 enrollment 스코프라 중복, 헤더 title/subtitle에 표시), 시간=`fmtTime(start~end)` 30분 슬롯, `is_makeup`이면 "보강" 칩). **상태 배지·필터·카운트는 시간 기반 파생**(`displayStatus(r,now)`: 취소면 취소, `now≥kstDateMinToMs(session_date,lessonEndMin(end_min))`면 **완료**, 아니면 예정 — DB `status`엔 완료 없음, 1분 틱; 강의실·상위 목록과 동일 기준. 배지색 예정=파랑·완료=회색·취소=빨강). **피드백 컬럼="보기" 버튼**(`Eye`, 피드백 있으면 활성·없으면 disabled, `stopPropagation`으로 행 클릭과 분리)→`FeedbackModal`(피드백 전용 읽기 모달, 커스텀 오버레이 `z-[110]`/패널 `z-[120]`·Esc·scroll lock, 작성일+전문 `whitespace-pre-wrap`). 모달 편집(일정변경/취소/강사대체)은 DB `status='예정'` 기준(완료=과거 수업도 노쇼 사후 처리 가능) — **단 과정 전체 종료 시 모든 편집 섹션 숨김**(`courseEnded`=ClassesManager가 `loadEndedEnrollmentIds` 기준을 클라 파생: 남은 미래 `예정` 수업(`now<kstDateMinToMs(date,lessonEndMin(end))`)이 하나도 없으면 종료, 단일 enrollment 스코프라 rows 전체가 한 과정). **행 클릭→`ClassDetailModal`**(커스텀 오버레이 `z-[110]`/패널 `z-[120]`): `예정` 행만(+과정 미종료) 액션 3종 — ⓐ**일정 변경**(날짜 input+시작/종료 `<select>` 06:00~24:00 30분)→`adminRescheduleClass(id,date,startMin,endMin)`(`actions.ts`, `requireAdmin()`→상태 `예정` 가드+입력검증+**강사 주간 가용 `teacherHasAllSlots` 검증**+**같은 날짜 다른 `예정` 수업(같은 강사∥학생) 분범위 겹침 차단**→update) ⓑ**수업 취소**("보강 수업 생성" 체크박스)→`adminCancelClass(id,generateMakeup)`(상태 `예정` 가드→`취소`+옵션 시 `createMakeupClass` 공유 호출+강사 이메일 best-effort; **학생 취소와 달리 6회 한도·1시간 컷오프 미적용**) ⓒ**강사 대체**(강사 사정으로 특정 회차만 다른 강사로 교체, 상세 page가 `loadEnrollTeachers()`로 전체 강사 카드 prop 전달→모달이 이 수업 요일·시간에 가용한 강사만 `teacherHasAllSlots` 클라 필터·원 강사 제외해 `<select>` 노출)→`adminReassignClass(id,newTeacherId)`(상태 `예정` 가드+새 강사 `role='teacher'`·**주간 가용 검증**+**같은 날짜 새 강사 시간 충돌 차단**→`teacher_id`/`teacher_name`만 update; 시간·학생 불변이라 입장 시 zoom은 `teacher_id`로 실시간 조회돼 새 강사로 라우팅·새 강사 My Classroom 노출/원 강사 제거 — **마이그레이션 불필요**; best-effort 학생 SMS+기존/새 강사 메일 `sendClassReassignToOld/NewTeacher`). 셋 다 `/admin/classes`·`/admin/classes/[enrollmentId]`·`/teacher`·`/mypage` revalidate. `취소` 행은 read-only. **남은 일정 일괄 변경(과정 단위, 담당 강사 유지·요일/시간만)**: 상세 헤더에 **"남은 일정 일괄 변경"** 버튼(미래 `예정` 수업 있을 때만; 상세 page가 `enrollments.slots`(현재 패턴)+담당 강사 `teacher_availability`를 `currentSlots`/`teacherSlots`/`enrollmentId` prop으로 전달)→`RescheduleRemainingModal`(`EnrollScheduleField`로 새 요일·시각 + 적용 시작일 date input(KST 오늘~) + 강사 가용 밖이면 경고·제출 차단)→`adminRescheduleRemaining(enrollmentId,newSlots,effectiveDate)`(`actions.ts`, `requireAdmin()`→**미래 `예정` 클래스만** 대상(`session_no` 오름차순, 과거·완료·취소 유지)+강사 주간 가용 `teacherHasAllSlots` 검증→`enumerateLessonSessions(effectiveDate,newSlots,남은수)`로 **1:1 remap**(행 id·회차 유지=진행률 불변, session_date/start/end만 update)+**같은 강사·학생의 다른 enrollment `예정` 클래스와 시간 충돌 차단**+`enrollments.slots`=newSlots 갱신(향후 가용/요약 반영)). 성공 시 `router.refresh()`; ClassesManager는 `useEffect(()=>setRows(classes),[classes])`로 새 prop 동기화. `/admin/classes`·`/admin/classes/[id]`·`/admin/enrollments`·`/teacher`·`/mypage` revalidate. **센터 관리**: `centers/page.tsx`(server)가 `from("centers")`(name·sort_order·**price_per_session·price_currency**) + `settings.php_to_krw`(환율) 조회→`<CentersManager>`(client, `useRouter.refresh()`+낙관 토스트). **추가 폼**(센터명·통화 `<select>`·단가)+**테이블 목록**(`#`·센터명·회당 단가·관리, `CurrentTeacherTable` 스타일)+**행 "수정"→`<CenterDetailModal>`**(`src/components/admin/`, 커스텀 오버레이 패널 `z-[110]`/`z-[120]`, Esc·scroll lock; 센터명·통화·단가 편집, 인라인 편집 폐지)+**환율 설정 카드**(1₱=N₩). **단가 통화 단일 소스 `src/data/currencies.ts`**: `CURRENCIES`(KRW `₩`/PHP `₱`)·`CURRENCY_VALUES`(검증)·`DEFAULT_CURRENCY`·`normalizeCurrency`·`formatPrice(amount,code)`(예 `₩30,000`/`₱1,500`)·`krwEquivalent(amount,code,phpToKrw)`(PHP만 환산, KRW/환율≤0이면 null). PHP 단가는 목록·모달에서 `≈ ₩환산` 함께 표시. 액션 `addCenter(name,price,currency)`/`updateCenter(id,name,price,currency)`/`deleteCenter`/**`updateExchangeRate(phpToKrw)`**(`settings` upsert, 양수만)(`actions.ts`, `requireAdmin()`+service_role, `revalidateCenterConsumers()`로 `/admin/centers`·`/teacher/apply`·`/teacher`·`/admin/teacher-requests` 갱신). `normalizePrice`(빈값/음수→null, 정수 내림). 센터 삭제 시 FK `on delete set null`로 참조 강사는 자동 None. (구 `availability/` "강사 시간표" 탭은 강사 관리 `TeacherInfoModal`로 통합·제거됨.) 각 server page가 **`createAdminClient()`(service_role, RLS 우회)**로 데이터 조회 → client 매니저(`src/components/admin/{EnrollmentsManager,ClassEnrollmentsManager,ClassesManager,MembersManager,TeacherRequestsManager,CentersManager,YoutubeManager}`)가 검색·필터·CRUD. 회원은 `admin.auth.admin.listUsers` + `profiles.role` merge. 쓰기는 `src/app/admin/actions.ts`(`"use server"`) — **모든 액션 첫 줄 `requireAdmin()`(세션 클라로 isAdmin 재확인) 후 service_role 쓰기**. 유튜브 `add/update/setVisibility/delete`(`revalidatePath("/admin/youtube")`+`"/"`로 랜딩 갱신). **회원 role 직접전환은 폐지** — `MembersManager`는 **role 읽기 전용 배지**만 표시. **role 변경은 강사 신청 flow로만**: `approveTeacherApplication`(**Postgres RPC `approve_teacher_application(p_app_id)`(security definer) 호출로 원자 처리** — 상태가 '신청'일 때만, role=teacher(`profiles.role`+`auth.users.app_metadata` 동기)+신청서 내용으로 profiles 채움(필수 first/last/bio 항상, 선택 experience·phone·zoom_url·avatar_url·nationality·gender·center_id은 `coalesce(신청서,기존)`)+status '승인'을 **단일 트랜잭션**으로. RPC 반환코드 `ok`/`not_found`/`not_pending`(재처리 차단=프로필 덮어쓰기 사고 방지)/`is_admin` 매핑. 성공 시 **지원자에 승인 알림 메일** best-effort, `revalidatePath("/teacher","layout")`)·`rejectTeacherApplication(id,note)`(**상태 가드: '신청'만**, `.eq("status","신청").select()` 0행=이미 처리, 성공 시 **지원자에 거절 알림 메일**(사유 포함) best-effort)·**`deleteTeacher(uid)`**(회수(→student)는 orphan/불일치 문제로 **폐지** → **계정 전체 삭제**: admin 보호 후 `auth.admin.deleteUser`→FK cascade로 profiles·teacher_applications·teacher_availability·reading_progress 제거, 아바타 Storage 파일은 cascade 비대상이라 `storage.from("avatars").list/remove`로 명시 정리, best-effort). `teacher-requests/page.tsx`는 `teacher_applications`(신청 우선 정렬)+`listUsers` 이메일 보강+현재 강사(`profiles.role='teacher'`, **프로필 필드 first/last_name·avatar_url·zoom_url·bio·experience·phone·nationality·gender·center_id 함께 select**(center_id는 `centers` 조회한 `centerNameById`로 이름 resolve해 `centerName` 전달) + 해당 강사들 `teacher_availability` 일괄 조회·`{day,min}[]` 그룹핑) 조회→`TeacherRequestsManager`(지원 목록=상태 탭·확장 카드, **승인/거절 버튼은 '신청' 행만**(처리완료는 read-only)·거절 사유 필수·승인 시 "현재 강사" 낙관적 추가(슬롯=[])·**toast**(alert 아님). **"현재 강사" 목록은 `<CurrentTeacherTable>`**(`src/components/admin/`, client, 정렬 가능 테이블 — 컬럼 이름·센터·국적·성별, `SortHeader` 토글 asc/desc, 빈 값 후순위·`localeCompare("ko")`, 정렬값은 원본 raw 기준이라 국기 이모지·라벨 미영향) — 행마다 **정보 보기**(→`<TeacherInfoModal>`)·**강사 삭제**(비가역 경고 AlertDialog, 삭제 시 강사·지원행 낙관적 제거)). **"현재 강사" 섹션 위에 `<TeacherAvailabilityFinder>`**(`src/components/admin/`, client, props `teachers`+`onView=setInfoTarget`): 원하는 요일(다중 토글)+**수업 횟수(1~8회, 1회=30분)**+시작 시각(`<select>`, 06:00~24:00 30분)으로 **종료=시작+횟수×30분 자동 계산**(읽기전용 표시, 종료≤24:00 되게 시작 옵션 클램프) → **해당 범위 전체가 비어 있는 강사** 필터(이미 로드된 `teacher.slots` 클라 필터, 추가 쿼리 0 — 매칭=**선택 모든 요일 AND** × **범위 내 모든 30분 슬롯 존재**). 결과 행 "정보 보기"는 동일 `setInfoTarget`로 `<TeacherInfoModal>` 재사용. **`<TeacherInfoModal>`**(`src/components/admin/`, client): base-nova `AlertDialog`는 폭이 `sm:max-w-sm`로 고정돼 넓은 콘텐츠에 부적합 → **`AvailabilityModal` 식 커스텀 패널**(오버레이 `z-[110]`+패널 `z-[120]` `w-[min(92vw,720px)]` `max-h-[90vh]` flex-col, 본문만 `overflow-auto`, a11y: Esc·body scroll lock·닫기 버튼 포커스·오버레이 클릭 닫기, 하단 고정 "닫기" 버튼). 내용: **프로필 사진**(avatar_url 있으면 80px `next/image`, 없으면 이름/이메일 **이니셜 폴백** 박스)+이름·이메일 → `dl`(이메일·전화·국적·성별·센터·Bio·경력·Zoom URL) → "주간 가능 시간" `<AvailabilityGrid readOnly>`(가용 셀 `bg-accent-blue/40`). `CurrentTeacher` 타입에 phone·nationality·gender·centerName·bio·experience·zoomUrl·avatarUrl·slots 포함. **승인 알림/거절 알림 메일은 `src/lib/mailer.ts`의 `sendTeacherApprovalNotification`/`sendTeacherRejectionNotification`(지원자 대상, 링크는 액션에서 `getOrigin` 생성).** **랜딩 유튜브 섹션은 `youtube_videos`(is_visible=true) 조회 → 비면 mock `VIDEOS` fallback.** **강사 시간표 열람**은 강사 관리 `TeacherInfoModal`(현재 강사 행 "정보 보기")의 `<AvailabilityGrid readOnly>`로 제공(별도 admin 페이지 없음).

### 인증 라우트 (Supabase Server Actions)

폼 5종 + OAuth는 `src/app/(auth)/` route group(괄호는 URL 미노출). `/auth/confirm`만 `src/app/auth/confirm/`로 분리(메일/OAuth가 URL 직접 참조).
- **`/login`** — page는 로그인 상태면 `/`로 redirect. `searchParams`: `error=auth-code-error`(빨강+회원가입 링크), `verified=pending`(녹색). `login()`은 `signInWithPassword` 실패 시 분기: `email_not_confirmed`→재발송(`canResend:true`); `invalid_credentials`→`getUserIdentitySummary()`로(`!found`→미가입 / `hasPassword`→비번 불일치 / `!hasPassword`→카카오 계정 안내), `null`→일시 오류. `resendConfirmation(email)`도 export(`resend({type:"signup"})`, Login/Signup 공유). rate limit + 이메일 검증. 배너는 `AuthBannerSlot`.
- **`/signup`** — **회원가입 동선 brand-blue 통일**(제출버튼·AlertDialogAction·"로그인" 링크·Eye ring·Navbar "회원가입"). 색 되돌리려면 이 네 곳 함께. page는 로그인 시 `/` redirect. 이메일/비번(**8자+**)/확인 검증 후 **`signUp()` 전 `getUserIdentitySummary(email)` 사전 검사**: `!found`→가입 진행 / `found&&!confirmed`→"이미 가입 시도"(`canResend`) / `found&&hasPassword`→로그인 안내 / `found&&!hasPassword`→카카오 안내. **⚠️ 핵심 보안: Supabase는 미확인 사용자 재가입 시 비번을 덮어쓰므로 이 사전 검사로 계정 탈취 차단. `!found`일 때만 `signUp({options:{emailRedirectTo:${origin}/auth/confirm}})` — 순서 절대 유지.** `null`→fail closed. signUp 후 `user_already_exists`/빈 `identities` 분기는 race condition 안전망. rate limit(IP 5분/5회).
- **`/logout`** — `logout()`: `signOut()` → `revalidatePath("/","layout")` → `redirect("/")`. Navbar 폼에서만 호출.
- **`/forgot-password`** — page 로그인 시 `/` redirect. `error=link-expired`→빨강 배너. `emailExists()` 확인 후 `resetPasswordForEmail(email,{redirectTo:${origin}/auth/confirm?next=/reset-password})`. rate limit(이메일 1분/1회).
- **`/reset-password`** — **세션 필수**(page에서 `getUser()`, 미인증 시 `/forgot-password`). `resetPassword()`: (1) 비번 검증(8자+, 확인), (2) **새 비번≠기존**(임시 `persistSession:false` client로 `signInWithPassword` 시도), (3) `updateUser({password})`, (4) **`signOut({scope:"others"})`**(타 기기 무효화), (5) `redirect("/?reset=success")`.
- **`/auth/confirm`** — GET route handler. 세 형식: `token_hash`+`type`→`verifyOtp()`; `code`→`exchangeCodeForSession()`(PKCE/OAuth 공용); `flow=oauth`→OAuth 식별. **`next`는 open redirect 방지로 `/`시작 & `//`아닌 경로만 허용**, 아니면 `/`. recovery 식별(`type==="recovery"` 또는 `next.startsWith("/reset-password")`). 성공: 비-recovery는 `verified=success`(이메일)/`login=success`(OAuth) 부착, recovery는 `next`로. 실패: recovery→`/forgot-password?error=link-expired`, 그 외→`/login?error=auth-code-error` 또는 `?verified=pending`. **두 분기 모두 유지.**
- **카카오 OAuth** — `src/app/(auth)/oauth/actions.ts`의 `signInWithKakao()`: `signInWithOAuth({provider:"kakao",options:{redirectTo:${origin}/auth/confirm?next=/&flow=oauth}})` → `data.url`로 외부 redirect. 실패 시 `/login?error=auth-code-error`. **rate limit 없음**(카카오 throttling 의존). UI: `KakaoButton.tsx`(client, `useTransition`+SVG+Loader2)가 Login/Signup 양쪽 "또는" 구분선 아래.
- **폼 컴포넌트** `src/components/auth/{LoginForm,SignupForm,ForgotPasswordForm,ResetPasswordForm}.tsx`: 모두 **`useActionState` + `<form action={formAction}>`**, **입력은 controlled(`useState`+`value`+`onChange`)**(React 19 auto-reset & Base UI `defaultValue` 경고 회피). 비번 필드: `Eye`/`EyeOff` 토글(`type="button"`, ring) + `useCapsLockWarning`(`src/hooks/use-caps-lock.ts`). a11y: 에러 시 `aria-invalid` + `aria-describedby`(id: `login-error`/`signup-error`/`forgot-error`/`reset-error` + caps/hint), `role="alert"`, 한국어 존댓말. query 배너는 `AuthBannerSlot`.
- **재발송 UX**(Login/Signup 공통): (1) `<AlertDialog>` 확인 모달, (2) success 시 **60초 클라 쿨다운**(`cooldownSec` + `setInterval`, 버튼 disabled). 서버 rate limit(1분/1회)과 이중 보호. **SignupForm은 success/error 양쪽에 트리거** — 한쪽 수정 시 다른 쪽도 검토(조건부 숨김 시 카운트다운 회귀 주의).
- **로딩 스피너**: `Loader2 animate-spin` + 컨텍스트 텍스트("로그인 중" 등), **trailing `...` 금지**. shadcn Button 내부는 `<Loader2 className="animate-spin"/>` 한 줄, raw button은 `<span className="inline-flex items-center gap-1.5">`로 감쌈.

### 전자책 `/textbook/[course]` (레지스트리 기반, 교재 5종)

- **레지스트리** `src/data/textbook/index.ts` — `TEXTBOOK_REGISTRY: Textbook[]`(5종: `workhol`·`kitchen`·`grammar1`·`grammar2`·`cosmetic`). 각 `{course,title,subtitle,eyebrow,freeUnits[],units[]}`. **course는 DB `reading_progress.course` & URL segment & `content/textbook/<course>/` 폴더와 1:1.** `units`는 `unitsFromBook(bookKey)`로 `landing.ts` `BOOKS`(셀프디벨롭)에서 파생(중복 방지, `titleKr=sub`·`situation=u.situation??sub??""`) — `workhol`만 `workhol.ts`의 `WORKHOL_UNITS`(실 situation 포함) 직접 사용. kitchen·cosmetic은 `BOOKS`에 `situation` 채워져 실 소개, grammar는 situation 없어 `sub` 폴백(=titleKr). `grammar2`는 unit 번호 **25–48**(`freeUnits:[25]`). export: `getTextbook`/`getTextbookUnit`/`getAdjacentUnits`(유닛 배열 순서 기준 prev/next)/`TEXTBOOKS`(`{course,title,href}`, Navbar "교재 보기"가 `.map()`).
- `src/data/textbook/workhol.ts` — `WORKHOL_COURSE="workhol"`, `WORKHOL_UNITS`(5필드×24). 워홀 콘텐츠(폴더 `content/textbook/workhol`). ⚠️ 과거 course 키는 `cooking`이었고 마이그레이션 `20260609013944_rename_cooking_course_to_workhol.sql`로 `reading_progress.course`를 일괄 갱신.
- `[course]/page.tsx`(server) — `generateStaticParams`=레지스트리, `getTextbook`→`notFound()`. Unit 목록(카드=제목+부제+**소개문단**, 단 소개문단은 `situation!==titleKr`일 때만 렌더 — grammar는 sub 중복이라 숨김=랜딩과 동일), 로그인 시 `reading_progress`(course별, `completed`만) 1회 조회→"학습 완료됨"/"시작 전" 텍스트·`CheckCircle2`·`Lock`·**완료 카드 배경 `bg-progress/5 border-progress/40`**(랜딩 완료 카드와 동일). 잠금=`!user && !freeUnits.includes(unit)`, 자물쇠는 `/login?next=...`.
- `[course]/[unit]/page.tsx`(server) — `generateStaticParams`=레지스트리 flatMap, `notFound()`(course/unit 무효), **무료 외 미로그인 `redirect("/login?next=/textbook/<course>/<unit>")`**. HTML `content/textbook/<course>/<htmlFile>` `fs.readFile`→`TextbookViewer`. **읽은 HTML의 `__AUDIO_BASE__` 토큰을 `${NEXT_PUBLIC_SUPABASE_URL}/storage/v1/object/public/textbook-audio/`로 `replaceAll`**(문장별 음성용 절대 URL; 토큰 미포함 교재엔 무영향). `prev/nextHref`는 `getAdjacentUnits`, `totalUnits`=최대 unit 번호(grammar2 라벨 "Unit 25/48"용).
- `src/app/textbook/actions.ts`(공유 `"use server"`) — **`setUnitCompleted(course,unit,completed)`**(수동 완료 정책 — 스크롤 자동 판정 없음). **진입 가드 순서 고정: (1) `getTextbook(course)` 화이트리스트, (2) unit이 해당 교재 `units`에 존재, (3) `getUser()` 없으면 silent.** `upsert({onConflict:"user_id,course,unit"})` 후 `revalidatePath("/")`+`"/textbook/<course>"`(랜딩·목록 완료 동기화). ⚠️ 과거 `saveScrollProgress`(스크롤 자동 진행률)는 제거됨.
- `src/components/textbook/TextbookViewer.tsx`(client) — **iframe `srcDoc`** 격리, `scrolling="no"` + `ResizeObserver` + 0.5/1.5s 재측정으로 높이 확장→부모 스크롤. 상·하단 `NavBar`(상단 `sticky top-[72px]`), prev/next는 **`prevHref`/`nextHref` 유무로 렌더**. 하단 **수동 완료 토글**(`CompletionToggle`, 로그인만): 미완료=`학습 완료로 표시` 버튼 / 완료=`학습 완료됨` 배지 + `완료 취소하기`(X) 버튼 분리. `initialCompleted` prop, `useTransition` 낙관적+실패 롤백. 액션 import는 `@/app/textbook/actions`.
- **HTML 본문**: 루트 `content/textbook/<course>/unit-XX.html`(완전한 HTML 문서, 2자리 zero-pad). **`src/` 아닌 `content/`**, `fs.readFile`(번들 미포함·public 아님). **새 교재 추가: (1) `content/textbook/<course>/unit-NN.html`, (2) `TEXTBOOK_REGISTRY`에 항목 추가(`units`는 `unitsFromBook` 또는 직접 작성), (3) 랜딩 연동하려면 `LINKED_COURSE`+`page.tsx` 쿼리 2곳.** 라우트·Navbar·진행률이 모두 레지스트리 기반이라 마이그레이션 불필요. ⚠️ 뷰어 iframe `srcDoc`은 sandbox 없어 **본문 내 `<script>` 실행됨**(모바일 가독성 검증은 헤드리스 Chrome으로 iframe 폭 렌더).
- **문장별 원어민 음성(workhol 전용)**: workhol 24유닛 HTML의 **TALK·EXTENSION·ASK** 문장 옆에 `.audio-btn`(▶) + 인라인 `<script>`(`new Audio(AUDIO_BASE+data-audio)`, 단일재생·토글·`.playing` pulse). `data-audio="unitNN/{talk,ext,ask}_N.mp3"`(DOM 순서), `AUDIO_BASE='__AUDIO_BASE__workhol/'`(page.tsx에서 치환). mp3는 **Supabase Storage 공개 버킷 `textbook-audio`**(키 `workhol/unitNN/...`, ~649개). 생성/동기화 스크립트: `scripts/add-workhol-audio-buttons.mjs`(HTML에 버튼 주입, 멱등) + `scripts/sync-workhol-audio.mjs`(라이브 사이트→버킷 업로드, service_role). PICK·MISSION엔 버튼 없음. 타 교재 확장 시 동일 패턴(버튼+토큰+녹음) 재사용.
- **grammar1·grammar2 콘텐츠**(`content/textbook/grammar{1,2}/`)는 타 교재와 달리 **desktop/mobile 디자인 분리**: 데스크톱=`.page` 인쇄형(182mm), 모바일=`body.mobile` 규칙(테이블→카드화 등, specificity 높아 자체 `@media`보다 우선). `<body>` 직후 인라인 스크립트가 `matchMedia("(max-width:768px)")`로 `.mobile` 클래스 자동 토글(FOUC 방지 동기 실행+리사이즈 추종). **color 모드 고정**(`:root` 기본값), 과거 Color/B&W·Desktop/Mobile **toolbar는 제거됨**. workhol/kitchen은 단순 `@media` 반응형(class 없음).

### 헬퍼

- `src/lib/utils.ts` — `cn()`.
- `src/lib/auth.ts` — `getUserRole`/`isAdmin`/`isTeacher`(supabase,userId)(`profiles.role`, app_metadata 대신 profiles 우선).
- `src/lib/mailer.ts` — `sendTeacherApplicationNotification`(강사지원, 관리자 대상) + **`sendEnrollmentNotificationToTeacher(to[],data)`(수강신청, 강사 대상·영문)** + **`sendEnrollmentPaymentConfirmedToTeacher(to[],data)`(결제 확정→수업 확정 알림, 강사 대상·영문, `EnrollmentEmailData` 재사용)** + **`sendEnrollmentCancellationToTeacher(to[],data)`(학생 취소 알림, 강사 대상·영문, `EnrollmentEmailData` 재사용)** + **`sendClassReassignToNewTeacher`/`sendClassReassignToOldTeacher(to[],data)`(개별 수업 강사 대체 알림, 강사 대상·영문, `ClassReassignEmailData`: 새 강사=배정·기존 강사=이관)** + **`sendEnrollmentNotificationToAdmin`(신규 신청 접수)/`sendEnrollmentApprovedToAdmin`(강사 승인→결제대기)/`sendEnrollmentRejectedToAdmin`(강사 거절, 사유 포함)(관리자 대상·영문, `EnrollmentAdminEmailData`(+`reason?`) 재사용, `getAdminEmails()` 수신)** + **`sendTeacherApprovalNotification`/`sendTeacherRejectionNotification`(강사 심사 결과, 지원자 대상)** — 모두 Resend, best-effort no-op, `import "server-only"`.
- `src/lib/origin.ts` — `getOrigin(headers)`: `NEXT_PUBLIC_SITE_URL`→`origin`→`x-forwarded-host`+proto→`host`.
- `src/lib/email.ts` — `isValidEmail()`(서버측 검증).
- `src/lib/url.ts` — `isValidZoomUrl()`(http/https 검증, 강사 프로필·강사 지원 폼 공유).
- `src/lib/phone.ts` — `normalizePhone`(숫자만)/`isValidKoreanMobile`(`/^01[016789]\d{7,8}$/`)/`formatPhone`(하이픈 표시). 마이페이지 전화번호 인증 클라·서버 공유.
- `src/lib/sms.ts` — `sendSms(to,text):Promise<boolean>`(Solapi `SolapiMessageService.send`, `import "server-only"`). env 미설정/예외 시 false. ⚠️ **`send()`는 "접수 성공"이지 "발송 성공"이 아님** — 발신번호 미등록·잔액부족 등은 예외가 아니라 응답 `groupInfo.count.registeredFailed`/`failedMessageList`에 담기므로, `registeredSuccess<1 ∥ registeredFailed>0 ∥ failedList`면 false 반환 + 사유 콘솔 로깅(OTP는 성공 여부 필요). 전화번호 인증 OTP + **수강신청 승인/거절 결과 통보**에 사용.
- `src/lib/availability.ts` — 주간 슬롯 공유 모델(`Slot{day,min}`·`slotKey`·`SLOT_MIN`·`GRID_START/END_HOUR`·`DISPLAY_DAYS`·`DAY_LABELS`/`DAY_LABELS_KO`·`ROW_MINS`·`teacherHasAllSlots`(강사 슬롯⊇요청)·`subtractSlots`·`slotsOverlap`·**`overlappingIds(items{id,group,slots})`**(같은 group=강사 내 슬롯 겹치는 id 집합 — 수강신청 목록 시간충돌 시각표시용, 활성 행만 걸러 호출)·`isValidSlot`·`summarizeSlots(slots,ko)`·**`lessonEndMin(endMin)`**(슬롯 종료−5분=레슨/입장 종료, 표시·입장 시간창 단일 소스)). 강사 그리드·수강신청 그리드·매칭·요약·충돌표시 단일 소스(클라/서버 공용, server-only 아님).
- `src/lib/booking.ts`(server-only) — `loadEndedEnrollmentIds(admin,teacherIds)`(종료된 `결제완료` enrollment id 집합 — 남은 `예정` 수업(`classes.session_date≥오늘KST`) 없으면 종료, 클래스 없는 레거시는 `lessonEndDate` 폴백) + `todayKst()`. 슬롯 점유로 취급하는 모든 지점(가용 차감·승인 충돌·그리드 표시)이 공용해 **종료 과정의 슬롯 자동 해제**. service_role/세션 client 양쪽 허용.
- `src/lib/avatar.ts` — `uploadAvatar(file,uid)`→`{publicUrl,path}` + `cleanupOldAvatars(uid,keepPath)`(본인 폴더 누적 파일 정리, best-effort). 강사 프로필·강사 신청폼 공유(브라우저 client 전용).
- `src/lib/makeup.ts` — `createMakeupClass(admin,cls)`→보강 날짜∥undefined(취소 1회당 자동 보강 생성, 마지막 수업일 뒤 첫 스케줄 요일, `is_makeup=true`, best-effort) + `weekdayOf`/`addDaysStr`(TZ 비종속 날짜 헬퍼). 학생 `cancelClass`·admin `adminCancelClass` 공용, `import "server-only"`.
- `src/lib/rate-limit.ts` — in-memory 토큰 버킷 `rateLimit/getClientIp/formatRetryAfter`. **⚠️ 프로세스 메모리라 멀티 인스턴스(Vercel)에선 효과 제한** → 필요 시 Upstash Redis로 함수 내부만 교체.
- `src/hooks/use-caps-lock.ts` — `useCapsLockWarning()`, `capsLockHandlers` spread.

### Supabase SSR

- **경로 `src/utils/supabase/`** — `client.ts`(`createBrowserClient`) · `server.ts`(`createServerClient`+cookies) · `middleware.ts`(`updateSession()` 매 요청 `getUser()`로 세션 갱신 — **`createServerClient`와 `getUser()` 사이 로직 금지**) · `admin.ts`(`service_role`, 첫 줄 `import "server-only"`).
- **`admin.ts` export**: `createAdminClient()` + `emailExists()`(@deprecated) + `getUserStatus()`(@deprecated) + **`getUserIdentitySummary(email)`**(권장: `{found:false}` | `{found:true,confirmed,hasPassword,oauthProviders[]}` | `null`) + **`getAdminEmails()`**(`profiles.role='admin'`→`getUserById`로 이메일, 신청 알림 수신자, 실패 시 `[]`). 내부: `listUsers`로 id 찾고 `getUserById(id)` 재호출해 `identities` 안전 확보(listUsers만으론 빈 배열로 옴). 모두 페이지네이션(perPage 1000, ≤50p) — **같은 액션에서 2번 호출 금지**. 에러 시 `null`(호출 측 "일시적 오류" 분기).
- `src/proxy.ts` — Next 16 `proxy` 컨벤션(v15 `middleware` 리네임), `updateSession` 호출, `matcher`로 정적 자산 제외. 헬퍼 `middleware.ts` 파일명은 유지.
- **환경 변수**: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`(필수), `SUPABASE_SERVICE_ROLE_KEY`(admin용, **`NEXT_PUBLIC_` 절대 금지**), `NEXT_PUBLIC_SITE_URL`(운영 권장). **`RESEND_API_KEY`**(신청 알림 메일, 미설정 시 발송 no-op) + **`APPLICATION_NOTIFY_FROM`**(선택, 발신 주소 — Resend 인증 도메인; 미설정 시 `onboarding@resend.dev` 테스트 발신) + **`SOLAPI_API_KEY`/`SOLAPI_API_SECRET`/`SOLAPI_SENDER`**(전화번호 SMS 인증, Solapi/CoolSMS — 발신번호는 사전등록 필수; 미설정 시 OTP 발송 실패 = 인증 불가).
- **Dashboard 설정**: Site URL = 운영 도메인, Redirect URLs에 `https://<도메인>/auth/confirm` + `http://localhost:3000/**`. 권장 메일 템플릿: `{{ .SiteURL }}/auth/confirm?token_hash={{ .TokenHash }}&type=email&next=/`. **카카오**: Kakao Developers에서 앱 생성 → REST 키+Secret → Redirect URI `https://<ref>.supabase.co/auth/v1/callback` → **이메일 "필수동의"**. Supabase Providers>Kakao 활성화 + **"Allow account linking" 활성화**(같은 이메일 이메일/카카오 자동 연결, 비활성 시 콜백 실패).

### DB 스키마 (`supabase/`)

마이그레이션은 `db:new`로만 생성(CLI는 timestamp 형식만 인식), Dashboard SQL 직접 실행 금지. link 정보는 `supabase/.temp/`(gitignore, 새 머신은 `npx supabase login` + `link --project-ref`). **⚠️ Claude는 CLI 원격 접근 불가(403) → `db:push`/`db:types`는 사용자가 직접 실행.** 핵심 마이그레이션 4개(+ `20260609013944` cooking→workhol 리네임, `20260609230959` `reading_progress.scroll_percent` 제거, **`20260610232255` profiles 강사 컬럼·role 자가변경 차단 트리거 / `20260610232258` `avatars` Storage 버킷 / `20260612052457` `teacher_availability` 테이블** — **모두 db:push·db:types 적용 완료**, + `20260612014004` 미구현 LMS 화상수업 객체 롤백(`classes`/`class_sessions`/`academic_events` `drop if exists`, 공유 `tg_set_updated_at` 보존 — 순net 무변경, CLAUDE.md 미문서화 기능):
- **`20260522065400_create_profiles_and_role_trigger.sql`**: `user_role` ENUM(`admin|teacher|student`); `profiles`(`id` PK FK→auth.users CASCADE, `role` DEFAULT 'student', `created_at`/`updated_at`); RLS + `profiles_select_own`/`_update_own`(`auth.uid()=id`, **INSERT/DELETE 정책 없음**); `handle_new_user()`(SECURITY DEFINER) + `on_auth_user_created` trigger(모든 가입 경로에서 `profiles` insert + `raw_app_meta_data`에 `{role:'student'}` merge); `tg_set_updated_at()` trigger. **backfill 포함**. **role 활용: admin 대시보드(`/admin`)·강사 페이지(`/teacher`)에서 사용** — `src/lib/auth.ts`의 `isAdmin`/`isTeacher`/`getUserRole`(`profiles.role`, app_metadata는 stale 가능해 profiles 우선). ⚠️ `_update_own`엔 WITH CHECK 없어 role 자가변경 가능했으나 **`20260610232255`의 `prevent_role_self_change` 트리거**(BEFORE UPDATE, `current_user not in (service_role,postgres,supabase_admin)`이면 role을 OLD로 되돌림)로 차단 — 일반 사용자 escalation 불가, admin 액션(service_role)만 변경 가능.
- **`20260610232255_extend_profiles_teacher.sql`**: `profiles`에 `full_name`/`avatar_url`/`zoom_url`/`bio`/`headline`/`phone` 컬럼(강사 프로필, 향후 학생 공용) + `prevent_role_self_change` 트리거(위). ⚠️ `headline`은 **`20260617004232_profiles_drop_headline.sql`로 제거됨**(강사 프로필 한 줄 소개 항목 폐지). + **`experience`는 `20260617011522`로 추가**(강사 신청폼↔프로필 일치). db:push·db:types 적용 완료.
- **`20260612234016_split_profiles_name.sql`**: `profiles`에 `first_name`/`last_name` 추가 + 구 `full_name`에서 백필(마지막 공백 split: 앞=first, 끝 토큰=last, 공백 없으면 전체=first). `full_name`은 **drop 안 함**(비파괴 보존, 앱은 first/last만 사용). db:push·db:types 적용 완료.
- **`20260623054826_add_address_to_profiles.sql`**: `profiles`에 `postcode`/`address`/`address_detail` 추가(학생 마이페이지 주소, **선택 입력 nullable**). 트리거/RLS 변경 없음(`profiles_update_own`로 본인 갱신). ⚠️ db:push·db:types 적용 완료.
- **`20260624225034_add_english_name_to_profiles.sql`**: `profiles`에 `english_name text` 추가(학생/공용 영문 이름, **수강신청 필수**·마이페이지 입력). 민감 컬럼 아니라 트리거/RLS 변경 없음(`profiles_update_own`). 강사 표시용 스냅샷은 `enrollments.student_english_name`(`20260624233056`). ⚠️ db:push·db:types 적용 완료.
- **`20260623075341_add_enrollments.sql`**: `enrollment_status` ENUM(`신청|승인|결제대기|결제완료|거절|취소` — `결제대기`는 `20260625001341`로 `after '승인'`, **`결제완료`는 `20260626015712`로 `after '결제대기'`**(admin 입금 확인 시 전환, 결제 1단계); 슬롯 점유: `승인`·`결제완료`=하드 확정(가용 차감, **단 `결제완료`는 과정 종료 후 자동 해제** — `loadEndedEnrollmentIds`), `신청`·`결제대기`=소프트 보류); `enrollments`(`id` uuid PK, `student_id`/`teacher_id` FK→auth.users **ON DELETE CASCADE**(로그인 필수), `course`/`course_title`, `start_date date`, `slots jsonb`(`[{day,min}]` 주간 30분 슬롯 스냅샷), `teacher_name`/`student_name`/`student_english_name`/`student_phone`(표시·SMS 스냅샷; `student_english_name`은 **`20260624233056`로 추가**+백필 — 강사 세션은 RLS로 학생 profiles 직접 조회 불가라 스냅샷), `status` DEFAULT '신청', `teacher_note`, `created_at`/`updated_at`); 인덱스 `(student_id)`/`(teacher_id,status)`/`(created_at desc)`; RLS + `_insert`(`to authenticated` `student_id=auth.uid()`) + `_select_own_student`(본인 학생, mypage) + `_select_own_teacher`(본인 강사, 대시보드). **UPDATE/DELETE 사용자 정책 없음**(승인/거절/**학생 자가 취소**는 강사·학생 액션 service_role: `cancelEnrollment`(`src/app/mypage/actions.ts`)=본인 소유+상태 `신청`/`승인`/`결제대기` 가드→`취소`, 강사에 취소 이메일 `sendEnrollmentCancellationToTeacher` best-effort). `tg_set_updated_at` 재사용. 강사 매칭형 수강신청의 저장소(구 상담신청 `applications` 폐지·대체). ⚠️ db:push·db:types 적용 완료. + **`20260628014316_add_enrollment_test_fields.sql`**: `enrollments`에 `total_sessions int`(null=기본 24, admin 테스트 수강신청의 자유 횟수→클래스 생성 개수)·`is_test boolean default false`(admin 테스트 생성분 식별/정리) 추가(additive, 기존 무영향). ⚠️ **db:push·db:types는 사용자가 직접 실행 필요**.
- **`20260626221643_add_classes.sql`**: `classes`(화상수업 클래스 — 결제 확정 시 `confirmPayment`가 날짜별 자동 생성, 위 화상수업 항목): `id` uuid PK, `enrollment_id`/`student_id`/`teacher_id` FK→auth.users·enrollments **cascade**, `course`/`course_title`, 표시 스냅샷 `teacher_name`/`student_name`/`student_english_name`, `session_no smallint`(회차), `session_date date`(KST), `start_min`/`end_min smallint`(30배수 CHECK), `unique(enrollment_id,session_no)`(멱등 생성); 인덱스 `(student_id,session_date)`/`(teacher_id,session_date)`/`(enrollment_id)`; RLS + `_select_own_student`/`_select_own_teacher`만(**INSERT/UPDATE/DELETE 사용자 정책 없음** — 생성 service_role·정리 cascade); `tg_set_updated_at` 재사용. **db:push·db:types 적용 완료.**
- **`20260627011057_add_class_cancellation.sql`**: `classes`에 `status text default '예정' check in('예정','취소')`(개별 수업 취소)·`is_makeup boolean default false`(자동 보강 회차 식별) 추가. 취소 시 보강은 `session_no=enrollment 최대+1`로 부여(기존 unique 충돌 없음). **db:push·db:types 적용 완료.**
- **`20260629234513_add_class_feedback.sql`**: `classes`에 `feedback text`·`feedback_at timestamptz` 추가(수업 종료 후 강사 피드백, 수강생 읽기 전용). 트리거/RLS 변경 없음(추가 컬럼만 — 쓰기는 `saveClassFeedback` service_role 액션이 강사 소유권+종료 가드로 처리, 읽기는 기존 `classes_select_own_student`/`_teacher`). ⚠️ **db:push·db:types는 사용자가 직접 실행 필요**.
- **`20260630021451_add_class_conducted.sql`**: `classes`에 `teacher_entered_at timestamptz`(강사 Enter 첫 입장 시각)·`conducted_at timestamptz`(수업 진행 인정, sticky) 추가. 트리거/RLS 변경 없음(쓰기는 `enterClass`/`saveClassFeedback` service_role 액션). 진행 인정 규칙=강사 입장 AND 피드백 저장. ⚠️ **db:push·db:types는 사용자가 직접 실행 필요**.
- **`20260610232258_create_avatars_bucket.sql`**: Storage 버킷 `avatars`(public read) + `storage.objects` 정책(공개 select + authenticated insert/update/delete는 본인 폴더 `(storage.foldername(name))[1]=auth.uid()`). 프로필 사진 업로드 경로 `avatars/<uid>/...`.
- **`20260612052457_add_teacher_availability.sql`**: `teacher_availability`(복합 PK `(teacher_id,day_of_week,start_min)`, `teacher_id` FK→auth.users CASCADE, `day_of_week smallint [0,6]`(0=일, JS getDay 동일), `start_min smallint`(자정 기준 분, 30배수·[0,1439] CHECK — UI 06:00~24:00 범위는 비종속), `created_at`/`updated_at`); 인덱스 `(teacher_id)`; RLS + `_select/_insert/_delete_own`(`auth.uid()=teacher_id`, **UPDATE 정책 없음**=슬롯 boolean이라 add/delete로 표현); `tg_set_updated_at` 트리거. admin 정책 없음(service_role 우회). 가용 슬롯 1개=row 1개, 강사 액션은 delete→bulk insert로 전체 교체.
- **`20260523231214_add_reading_progress.sql`**: `reading_progress`(복합 PK `(user_id,course,unit)`, `user_id` FK CASCADE, `course text`, `unit int [1,100]`, `completed bool DEFAULT false`, `last_viewed_at`); 인덱스 `(user_id,course)`; RLS + `_select/_insert/_update_own`(`auth.uid()=user_id`, **DELETE 정책 없음**). ⚠️ 최초엔 `scroll_percent int [0,100]` 컬럼도 있었으나 `20260609230959`에서 제거(수동 완료 정책).
- ~~**`20260608233330_add_applications.sql`**(상담신청 `applications` 테이블 + `application_status` enum)~~ → **`20260627224406_drop_applications.sql`로 폐지**(`drop table … cascade` + `drop type application_status`). 상담신청 동선이 수강신청 `enrollments`로 완전 대체되어 admin '신청 관리' UI·메일러·테이블 일괄 제거. 공유 `tg_set_updated_at`는 보존(트리거만 cascade 제거). ⚠️ **db:push·db:types는 사용자가 직접 실행 필요**.
- **`20260616013935_add_teacher_applications.sql`**: `teacher_application_status` ENUM(`신청|승인|거절`); `teacher_applications`(`id` uuid PK, `user_id` FK→auth.users **ON DELETE CASCADE**(로그인 필수, 익명 불가), `name`/`first_name`/`last_name`/`phone`/`bio`(필수)/`experience`/`zoom_url`/`avatar_url`, `status` DEFAULT '신청', `admin_note`, `created_at`/`updated_at`); 인덱스 `(user_id)`/`(status)`/`(created_at desc)` + **부분 유니크 `(user_id) where status='신청'`**(중복 대기 신청 차단, 거절/승인 후 재신청 허용); RLS + `teacher_applications_insert`(`to authenticated` `with check (user_id=auth.uid())` — 익명 제외) + `_select_own`(본인, mypage 상태표시). **UPDATE/DELETE 사용자 정책 없음**(승인/거절/role은 admin service_role). `tg_set_updated_at` 재사용. db:push·db:types 적용 완료. **role 부여는 이 flow로만**(Members 직접전환 폐지). + **`20260616234643_..._phone_nullable.sql`**(phone `drop not null`+빈문자열→null) + **`20260617000547_..._split_name.sql`**(`first_name`/`last_name` 추가·name에서 백필, 구 `name`은 표시/검색/메일 호환 위해 보존하고 조합값으로 계속 채움) + **`20260617003445_..._drop_headline.sql`**(headline 컬럼 제거) + **`20260617011522_align_teacher_application_profile.sql`**(강사 신청폼↔프로필 필드 일치: `teacher_applications` `intro`→`bio` 리네임 + `zoom_url`/`avatar_url` 추가, `profiles`에 `experience` 추가) + **`20260617072141_approve_teacher_application_fn.sql`**(승인 원자 처리 RPC `approve_teacher_application(uuid) returns text`, security definer, service_role grant — 상태 '신청' 가드 + role/profile/status 단일 트랜잭션) + **`20260620000000_add_nationality_teacher.sql`**(`teacher_applications`·`profiles`에 `nationality text` 추가(국가명 문자열 저장, 9개국 `src/data/nationalities.ts`) + 승인 RPC `create or replace`로 `nationality=coalesce(신청서,기존)` 한 줄 추가) + **`20260620010000_add_gender_teacher.sql`**(`teacher_applications`·`profiles`에 `gender text` 추가(코드 male/female 저장, `src/data/genders.ts`) + 승인 RPC `create or replace`(nationality·gender coalesce 모두 포함한 최신 정의)) + **`20260620020000_add_centers.sql`**(`centers` 테이블(id·name·sort_order, RLS 공개 select·쓰기 admin service_role만, `tg_set_updated_at`, 시드 Lani·Sebu) + `teacher_applications`·`profiles`에 `center_id uuid` FK→centers `on delete set null` + 승인 RPC `create or replace`(nationality·gender·center_id coalesce 포함 최신 정의)) + **`20260621233848_add_center_price.sql`**(`centers.price_per_session integer` nullable=센터 1회당 단가) + **`20260622013926_add_center_currency.sql`**(`centers.price_currency text not null default 'KRW'`=단가 통화 KRW/PHP, `src/data/currencies.ts`) + **`20260622014843_add_settings_table.sql`**(`settings` key-value 테이블(key PK·value·updated_at, RLS 공개 select·쓰기 service_role만, `tg_set_updated_at`, 시드 `php_to_krw='25'`)=앱 전역 설정, 현재 페소 환율 1건) + **`20260622230424_add_phone_verification.sql`**(`profiles.phone_verified_at timestamptz` + `phone_verifications` 테이블(user_id PK FK→auth.users cascade·phone·code_hash·expires_at·attempts·last_sent_at, RLS enabled+정책 없음=service_role만)=SMS OTP 챌린지 + **트리거 `prevent_phone_self_change`**(BEFORE UPDATE profiles, `prevent_role_self_change` 미러: service_role 외엔 `phone`/`phone_verified_at`를 OLD로 되돌림 → 인증 우회 차단)) — **모두 db:push·db:types 적용 완료**.
- **`20260608235010_add_youtube_and_admin.sql`**: `youtube_videos`(`id` uuid PK, `tag`/`url`/`title`/`description`, `is_visible` bool DEFAULT true, `sort_order` int, `created_at`/`updated_at`); 인덱스 `(is_visible,sort_order)`; RLS + **`youtube_videos_select_visible`(`to anon,authenticated` `using (is_visible=true)`)**, **쓰기 정책 없음**(admin service_role만); `tg_set_updated_at` 트리거; **시드 3건**. + **admin 부여 DO 블록**: `82orez@gmail.com`의 `profiles.role='admin'` & `auth.users.raw_app_meta_data.role='admin'`(계정 미존재 시 no-op).
- **타입** `src/types/database.types.ts`: `db:types`로 생성. **`db:push` 후 항상 재생성**(drift 방지). `Database["public"]["Tables"][...]["Row"|"Insert"|"Update"]`. (현재 SSR client는 `<Database>` 미적용 → `.from("enrollments")`·`.from("youtube_videos")` 등 untyped.)

## 주의사항

- **TS 느슨함**: `strict:false`, `noImplicitAny:false` — 타입 명시 확인 권장.
- **Tailwind v4 `@apply` 제한**: `@layer components` 안에서 다른 커스텀 컴포넌트 클래스 `@apply` 시 빌드 실패 → 유틸 추출 또는 JSX className 조합.
- **shadcn + `cn()`**: 신규 UI는 `src/components/ui/` 재사용 우선, 추가는 CLI. `base-nova`/`neutral` 변경 금지. className은 항상 `cn()`(직접 결합 시 tailwind-merge 누락).
- **Server Action + `useActionState` 유지**: `fetch` 없이 `<form action>`. 성공 시 `revalidatePath`→`redirect`, 실패 시 `{error}`(/`{success}`). 인증 액션 import는 `@/app/(auth)/<route>/actions`.
- **Controlled input 필수**: Base UI `defaultValue` 경고 + React 19 auto-reset → 항상 `useState`+`value`+`onChange`, round-trip 금지.
- **인증 액션 순서**: (1) 필수값 검증 (2) `rateLimit()` (3) `isValidEmail()` (4) Supabase 호출. rate limit 키는 `email.toLowerCase()`. redirect는 `getOrigin()`.
- **사용자 열거 노출은 의도된 정책**: login/signup/forgot 모두 가입/미가입 구분 — 변경 시 세 곳 함께.
- **⚠️ 미확인 사용자 비번 덮어쓰기 방지(절대 우회 금지)**: signup은 `signUp()` 전 `getUserIdentitySummary()`로 확인, `found&&!confirmed` 차단. `!summary.found`일 때만 `signUp()` 호출 순서 유지.
- **Prettier**: `printWidth:150`, `endOfLine:"crlf"`, `singleQuote:false`, `trailingComma:"all"`, `semi:true`, `tabWidth:2`, `prettier-plugin-tailwindcss`.
- **버튼 커서 금지**: 전역 규칙 있음 → `cursor-pointer` 추가 금지(`disabled:cursor-not-allowed`만 허용).
- **색상 토큰 우선**: hex arbitrary value 피하고 시맨틱 토큰 사용.
- **CTA/anchor**: 랜딩 `#courses`, 과정 상세 `#apply-form` 등 anchor + `scroll-behavior:smooth`. `<a>` 버튼화는 `buttonVariants()`+`cn()`.
- **모바일 메뉴 a11y 회귀 주의**(위 Navbar 항목 체인 유지).
- **DB 변경은 항상 마이그레이션**: `db:new`→검토→`db:push`. **`db:push`는 destructive** — 직후 `/signup` 회귀 테스트(가입 성공 + `profiles` row + `raw_app_meta_data.role='student'`). 파일명 수동 명명 금지.
- **⚠️ role은 `app_metadata`에만, `user_metadata` 절대 금지**(후자는 클라가 `updateUser({data})`로 수정 가능 → 권한 우회). role 읽기는 JWT `app_metadata.role` 또는 `profiles.role`만. role 부여 시 `admin.auth.admin.updateUserById(id,{app_metadata:{role}})` + `profiles` update 함께.
- **profiles RLS 최소(본인 row만)**: admin 전체 조회 정책 미구현 → 필요 시 `createAdminClient()` 우회. 향후 `profiles_admin_all` 정책 추가.
- **교재 완료 액션 가드 유지**(위 `src/app/textbook/actions.ts` 항목): `setUnitCompleted`은 레지스트리 화이트리스트+unit 존재 검증 후 upsert. 완료는 **사용자 수동 토글만**(스크롤 자동 판정 부활 금지).

## 별칭

`@/*` → `./src/*`. 이미지는 `next/image`만(`public/images/`). **강사 아바타는 Supabase Storage 공개 URL** → `next.config.ts` `images.remotePatterns`에 `*.supabase.co` `/storage/v1/object/public/**` 등록됨. Favicon은 `src/app/icon.svg`(파일 컨벤션).

## 배포

Vercel 배포. `vercel.json`은 `regions:["icn1"]`(서울 리전 고정)만 설정 — 한국 사용자 대상 레이턴시 최적화. 환경 변수는 위 Supabase SSR 항목 참조.
