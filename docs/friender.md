# 프렌더 지원/승인 · 연습방 · 프렌딩

> `CLAUDE.md`에서 분리된 상세 문서. 본문의 "위 …항목" 상호참조는 다른 `docs/*.md`일 수 있음(색인=CLAUDE.md).

**프렌더 지원** (`/friender/apply`, **UI 전체 한국어**, 강사 지원 flow의 한국어 클론 — role `friender`는 teacher와 유사하나 별개): 진입점=Footer "프렌더 지원하기". **강사 flow와 다른 4가지**: ①한국어 UI ②**전화번호 SMS 인증 필수** ③센터 선택 없음 ④학력(bio)·경력(experience) 2개 대신 **`intro`("회원들에게 자신을 소개하기") 1개**, zoom_url 필수. `page.tsx`(server) 가드 `!user`→`/login?next=…`, role∈{프렌더 계열(`isFrienderRole`),teacher,admin}→redirect(단일 role이라 중복 신분 불가) → `profiles`(first/last_name·phone·**phone_verified_at**·nationality·gender·avatar_url) + 본인 `friender_applications` 최신 1건 → `status='신청'`이면 "심사 중"+read-only dl, 그 외 `<FrienderApplicationForm>`. ⚠️ **자격 해제 상태 표시**: 자격 회수(`revokeFriender`)는 신청서 `status`를 건드리지 않아 `승인`으로 남는데, role=friender는 위 가드에서 `/friender`로 리다이렉트되므로 **이 페이지에서 최신 신청이 `승인`이면 = 회수된 상태**(`revoked` 파생). 그대로 두면 "승인됨" 배지와 재신청 폼이 함께 떠 혼란스러우므로 배지를 **"자격 해제됨"(회색)** 으로 바꾸고 안내 배너를 노출(실제 겪은 버그)(`src/components/friender/`, client). 폼은 `TeacherApplicationForm`과 동일 메커니즘(useActionState·controlled·**지연 아바타 업로드**(`handleFinalConfirm`에서 `uploadAvatar`+`cleanupOldAvatars`, `startTransition` dispatch)·2단계 AlertDialog·blob revoke) + **`<PhoneVerifyField>`**(`src/components/mypage/`, 재사용 — name 없는 입력·`type="button"`이라 폼 제출에 개입 안 함) 임베드: `onVerifiedChange`로 `phoneVerified` 끌어올려 **`handleReview` 차단 + 제출 버튼 비활성**, 인증 성공 시 `router.refresh()`로 갱신되는 `initialPhone` prop을 `useEffect`로 동기(확인 다이얼로그 표시용). 필드=**사진(선택, 미리보기 있을 때 "사진 삭제" 버튼으로 초기화 — 업로드 전이라 참조만 비움(`savedAvatarUrl=""`)→제출 시 null 저장)**·**성→이름**(한국어 순)·**nickname(선택, ≤30자 — 서버가 trim+slice로 흡수·검증 반려 없음)**·전화(인증)·국적(`{flag} {한국어명}`)·성별(`g.ko`)·intro(≥10)·zoom_url. 액션 `submitFrienderApplication`(`src/app/friender/apply-actions.ts`) 가드 (1)로그인 (2)role≠프렌더 계열(`isFrienderRole`)/teacher/admin (3)필수값(intro≥10·zoom; **avatar는 선택** — 값이 있을 때만 본인폴더 검증, 저장은 `avatarUrl || null`(빈 문자열이면 승인 RPC coalesce를 통과해 `profiles.avatar_url`을 `''`로 덮어씀)) (4)**전화 인증 서버 재확인**(`profiles.phone`+`phone_verified_at`, **저장 phone은 폼 값이 아니라 이 DB 값**=인증 우회 차단) (5)`rateLimit(friender-apply:IP,5/10분)` (6)'신청' 중복 차단 (7)본인 세션 client insert(`name`=`성+이름` 공백 없이=학생 표시명 규칙) → best-effort `sendFrienderApplicationNotification`(관리자 대상 한국어) + `revalidatePath` → **`redirect("/friender/apply?submitted=1")`**(제출 성공 토스트 "프렌더 신청이 접수되었습니다. 심사 결과는 문자로 안내드립니다." — `docs/teacher.md`의 제출 성공 토스트 항목의 `ToastOnMount` 방식 공유).

**프렌더 승인/거절 (admin) + 프렌더 페이지**: **⚠️ 결과 통보는 이메일이 아닌 SMS**(프렌더는 신청 시 전화 인증이 필수라 `friender_applications.phone`에 검증된 번호 스냅샷이 항상 있음 — 강사 flow의 `sendTeacherApproval/RejectionNotification` 같은 메일러 함수는 만들지 않음). **admin `/admin/friender-requests`**(`page.tsx` server, service_role: `listUsers` 이메일 맵 + `friender_applications` 전량(`STATUS_ORDER` 신청→거절→승인 재정렬) + `profiles.role.in(friender,friender_plus)` → `CurrentFriender[]`(`role` 포함)) → **`<FrienderRequestsManager>`**(`src/components/admin/`, client — `TeacherRequestsManager`에서 강사 전용(센터·가용·수업·정산)을 걷어낸 축소판. `StatCard`·`ApplicationRow`·`CurrentFrienderTable`·`SortHeader`를 한 파일에 콜로케이트): StatCard 3종 + 검색(이름·전화·이메일) + 상태 pill(기본 `신청`) + `ApplicationRow` 아코디언(dl=이메일·전화(`formatPhone`)·국적·성별·자기소개·Zoom URL, `신청` 행만 거절사유 textarea + `승인 (프렌더 전환)`(`bg-cta`)/`거절` + controlled `AlertDialog`) + 하단 「현재 프렌더」 정렬 테이블(이름·**등급**(`TIER_BADGE`/`TIER_LABEL`: 프렌더=`bg-cta/10`·프렌더 Plus=`bg-cta text-white`, 정렬키 `role`)·국적·성별, 행 액션=`정보 보기`→**`<FrienderInfoModal>`**(읽기 전용, dl에 등급 행)·**`Plus 승격`/`Plus 해제`**(등급 따라 라벨 토글)·`프렌더 해제`). StatCard "현재 프렌더" sub=`일반 N · Plus M`. 액션(`src/app/admin/actions.ts`, 모두 `requireAdmin()` 선행 + 공용 `revalidateFrienderConsumers()`=`/admin/friender-requests`·`/friender`(layout)·`/friender/apply`): **`approveFrienderApplication(id)`**(→ **RPC `approve_friender_application`**, 반환 `ok`/`not_found`/`not_pending`/`is_admin`/**`is_teacher`**(강사→프렌더 전환은 강사 데이터가 고아가 돼 차단)/**`is_friender_plus`**(상위 등급을 일반 프렌더로 덮어쓰기 차단) → best-effort 승인 SMS)·**`rejectFrienderApplication(id,note)`**(`.eq("status","신청")` 가드 → `거절`+`admin_note` → 거절 SMS, **사유 120자 절단**)·**`revokeFriender(userId, adminNote?)`**(**role→student**, 계정 유지·재신청 가능 — 프렌더는 종속 데이터(수업·정산)가 없어 회수가 안전, 강사가 회수를 폐지하고 계정 삭제만 남긴 것과 의도적 차이. **프렌더 계열(`isFrienderRole`)이 아니면 거부**(Plus도 바로 해제 가능) + `app_metadata` 동기. **해제 사유는 선택 입력**(해제 `AlertDialog`에 textarea — ⚠️ `AlertDialogDescription`은 `<p>`라 그 **바깥**에 배치, `confirmRevoke`가 대상·사유를 스냅샷 후 state 초기화) → **본인에게 한국어 안내 메일 `sendFrienderRevokedNotification` best-effort 발송**(이메일은 profiles에 없어 `auth.admin.getUserById`로 조회, 사유 있으면 블록 표시, CTA=`/friender/apply`). ⚠️ **승인/거절·등급변경은 SMS·해제는 이메일**로 수단이 다름(정책))·**`setFrienderTier(userId,"friender"|"friender_plus")`**(**등급 승격/강등** — 프렌더=Zoom 무료 연습방, 프렌더 Plus=유료방까지. 가드: `requireAdmin`→**target 화이트리스트**(임의 role 주입 차단)→현재 role이 **반대쪽 등급일 때만** 전환→`profiles.role` update+`app_metadata` 동기→**본인 SMS best-effort**(프렌더는 전화 인증 필수라 `profiles.phone`이 검증된 원본=신청서 스냅샷 대신 사용)→`revalidateFrienderConsumers()`. 확인 `AlertDialog`는 `confirmRevoke`와 동일 스냅샷 패턴(base-nova라 자동 안 닫힘) + 낙관적 `role` 갱신). 헬퍼 `getFrienderApplicantContact`(신청서에서 `{phone,name}` 직접 조회, 이메일 조회 불필요). **프렌더 페이지 `/friender`**(`src/app/friender/(dashboard)/`, **route group이라 `/friender/apply`는 미적용** — 강사와 동일 구조): `layout.tsx`가 role 가드(**`isFrienderRole`**∥`admin`)+라벨바(**등급 pill** `isFrienderPlusRole`이면 "프렌더 Plus", 아니면 "프렌더" — 본인이 자기 등급을 확인하는 유일한 지점)+배너+**`<FrienderTabs>`**(`src/components/friender/`, client, `TeacherTabs` 복제·라벨 한국어: **프로필 `/friender` · 방 관리 `/friender/rooms` · 프렙 강좌 `/friender/prep`(⚠️ `plusOnly` — Plus에게만 노출, layout이 `isPlus` prop 전달. 탭 숨김만으로는 부족해 page에서도 Plus 가드) · 받은 후기 `/friender/reviews`**, 프렙 상세는 `docs/prep.md`), `page.tsx`가 `profiles` 조회 → **`<FrienderProfileForm>`**(`src/components/friender/`, client, 한국어 — `TeacherProfileForm` 패턴의 보기/편집 모드·`VIEW_TEXT`/`LOCKED` 커서 규칙·`snapshotRef` 취소 복원·프로필 사진 **즉시 업로드**(`uploadAvatar`→`updateFrienderAvatar`→성공 후 `cleanupOldAvatars`, 실패 시 롤백; **사진은 선택 입력**이라 미등록이어도 저장 차단 없음=강사 폼과 의도적 차이) + **사진 삭제**(등록돼 있을 때만 "사진 삭제" 버튼 노출→`AlertDialog` 확인(⚠️ base-nova라 핸들러에서 `setRemoveOpen(false)`)→낙관적 비움 후 `removeFrienderAvatar()`(`avatar_url=null`), 성공 시 `cleanupOldAvatars(userId,"")`로 본인 폴더 전체 정리(best-effort)·실패 시 롤백)). **편집 가능=닉네임(선택)·자기소개(`bio`)·Zoom URL 3개**, 이메일·성·이름·전화·국적·성별은 항상 `disabled`(승인 시 확정). 액션 `src/app/friender/actions.ts`(`requireFriender()`(**프렌더 계열**∥admin)→`clean()` 후 **service_role로 `nickname`·`bio`·`zoom_url`만 화이트리스트 update(닉네임은 선택이라 필수 검증 제외 — 비우면 `null`로 지워짐)**(이름·전화·국적·성별·role은 서버가 읽지도 않음=폼 우회 방어)+`isValidZoomUrl`, `updateFrienderAvatar`는 본인 폴더 URL 검증). **⚠️ 프렌더는 `마이페이지` 링크를 계속 노출**(강사와 의도적 차이 — 프렌더는 일반 회원 출신이라 수강신청 내역·내 강의실 데이터 접근 경로가 필요). ⏳ 미구현: 프렌더 주간 가능시간(전용 테이블 없음).

**프렌더 연습방** (`/friender/rooms`, 방 관리 탭, **UI 한국어**): 프렌더가 **Zoom 연습방을 개설·관리**. **현재 전부 무료**(가격 컬럼 없음 — 유료방=프렌더 Plus 전용은 결제 동선과 함께 별도 작업). `page.tsx`(server)가 본인 `friender_rooms`(**`.eq("friender_id", user.id)` 명시** + `session_date desc, start_min desc`) — ⚠️ **RLS에 소유권을 맡기면 안 된다**: 이 테이블엔 SELECT 정책이 둘(`_select_own`·`_select_public`)이고 permissive 정책은 **OR로 합쳐지는데**, `_select_public`이 `is_visible` 폐지(`20260820220759`) 이후 **`using (true)`** 가 되어 필터 없는 조회가 **전체 방**을 돌려줬다(실제 발생한 회귀 — 남의 방이 관리 목록에 노출) + `profiles.zoom_url` 조회 → **`<RoomsManager rooms hasZoomUrl>`**(`src/components/friender/`, client — `NoticesManager`의 **`run()` 헬퍼**(액션→`router.refresh()`→toast) + 인라인 수정 버퍼(`editingId`/`editFields`) + `AlertDialog` 삭제 패턴 이식, 입력은 shadcn `Button`/`Input`/`Textarea`). **개설 폼 = 공용 `<RoomFields>`**(개설·수정 공유): 주제(필수·≤100)·**개설 날짜 `<input type="date" min=오늘KST max=+90일>`**·**시작 시각 = 시(00~23)·분(00/10/20/30/40/50) 2개 `<select>`**(10분 단위 24시간 — ⚠️ 단일 드롭다운이면 144개라 스크롤 부담이 커서 분할, 상태는 `fields.startMin` 하나를 시·분으로 파생/합성하므로 `Fields`·중복검사는 무변경. 컨트롤이 둘이라 `<label>` 대신 `<div>`+각 select `aria-label`)(⚠️ 강사 그리드 `EnrollScheduleField`의 06:00 하한을 **따르지 않음** — 해외 회원 시차 대응으로 새벽 개설 허용, 서버·DB는 원래 0~1439 허용이라 클라 드롭다운만 좁혀져 있던 것)·진행 시간(**20분~2시간·10분 단위, 기본 40분**)·난이도·제한 인원(**1~100**, 기본 4명 — 1은 개설자 제외 정원이라 프렌더 1:회원 1 대화) — ⚠️ **시작 시각은 기본값을 두지 않는다**(시·분 select 모두 `value=""` 플레이스홀더 "시"/"분"에서 출발, 라벨에 빨강 `*`) — 실제 약속 시각이라 ①기본값이 있으면 확인 없이 그대로 제출되기 쉽고 ②자동으로 잡힌 시각이 기존 방과 겹쳐 **폼이 열리자마자 빨간 경고+비활성 버튼**으로 시작한다(실제 겪음. 겹치지 않는 슬롯을 찾아 미는 `firstFreeSlot` 방식도 썼다가, 기본값 자체를 없애는 쪽으로 정리). 상태는 **`startHour`/`startMinute` 각각 nullable**이고 `startMinOf(f)`가 둘 다 선택됐을 때만 분값을 돌려준다(하나만 고른 상태=미선택) — `canCreate`·수정 저장 버튼·`conflictOf`가 모두 이 값으로 게이팅. **개설 날짜만 기본값 유지**(`defaultSessionDate()`=오늘, 23:50 지났으면 내일 — 서버가 `kstDateMinToMs(...) > now`(미래 시각만)를 강제해 오늘을 고르면 무슨 시각을 넣어도 반려되기 때문)·소개(선택·≤1000). ⚠️ **`datetime-local` 금지** — 브라우저 로컬 시간이라 KST 스케줄에 부적합 → `classes`와 동일하게 **`session_date`(date) + `start_min`(분) 분리 저장**(TZ 안전, `kstDateMinToMs`와 짝). **개설은 `AlertDialog` 확인 1단계**(입력값 요약 dl=주제·날짜·시간·난이도·정원·소개 — 드롭다운 기본값이 미리 채워져 있어 확인 없이 제출되기 쉬웠다. ⚠️ `AlertDialogDescription`은 `<p>`라 dl은 그 **바깥** 형제로 배치, 확인 핸들러가 `setConfirmCreate(false)`로 직접 닫음(base-nova)). **Zoom URL 미등록이면** 안내 배너 + 개설 버튼 비활성(입장이 개설자 zoom_url로 연결되므로 무의미) — 서버 액션도 동일 가드. **동일 프렌더 시간 중복 개설 차단**: 공유 헬퍼 **`src/lib/room-time.ts`**(`RoomSlot`·`roomRangeMs`·`roomsOverlap`, 클라/서버 공용) — ⚠️ **반개구간 `[시작,종료)`라 맞닿음은 허용**(08:00~08:40 다음 08:40~09:20 정상), ⚠️ **`session_date`+`start_min` 단순 비교 금지** — 자정 넘김(23:30+120분→익일 01:30)을 놓치므로 `kstDateMinToMs`로 **절대 ms 환산 후** 비교. 서버 `findOverlappingRoom(admin,userId,slot,excludeId?)`가 authoritative(`createRoom`·`updateRoom`, update는 자기 자신 제외; 조회 범위 `session_date >= 어제`=어제 23:30 시작분 포착, **숨김 방도 포함**=숨김은 공개 목록 제외일 뿐 일정 취소가 아님, 에러에 충돌 방 이름·시각 명시), 클라 `RoomsManager`는 `rooms` prop으로 추가 쿼리 없이 `ConflictNotice` 사전 경고+버튼 게이팅(UX 레이어). ⚠️ **원자성 미보장**(read-then-write) — 참여 정원 RPC와 달리 경쟁 주체가 본인 1명이고 제출 버튼이 pending 중 잠겨 EXCLUDE 제약(btree_gist)까지 가지 않음. ⚠️ 기존에 저장된 겹치는 방은 **소급 정리되지 않음**(수정 시도 시 새 규칙에 걸림). **목록 2섹션**(`예정된 방`/`지난 방`): 판정=`kstDateMinToMs(session_date, start_min+duration_min) > now`(**1분 틱** — 입장 시간창 진입 시 `입장` 버튼이 자동 노출되고 예정/지난 분리도 실시간 갱신). 예정 방 행에 **`<EnterRoomButton label="입장">`**(시간창 안일 때만), 지난 방은 `opacity-60`+**수정 버튼 없음**(공개토글도 없음, 삭제만). 행=주제·`formatDateKo`(요일 병기)·`fmtTime(start)~`**`fmtEnd(start+duration)`**(⚠️ 자정 넘김 전용 로컬 헬퍼 — 합이 1440 이상이면 24h 되감고 `(익일)` 접미. `fmtTime`은 1440 초과를 `25:30`처럼 흘려보내는데 소비처가 많아 **수정하지 않고** 여기서만 감쌌다. 저장 값·`kstDateMinToMs` 경과 판정은 원본 분값 그대로가 정상)·난이도 배지·**예약 인원 `N/정원`**(⚠️ 참가자 RLS가 `_select_own`뿐이라 **개설자도 자기 방 참가자를 세션 client로 못 읽는다** → page가 `createAdminClient()`로 카운트만 집계해 `FrienderRoom.participants`로 전달, `/friending`·`/mypage/rooms`와 동일 방식. **명단(신원)을 보는 화면은 admin `/admin/rooms` 하나뿐**=유일한 예외, `docs/admin.md`). **예약이 1명 이상인 행은 좌측 `border-l-4 border-cta` + `bg-cta/[0.04]` 액센트 + 인원 칩 `text-cta` 강조**로 구분(0명은 평소대로 흐림). 행에 **「방 소개글 보기」 버튼**(소개 없으면 비활성 → `RoomInfoModal`, 프렌딩·마이페이지와 동일 규칙=문단을 인라인으로 펼치면 행 높이가 들쭉날쭉해진다). 행 액션 아이콘 2종(수정·삭제)에는 **툴팁**(`ui/tooltip`, `docs/ui.md`의 shadcn 항목) — 아이콘만으로는 기능 식별이 어려워 `aria-label`과 같은 문구를 시각적으로도 노출. **난이도 단일 소스 `src/data/room-levels.ts`**(`genders.ts` 형태: `ROOM_LEVELS`{value·ko} 4종 any/beginner/lower_int/upper_int·`ROOM_LEVEL_VALUES: string[]`(⚠️ 어노테이션 없으면 `.includes(string)` 빌드 실패)·`DEFAULT_ROOM_LEVEL`·`roomLevelLabelKo`) — **PG enum 대신 text + 앱 검증**(enum은 값 추가 시 `ALTER TYPE ADD VALUE` 단독 마이그레이션 강제). 액션(`src/app/friender/actions.ts`에 콜로케이트, 모두 `requireFriender()` 후 **service_role + `.eq("friender_id", userId)` 이중 스코프**=소유권을 쿼리에서 강제, 끝에 `revalidatePath("/friender","layout")`): **`createRoom`**(공용 `validateRoomInput` → `profiles`에서 zoom_url 확인+이름 스냅샷(`성+이름` 공백 없이·nickname) 채워 insert)·**`updateRoom`**(+**지난 방 가드**: 대상 시작 시각이 지났으면 거부)·**`deleteRoom`**. ⚠️ **예약자가 있는 방 보호**: 참가 행이 1건이라도 있으면 **삭제 불가**(cascade로 예약자의 마이페이지 기록까지 사라짐)이고 **일정 3필드(`session_date`·`start_min`·`duration_min`) 변경 불가**(방 알림 인프라가 없어 옮기면 예약자가 통보 없이 끌려감) + **정원을 현재 예약 인원보다 작게 축소 불가**. 주제·소개·난이도는 계속 수정 가능. 판정은 **노쇼 포함**(참가 행 존재 여부 — 노쇼는 시작 후에야 갈리는데 수정은 시작 전만 가능). ⚠️ **관리자는 이 보호를 우회한다** — `deleteRoomAsAdmin`(`/admin/rooms`)은 예약자가 있어도 삭제한다(부적절한 방·자격 해제된 프렌더가 남긴 방을 아무도 못 내리던 문제). 대신 여기서 미룬 통보를 그쪽이 대신 치른다: 프렌더에게 항상 SMS, 예약자에게 best-effort SMS(끝나지 않은 방만). 카운트는 `countParticipants`(service_role — 참가자 RLS가 `_select_own`뿐이라 개설자도 세션 client로 못 읽음). 서버가 authoritative고 `RoomsManager`는 사전 안내 레이어: 삭제 아이콘 비활성(⚠️ 비활성 버튼은 hover가 죽어 툴팁이 안 뜨므로 `disabled:pointer-events-auto` 필요) + 수정 폼 `lockSchedule` prop으로 날짜·시·분·진행시간만 잠금 + 안내 문구 + 정원 `min`을 예약 인원으로 좁힘. `validateRoomInput`=주제/난이도(화이트리스트)/인원 1~100/날짜 형식+KST 오늘~+90일/`start_min` 10분 배수/진행시간 화이트리스트 + **`kstDateMinToMs(...)>now`**(클라 `min` 우회 재검증). 반환 타입은 **`RoomActionResult{ok: boolean}`**(기존 `FrienderActionState`는 `ok?` 옵셔널이라 `run()`과 불일치). ⏳ 미구현: **유료방**(프렌더 Plus 전용, 결제 동선 필요).

**프렌딩(공개 방 목록)** (`/friending`, server, **UI 한국어**, **비로그인도 목록 열람 가능**): 프렌더가 연 방을 일반 회원이 둘러보고 참여하는 소비자 화면. 시각 레퍼런스는 `v9_project/pages/friending/friending.html`(정적 목업)이나 **데이터 모델이 달라 재해석**했다 — 목업은 "지금 접속 중"(online/offline) 모델, 우리는 **예약제**(`session_date`+`start_min`+`duration_min`)라 **시간 기반 상태**(진행 중/예정/마감)로 바꿨다. **목록 섹션은 「진행 중」/「예정」 2분할** — 기준은 **`canEnter`(입장 시간창=시작 15분 전~종료)**라 **섹션과 입장 버튼 노출이 항상 일치**한다(⚠️ 실제 시작 기준 `isLive`로 나누면 "진행 중인데 못 들어가는" 15분 구간이 생겨 폐기). 상단 요약 카운트도 같은 기준. 날짜 그룹(오늘/내일/이후)은 폐기 — 날짜는 카드에 `8월 22일 · 08:00~08:20`으로 이미 있고, 섹션 내부는 서버 정렬(`session_date`,`start_min` asc) 그대로. 라이브 점(`LiveDot`)은 「진행 중」 섹션 제목에 붙고, 카드 인라인 `진행 중` 라벨은 섹션 제목과 중복이라 제거했다. **시각 구분**: 「진행 중」은 **연초록 박스**(`rounded-2xl border-[#22c55e]/25 bg-[#eafff1]/60 p-3 md:p-4`)로 카드 그리드까지 감싸고 제목도 `text-[#22c55e]`, 「예정」은 박스 없이 `text-ink` — 제목이 `text-xs` 회색이던 시절엔 두 영역이 카드 사이에 묻혀 안 갈렸다(실제 겪음). 제목은 `text-sm font-extrabold` + 개수, 상단 요약은 총 방 수(`열린 방 N개`)만 담당(개수 중복 제거). 섹션 사이에는 `border-t` 구분선(⚠️ **첫 섹션 위에는 긋지 않는다** — 상단 헤더와 붙어 이중선이 된다 → `i > 0` 조건). 초록 hex는 `LiveDot`·FREE 배지와 같은 예외값 재사용. 목업의 전역 "오늘의 주제"·`topic-schedule` 서브페이지는 뒷받침 데이터가 없어(방마다 `title`이 곧 주제) **미포팅**. `page.tsx`가 **세션 client**로 조회(RLS `friender_rooms_select_public`이 anon 허용, `.eq("is_visible",true)`는 랜딩 youtube와 같은 belt-and-braces 중복)+`.gte("session_date",todayKst())` → **오늘이지만 이미 끝난 방은 SQL로 못 걸러** `kstDateMinToMs(date,start+duration)>now`로 JS 필터(`RoomsManager`와 동일) → **참여 인원은 `createAdminClient()`로 집계**(참가자 RLS가 select_own뿐이라 카운트만 노출하고 신원은 비공개) + 본인 참여 여부는 세션 client(RLS select_own) → **`<FriendingRooms rooms isLoggedIn>`**(`src/components/friending/`, client). **카드**: **아바타=등록된 프로필 사진 우선**(`next/image`, `avatars` 버킷은 `avatars_public_read`라 URL 노출 안전 + `next.config.ts` remotePatterns 등록됨), 없으면 **이니셜 원 폴백**(방 id 해시로 그라디언트 4종 고정 배정, v9 목업 방식). ⚠️ 사진은 **스냅샷 컬럼을 두지 않고 `createAdminClient()`로 매 요청 조회**(`profiles_select_own` RLS 우회, `friender_id` distinct `.in()` 1회) — 스냅샷으로 두면 ①사진 교체가 반영 안 되고 ②`cleanupOldAvatars`가 옛 파일을 지워 **URL이 깨진다**(이름은 반대로 스냅샷 유지=표시 문자열이라 안전). ⚠️ `[...new Set()]`은 tsconfig `target:es5`라 빌드 실패 → **`Array.from(new Set(...))`**. **개설자 정보는 `hosts: Record<frienderId, HostProfile>` 맵을 별도 prop으로** 전달(방마다 bio가 중복 직렬화되는 것 방지, 방에는 `frienderId`+`fallbackName`만). **이름/닉네임 클릭 → `<HostProfileModal>`**(`src/components/friending/`, admin `FrienderInfoModal` 스켈레톤 이식=오버레이 `z-[110]`/패널 `z-[120]`·Esc·scroll lock·닫기 포커스): **사진·이름·국적·성별·자기소개(bio)만** 표시. ⚠️ **이메일·전화·`zoom_url`은 서버 select 단계에서 제외**(클라에서 숨기는 게 아니라 HTML 페이로드에 아예 안 실림) — 공개 페이지라 비로그인도 보고, 특히 **zoom_url은 방 입장의 사실상 열쇠**라 노출되면 미참여자가 입장 가능해진다(`friender_rooms`가 zoom_url을 저장하지 않는 이유와 동일)·`{닉네임∥이름}님`+물방울 `F` 배지·주제·`N/M명`·`M월 D일 · HH:MM~HH:MM`(`fmtEnd`로 자정 넘김 처리)·난이도 pill·**「방 소개글 보기」 버튼**(⚠️ 소개 문단을 조건부 렌더하면 카드마다 CTA 세로 위치가 어긋나 스캔이 어려워짐 → **버튼은 항상 렌더**하고 `description` 없으면 `disabled`+`cursor-default`(전역 규칙상 `cursor-pointer` 금지), 클릭 시 **`<RoomInfoModal>`**(`src/components/friending/`, `HostProfileModal` 스켈레톤 이식=오버레이 `z-[110]`/패널 `z-[120]`·Esc(⚠️ `[role="alertdialog"]` 가드로 참여취소 `AlertDialog`에 양보)·scroll lock). **모달 본문은 소개 전문만**(`whitespace-pre-wrap`, 잘림 없음) — 주제·개설자·난이도·일시·인원은 카드에 이미 보이므로 중복 제거. props도 `description: string | null` 하나뿐이라 `FriendingRooms`의 날짜 헬퍼를 import 할 일이 없다(값 단위 순환 의존 회피)). **CTA 상태 머신**(1분 틱): 비로그인=`로그인하고 예약`(`/login?next=/friending`) / 개설자 본인=`내 방`(비활성) / 예약+입장창(`canEnterClass` 15분 전~종료)=**`입장하기`**(+ 옆에 텍스트 버튼 `예약 취소`) / 예약=`예약 취소` / 정원참=`마감`(비활성) / 그 외=**`예약하기`**. ⚠️ **라벨은 「예약하기」로 고정** — 한때 진행 중인 방만 "참여하기"로 갈랐으나(`joinWord`), 같은 자리의 같은 동작(`joinRoom`)에 라벨이 오가면 일관성이 떨어져 폐기했다. 진행 중 여부로 갈리는 것은 **확인 다이얼로그 본문 안내뿐**("지금 진행 중인 방이라 바로 입장할 수 있어요" ↔ "시작 15분 전부터…"). 취소는 **입장창 안에서도 계속 노출**한다 — ⚠️ 한때 `joined && !enterable`일 때만 보였으나, 안 오면 어차피 노쇼로 자리가 반환되므로(유예 10분) 감춰 봐야 자리가 그만큼 늦게 열릴 뿐이고, 사정이 생긴 회원이 명시적으로 비켜 줄 길이 없었다. 대신 확인 다이얼로그에 **임박 경고**("곧 시작하는 방입니다…")를 띄운다. 위계는 입장=주 CTA(pill), 취소=텍스트 버튼. **예약·취소 둘 다 `AlertDialog` 확인 1단계**(예약 다이얼로그는 방 제목·일시 + 진행 중이면 "바로 입장"·아니면 "시작 15분 전부터 입장" 안내, ⚠️ base-nova라 확인 핸들러가 `setJoinTarget(null)`로 직접 닫음). 초기 12개+`더보기`(클라 점진 노출). 액션 `src/app/friending/actions.ts`(**역할 가드 없음**=로그인만 확인): **`joinRoom`**(표시명 스냅샷 조립 후 **본인 세션 client로 RPC 호출** — ⚠️ service_role로 부르면 RPC 안 `auth.uid()`가 null, `already`는 멱등 성공 처리)·**`leaveRoom`**(service_role delete, room+user 이중 스코프)·**`enterRoom`**(`enterClass` 미러: 참가자∥개설자 검증→시간창→개설자 `profiles.zoom_url` 최신값+`isValidZoomUrl`→`{url}`; ⚠️ **`lessonEndMin` 미사용**=수업 전용 30→25분 축소라 방엔 부적합). 입장은 **공유 `<EnterRoomButton>`**(`src/components/friending/`, `EnterClassButton` 역할 — 프렌딩 카드와 **방 관리 목록**(`RoomsManager`, friender 폴더에서 cross-import)이 공용): ⚠️ **빈 탭 먼저 열고 URL 주입**(응답 후 `window.open`하면 팝업 차단됨) + `withGuide`로 안내 `AlertDialog` 분기 — **참가자만 안내를 거치고 호스트는 바로 입장**(에티켓 문구가 참가자 대상이라). **입장 시간창은 호스트·참가자 동일**(시작 15분 전~종료, `lessonEndMin` 미사용). 개설자 CTA는 시간창 안이면 `입장하기`, 밖이면 `내 방`(비활성). 색은 앱 토큰 우선이나 **라이브 초록(`#4ade80`/`#22c55e`/`#eafff1`)·프렌더 핑크(`#DC52B8`)는 대응 토큰이 없어 arbitrary hex 예외**(환불 배지 선례와 동일). **Navbar 진입점**: 슬라이드 메뉴 최상단 flat 링크 + **데스크톱 인라인 nav 첫 항목**(⚠️ 인라인 nav는 원래 인증/역할 링크만 두는 영역인데 프렌딩은 일반 회원 핵심 동선이라 **의도적 예외**). **예약 내역은 마이페이지 「프렌딩 예약」 탭(`/mypage/rooms`)에서 관리** — `page.tsx`(server)가 `friender_room_participants`를 세션 client로 조회하며 `friender_rooms`를 임베드(참가자 RLS `_select_own` + 방 RLS `_select_public` 조합, ⚠️ **many-to-one 임베드는 런타임에 객체지만 untyped client의 타입 추론은 배열**이라 `Array.isArray` 정규화 필요, 임베드 컬럼 정렬은 PostgREST로 안 돼 JS에서), 인원 카운트·개설자 프로필은 `/friending`과 같은 이유로 service_role (⚠️ zoom_url·email·phone은 select 금지) → **`<MyRoomReservations>`**(`src/components/mypage/`, client — `RoomsManager`의 예정/지난 2섹션 목록 + 1분 틱, `EnterRoomButton`·`RoomInfoModal`·`HostProfileModal` cross-import). 행 액션=**입장**(시간창 안)/**예약 취소**(시작 전 항상, `AlertDialog`+임박 경고), ⚠️ **취소 노출 규칙은 `/friending` CTA 상태 머신과 한 쌍** — 한쪽만 바꾸면 두 화면이 어긋난다. ⚠️ **서버 `leaveRoom`에는 시간 가드가 없다**(로그인만 확인) — 취소 제한은 처음부터 UI 레이어였고, 지금은 UI도 막지 않으므로 정책과 구현이 일치한다. `joinRoom`/`leaveRoom`이 `/friending`과 `/mypage/rooms`를 함께 revalidate. **노쇼 처리(크론 없이 조회 시점 계산)**: `enterRoom`이 참가자 **첫 입장 시 `entered_at` 기록**(sticky·best-effort, 개설자는 참가 행이 없어 대상 아님) → **`시작 + NO_SHOW_GRACE_MIN(10분)`까지 미입장이면 자리를 정원 카운트에서 제외**(공유 판정 `seatHeld`/`isNoShow` in `src/lib/room-time.ts`; 표시 카운트 3곳(`/friending`·`/mypage/rooms`·`/friender/rooms`)과 **`join_friender_room` RPC의 정원 검사**가 같은 규칙). ⚠️ **참가 행은 지우지 않는다** — 노쇼 기록을 남기고 **늦은 입장도 허용**하는 정책이라, 그 사이 다른 사람이 예약했다면 실제 인원이 정원을 1명 넘을 수 있다(의도된 트레이드오프). 노출: 프렌더 방 관리 행에 **"미입장 K"** 회색 칩(신원은 RLS로 못 봐 수만), 마이페이지 예약 행에 **"자리가 반환되었습니다"** 안내(입장 버튼은 유지). ⚠️ 유예 10분은 TS 상수와 RPC 양쪽에 있으니 함께 고칠 것. **평점·후기**: 회원이 **마이페이지 지난 예약**에서 별점(1~5)+후기 작성 — 자격은 **`entered_at`이 찍힌(실제 입장한)** 예약 + **방 종료 후**, 한 방에 하나, 수정·삭제 가능(`saveRoomReview`/`deleteRoomReview` in `src/app/friending/actions.ts`, 서버가 종료·입장·1~5·1000자를 재검증). UI=`<RoomReviewModal>`(`src/components/mypage/`) + 공용 **`<StarRating>`**(`src/components/`, `onChange` 없으면 읽기 전용·별 색은 예외 hex `#F5A623`). ⚠️ **열람은 프렌더 본인 + 관리자만**(프렌딩 카드·호스트 프로필에 평균 별점 노출 안 함 — 초기 표본이 적어 별점 하나가 프렌더 이미지를 좌우하는 것을 피함): 프렌더=**「받은 후기」 탭 `/friender/reviews`**(`FrienderReviews`, **`.eq("friender_id", user.id)` 명시**, 평균·건수+목록 — ⚠️ 여기도 정책이 둘(작성자 `_select_own` OR 프렌더 `_select_own_friender`)이라 필터가 없으면 **프렌더가 남의 방에 쓴 자기 후기**까지 '받은 후기'로 섞여 평균 별점이 오염된다), admin=`/admin/friender-requests` 「현재 프렌더」 **평점 컬럼**(정렬 가능)+`FrienderInfoModal` **최근 5건**(service_role 집계). ⚠️ **`friender_room_reviews.room_id`는 `ON DELETE SET NULL`** — 방을 지워도(관리자 강제 삭제 포함) `room_title`·`session_date` 스냅샷 덕에 후기·평균이 그대로 남는다(**평점 세탁 불가**, 의도된 설계). ⏳ 미구현: 참가자 명단 **회원 대상** 공개·방 상세 페이지·일반 알림(**관리자 열람은 `/admin/rooms`에서 구현됨**, 알림도 **관리자 강제 삭제 통보 SMS 한정**으로 구현됨). `/login?next=/friending`으로 보내면 로그인 후 프렌딩으로 되돌아온다(`docs/auth.md`의 인증 항목의 `safeNextPath`).
