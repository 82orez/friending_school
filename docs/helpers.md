# 헬퍼 (src/lib · src/hooks)

> `CLAUDE.md`에서 분리된 상세 문서. 본문의 "위 …항목" 상호참조는 다른 `docs/*.md`일 수 있음(색인=CLAUDE.md).

## 헬퍼

- `src/lib/utils.ts` — `cn()`.
- `src/components/LangProvider.tsx`(client) — UI 로케일 컨텍스트 `LangProvider`/`useLang()`(`"ko"|"en"`, **기본 `"ko"`**). 앱 전반은 오디언스별 하드코딩이나, admin↔센터 매니저 공유 컴포넌트를 이중언어로 만들기 위한 최소 컨텍스트. `/center` 레이아웃만 `lang="en"`으로 감싸 provider 없는 admin은 자동 한글(무변경). 공유 컴포넌트는 `useLang()==="en"` 분기(`docs/center.md`).
- `src/lib/auth.ts` — `getUserRole`/`isAdmin`/`isTeacher`/**`isFriender`**(supabase,userId)(`profiles.role`, app_metadata 대신 profiles 우선) + **순수 role 판별자 `FRIENDER_ROLES`·`isFrienderRole(role)`(프렌더 계열=`friender`∥`friender_plus`)·`isFrienderPlusRole(role)`(Plus 전용 기능 게이팅 지점 — 유료방 개설 추가 시 여기에 건다)**. ⚠️ **role은 단일값이라 `friender_plus`는 `friender`가 아님** → 프렌더 가드는 전부 `isFrienderRole` 사용(리터럴 비교 금지).
- `src/lib/mailer.ts` — `sendTeacherApplicationNotification`(강사지원, 관리자 대상) + **`sendFrienderApplicationNotification(to[],data)`(프렌더 지원, 관리자 대상·한국어, `FrienderApplicationEmailData`{name·phone·intro·zoomUrl·email·createdAt})** + **`sendFrienderRevokedNotification(to[],{name,reason,applyUrl})`(프렌더 자격 해제, 본인 대상·**한국어** — 사용자 대상 메일 중 유일한 한국어(나머지는 강사 대상이라 영문), `reason` 빈 문자열이면 사유 블록 미렌더)** + **`sendEnrollmentNotificationToTeacher(to[],data)`(수강신청, 강사 대상·영문)** + **`sendEnrollmentPaymentConfirmedToTeacher(to[],data)`(결제 확정→수업 확정 알림, 강사 대상·영문, `EnrollmentEmailData` 재사용)** + **`sendEnrollmentCancellationToTeacher(to[],data)`(학생 취소 알림, 강사 대상·영문, `EnrollmentEmailData` 재사용)** + **`sendEnrollmentRefundToTeacher(to[],data)`(환불→수강 취소 알림, 강사 대상·영문, `EnrollmentRefundEmailData`; `refundEnrollmentPayment`가 호출)** + **`sendClassReassignToNewTeacher`/`sendClassReassignToOldTeacher(to[],data)`(개별 수업 강사 대체 알림, 강사 대상·영문, `ClassReassignEmailData`: 새 강사=배정·기존 강사=이관)** + **`sendClassReassignToAdmin(to[],data)`(개별 회차 강사 대체를 **센터 매니저**가 했을 때 관리자 알림, 영문, `ClassReassignAdminEmailData`: 기존/새 강사·센터·실행 매니저 포함; `getAdminEmails()` 수신)** + **`sendCourseReassignToNewTeacher`/`sendCourseReassignToOldTeacher(to[],data)`(강사 중도 하차 시 과정 전체 이관 알림, 강사 대상·영문, `CourseReassignEmailData`: schedule·remainingCount·nextDate·old/newTeacherName)** + **`sendEnrollmentNotificationToAdmin`(신규 신청 접수)/`sendEnrollmentApprovedToAdmin`(강사 승인→결제대기)/`sendEnrollmentRejectedToAdmin`(강사 거절, 사유 포함)/**`sendEnrollmentPaidToAdmin`(카드 결제 완료→수업 확정, `finalizeEnrollmentPayment`가 `actor.role!=="admin"`=카드(student/system)만 발송·무통장 admin 확인은 제외)**/**`sendEnrollmentCancelledToAdmin`(학생 자가 취소, `fromStatus?`=취소 직전 상태 영문 라벨 `statusLabelEn`)**(관리자 대상·영문, `EnrollmentAdminEmailData`(+`reason?`/`fromStatus?`, `endDate?`·`totalSessions?`는 옵셔널) 재사용, `getAdminEmails()` 수신)** + **`sendClassPostponedToAdmin(to[],data)`(학생 개별 수업 연기, 관리자 대상·영문, `ClassPostponeAdminEmailData`: 회차·시간·보강일(없으면 "Not created — manual action required")·남은 연기 `remaining/max`·`adminUrl`=`/admin/classes/<enrollmentId>`)** + **`sendEnrollmentEventToCenterManager(to[],data)`(센터 매니저 대상·영문 — 6이벤트(`created`/`approved`/`declined`/`paid`/`cancelled`/`refunded`)를 `CENTER_EVENT_META`(제목·헤딩·리드)로 분기하는 **단일 함수**, `CenterEnrollmentEmailData`; 제목 `[센터명] tag · 과정 · 학생`, CTA=`paid`면 `/center/schedule` 그 외 `/center`(**admin 링크 금지**=매니저 접근 불가); 수신자·발송은 `center-notify.ts`가 결정)** + **`sendClassEventToCenterManager(to[],data)`(센터 매니저 대상·영문 수업 변경 알림 — 5이벤트 `CENTER_CLASS_META` 분기 단일 함수, `CenterClassEmailData`; CTA 항상 `/center/schedule`)** + **`sendRemainingRescheduleToTeacher(to[],data)`(남은 일정 일괄 변경 시 담당 강사 통보, 영문 — 이전→새 주간 일정·적용일·다음 수업일·대상 회차 수)** + **`sendPrepEnrollmentNotification(to[],data)`(프렙 **수강신청** 접수, 관리자 대상·한국어, `PrepEnrollmentEmailData`{강좌·프렌더·신청자·연락처·기간·시각·수강료·신청 인원} — 무통장 입금 확인이 관리자 몫이라 "누가 어느 강좌에 얼마를"가 한눈에 보이게)** + **`sendPrepCourseReviewRequestNotification(to[],data)`(프렙 강좌 개설 승인 요청, 관리자 대상·한국어, `PrepReviewEmailData`{frienderName·nickname·email·title·level·capacity·priceKrw·period·time·firstTopic·requestedAt·**isResubmit**} — 재요청이면 제목이 `[프렙 재승인요청]`, `getAdminEmails()` 수신. 프렌더에게 가는 승인/거절 결과는 메일이 아니라 **SMS**)** + **`sendTeacherApprovalNotification`/`sendTeacherRejectionNotification`(강사 심사 결과, 지원자 대상)** — 모두 Resend, best-effort no-op, `import "server-only"`.
- `src/lib/origin.ts` — `getOrigin(headers)`: `NEXT_PUBLIC_SITE_URL`→`origin`→`x-forwarded-host`+proto→`host`.
- `src/lib/email.ts` — `isValidEmail()`(서버측 검증).
- `src/lib/url.ts` — `isValidZoomUrl()`(http/https 검증, 강사 프로필·강사 지원 폼 공유) + **`safeNextPath(raw)`·`DEFAULT_NEXT`**(로그인 후 복귀 경로 `?next=` 검증 — **open redirect 방지**: 내부 절대경로만 허용하고 `//host`·`/\host`·외부 URL·`javascript:`는 `/`로 떨어뜨림). 로그인 page·`login()`·`signInWithKakao()`·`/auth/confirm` 4곳 공용 단일 소스 — ⚠️ 새 진입점을 만들면 반드시 이걸 쓸 것.
- `src/lib/phone.ts` — `normalizePhone`(숫자만)/`isValidKoreanMobile`(`/^01[016789]\d{7,8}$/`)/`formatPhone`(하이픈 표시). 마이페이지 전화번호 인증 클라·서버 공유.
- `src/lib/sms.ts` — `sendSms(to,text):Promise<boolean>`(Solapi `SolapiMessageService.send`, `import "server-only"`). env 미설정/예외 시 false. ⚠️ **`send()`는 "접수 성공"이지 "발송 성공"이 아님** — 발신번호 미등록·잔액부족 등은 예외가 아니라 응답 `groupInfo.count.registeredFailed`/`failedMessageList`에 담기므로, `registeredSuccess<1 ∥ registeredFailed>0 ∥ failedList`면 false 반환 + 사유 콘솔 로깅(OTP는 성공 여부 필요). 전화번호 인증 OTP + **수강신청 승인/거절 결과 통보**에 사용.
- `src/lib/availability.ts` — 주간 슬롯 공유 모델(`Slot{day,min}`·`slotKey`·`SLOT_MIN`·`GRID_START/END_HOUR`·`DISPLAY_DAYS`·`DAY_LABELS`/`DAY_LABELS_KO`·`ROW_MINS`·`teacherHasAllSlots`(강사 슬롯⊇요청)·`subtractSlots`·`slotsOverlap`·**`overlappingIds(items{id,group,slots})`**(같은 group=강사 내 슬롯 겹치는 id 집합 — 수강신청 목록 시간충돌 시각표시용, 활성 행만 걸러 호출)·`isValidSlot`·`summarizeSlots(slots,ko,sep=" · ")`(요일+시각 "월 14:00 · 수 14:00", **`sep`으로 구분자 교체** — 학생 마이페이지·강사 모달·강사/관리자 이메일은 `" / "` 전달)·**`summarizeWeekdays(slots,ko)`**(요일-only "월/수/금" — 시각 제외, admin 주간 뷰 `SlotModal` 과정 행용)·**`lessonEndMin(endMin)`**(슬롯 종료−5분=레슨/입장 종료, 표시·입장 시간창 단일 소스)·**`dowOf(YYYY-MM-DD)`**(UTC 파싱 요일 인덱스 0=일~6=토, TZ 비종속)·**`formatDateKo(YYYY-MM-DD)`**(→`"2026-07-04 (토)"`, 날짜 원문+요일 접미; **로케일 버전 `formatDate(d, ko=true)`**=en `"2026-07-04 (Sat)"`, `DAY_LABELS_EN` 추가 — 센터 영문 뷰용)·**`scheduleDateRange(startDate,slots,totalSessions)`**(수업 일정 "시작일 ~ 종료일", `lessonEndDate` 기반 — 학생/강사 패널·주간 SlotModal 공용)·**`BookedSlot{day,min,tier,label?}`·`TIER_RANK`·`deriveBookedSlots(rows)`**(enrollment 행→가용 그리드 오버레이 슬롯. **tier 3종** `confirmed`(승인/결제완료)>`pending`(결제대기)>`requested`(신청) — 랭크 높은 쪽 우선, 셋 다 **강사 그리드 편집 잠금** 대상. 소비처=`AvailabilityGrid`·`timetable-print`·`loadTeacherBookedSlots`·admin/center 강사 디렉터리)). 강사 그리드·수강신청 그리드·매칭·요약·충돌표시 단일 소스(클라/서버 공용, server-only 아님).
- `src/lib/booking.ts`(server-only) — **`ACTIVE_BOOKING_STATUSES`**(=`신청`·`승인`·`결제대기`·`결제완료`, 슬롯 점유로 취급하는 상태) + **`loadTeacherBookedSlots(client,teacherId)`**(강사 1명의 예약 슬롯 `BookedSlot[]` — enrollments 조회→종료된 `결제완료` 제외→`deriveBookedSlots`. **강사 프로필 page(세션 client)와 `updateTeacherAvailability` 서버 가드(service_role)가 공용** = 잠금 판정 단일 소스. ⚠️ admin `/admin/teacher-requests`·`teacher-directory.ts`의 **다중 강사** 경로는 select 컬럼이 달라 별도 유지) + **`loadBookedSlotsByTeacher(client,teacherIds[],excludeStudentId?)`**(강사별 `Map<id,Slot[]>` — 수강신청 매칭/가용 차감용 순수 슬롯. `excludeStudentId`면 그 학생 본인 신청은 차감 제외=본인에겐 강사가 계속 보임. `loadEnrollTeachers`·`submitEnrollment` 공용) + **`loadStudentBusySlots(client,studentId)`**(학생이 이미 잡은 시간, 강사 무관 통합 — 위저드 `myBusySlots` 안내 + `submitEnrollment` 본인 충돌 차단 공용. ⚠️ **service_role 전용**: 종료 판정이 `classes`를 `teacher_id`로 읽어 학생 세션 client면 "남은 예정 수업 없음"으로 오판) + `loadEndedEnrollmentIds(admin,teacherIds)`(종료된 `결제완료` enrollment id 집합 — 남은 `예정` 수업(`classes.session_date≥오늘KST`) 없으면 종료, 클래스 없는 레거시는 `lessonEndDate` 폴백) + `todayKst()`. 슬롯 점유로 취급하는 모든 지점(가용 차감·승인 충돌·그리드 표시)이 공용해 **종료 과정의 슬롯 자동 해제**. service_role/세션 client 양쪽 허용.
- `src/lib/classtime.ts` — `ENTRY_LEAD_MS`(15분)·`CANCEL_CUTOFF_MS`(1시간)·`MAX_CANCELLATIONS`(6)·`kstDateMinToMs(sessionDate,min)`(KST=UTC+9 고정)·`canEnterClass`·`canCancelClass`. 입장 시간창·연기 컷오프 단일 소스(클라/서버 공용).
- `src/lib/room-time.ts` — 연습방 시간 구간 판정(클라/서버 공용): `RoomSlot`·`roomRangeMs`·**`roomsOverlap`**. ⚠️ **반개구간 `[시작,종료)`라 맞닿음은 겹침이 아님**(08:00~08:40 다음 08:40~09:20 허용), ⚠️ **`session_date`+`start_min` 단순 비교 금지** — 자정 넘김(23:30+120분→익일 01:30)을 놓치므로 `kstDateMinToMs`로 절대 ms 환산 후 비교. 동일 프렌더 중복 개설 차단(`friender/actions.ts` 서버 가드 + `RoomsManager` 사전 경고) 공용.
- `src/lib/classroom.ts` — `CLASS_SELECT`·`mapClassRows`·`loadClasses(supabase,userId,isTeacher)`. 학생/강사 내 강의실 공용 로더(`docs/classroom.md`의 화상수업 항목).
- `src/lib/rates.ts` — 정산 단가 적용일 이력 역산: `RateRow`·`resolveScopeRate`·**`effectiveRate`**(강사 개별>센터, 수업 날짜 기준)·`currentRate`(오늘 기준)·`todayKstStr`. `rate_schedules`가 진실 소스.
- `src/lib/center.ts` — `resolveCenterId(supabase,raw)`(`"none"`→null·uuid 존재검증, 강사 지원/승인 폼 공용).
- `src/lib/avatar.ts` — `uploadAvatar(file,uid)`→`{publicUrl,path}` + `cleanupOldAvatars(uid,keepPath)`(본인 폴더 누적 파일 정리, best-effort). 강사 프로필·강사 신청폼 공유(브라우저 client 전용).
- `src/lib/timetable-print.ts`(브라우저 client 전용) — `buildTimetablePrintHtml({teacherName,slots,bookedSlots})`(강사 주간 가용을 A4 세로 자립형 HTML로 렌더, `@page A4 portrait`+`print-color-adjust:exact`, `@/lib/availability` 상수 재사용) + `openTimetablePrint(...)`(전용 창 열어 `window.print()`=PDF 저장). admin `TeacherInfoModal` "PDF로 내보내기" 버튼 전용, 새 의존성 없음.
- `src/lib/makeup.ts` — `createMakeupClass(admin,cls)`→보강 날짜∥undefined(취소 1회당 자동 보강 생성, 마지막 수업일 뒤 첫 스케줄 요일, `is_makeup=true`, best-effort) + `weekdayOf`/`addDaysStr`(TZ 비종속 날짜 헬퍼). 학생 `cancelClass`·admin `adminCancelClass` 공용, `import "server-only"`.
- `src/lib/reassign.ts`(server-only) — **개별 회차 강사 대체 공유 코어 `reassignClassCore(admin,{classId,newTeacherId,actor,constrainCenterIds?})`**: `예정`·시작 전·새 강사 가용·같은 날 충돌 검증→update(teacher_id/name·`teacher_reassigned_at`·`original_teacher_id` coalesce)→학생 SMS·강사 메일·**센터 매니저 알림**(`class_reassigned`, **admin 실행 시만**·`teacherIds=[old,new]`)·`class_reassigned` 이벤트·revalidate. **`actor.role==='center_manager'`면 관리자 알림 메일도**(`getAdminEmails()`→`sendClassReassignToAdmin`, 강사 2건 직후라 Resend 초당 2건 제한 회피 위해 ~1.1s 지연 후 발송; admin 본인 대체는 미발송). `constrainCenterIds`(센터 매니저)면 현재·새 강사 모두 그 센터 소속이어야 통과. admin `adminReassignClass`(제약 없음)·센터 `centerReassignClass` 공용.
- `src/lib/center-manager.ts`(server-only) — `requireCenterManager()`(세션→담당 센터 없으면 null, 가드)·`loadManagedCenters(admin,uid)`(centers.manager_id=uid)·`isCenterManager`. 센터 매니저 페이지·액션 진입 가드.
- `src/lib/center-notify.ts`(server-only) — **센터 매니저 수강신청 알림 오케스트레이션**: **`loadCenterManagerRecipients(admin,teacherIds[])`**(강사(들)→**현재 소속 센터**(`profiles.center_id`)→`centers.manager_id`→`getUserById` 이메일 → `{managerId,email,centerName}[]`, **managerId dedupe**; 강사 2명이 다른 센터면 매니저 각각 자기 센터명으로 1통. 미지정이면 빈 배열) + **`notifyCenterManagerOfEnrollment(admin,{event,teacherId,…,origin,excludeEmails?,delayMs?})`** — `logEnrollmentEvent`와 동일 규약(**자체 try/catch, 절대 throw 안 함** → 호출부는 try/catch 없이 1줄). `excludeEmails`=같은 액션의 관리자 수신자(매니저가 admin 겸직 시 중복 방지), `delayMs`=직전 메일 2통 뒤 **Resend 초당 2건 제한 회피**(1100, `reassign.ts` 방식). 6이벤트=`created`(신청 접수)·`approved`·`declined`·`paid`(**결제수단 무관** — 관리자 알림과 달리 무통장·카드·웹훅 모두)·`cancelled`(학생/admin, `cancelledBy`)·`refunded`. 계측 지점 7곳: `submitEnrollment`·`approveEnrollment`/`rejectEnrollment`·`finalizeEnrollmentPayment`·`cancelEnrollment`·`adminCancelEnrollment`·`refundEnrollmentPayment`. + **`notifyCenterManagerOfClass(admin,{event,teacherIds[],…,actorId?,delayMs?})`**(수업 단위 변경, 같은 규약·메일=`sendClassEventToCenterManager`, CTA 항상 `/center/schedule`): 5이벤트 `class_reassigned`(1회성 대타)·`class_postponed`/`class_cancelled`(연기/취소, `postponeReason` student|company·`makeupDate`)·`remaining_rescheduled`(old→new 주간 일정·적용일·대상 회차)·`remaining_reassigned`(강사 이관). **`actorId`=실행 주체가 수신 매니저 본인이면 생략**(자기가 한 작업). 계측 5곳: `reassignClassCore`(**`actor.role==="admin"`일 때만** — center_manager 분기는 기존 관리자 알림)·`adminCancelClass`·`adminRescheduleRemaining`·`adminReassignRemaining`(대체 2건은 `teacherIds=[old,new]`)·**`cancelClass`(학생 본인 연기, `postponeReason:"student"`)**. ⚠️ `class_postponed` 리드 문구는 **주체 중립**(학생·admin 공용, 주체는 Reason 행으로 구분).
- `src/lib/teacher-directory.ts`(server-only) — 센터 스코프 강사 데이터: `loadCenterTeachers`(→`CurrentTeacher[]`, 강사 관리 탭)·`loadCenterSessions`(→`AdminSession[]`, 주간 일정)·`loadCenterTeacherCards`(→`CenterTeacher[]`, 대체 후보 — **`slots`는 `loadBookedSlotsByTeacher` 차감 후 값**. 서버 `reassignClassCore`는 `classes` 충돌만 보는데 `신청`/`승인`/`결제대기` 건은 아직 classes가 없어 여기서만 걸러진다). + **admin·center 공용 「수업 보기」 파생**: `TEACHER_CLASS_SELECT`·`teacherClassesOrFilter(ids)`(classes를 `teacher_id` **또는** `original_teacher_id`로 조회=대체로 넘어간 회차 포함, 센터 스코프 집계 누락도 교정)·`buildCoverSessions(rows,ids,nameById)`(→강사별 `TeacherCoverItem[]`, 미래 회차만·날짜/시각 정렬). admin teacher-requests·classes 조립을 센터 소속으로 이식.
- `src/lib/settlements.ts`(server-only) — **강사 정산 row 조립 공유 로더 `loadSettlementRows(admin,teacherIds?)`**(→`{rows:SettlementRow[],rates}`): conducted 수업(`conducted_override ?? conducted_at`·취소 제외)→강사 현재 소속 센터·`rate_schedules` `effectiveRate` 역산·환율. 각 row에 **`krwPerSession`**(수업 진행일 환율로 `krwAtOrNull` 사전환산)·`isTest`(enrollment `is_test`) 채움. `teacherIds` null=전체(admin `/admin/settlements`)·배열=그 강사만(센터 `/center/settlements`)·빈 배열=빈 결과. admin·센터 정산 page 공용(단일 소스).
- `src/lib/revenue.ts`(server-only) — **매출 row 조립 공유 로더 `loadRevenueRows(admin)`**(→`{rows:RevenueRow[],rates}`, `loadSettlementRows` 미러): payments+enrollment 스냅샷+센터역산+환율. `failed` 제외, `isTest` 플래그 유지, `teacherId`+행별 사전환산 `krwAmount`/`krwCancelledAmount`(결제일 환율)+**행별 부가세 역산 `krwSupply`/`krwVat`/`krwCancelledSupply`/`krwCancelledVat`**(`vat.ts`) 포함. 매출 현황·매출이익 page 공용.
- `src/lib/kst.ts` — timestamptz(ISO)→KST 표시 문자열(**순수 산술 UTC+9**, 서버·클라 동일 결과): `kstDateText`(`2026.07.19`)·`kstDateTimeText`(`2026.07.19 10:43`)·`kstTimeText`(`10:43`, 같은 날짜 안에서 시각만)·`kstShortDate`(`7.19`). ⚠️ 표시용 `toLocaleString("ko-KR")` 금지(하이드레이션 불일치). 공지 사항 Footer·목록·상세·admin 공용.
- `src/lib/vat.ts` — 부가세(VAT 10%) 역산 헬퍼(순수 로직, server-only 아님=로더/클라 공용): `VAT_RATE`·`vatSupply(total)=round(total/1.1)`(공급가액)·`vatAmount(total)=total−vatSupply`(부가세)·`vatBreakdown(total)`. 통화무관(÷1.1). 저장 결제금액=부가세 포함 합계 전제. 매출 현황(`RevenueManager`·`revenue.ts`)·매출이익(`ProfitManager`)·목표 시뮬레이션(`simulation.ts`) 공용.
- `src/lib/simulation.ts` — 목표 매출이익 역산(순수 로직, server-only 아님): `WEEKS_PER_MONTH`(365÷12÷7)·`sessionRateKrw(price,cur,fx)`(`fx.ts krwAtOrNull` 미러)·**`simulate(SimInputs):SimResult`**(건당 공급가액·정산·PG·매출이익·이익률 → **`requiredEnrollments=ceil(목표÷건당이익)`**, 건당이익≤0이면 null=달성 불가 + 운영 부하(과정기간·`activeStudents`=건수×기간(월)·월 수업수·주당 슬롯·필요 강사수) + 목표 달성 시 월 손익)·`sensitivity(base,axis,values)`(축=`rate|fx|tuition`). **이익 공식·반올림은 `ProfitManager`와 동일**(`vatSupply`·`pgFeeOf` 재사용, 단가 미설정=0 처리도 동일). 입력은 `num()`로 정제해 NaN/Infinity 누출 없음. `/admin/simulation` 전용.
- `src/lib/cohort.ts` — **선수취/후지급 시점차 코호트 전개**(순수 로직, `simulation.ts`와 관심사 분리): **`settlementSpread(totalSessions,sessionsPerWeek)`**(정산 원가가 신청월 M+0·M+1·… 에 떨어지는 비중 — 수업이 기간 L개월에 균등 제공되고 시작일이 월 내 균등분포라 보고 u∈[0,1] 적분(1000스텝), **요일·월길이·TZ 비종속**·합계 항상 1; 주3회/24회 → 27.2/53.6/19.2%로 실제 수업 달력 시뮬레이션(28.0/53.3/18.7%)과 ±1pp)·**`projectCohorts(SimResult,SimInputs,arrivals[])`**(월별 `dashboardProfit`(=`/admin/profit`이 보여줄 값: 결제일 공급가액 − 그 달 진행 수업 정산 − PG) vs `economicProfit`(=건별 매칭)·`gap`·**`deferredRevenue`**(월말 선수금=미제공 회차분 수강료, 환불 정책이 회차 비례라 전액 반환 의무)·`unpaidTeacherCost`)·`convergenceMonth`·`buildArrivals`. **왜 필요한가**: 매출은 `payments.created_at`, 정산은 `classes.session_date` 기준이고 `ProfitManager.tsx:102-103`이 둘을 **서로 다른 날짜로 필터**해 정상상태에선 상쇄되지만 램프업/급감 구간엔 어긋남. DB 미조회(순수 전개).
- `src/lib/pgfee.ts` — PG 결제 수수료 적용일 이력(`pg_fee_schedules`) 역산(순수 로직, `fx.ts` 미러): `PgFeeRow`·`pgFeeRateAt(rows,date)`(effective_from≤date 최신, 없으면 0=미적용)·`pgFeeOf(amount,ratePercent)`(행별 반올림)·`isPgPayment(method)`(무통장 `bank_transfer`만 false). 로더(`revenue.ts`)·거래목록/CSV(`RevenueManager`)·편집기(`PgFeeHistoryEditor`) 공용.
- `src/lib/fx.ts` — 외화 환율 적용일 이력 역산(`rates.ts` 환율 버전, 순수 로직): `FxRow`·`fxRateAt(rows,currency,date)`(effective_from≤date 최신, 없으면 0)·`toKrwAt`(집계용, KRW passthrough·미설정 0)·`krwAtOrNull`(표시용, 미설정 null)·`fxRatesAt(rows,date)`(날짜 기준 Rates 맵). 정산·매출 로더·정산 확정 액션(조정 KRW 스냅샷) 공용.
- `src/lib/payment.ts`(`import "server-only"`) — 결제 확정 공유 코어. `finalizeEnrollmentPayment(enrollmentId,{id,role:"admin"|"student"|"system"})`(상태 `결제대기` 게이트+role='student'면 소유권 재검증(admin·system은 skip=이미 authoritative)→`결제완료` race-safe update→**무통장(role='admin')이면 `payments` 기록**(`recordPayment`, `payment_id='bank-{enrollmentId}'`·`method='bank_transfer'`·`amount=opts.amount ?? (enr.price_krw ?? COURSE_PRICE_KRW)`·`note=opts.note`; **`finalizeEnrollmentPayment(_, _, opts?:{amount?,note?})` 3번째 인자로 admin 무통장 확정 시 실입금액(정가 이하 할인)·메모 전달** — `confirmPayment(id,{amount,note})`가 enrollment `price_krw` 조회해 `1~유효가격` 검증 후 위임, `payment_confirmed` 이벤트 detail에 `bankAmount`·`discounted`·`note` 기록; 카드/웹훅은 opts 미전달; 상류 `settlePortonePayment`가 이미 기록해 이중 방지=매출·환불 단일 소스화). **확정 후 재조정**: 무통장 결제는 `EnrollmentsManager` 상세 모달의 "실입금액·메모 수정" 버튼(`adjustable=isBankPay && payment.status='paid'`)→`adjustBankPayment(paymentId,{amount,note})`(`src/app/admin/actions.ts`, `requireAdmin`→무통장·`paid` 가드(카드/환불 거부)→enrollment `price_krw` 조회해 `1~유효가격` 검증→`payments.amount`/`note` update→`payment_adjusted` 감사 이벤트(detail old/newAmount·old/newNote)→`/admin/enrollments`·`/admin/revenue` revalidate)로 사후 정정, **매출 대시보드 자동 반영**+`generateClassesForEnrollment`(멱등 클래스 생성, 여기로 이동)+학생 SMS+강사 결제확정 메일+**센터 매니저 알림**(event `paid`, **결제수단 무관**=무통장 admin 확인도 발송)+`payment_confirmed` 이벤트(actorId: system이면 null, `detail.via`: student=`portone_card`·system=`portone_webhook`)+revalidate 4곳) + `generateClassesForEnrollment`/`EnrollmentForClasses`. finalize 호출부는 상위 파이프라인. ⚠️ `"use server"` 아닌 모듈이라 클라 직접 호출 불가(우회 방지). **결제 기록·정산·환불 코어(같은 파일)**: `recordPayment(admin,{...})`(payments 멱등 insert-once, **카드·무통장 공용** — 무통장은 `finalizeEnrollmentPayment` admin 경로가 호출, **프렙은 `confirmPrepPayment`/`refundPrepEnrollment`가 `prepEnrollmentId`로 호출**(`payment_id='prep-bank-{id}'`) — `docs/prep.md`의 「결제 기록·환불」), **`settlePortonePayment(paymentId,{role:"student"|"system"})`**(검증(`getVerifiedPortonePayment`)→**enrollment 조회(`student_id, price_krw`)해 `expected=price_krw ?? COURSE_PRICE_KRW`와 `v.amount` 대조**(불일치=`recordPayment` 전에 거부, 기존 "불일치=미기록" 보존)→기록→finalize→**확정 불가 시 자동 환불**: finalize 실패 후 상태 재조회 — (a)`결제완료`+타 `paid` payment=**과오납(중복결제)** 자동취소(`overpaid:true`) / (b)enrollment `취소`·`거절`·삭제(`!cur`)=**취소된 신청에 캡처된 결제** 자동취소(금전 손실 방지) / (c)`결제완료`+타 결제 없음=멱등 성공 / (d)그 외(결제대기 등)=확정 실패 반환. **공유 헬퍼 `attemptAutoRefund`**: `cancelPortonePayment` 성공 시 payments `cancelled`+`payment_refunded` 이벤트, **실패 시 `payments.note="[자동환불실패]…"` 표식+`payment_refund_failed` 이벤트**(admin 결제목록·매출·감사로그에 노출→수동 환불 유도, 돈 방치 방지) — 학생 confirm·웹훅 Paid 공용), **`refundEnrollmentPayment(admin,{payment,totalCancelledAmount,reason,actor})`**(환불 후 DB 동기화 — payments 상태(`cancelled`/`partial_cancelled`)·`cancelled_amount`(절대값=멱등) + enrollment `결제완료`→`취소` CAS + **미래 `예정` 수업 일괄 `취소`**(`cancel_reason='cancel'`, 과거 유지) + 학생 SMS·강사 메일(`sendEnrollmentRefundToTeacher`)·**센터 매니저 알림**(event `refunded`)·`payment_refunded` 이벤트; admin 환불·취소 웹훅 공용, PortOne 취소는 호출측이 이미 수행).
- `src/lib/portone.ts`(`import "server-only"`) — `getVerifiedPortonePayment(paymentId)`: PortOne V2 REST(`GET /payments/{id}`, `Authorization: PortOne {PORTONE_V2_API_SECRET}`) 조회+검증(status=PAID·**통화 KRW만** 검증, ⚠️ **금액 상등은 여기서 안 함** — enrollment 유효가격(`price_krw ?? COURSE_PRICE_KRW`)을 아직 모르므로 `amount`만 반환하고 호출자 `settlePortonePayment`가 대조)→`customData.enrollmentId` 반환. 반환 `{ok, enrollmentId?, error?, transient?, amount?, currency?, pgTxId?, method?, receiptUrl?, raw?}`(strictNullChecks off라 평탄 타입; `transient`=일시오류=웹훅 500 재시도; 성공 시 payments 기록용 결제상세 포함). 학생 액션·웹훅 공용(결제 검증 단일 소스, 금액 서버 authoritative). + **`fetchPortonePaymentRaw(paymentId)`**(검증 없는 raw 조회 — 취소 웹훅의 누적 취소금액 `amount.cancelled` 읽기용) + **`cancelPortonePayment(paymentId,{reason,amount?})`**(`POST /payments/{id}/cancel`, 환불; amount 미지정=잔액 전액. 실패 시 **PG 원문 사유 노출**=`pgMessage`>`message`>`type` 우선순위+`pgCode` 병기(예 "환불 실패: 취소가능 금액을 초과했습니다.… (P568)"), raw body는 `console.error` 유지. ⚠️ **현 KPN 테스트 채널(공용 MID `merchantest6`)은 취소 자체가 불가** — 전액·부분 모두 `P568`(PortOne 콘솔 직접 취소도 동일)이라 **환불 동선은 운영 채널에서 재검증 필요**(부분취소 지원 여부도 계약 시 확인)).
- `src/lib/events.ts`(server-only) — **과정 라이프사이클 감사 로그**. `logEnrollmentEvent(admin,{enrollmentId?,classId?,eventType,actorId?,actorRole?,actorName?,course?,courseTitle?,studentName?,teacherName?,detail?})`(service_role insert into `enrollment_events`, **best-effort**=자체 try/catch·절대 throw 안 함=로그 실패가 본 뮤테이션 차단 안 함, `createMakeupClass`처럼 호출측 admin 클라 재사용) + `EnrollmentEventType` union(14종: `enrollment_created`(실/테스트 `detail.isTest`)·`enrollment_approved`·`enrollment_rejected`·`payment_confirmed`·**`payment_adjusted`(무통장 실입금액·메모 재조정, detail old/newAmount·old/newNote)**·`payment_refunded`(환불→수강취소, admin/자동/취소웹훅)·**`payment_refund_failed`(자동환불 시도 실패=수동 환불 필요, `attemptAutoRefund`가 기록)**·`enrollment_cancelled`(학생/admin, actor_role 구분)·`class_postponed`·`class_cancelled`·`class_reassigned`·`remaining_rescheduled`·`remaining_reassigned`·`conducted_overridden`). **DB enum 아닌 앱 union**(확장 시 마이그레이션 불필요). 각 뮤테이션 액션이 성공 직후 revalidate 앞에서 1줄 호출(계측 지점=`submitEnrollment`·`createTestEnrollment`·`approveEnrollment`·`rejectEnrollment`·`confirmPayment`·`cancelEnrollment`·`adminCancelEnrollment`·`cancelClass`·`adminCancelClass`·`adminReassignClass`·`adminRescheduleRemaining`·`adminReassignRemaining`·`adminSetClassConducted`). `detail` jsonb에 before/after(강사 대체=old/newTeacher, 일정 변경=old/newSlots·effectiveDate·affectedCount 등) 담아 덮어쓰기 복원성 확보. 조회는 **admin 전용**(RLS 정책 없음=service_role만). **저빈도 상세(피드백·conducted 자동 인정)·출결(입장)은 미계측**(향후 tier 확장 후보).
- `src/lib/prep-session.ts`(server-only) — **프렙 회차 입장·출결 조회 계층**(`classroom.ts`의 `loadClasses`/`mapClassRows`와 같은 역할): `PrepSessionItem`(**`startMs`/`endMs`를 서버에서 미리 계산** → 클라는 숫자만 비교)·`loadPrepSessionsForStudent(userId)`(`수강확정` 강좌 + 내 출결)·`loadPrepSessionsForFriender(userId, courseIds)`(내 강좌 회차 + `host_entered_at` + **출석 수만**, 신원 비공개)·`loadTodayPrepSessions(userId, todayKst)`(마이페이지 상단 배너 — 수강생·호스트 양쪽). ⚠️ **전부 `createAdminClient()`**: `prep_sessions`의 SELECT 정책이 `_select_own`(개설자)·`_select_public`(`course.status='승인'`)뿐이라 프렌더가 승인된 강좌를 고쳐 승인이 풀리면 수강생 화면에서 회차가 통째로 사라진다 → 자격은 `prep_enrollments`로 직접 확인한다. ⚠️ 종료 시각은 `start_min + duration_min`(**`lessonEndMin` 금지** — 정규 수업 전용).
- `src/lib/rate-limit.ts` — in-memory 토큰 버킷 `rateLimit/getClientIp/formatRetryAfter`. **⚠️ 프로세스 메모리라 멀티 인스턴스(Vercel)에선 효과 제한** → 필요 시 Upstash Redis로 함수 내부만 교체.
- `src/hooks/use-caps-lock.ts` — `useCapsLockWarning()`, `capsLockHandlers` spread.
