# 화상수업(zoom) · 클래스 · 내 강의실

> `CLAUDE.md`에서 분리된 상세 문서. 본문의 "위 …항목" 상호참조는 다른 `docs/*.md`일 수 있음(색인=CLAUDE.md).

**화상수업(zoom) — 클래스 + 내 강의실**: 결제 확정(`confirmPayment`→`결제완료`) 시 enrollment의 (start_date+slots+**`total_sessions ?? TOTAL_SESSIONS`**)에서 **날짜별 클래스 자동 생성**(`generateClassesForEnrollment`, service_role, **멱등** `upsert onConflict:"enrollment_id,session_no" ignoreDuplicates`; `total_sessions`는 admin 테스트 수강신청의 자유 횟수, 실 신청은 null→24). **클래스 1개=수업 1회(매칭 요일 하루)** — 같은 날 여러 슬롯이면 한 클래스(start=최소 min, end=최대 min+30). 열거는 `enumerateLessonSessions(start,slots,total)`(`src/lib/availability.ts`, `lessonEndDate` 미러·요일당 1회·TZ 비종속). **`classes` 테이블**(`20260626221643_add_classes.sql`): `enrollment_id`/`student_id`/`teacher_id` FK cascade·course/title·표시 스냅샷(teacher_name·student_name·student_english_name)·`session_no`·`session_date`·`start_min`/`end_min`·unique(enrollment_id,session_no); RLS `_select_own_student`/`_select_own_teacher`(enrollments 패턴), **사용자 INSERT/UPDATE/DELETE 없음**(생성=admin service_role, 정리=cascade); zoom_url 미저장(입장 시 강사 profiles 최신값 조회). **내 강의실 진입점**: 학생=`/mypage/classroom`(마이페이지 탭)·강사=`/teacher` 내 "My Classroom" 섹션(별도 `/classroom` 라우트 제거). **⚠️ 학생 `/mypage/classroom`은 「필리핀 화상영어 과정」+「프렙 강좌」 2섹션**(`docs/prep.md`) — 입장 동선을 과정 종류와 무관하게 한 곳으로 모은 결과다. **프렙도 정규 과정과 같은 2단계**(카드 그리드 → 카드 클릭 → 상세): `<PrepCourseCard>`(`CourseCard` 미러 — 강좌명·프렌더·다음 수업·기간/시각 dl·진행률 바) → `<PrepCourseDetail>`(`CourseDetail` 헤더 미러 + `<PrepSessionList defaultView="calendar" bare>`; ⚠️ 「연기 N/6」은 프렙에 없는 개념이라 미렌더). **`ClassroomList`가 선택 prop `prepCourses?: PrepCourseSessions[]`로 받아 카드·상세를 함께 소유한다** — ⚠️ 한때 페이지가 형제 섹션으로 합성했는데, `ClassroomList`가 정규 카드 선택 시 **자기 자신을 `CourseDetail`로 통째 교체**하는 구조라 **정규 상세를 보는 중에도 프렙 섹션이 아래에 남았다**(실제 버그). 선택 상태는 한 컴포넌트가 가져야 한다. 강사 오염은 **선택 prop을 `/teacher/classroom`이 넘기지 않는 것**으로 막고(타입 보장), 빈 상태 early return은 `classes.length===0 && prepCourses.length===0`으로 넓혔다. 프렙 회차를 **`ClassItem`으로 매핑하지는 않는다**(연기·취소·보강·강사 대체·피드백·conducted가 프렙엔 전부 없어 가짜 값이 `ClassRow` 상태 머신을 오작동시킨다) — 그래서 **「주간」 뷰에는 프렙이 빠지고**(`WeekSchedule`은 `ClassItem` 전용) 대신 「프렙 강좌 일정은 「과정별」 뷰에서 볼 수 있어요」 안내 한 줄을 둔다. 섹션 제목은 **둘 다 있을 때만** 붙어, 프렙이 없는 회원에겐 기존 화면 그대로다(회귀 0). 공통 로더 **`src/lib/classroom.ts`**(`loadClasses(supabase,userId,isTeacher)`·`mapClassRows`·`CLASS_SELECT`)로 `classes` 조회(RLS 본인 것; **환불/거절(enrollment.status∈{취소,거절}) 과정은 강의실에서 완전 제외** — loadClasses가 slots 조회 시 `status`도 함께 읽어 해당 enrollment 클래스를 필터, 학생·강사 공통. 개별 수업 연기(enrollment `결제완료` 유지)는 제외 대상 아님=취소선으로 계속 노출; **강사는 `.or(teacher_id.eq,original_teacher_id.eq)`로 현재 담당 + 1회성 대체로 넘어간 원 강사 회차까지**, 학생은 `student_id`만) — **과정 카드 대표 강사(학생 뷰)**: `loadClasses`가 enrollments select에 `teacher_name`도 읽어 `mapClassRows`가 `ClassItem.enrollmentTeacherName`(현재 담당, 강사 대체 반영)로 스탬프 → `ClassroomList`가 과정 그룹 카드 대표 강사를 `!isTeacher`면 이 값 우선(첫 과거 회차 스냅샷 대신, admin 목록과 동일 원칙). per-session 행·주간 뷰의 강사 표시는 실제 담당(과거=옛·미래=새) 유지. **1회성 강사 대체 read-only 표시**: `mapClassRows`가 `reassignedAway=isTeacher && original_teacher_id===uid && teacher_id!==uid`·`coveredByName`(=현재 대타명) 파생 → 원 강사 뷰에서 그 회차를 목록·주간 타임테이블에 **read-only**로 노출("Reassigned" 배지·"Covered by {대타}"·dim·**주간 그리드/아젠다·달력 취소선(line-through)**, 입장/연기/피드백작성/진행마커 없음, 대타 피드백 있으면 열람만=`ClassFeedbackModal readOnly`). **카운트**: 회차 수·진행률·`sessionNo/total`은 과정 전체 기준(대체됨 포함), **다음 수업·그룹 정렬·달력 진행마커만 대체됨 제외**(원 강사가 실제 담당할 회차 기준). 대타는 평소대로 정상(teacher_id=대타) + **대타 뷰 `ClassInfoModal`에 "Covering" 배지·"You're covering this class for another teacher." 안내**(`coveringForOther=isTeacher && teacher_id===uid && original_teacher_id && ≠uid` 파생, `mapClassRows`·`ClassItem`; 입장/피드백 등 액션은 정상=대타가 실제 담당), 학생은 기존 "강사 변경" 불변. 정산은 `teacher_id`(대타) 기준이라 자동 정합)→`<ClassroomList classes isTeacher>`(`src/components/classroom/`, client, **강사 화면 전체 영문·학생 한글**: 헤더·섹션·버튼·날짜포맷(`M월 D일 (요일)`↔`Jul 1 (Mon)`)·과정명(강사=`getCourse().englishTitle`)) — **진입 랜딩=카드/주간 뷰 토글**(`landingView` cards|weekly 세그먼트 pill, 기본 카드). **카드 뷰 2뷰**: ①기본=**과정(enrollment_id)별 카드 그리드**(`grid sm:grid-cols-2`, 카드=과정명·상대방·다음 수업 일시·**기간(첫~마지막 수업일, 보강 포함 `shortDate`)·주간 요일(`weekdaySummary`=**`enrollments.slots`(현재 템플릿) → `summarizeWeekdays`**, `/` 구분 예 "월/수/금"; `loadClasses`가 본인 enrollment `slots`를 조회해 `ClassItem.enrollmentSlots`로 스탬프, 비면 정규 수업 요일 폴백 — 일정 변경 반영)**·진행률 바(완료/전체)) ②카드 클릭→같은 페이지에서 **해당 과정 수업 목록**(상단 "← 목록으로/Back", **예정/지난 분리**, 지난은 흐림·입장 버튼 숨김, 행=날짜·시간(`fmtTime`)·회차(`sessionNo/총`, **총=과정 계획 회차=보강 제외 클래스 수**, `TOTAL_SESSIONS` 하드코딩 아님 — 테스트 자유 횟수 반영)). **주간 뷰**(`<WeekSchedule>`, 전체 과정·학생 통합 주간 스케줄, 주 이동(월요일 시작·`mondayOf`)·이번주 하이라이트): 데스크톱=월~일 타임그리드(06:00~24:00 `ROW_MINS`, 시간 거터 sticky, enrollment별 고유 배경색 `COURSE_PALETTE`, 진행중=`ring-cta`, **종료(레슨 종료 지남)=회색 음영**, 취소=흐림/line-through, 주말 헤더 일=`text-brand`/토=`text-accent-blue-ink`, **강사 뷰 블록 우상단 ○/△ 진행 마커**(`conductMark`, 목록·달력과 동일))·모바일=요일별 아젠다(`WeekAgendaRow`, 수업 있는 날만, 종료·취소 흐림, 행에 입장/연기 인라인). "이번 주" 버튼은 항상 노출(이번주면 `disabled`). **입장·연기·피드백 직접 실행**: 블록/행 클릭→`ClassInfoModal`(수업 요약 + **제목 옆 상태 배지 + 본문 `Info` 안내 문구**(`ClassRow`/`WeekAgendaRow`와 동일 문구·색 재사용: 취소=`취소/Cancelled`·연기=`연기/Postponed`(`cancelReason==='cancel'` 여부로 구분)·보강=`보강/Makeup`·학생 강사대체=`강사 변경`; 문구=연기 "맨 뒤 보강 편성"·취소 "취소됨"·보강 "연기 대신 보강" 안내, 강사대체는 배지만) + **입장**(`EnterClassButton` 라이브 시, 학생·강사 공통) + **연기**(`PostponeButton`, 학생·그 과정 다음 예정 1건만) + **피드백**(종료 수업만: 강사=작성/수정·학생=피드백 있을 때 보기 → `ClassFeedbackModal`을 launcher 패턴으로 승격=정보 모달 닫고 오픈, 중첩 Esc/z 충돌 방지) + **입장 시간창 전 미래 수업엔 "시작 15분 전 입장/Opens 15 min before" 안내 문구**(`showEntryHint`, ClassRow 힌트와 동일 미러) + "과정 상세로" `onGoToCourse`→그 과정 `CourseDetail`; 액션(입장·연기·피드백) 있으면 "과정 상세로"는 보조 버튼으로 강등, 안내 문구는 정보 텍스트라 강등 안 함). 모달·아젠다의 연기 노출 판정은 `WeekSchedule`가 `groups`에서 파생한 `ctxByEnrollment`(enrollment별 `{cancelAllowed(!isTeacher && count<MAX), cancelledCount, nextUpcomingId}`, `nextUpcomingIdOf`·1분 틱 `now`)로 공급. **`EnterClassButton`·`PostponeButton`은 ClassRow·모달·아젠다 공용 단일 소스**(입장=팝업차단 회피, 연기=확인 다이얼로그+`cancelClass`+남은 연기 토스트; `fullWidth`/`compact` variant). 연기 규칙(학생만·다음 예정 1건·1시간 컷오프·진행중이면 숨김)은 서버 `cancelClass` 가드와 동일, UI는 노출 제어만. 1분 틱 `now` state로 다음수업·진행률·입장창 갱신. **진입=과정 카드 → 과정 선택 시 그 과정 상세**(`CourseDetail`): **달력/목록 토글(달력 기본)**. 달력=그 과정 월간(`ui/calendar` 재사용·`date-fns/locale` ko/enUS, 예정=accent-blue·지난=rule 색 구분·오늘=노랑 배경(#FFF3CD)+앰버 링·주말 색(일=brand 빨강/토=accent-blue-ink, `dayOfWeek` 모디파이어+헤더 nth-child, `weekStartsOn=0`·outside days off), `mode=single required`, 기본 포커스=다음 예정 수업 달), 날짜 클릭 시 그 날 수업 목록·입장. 목록=예정/지난 섹션. **수업 1회=25분(+5분 휴식)**: 내 강의실 행 종료 시각은 `lessonEndMin(end_min)`=`end_min−(SLOT_MIN−LESSON_MIN)`=`end_min−5`로 `:25`/`:55` 표시(`LESSON_MIN=25`·`lessonEndMin` `availability.ts` 단일 소스, 위저드 `EnrollScheduleField`도 공유). 슬롯 점유·매칭은 30분(`end_min`) 그대로이나, **입장 시간창 종료·예정/지난 전환은 레슨 종료(`lessonEndMin`=`end_min−5`)=표시 종료와 일치**(클라 `mapClassRows`의 `endMs`·서버 `enterClass` 모두 `lessonEndMin` 적용 — 표시 종료 후 입장 가능했던 5분 간극 제거). **입장하기**: 시작 15분 전~레슨 종료 시각만 활성(`canEnterClass`/`kstDateMinToMs`/`ENTRY_LEAD_MS` `src/lib/classtime.ts`, KST=UTC+9 고정). 클릭→**팝업 차단 회피**(빈 탭 먼저 open 후 액션 URL로 이동)→`enterClass(classId)`(`src/app/classroom/actions.ts`): 로그인+**소유 검증**(student_id∥teacher_id)+**시간창 서버 강제**+강사 `zoom_url` 최신값(service_role)·`isValidZoomUrl` 검증→`{url}∥{error}`. 학생·강사 동일(15분 전). **개별 수업 연기(학생, 자동 보강)**: 학생 뷰 예정 수업에 **"수업 연기"** 버튼(영문 "Postpone"; **UI 문구만 "연기"(영문 버튼 "Postpone"/배지 "Postponed")로 통일**하고 내부 `classes.status`·코드 식별자는 `'취소'`/`cancelClass`/`cancellable`/`ClassCancellationEmailData`/`sendClassCancellationToTeacher` 그대로 — 실동작=이 회차 건너뛰고 맨 뒤 보강 1회, "취소" 어감이 부적절해 **학생 UI·학생 대상 서버 에러·강사 알림 메일까지 카피만 "연기/postpone"으로 변경**) → `cancelClass(classId)`(`src/app/classroom/actions.ts`): 소유(student_id)+상태 `예정`+**다음(가장 이른) 예정 수업 1건만**(같은 enrollment의 `예정` 중 미시작(`startMs>now`) 최소 시작시각 = `nextId`, 서버 가드 + 클라 `ClassRow`의 `cancellable`에 `item.id===nextUpcomingId` 조건 — `CourseDetail`이 `nextUpcomingId` 산출해 `SessionList`·`ClassroomCalendar`→`ClassRow` 전달, 취소 시 다음 수업이 새 대상)+**진행 중(입장 가능 창) 수업 있으면 연기 불가**(같은 enrollment에 `canEnterClass`(시작 15분 전~`lessonEndMin`) 참인 수업 하나라도 있으면 클라 `nextUpcomingId=null`로 버튼 숨김 + 서버 `cancelClass` `inProgress` 가드로 차단 — 진행 중 수업 입장 버튼과 다음 수업 연기 버튼 동시 노출 방지, 두 곳 동일 규칙)+**시작 1시간 전 컷오프**(`canCancelClass`/`CANCEL_CUTOFF_MS`)+**과정당 6회 한도**(`MAX_CANCELLATIONS`, **`status='취소' && cancel_reason='student'` 카운트** — 학생 연기만 차감, admin의 회사 사유 연기·취소는 미차감) 가드 → `status='취소'` + **자동 보강 생성**(과정 전체 `최대 session_date` 다음으로 **주간 스케줄 요일 집합 중 가장 빠른 날**=마지막 수업일 뒤 첫 스케줄 요일, 요일별 시각 맵 적용, `session_no=enrollment 최대+1`, `is_makeup=true`, best-effort; **공유 헬퍼 `createMakeupClass` `src/lib/makeup.ts` — 학생 `cancelClass`·admin `adminCancelClass` 공용**) + 강사 연기 알림 이메일(`sendClassCancellationToTeacher`, 영문 "A class has been postponed") + **관리자 알림 이메일**(`sendClassPostponedToAdmin`, 독립 try/catch·보강 생성 실패 시 "수동 조치 필요" 문구) + **센터 매니저 알림**(`notifyCenterManagerOfClass` event `class_postponed`·`postponeReason:"student"`, admin 연기와 동일 이벤트로 통일) + `/mypage`·`/teacher`·`/admin/classes` revalidate(클라 `router.refresh()`). 연기 행=연기 배지(한글 "연기"·영문 "Postponed")·흐림·입장 숨김, 보강 행=보강 배지. **강사 대체 시 학생 예정 수업 행에 "강사 변경" 배지+안내 문구**(`reassigned=!isTeacher && reassignedAt && !cancelled && !isPast`, `teacher_reassigned_at` 기반, 지나가면 자동 숨김; `CLASS_SELECT`/`mapClassRows`/`ClassItem`에 `reassignedAt` 포함), 달력 취소일=취소 모디파이어(line-through). 진행률·예정 분리에서 취소 회차 제외. **남은 연기 횟수 안내**: `cancelClass`가 `remaining`(=`MAX−연기수`) 반환 → 연기 확인 모달("이번에 연기하면 N회 남습니다", `cancelledCount` prop으로 산출)·완료 토스트("남은 연기 N회")에 표시. **Navbar에 내 강의실 직접 링크 없음**(학생=마이페이지 탭·강사=/teacher 섹션으로 접근). **admin은 `/admin/classes`에서 전체 수업 조회·개별 취소(보강 옵션)·일정 변경·강사 대체**(강사 가용+충돌 검증, 과정 종료 시 편집 차단, admin 대시보드 항목). 기존 `결제완료` 건은 클래스 백필 필요(신규 결제부터 자동). **수업 종료 후 강사 피드백(텍스트, 수강생 읽기 전용)**: `classes.feedback`/`feedback_at`(`20260629234513_add_class_feedback.sql`). `ClassRow`(`ClassroomList`)에서 **종료된 수업**(`ended=!cancelled && now≥endMs`, 레슨 종료 시각 이후)은 **행에 버튼만** 두고 내용/편집은 `ClassFeedbackModal`(같은 파일, 커스텀 오버레이 `z-[110]`/패널 `z-[120]`·Esc·scroll lock, 긴 피드백 UX)에서 처리 — 강사=버튼 "Write/Edit feedback"(`MessageSquare`)→모달 textarea 작성/수정/삭제·수강생=피드백 있으면 "피드백 보기"(`Eye`)→모달 읽기 전용(전문+`feedbackAt` 날짜). (인라인 전문 노출 폐지.) 저장 `saveClassFeedback(classId,text)`(`src/app/classroom/actions.ts`): 로그인+**강사 소유권**(`teacher_id`)+취소 아님+**종료 가드**(`Date.now()≥kstDateMinToMs(session_date,lessonEndMin(end_min))`)→service_role update(빈 문자열=null 삭제, ≤2000자), `/teacher`·`/mypage` revalidate. **새 RLS 없음**(쓰기=service_role 액션, 읽기=기존 student/teacher select). `CLASS_SELECT`/`mapClassRows`/`ClassItem`에 `feedback`·`feedbackAt` 포함. **수업 진행 인정**(`classes.teacher_entered_at`·`conducted_at`, `20260630021451`): **강사 Enter 클릭(`enterClass`가 강사 본인 첫 입장 시 `teacher_entered_at` 기록) AND 종료 후 비어있지 않은 피드백 저장**(`saveClassFeedback`가 `teacher_entered_at` 있고 미인정이면 `conducted_at` 박음) → **진행 인정**. **sticky**(이후 피드백 비워도 `conducted_at` 유지). 학생 노쇼라도 강사 입장+피드백이면 인정(학생 입장 미기록). **강사 뷰 진행 마커(강사 전용, `conductMark(item,now)`)**: `ClassItem.conductedAt`이면 **○ "Conducted"**(초록 `Circle` pill)·`teacherEnteredAt` 있고 미인정+종료(`now≥endMs`)면 **△ "Feedback pending"**(앰버 `Triangle` pill, 입장했으나 피드백 전) — 지난 수업 목록 행 + **달력 셀 우상단 코너 마커**(`modifiers` conducted=●/pending=▲ + `after:` 절대배치, 학생 뷰엔 빈 배열=미표시) + 달력 아래 강사 범례. `CLASS_SELECT`/`mapClassRows`/`ClassItem`에 `teacher_entered_at`(→`teacherEnteredAt`) 포함(마커 판정용). admin 상세=`ClassesManager` "진행" 컬럼(진행됨/미진행=종료됐는데 미인정/−)+모달 dl(진행 여부·강사 입장(입장함·시각/미입장)·**피드백 작성**(작성함·날짜/미작성))+**미진행 사유 노티스**(dl 아래, `완료 && !conducted_at`일 때만; `unconductedReason(r)`: `!teacher_entered_at`→"강사가 입장하지 않았습니다.", `!feedback`→"강사는 입장했으나 피드백을 작성하지 않았습니다."; brand 색 블록). ⚠️ 강사가 Enter 안 누르고 Zoom 직접 열면 미인정(정책). **admin `/admin/classes/[enrollmentId]`(`ClassesManager`)도 읽기 전용 열람**(행에 "피드백" 칩·`ClassDetailModal`에 강사 피드백 블록; 상세 페이지 select·`AdminClass` 타입에 `feedback`/`feedback_at` 포함, admin은 작성 불가).
