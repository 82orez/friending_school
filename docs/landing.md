# 랜딩 · 공통 레이아웃 · 공지 · 약관

> `CLAUDE.md`에서 분리된 상세 문서. 본문의 "위 …항목" 상호참조는 다른 `docs/*.md`일 수 있음(색인=CLAUDE.md).

**스쿨 소개 랜딩** `/school`(`src/app/school/page.tsx`, async server). ⚠️ **루트 `/`는 프렌딩 홈**(=`docs/friender.md`)이고, 인증 성공 배너(`?reset|verified|signup=success` → `SuccessBanner`)도 그쪽이 소유한다. **5섹션**: ①히어로(`hero-bg.jpg` 배경 + `text-brand-gradient` "세계로") ②셀프디벨롭(`<SelfDevelop/>` client, props `isLoggedIn`+`completedByCourse` — 교재 탭 5종/진행바/더보기 아코디언. **`LINKED_COURSE`(book.key→레지스트리 course)에 등록된 교재만 전자책 연동**: 현재 **5종 전부 연동**(workhol·kitchen·grammar1·grammar2(키 basic1/basic2)·cosmetic) — 비연동 mock 분기(구 `UnitCard`)는 **제거됨**(`LinkedUnitCard` 단일 경로). 연동 카드=`LinkedUnitCard`(유닛 `/textbook/<course>/<n>` 링크, 잠금=`!isLoggedIn && !freeUnits`(자물쇠→`/login?next=`), 로그인 시 우상단 ○/✓ **수동 완료 토글** `setUnitCompleted` 낙관적, 완료 카드 `bg-progress/5`). **부제/소개**: `renderUnit`이 레지스트리 `getTextbook(course)` 유닛에서 `subtitle=u.sub??titleKr`·`situation`(부제와 **다를 때만** 주입=사실상 workhol·kitchen·cosmetic)을 전달 — `situation` 있으면 카드 **좌측정렬 + 소개문단**(`line-clamp-3`), 없으면 가운데정렬(grammar). 완료 상태는 `BookPanel` 로컬 state로 상단 진행바와 공유. **연동 확장 시 2곳 동기**: `SelfDevelop`의 `LINKED_COURSE` + `school/page.tsx`의 진행률 쿼리 `reading_progress.in("course",[...])`) ③유튜브 생존기(`youtube_videos` 노출분 SSR 조회→비면 mock `VIDEOS` fallback, `getYoutubeId`로 썸네일) ④실전 스피킹(**`<SpeakingDevelopSection id?/className?>`** `src/components/landing/SpeakingDevelopSection.tsx` server — 과정카드 5종 → `/courses/<slug>`, **`/philippines-english`와 공유**. 앵커 `#courses`는 **id prop을 넘기는 `/school`에서만** — 외부 링크는 `/school#courses`(수강신청 위저드·마이페이지)) ⑤액티비티(**`<ActivitySection id?/className?>`** `src/components/landing/ActivitySection.tsx` server — `ACTIVITIES` 카드 + 배지맵 `ACTIVITY_BADGE`(로컬 상수, 여기로 함께 이동), **`/activities`와 공유**). **섹션 헤더 헬퍼 `SectionIntro`는 `src/components/landing/SectionIntro.tsx`로 승격**(그라디언트 알약 + `<h2>` + desc — 섹션 2·3과 `ActivitySection`이 공유, `school/page.tsx` 로컬 정의는 제거됨). **데이터는 `src/data/landing.ts`**(`BOOKS`·`VIDEOS`·`COURSE_CARDS`·`ACTIVITIES` — 교재 유닛·과정 커리큘럼이 모두 `BOOKS`에서 파생되는 단일 소스). **수강료 단일 소스 `src/data/pricing.ts`**(**정가/할인가 이원 구조**: `COURSE_PRICE_KRW`(numeric — **실제 청구가 210,000원**, 전역 고정가·PortOne 결제 금액·서버 검증 기준)·**`COURSE_LIST_PRICE_KRW`(정가 240,000원, ⚠️ **표시 전용 — 결제·검증 경로에서 참조 금지**(과청구))**·파생 `COURSE_DISCOUNT_KRW`·**`HAS_COURSE_DISCOUNT`**(할인 UI 스위치 — 정가를 청구가와 같게 두면 리본·취소선이 자동으로 사라짐)·`COURSE_DISCOUNT_LABEL`(금액에서 파생 "3만원 할인", 하드코딩 금지)·`COURSE_PRICE_LABEL`/`COURSE_LIST_PRICE_LABEL`(`formatPrice`)·`COURSE_PER_LABEL`(`/ ${TOTAL_SESSIONS}회`)) — 할인 시각효과는 **`src/components/CoursePrice.tsx`**(server, JS 없음)의 **`<CoursePriceLine size="sm"|"lg">` 하나**: `정가(취소선 `<s>`) → [**`bg-brand` 할인 배지** / 할인가 / 24회]`(할인 없으면 배지 없이 기존 한 줄). ⚠️ **배지가 "할인가" 바로 위에 정렬되도록 정가·화살표를 왼쪽 열, 배지+할인가를 오른쪽 열로 분리**(배지를 블록 최상단에 두면 그 줄 첫 요소인 **정가** 위로 붙음 — 실제 겪음), 정가는 `items-end`로 밑선 맞춤. 이미지 코너 리본은 폐기(가격 옆이 시선 흐름상 유리). 적용 2곳=스쿨 소개 과정 카드(`school/page.tsx`)·과정 상세 과정 개요 카드(`courses/[slug]/page.tsx`, 히어로엔 미표시); **결제·정산 화면(위저드·마이페이지·admin)은 장식 없이 실제 금액만**(상수 변경으로 자동 반영) — `courses.ts`·`landing.ts`의 `price`/`per`가 참조(전 과정 공통, 차등 시 슬러그 맵으로 확장). **⚠️ per-건 수강료 오버라이드**: `enrollments.price_krw`(nullable, admin 테스트 신청만 세팅)가 있으면 **유효가격 = `price_krw ?? COURSE_PRICE_KRW`**가 표시·무통장 입금확인 캡/기본값·카드 검증(`settlePortonePayment`가 검증, `getVerifiedPortonePayment`는 통화만)·매출 전 경로에 authoritative(실 신청은 null→고정가 그대로). `EnrollmentsManager`·`StudentEnrollments`는 행별 `priceKrw` 숫자로 캡/결제창 금액. `BookUnit`={`n`/`t`/`sub?`/**`situation?`**/`s`} — **`situation?`(유닛 상황 소개)은 kitchen·cosmetic 24유닛에 채워짐**(각 `content/textbook/<course>/unit-NN.html`의 `.situation`에서 추출, 카드 소개문단용); workhol은 `WORKHOL_UNITS.situation` 별도, grammar는 없음. 카피 수정 시 이 파일 먼저 편집. 시맨틱 `<h1>` 1개(히어로)·섹션 `<h2>`. 반응형 모바일 기본 + `md:`. 전체를 `bg-surface` 래퍼로 감싸 흰 카드 대비.

**필리핀 화상영어** `/philippines-english`(`src/app/philippines-english/page.tsx`, sync server). Navbar 상시 탭. **텍스트 히어로**(`bg-brand-gradient` 알약 + `<h1>` + lead + 포인트 칩 3개, 문구=`src/data/philippines-english.ts`의 `PHILIPPINES_HERO`, 이미지 에셋 없음) + **`<SpeakingDevelopSection/>`**(id 미전달 → `/school`의 `#courses` 앵커와 중복되지 않음). `/school` 4번 섹션과 **동일 컴포넌트·동일 카피** — 카드 문구/링크 수정은 `COURSE_CARDS` 한 곳.

**액티비티** `/activities`(`src/app/activities/page.tsx`, sync server). Navbar 상시 탭. `/philippines-english`와 **동일 구조** — 텍스트 히어로(`bg-brand-gradient` 알약 + `<h1>` + lead + 포인트 칩 3개, 문구=`src/data/activities-page.ts`의 `ACTIVITIES_HERO`) + **`<ActivitySection/>`**. 카드 문구 수정은 `landing.ts`의 `ACTIVITIES` 한 곳.

**공통 컴포넌트** `Navbar.tsx`(client, 슬라이드 메뉴)·`Footer.tsx`(server)는 `layout.tsx`에서 children 감쌈. **Footer**(`bg-ink`)=**3열**(①(주)프렌딩 사업자 정보(대표·사업자등록·통신판매업·사업장 소재지·Phone·Email, lucide 아이콘) ②공지 사항(**DB 연동** — `notices` 최근 3건(`is_pinned desc, published_at desc`) 세션 client 조회(RLS가 노출·게시일 필터), 제목+`M.D` 링크 `/notices/[id]` + "전체 보기 →" `/notices`, 비면 "등록된 공지 사항이 없습니다." ⚠️ **칼럼 div `min-w-0` + 목록 `pr-6 md:pr-12` 필수** — 그리드 아이템 기본 `min-width:auto` 때문에 `min-w-0`이 없으면 긴 제목이 `truncate`되지 않고 칼럼을 밀어 **Footer 전체가 가로로 넘침**(실제 겪음), `pr-*`는 날짜가 우측 'Other INFO' 칼럼에 붙어 보이는 것 방지; **Footer가 async server component**라 전 페이지에서 조회) ③Other INFO=약관 링크 3종 `/terms`·`/privacy`·`/refund`) + 하단 구분선 아래 카피라이트 + **지원 링크 2종(`/friender/apply` "프렌더 지원하기" · `/teacher/apply` "Become a Teacher", `·`로 구분)**.

**공지 사항** (`/notices` 목록 + `/notices/[id]` 상세, server, **본문=일반 텍스트**(`whitespace-pre-wrap`, 마크다운/HTML 미사용)): 조회는 세션 client(RLS `notices_select_public`=`is_visible && published_at<=now()`가 노출·예약게시 필터를 강제 — 비공개·미래분은 목록·Footer·상세 전부 미노출, 상세 직접 접근 시 `notFound()`). 목록=`searchParams.page` 20건 페이지네이션(`range()`)·고정 배지, 상세=`generateMetadata` 제목 반영(`generateStaticParams` 없음=DB 기반). **조회수**: 렌더 중 뮤테이션 금지라 `<NoticeViewCounter>`(`src/components/notices/`, client, mount 1회·StrictMode ref 가드)가 공개 액션 `recordNoticeView`(`src/app/notices/actions.ts`, 인증 불필요·service_role로 **security definer RPC `increment_notice_view`** 호출=원자 증가, RPC가 공개조건 재검사라 비공개 건 조작 불가, best-effort). admin은 `/admin/notices`(`NoticesManager`, `YoutubeManager` 패턴 이식: 작성 폼·인라인 수정·📌고정/👁공개 즉시 토글·`AlertDialog` 삭제·`toast`) — 액션 `addNotice`/`updateNotice`/`setNoticeVisibility`/`setNoticePinned`/`deleteNotice`(`requireAdmin()`→service_role→`revalidateNoticeConsumers()`=`/admin/notices`·`/notices`·**`revalidatePath("/","layout")`**(Footer가 루트 layout)). ⚠️ 날짜 표시는 **`src/lib/kst.ts`**(로케일 비의존) 사용 — `toLocaleString("ko-KR")`은 Node/브라우저가 "오전"/"AM"으로 갈려 **하이드레이션 불일치** 발생(실제 겪음).

**약관/정책 페이지** (`/terms`·`/privacy`·`/refund`, server, 정적 SSG): 본문 단일 소스 **`src/data/legal.ts`**(`LEGAL_DOCS`, 신규 의존성 없이 구조화 데이터 — `LegalDoc{slug·title·status(ready|preparing)·effectiveDate?·intro?·sections}`, 블록 타입 `string`(문단)·`{list}`(번호목록, `{text,note?,sub?}` 중첩)·`{bullets}`(• 목록, 연락처 블록)·`{table}`) → 공용 렌더 **`src/components/legal/LegalDocument.tsx`**(server, `bg-surface` 래퍼·단일 `<h1>`·조항 `<h2>`·수동 번호/불릿/표 렌더, `preparing`이면 "준비 중" 안내). 각 page.tsx는 metadata title + `LEGAL_DOCS[slug]` 전달만. **terms**(회원이용약관 제1~24조+부칙)·**privacy**(개인정보처리방침 제1~14조, 제5조 위탁사 표=PortOne·Solapi·Resend·Supabase·Vercel 추정, 추후 확정)·**refund**(환불 정책, **회차 비례·위약금 없음**: `환불액=결제 수강료×(잔여 회차÷총 회차)`, 금액·회차 하드코딩 없음=과정/수강료 변경에도 유효) 모두 `ready`. 새 정책·본문 수정은 `legal.ts`만 편집. 회원이용약관 제17조(포인트 미운영)·제18조와 환불정책 정합.

- Navbar는 `user: { email? } | null` + `isAdmin` + `isTeacher` + **`isFriender`** prop을 SSR로 받고(`layout.tsx`가 `getUserRole` 1회로 파생해 전달, friender는 **`isFrienderRole`**=일반·Plus 공통), `onAuthStateChange` 구독 + `pageshow`(bfcache) 리스너로 stale 방지. **상시 노출(로그인 무관) 링크는 4종 — 스쿨 소개(`/school`)·필리핀 화상영어(`/philippines-english`)·프렌딩 프렙(`/prep`)·액티비티(`/activities`)**(슬라이드 메뉴 최상단 flat 링크 4개 연속 + 데스크톱 nav **중앙**(`gap-4 lg:gap-6` — 4개가 되며 md 폭에서 좌우 컬럼과 부딪혀 좁혔다) — 우측 인라인은 인증/역할 링크 전용 영역이라 중앙 배치가 **의도적 예외**. 넷 다 아코디언 아닌 flat 링크라 별도 state 없음). 프렌딩 홈은 로고(`/`)가 겸한다. `/prep`은 프렙 정적 홍보 페이지(`docs/prep.md`) — 네비 탭("프렌딩 프렙")으로만 들어가고 랜딩 과정 카드에는 넣지 않았다(카드가 `CoursePriceLine`으로 전역 21만원을 그려 프렙 가격과 어긋난다). 로그인 시 admin→"관리자"·teacher→"Teacher"(`/teacher`)·**프렌더 계열(friender·friender_plus)→"프렌더"(`/friender`, `text-cta`, 등급 무관 동일 라벨)**·center manager→**"Center Management"**(영문, `/center`, `isCenterManager`일 때) 링크 노출(모바일·데스크톱 2곳). **학생용 "마이페이지" 링크는 `!isTeacher && !isCenterManager`일 때만 노출**(강사·센터 매니저는 숨김, 학생·admin·**프렌더**는 노출 — 프렌더는 일반 회원 출신이라 수강신청 내역 접근 경로 유지). 로고는 `next/image`(`/images/logo.png`, `width=123 height=36`, `priority`).
- 슬라이드 메뉴: **커리큘럼 아코디언**(과정 5종 `/courses/<slug>`) + **교재 보기 아코디언**(`TEXTBOOKS` `src/data/textbook/index.ts`) + 인증 영역(로그인 시 **관리자**(admin만)·**마이페이지**·이메일·로그아웃 / 비로그인 시 로그인·회원가입). **이메일은 사이드 메뉴에만 표시**(데스크톱 상단 인라인에선 미표시). 두 아코디언 모두 `<button>` 토글(`curriculumOpen`/`textbookOpen` state, `aria-expanded/controls`, grid `0fr→1fr` 트릭), **메뉴 닫힐 때 둘 다 false 동기화**. 라우트는 `<Link>`+`onClick={closeMenu}`. 데스크톱 인라인에도 (관리자)·마이페이지·로그아웃/회원가입(**이메일은 미표시**). 새 교재는 `TEXTBOOKS`에 추가만 하면 자동 반영.
- a11y: 햄버거 `aria-expanded`·`aria-controls="mobile-menu"`, 메뉴 `role="dialog"`·`aria-modal`·`aria-hidden`·`inert={!menuOpen}`, `bg-black/40` 오버레이, 열림 시 닫기버튼 포커스·닫힘 시 햄버거 복귀(`triggerRef`/`closeButtonRef`/`prevMenuOpen`), body scroll lock, Esc 닫기. **수정 시 이 체인 유지.**
- 로그아웃은 `@/app/(auth)/logout/actions`의 `logout()`을 `<form action={logout}>`로 호출.
