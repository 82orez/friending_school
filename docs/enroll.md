# 수강신청(강사 매칭형) · 마이페이지 · 결제

> `CLAUDE.md`에서 분리된 상세 문서. 본문의 "위 …항목" 상호참조는 다른 `docs/*.md`일 수 있음(색인=CLAUDE.md).

⚠️ **`ApplyForm.tsx`(`src/components/`)는 현재 orphan**(Phase 1 랜딩 리디자인에서 최종 CTA 섹션 제거 — 미사용 mock). 구 상담신청 동선(폼 `CourseApplyForm.tsx`·액션 `submitApplication`·메일러 `sendApplicationNotification`/`ApplicationEmailData`·admin `ApplicationsManager`·`applications` 테이블)은 **전부 제거됨**(`20260627224406_drop_applications.sql`로 테이블·`application_status` enum drop). 실제 신청 동선은 **강사 매칭형 수강신청**(`/courses/[slug]/enroll`, 아래).

**수강신청(강사 매칭형) + 마이페이지**: 과정 상세 CTA "수강 신청하기"→ **`/courses/[slug]/enroll`**(`page.tsx` server, **로그인 가드**`redirect("/login?next=…")` + **휴대폰 인증·영문 이름 가드**(`ready=phone_verified_at && english_name`; 폰 미인증=마이페이지 인증 안내 배너, 영문 이름 미등록=마이페이지 등록 안내 배너 — 결과 SMS 발송 위해 폰 필수, 영문 이름은 수강신청 필수)) (+ **page가 `loadEnrollTeachers()` 호출해 전체 강사 prop 전달** — `createAdminClient()`로 `profiles.role='teacher'`+`centers`+`teacher_availability` 일괄 조회·그룹핑, 공개안전필드(이름·아바타·국적·성별·센터·bio)+`slots`+**`heldSlots`** 반환 — **`slots`는 `teacher_availability` − 그 강사의 확정(`status='승인'`·`결제완료`) enrollment 슬롯(`subtractSlots`)**; **`결제대기`/`신청`은 더 이상 차감 안 함**(슬롯 노출, 다른 학생도 신청 가능) → 대신 **`heldSlots`=다른 학생의 진행중(`신청`/`결제대기`) 슬롯**(본인 제외, `loadEnrollTeachers(userId)`)로 노출해 **겹치면 확인 단계 경고**(하드 차단 아님). 폰 미인증 시 빈 배열) → **`<EnrollWizard teachers>`**(`src/components/course/`, client, **3스텝**): ①**일정·강사 선택**(통합 라이브 패널) = `<EnrollScheduleField>`(**admin `TeacherAvailabilityFinder` 입력 모델 도입**: 요일 다중 토글(한국어) + 요일당 수업 횟수(1~8회, 1회=30분) + 시작 시각 → 종료 자동 계산, 선택값에서 **요일×범위 30분 슬롯 배열 파생**해 `onChange` 통지; **옵셔널 `initialSlots?` prop**=마운트 1회 역산(`deriveInitial`)해 요일·시작시각·횟수 프리필+`초기화`가 그 초기값으로 복원 — 미전달 시 빈 기본값(위저드·테스트 생성은 미전달, reschedule-remaining만 `currentSlots` 전달)) + **그 아래 라이브 강사 목록**(`matches=useMemo(teachers.filter(teacherHasAllSlots(t.slots,slots)))` — **버튼 없이** 일정 바꾸면 즉시 갱신, admin finder와 동일 클라 필터·추가쿼리 0; 선택 강사가 매칭서 빠지면 `selectedTeacher=matches.find(...)` 자동 무효) → 강사 카드 택일 ②**시작일 선택** = 달력(`src/components/ui/calendar.tsx`=react-day-picker, `locale={ko}`) — **선택 범위 제한 3중**: (a)`before:minDate`(=today+3, **오늘+3일(D+3)부터 선택**) (b)`after:maxDate`(=today+14, **2주 이내만**) (c)`!allowedDays.has(d.getDay())`(**1단계에서 신청한 요일만 활성** — 주간 반복 수업 첫 수업일이라; 1단계로 돌아가 요일 바꿔 기존 선택일이 빠지면 `useEffect`로 `setDate(undefined)` 초기화) + **오늘 셀 강조 표시**(`classNames.today` accent-blue ring) ③**확인** = 요약(선택 시간이 `selectedTeacher.heldSlots`와 `slotsOverlap`이면 **앰버 경고 노티스 + `AlertDialog` 문구를 "다른 학생이 먼저 신청…확정 시 취소될 수 있음" 경고로 교체**) + `AlertDialog` → `submitEnrollment`(`useActionState`). 액션 `submitEnrollment`(`src/app/courses/enroll-actions.ts`) 가드 순서: (1)`getUser` 로그인 (2)`profiles.phone_verified_at`+`phone`+`english_name` 확인(미인증/영문 이름 미등록 차단, 서버 authoritative) (3)slots(`isValidSlot` day0-6·min30배수·중복제거·cap)·`start_date`(`YYYY-MM-DD`·**KST 오늘+3일~오늘+14일(2주) 이내**·**요일이 신청 slots 요일 중 하나**, 클라 달력 제약 서버 재검증)·teacherId 검증 (4)`rateLimit(enroll:IP,5/10분)` (5)**예약 무결성 재검증**(service_role): **유효가용=`teacher_availability`−강사 `승인`·`결제완료`(확정) 슬롯(`subtractSlots`)** → `teacherHasAllSlots(effective,slots)`(`승인`·`결제완료` 겹침만 하드 차단; `결제대기`/`신청` 겹침은 **허용**=경고 후 신청, 클라가 `heldSlots`로 경고) + **학생 본인 시간 충돌**(본인 `신청`/`승인`/`결제대기`/`결제완료` enrollment 슬롯과 `slotsOverlap`이면 차단) (6)본인 세션 client `enrollments` insert(`student_id`=uid, slots jsonb, `teacher_name`/`student_name`/`student_english_name`/`student_phone` 스냅샷) (7)**best-effort 강사 알림 메일**(`sendEnrollmentNotificationToTeacher`, 강사 이메일은 `admin.auth.admin.getUserById`, 영문) (8)관리자 메일 + **센터 매니저 알림**(`notifyCenterManagerOfEnrollment` event `created`). **슬롯 공유 모델 `src/lib/availability.ts`**(`Slot{day,min}`·`slotKey`·`SLOT_MIN`·`GRID_START/END_HOUR`·`DISPLAY_DAYS`·`DAY_LABELS`/`DAY_LABELS_KO`·`ROW_MINS`·`teacherHasAllSlots`(set⊇)·**`subtractSlots(base,remove)`**(예약 소비 차감)·**`slotsOverlap(a,b)`**(시간 충돌)·`isValidSlot`·`summarizeSlots(slots,ko)` 요일별 요약·**`enumerateLessonSessions(start,slots,total)`**(날짜별 수업 열거, 화상수업 클래스 생성용)·`lessonEndDate`·`TOTAL_SESSIONS`·`fmtTime`) — 그리드·매칭·요약·중복예약·클래스 단일 소스. **승인 시점 가드**: `approveEnrollment`(`src/app/teacher/actions.ts`)가 **승인 시 곧바로 `결제대기`로 전환**; update 직전 이 강사의 다른 `승인`·`결제대기`·`결제완료` 슬롯과 `slotsOverlap` 검사(race-safe, 겹치면 차단). **결제 1단계(무통장 입금 수동 확인)**: 회사 계좌 입금이라 확인 주체=admin → `confirmPayment(id)`(`src/app/admin/actions.ts`, `requireAdmin` 후 **공유 코어 `finalizeEnrollmentPayment`(`src/lib/payment.ts`, server-only — admin 입금확인·학생 테스트결제 공용, ⚠️ `"use server"` 아닌 모듈이라 클라 직접 호출 불가)** 위임: 상태 `결제대기` 가드→`결제완료`+**날짜별 클래스 자동 생성**(`generateClassesForEnrollment`, payment.ts로 이동)+학생 SMS+**강사 결제확정 알림 메일**(`sendEnrollmentPaymentConfirmedToTeacher`, 강사 이메일은 `admin.auth.admin.getUserById`, 영문, best-effort), `/admin/enrollments`·`/teacher`·`/mypage`·`/mypage/classroom` revalidate). `결제완료`는 `승인`과 함께 **하드 확정**(가용에서 차감·신규 신청 하드 차단) — **단, 과정 종료 시 자동 해제**(남은 `예정` 수업 없음=마지막 수업 다음날부터): `loadEndedEnrollmentIds`(`src/lib/booking.ts`, server-only)가 종료된 `결제완료` id를 산출해 **가용 차감(`loadEnrollmentSlotsByTeacher`)·승인 충돌(`approveEnrollment`)·그리드 표시(`deriveBookedSlots` 소비처)** 모든 지점에서 제외(클래스 없는 레거시는 `lessonEndDate` 폴백). 입금 계좌 단일 소스 `src/data/payment.ts`(`PAYMENT_BANK`). **2단계 PortOne V2 카드 결제 도입됨**(테스트 채널, 위 `confirmPortonePayment` — 클라 결제창 성공→서버 REST 검증→`finalizeEnrollmentPayment`). **PortOne 웹훅 라우트 `src/app/api/payments/portone/webhook/route.ts`(첫 route handler, `POST`)**: 원문 body+헤더로 `@portone/server-sdk` `Webhook.verify(PORTONE_V2_WEBHOOK_SECRET,…)` 서명 검증(실패=400)→ **`Transaction.Paid`→`settlePortonePayment(data.paymentId,{role:"system"})`**(기록+확정+과오납 자동환불) / **`Transaction.Cancelled`·`PartialCancelled`→`fetchPortonePaymentRaw`로 누적 취소금액 조회→`refundEnrollmentPayment`**(콘솔/외부 환불도 DB 동기화=수강 취소·미래 수업 취소). **멱등**: 이미 처리/취소는 200(클라 콜백·이중 도착 안전), 일시오류만 500(PortOne 재전송 유도), 무관/미기록 결제 200 무시. 인증 세션 불필요(서명 검증), `proxy.ts` `updateSession`은 차단 안 해 통과. **취소(학생/admin) 시 슬롯 자동 해제**. **`/mypage`(탭 구조)**: 공유 `src/app/mypage/layout.tsx`(server, 로그인 가드+라벨바+웰컴 배너+`<MyPageTabs>` `src/components/mypage/`, client `usePathname` 활성표시) 아래 **4개 하위 라우트** — `/mypage`(기본 정보=회원정보 dl+`StudentProfileForm`)·`/mypage/enrollments`(`<StudentEnrollments>`) — ⚠️ **이 탭은 이제 2섹션**: 「**필리핀 화상영어 과정**」(`StudentEnrollments`, 카드 헤더 문구가 예전엔 "수강신청 내역"이었다) + 「**프렙 강좌**」(`MyPrepEnrollments`, `docs/prep.md`). 프렙 전용 탭(「프렙 수강」 `/mypage/prep`)을 여기로 흡수했다 — 회차 입장이 「내 강의실」로 옮겨간 뒤 남은 게 신청·입금 기록뿐이라 같은 일을 하는 탭이 둘이었다(`/mypage/prep`은 리다이렉트만 남음). 두 섹션은 **행 내용이 달라 병합하지 않고**(정규=결제 상세 아코디언 / 프렙=기간·시각·수강료·입금 안내) 껍데기·헤더·행 여백만 같은 값을 쓴다. 마이페이지 탭은 5개→**4개**.·`/mypage/classroom`(`<ClassroomList isTeacher={false}>` via `loadClasses`)·**`/mypage/rooms`(프렌딩 예약 — `<MyRoomReservations>`, `docs/friender.md` 참조)**. 각 page는 본인 데이터 조회(`enrollments` student_id / `profiles` / `classes` / `friender_room_participants`). **수강신청 내역**(`<StudentEnrollments>` `src/components/mypage/`, client) — 네이티브 `<details>` 아코디언(**확장 패널 dl=강사·주간 일정(`summarizeSlots(slots,true," / ")` 요일 `/` 구분)·수업 일정(시작~종료=`lessonEndDate`)·수업 횟수(`total_sessions`; page가 전달)·수강료(`priceLabel`)**, 헤더 강사명·시작일·상태 배지(`신청=승인대기`/`승인`/`결제대기=결제 대기`/`결제완료=결제 완료`/`거절`+사유/`취소`; **환불 건(`취소`+본인 payment cancelled/partial, page가 `payments_select_own`으로 조회한 `refunded`)은 "환불됨" 앰버 배지로 구분**)) + **`결제대기`면 확장 패널에 결제 수단 선택 UI**(`payMethod` state `null|bank|card` — **무통장/카드 중 택1 우선**, 선택 전엔 "🏦 무통장 입금 / 💳 카드 결제" 2토글만 노출·상호 전환 가능): **무통장 선택**→입금 안내 블록(과정 고정가 `priceLabel`(서버가 `getCourse(course).price` 해석)·`PAYMENT_BANK` 계좌·예금주) / **카드 선택**→결제 금액 요약 + **"카드로 결제하기" 버튼**→**PortOne V2 카드 결제**(`@portone/browser-sdk` `PortOne.requestPayment` 결제창=자체 확인 단계라 별도 다이얼로그 없음; `storeId`/`channelKey`=`NEXT_PUBLIC_PORTONE_*`, `totalAmount=row.priceKrw`(유효가격=`price_krw ?? COURSE_PRICE_KRW`, page가 전달)·`currency:"CURRENCY_KRW"`·`payMethod:"CARD"`·`customData:{enrollmentId}`·`redirectUrl`(모바일 대비)·**`paymentId`=영문·숫자만·≤32byte**(⚠️ PG별 제약 상이 — KPN(한국결제네트웍스)은 하이픈 등 특수문자 불가(`otherCharacters=""`)+`MxIssueNO` 최대 32byte라 `e{enrollment id 앞12hex}{Date.now().toString(36)}{rand3}` ~24자로 생성; 토스페이먼츠는 하이픈·장문 허용했으나 **PG 전환 시 paymentId 포맷 재검증 필수**))→성공(code null·paymentId) 시 `confirmPortonePayment(enrollmentId,paymentId)`(`src/app/mypage/actions.ts`) 서버 검증→낙관적 `결제완료`+toast. **`confirmPortonePayment`**: 로그인 가드→`GET api.portone.io/payments/{paymentId}`(`Authorization: PortOne {PORTONE_V2_API_SECRET}`(`.trim()` 없이), 서버 전용 시크릿; 401=시크릿 오류)→**`settlePortonePayment(paymentId,{role:"student"})`(`src/lib/payment.ts`)** 위임(검증(`getVerifiedPortonePayment`)→payments 기록(`recordPayment`)→`finalizeEnrollmentPayment` 확정→과오납 자동환불). 금액 클라 신뢰 금지. 실패 토스트에 `PortOne <status>·type` 노출(테스트 편의, ⚠️ **운영 전환 시 복원 2곳**: 이 문구 + `StudentEnrollments.tsx` 카드 결제 패널의 "개발용 테스트 결제입니다…" 안내문 제거). **웹훅 연동됨**(아래) — 창 닫힘/모바일 리다이렉트로 클라 콜백 유실돼도 서버가 확정 + **상태가 `신청`/`승인`/`결제대기`이면 "신청 취소" 버튼**(`결제완료`는 취소 불가=환불 별도)(`AlertDialog` 확인→`cancelEnrollment(id)` 낙관적 갱신+toast, 강사에 취소 이메일). `거절`/`취소` 행엔 미노출. `displayName` 우선순위 `성+이름(last_name+first_name 붙임) → email앞`. **회원정보 카드 필수화**: 이름(성·이름)·**영문 이름**·전화번호 모두 **필수**(전화번호는 **인증 완료=`phone_verified_at`**일 때만 충족) — `nameComplete`/`englishNameComplete`/`phoneComplete` 계산해 미완료 항목 있으면 카드 summary 아래 **안내 배너**(`bg-brand/5 border-brand/30 text-brand`, "회원 정보 완성을 위해 …항목을 완료해 주세요", 서버 렌더라 저장/인증 후 자동 갱신). **회원정보 아코디언 안에 `<StudentProfileForm>`**(`src/components/mypage/`, client, useActionState+controlled+toast) — 학생 본인 **성(last_name)·이름(first_name) 분리 입력**(성→이름 순, **둘 다 필수**: 라벨 빨강 `*`+native `required`, `<form id="student-profile-form" action={updateStudentProfile}>`) + **영문 이름(`english_name`, 수강신청 필수)**(`sanitizeEnglishName`로 onChange 시 비영문 즉시 제거, 영문자/공백/`.-'`만) + **전화번호 SMS 인증**(`<PhoneVerifyField>`, 이름 폼과 독립). **이름 폼은 강사 프로필과 동일한 보기/편집 모드**(`editing` state, 실수 저장 방지): 평소 이름 필드 `disabled`(`VIEW_TEXT="disabled:opacity-100 disabled:text-ink"`로 값 진하게)+버튼 **"수정"**(연필 아이콘)만, 클릭 시 필드 활성+**"취소"/"저장"** 노출. 진입 시 스냅샷(`snapshotRef`) → 취소는 이름 값 복원 후 보기 모드, 저장 성공(`state.ok`) 시 보기 모드 복귀. **`저장` 버튼은 폼 밖 카드 맨 아래 우측**(`form="student-profile-form"`로 이름 폼에 연결) — **전화 미인증이면 비활성**(`phoneVerified` state를 `PhoneVerifyField`의 `onVerifiedChange` 콜백으로 끌어올림, `disabled={pending || !phoneVerified}`). + **주소(선택 입력, `<AddressField>` `src/components/mypage/`, client)**: 이름 폼 안에 함께 배치돼 같은 "저장"으로 제출, 보기/편집 모드 연동(평소 비활성). **다음(카카오) 우편번호 검색 팝업**(`postcode.v2.js` on-demand 로드, `daum.Postcode` `oncomplete`→`zonecode`+`roadAddress||jibunAddress` 자동 채움→상세주소 focus) — 우편번호·기본주소 readOnly + 상세주소 수동, 필드 `name=postcode/address/address_detail`. **선택이라 `missing` 배너·필수 가드 제외**. 이름 액션 `updateStudentProfile`(`src/app/mypage/actions.ts`)은 **본인 세션 client로** `profiles` `first_name`·`last_name`·**`english_name`**·**주소 3필드(`postcode`·`address`·`address_detail`)**만 화이트리스트 update(RLS `profiles_update_own`+role/phone 트리거 보호라 service_role 불필요, `clean()`으로 trim/빈값 null/길이 제한, **영문 이름은 정규식 `/^[A-Za-z][A-Za-z .'-]*$/` 검증**) — **가드: 이름·영문 이름 누락 시 차단 + `phone_verified_at` 조회해 미인증 시 차단**(서버 authoritative, 클라 우회 방어)+`revalidatePath("/mypage")` — **phone은 미포함**(인증 플로우 전용). **전화번호 인증**(`<PhoneVerifyField>`, client, `src/lib/phone.ts` `normalizePhone`/`isValidKoreanMobile`/`formatPhone` 공유): 상태머신 (a)인증됨=`formatPhone`+✓배지+"번호 변경" (b)입력+"인증번호 받기" (c)6자리 코드+"확인", 60초 재전송 쿨다운. 액션 `sendPhoneOtp(phone)`(가드→번호검증→`rateLimit(otp-send:IP,5/10분)`+DB `last_sent_at` 60초 min-interval→6자리 코드 `sha256(uid:code)` 해시로 `phone_verifications` upsert(만료 3분)→`sendSms`(`src/lib/sms.ts`, Solapi)) / `verifyPhoneOtp(phone,code)`(만료·`attempts≥5`·번호·해시 검증, 실패 시 attempts++; 성공 시 **service_role로** `profiles` `phone`+`phone_verified_at` 기록+행 삭제). **인증 통과 시에만 phone 저장**(정책). `phone_verifications` 조회·쓰기는 `createAdminClient()`(service_role). **한국 관례라 표시는 `성+이름` 공백 없이 붙임**(강사 폼의 `[first,last].join(" ")` 영문 공백 방식과 의도적 차이). Navbar 링크 노출 규칙은 `docs/landing.md`의 Navbar 항목 참조(마이페이지는 `!isTeacher && !isCenterManager`일 때만). **mypage는 강사 지원과 무관**(학생 신청내역·회원정보 전용, 전부 한국어).
