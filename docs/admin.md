# admin 대시보드 (12탭)

> `CLAUDE.md`에서 분리된 상세 문서. 본문의 "위 …항목" 상호참조는 다른 `docs/*.md`일 수 있음(색인=CLAUDE.md).

**admin 대시보드** (Phase 5, `src/app/admin/`): `layout.tsx`(server)가 **role 가드**(`!user`→`/login?next=/admin`, `!isAdmin`→`/`) + `AdminNav`(client, **12탭**). **레이아웃=`max-w-[1400px]` 컨테이너 + `md:grid-cols-[200px_minmax(0,1fr)]` 2열**(좌 나브·우 `<main>`) — ⚠️ **`minmax(0,1fr)` 필수**(그냥 `1fr`이면 그리드 아이템 기본 `min-width:auto` 때문에 매니저 내부 `overflow-x-auto` 테이블(`min-w-[760px]` 등)이 컬럼을 밀어 페이지 전체가 가로로 넘침 — Footer 공지 칼럼 `min-w-0`과 동일 함정). `AdminNav`는 `ITEMS` 하나로 **두 벌 렌더**: 모바일(`md:hidden`)=가로 스크롤 pill, 데스크톱(`hidden md:block`)=**좌측 세로 사이드바**(`sticky top-[72px]`, Navbar 실측 65px 아래; 활성 `bg-ink text-white`). 탭 증가로 가로 pill 우측이 잘리던 문제 해결. 활성 판정은 **`pathname.startsWith(href)`**(`/admin/classes/[id]` 등 중첩 라우트에서 하이라이트 유지, href 간 접두 충돌 없음). admin page들은 자체 컨테이너 없이 매니저가 bare `<div>`로 시작 → **폭 조정은 이 layout 한 곳만**. 12 섹션(`/admin` 인덱스 `page.tsx`는 `redirect("/admin/enrollments")`) — **`enrollments/`(수강신청 관리)**, **`classes/`(화상수업 관리)**, `members/`, **`teacher-requests/`(강사 승인/삭제 + 강사 시간표 열람)**, **`friender-requests/`(프렌더 승인/거절 + 자격 해제)**, **`centers/`(센터 CRUD)**, **`revenue/`(매출 현황)**, **`profit/`(매출이익)**, **`simulation/`(목표 시뮬레이션)**, **`settlements/`(강사 정산 리포트)**, `youtube/`, **`notices/`(공지 사항 CRUD)**. **목표 시뮬레이션**: `simulation/page.tsx`(server)가 **오늘 기준 유효값**(센터별 `effectiveRate`(`rates.ts`)·`pgFeeRateAt`(`pgfee.ts`)·`fxRateAt`(`fx.ts`) + `COURSE_PRICE_KRW`·`TOTAL_SESSIONS`)만 파생해 `<SimulationManager>`(client)에 기본값으로 전달 → **"목표 월 매출이익 → 필요 월 신청 건수" 역산**(실적 대시보드가 과거라면 이쪽은 목표). 계산은 **`src/lib/simulation.ts`**(순수, 아래 헬퍼) 전담. UI=입력 카드(목표이익·수강료·회차·주당횟수·단가+통화·환율·PG율·카드비중·강사 주당슬롯)+**센터 단가 프리셋 칩**+건당 손익 KPI 6종+**운영 부하**(과정기간·동시 수강생(Little's Law)·월 수업수·주당 슬롯·필요 강사수)+목표 달성 시 월 손익표(=`/admin/profit` KPI와 같은 항목 순서로 대조)+민감도 표(회당단가/환율/수강료 축 `ToggleChip`, 현재값 행 하이라이트)+전제 노티스(고정비 미반영·정상상태·단가 미설정·이탈 미반영). **읽기 전용**(서버액션·마이그레이션·RLS 없음). ⚠️ 이익 정의·반올림은 `ProfitManager`와 **반드시 동일**(`vatSupply`·`pgFeeOf`·`Math.round(단가×환율)`) — 한쪽만 바꾸면 두 화면 숫자가 어긋남. **주당 횟수는 필요 건수를 바꾸지 않음**(건당 손익이 총 회차 기준=빈도 무관, 빈도는 기간·동시 수강생·강사 소요만 결정). + **뷰 토글 2종**(`ToggleChip`, 기본 `정상상태`): **정상상태**=위 균형점(운영 부하·월 손익표) / **월별 전개**=**수강료 선수취·정산 후지급 시점차 모델링**(`src/lib/cohort.ts`) — 유입 시나리오 입력(1개월차 건수·목표 도달 개월·신규 중단 시점 + 프리셋 3종)→ 정산 분산 막대(신청월 M+0/M+1/M+2 비중)·12개월 표(신규·공급가액·정산·**대시보드 이익** vs **실제 이익**·괴리 ▲·**선수금 잔고**)·수렴 시점 안내. 매출은 결제일·정산은 수업일 인식이라 램프업 초기엔 `/admin/profit`이 이익을 부풀려 보여주는데(주3회 32건 기준 1개월차 +23%, 3개월차 수렴), 그 과도구간을 눈으로 확인하는 용도. 민감도·전제 노티스는 두 뷰 공통. **매출 현황**: `revenue/page.tsx`(server)가 `payments` 전량 + `enrollments` 스냅샷(course·teacher·student·is_test) 병합 + settings 환율 조회(service_role, payments RLS 우회)→`RevenueRow[]`(**`failed`·`is_test` 결제 제외**, `created_at`→KST `kstDate` 파생)+rates로 `<RevenueManager>`(client, `src/components/admin/`) — **읽기 전용 모니터링**. `SettlementsManager`의 기간 토글(일간/주간/월간·이전/다음/오늘)·`aggregate`·`SortHeader`·`ToggleChip` 패턴 재사용(TZ 날짜 헬퍼 인라인 복제, 기준 필드 `kstDate`). 구성: **KPI 카드 7종**(총매출·환불·순매출(=amount−cancelled_amount, KRW 환산)·**공급가액·부가세·PG 수수료**·결제 건수) + **일자별 순매출 세로 막대 차트**(자체 SVG, 단일 시계열·per-bar 툴팁) + **분류별 집계 테이블**(과정별/**센터별**(강사 현재 소속 센터 역산 `enrollments.teacher_id→profiles.center_id→centers.name`, 미지정=미지정 센터)/결제수단별/학생별 `ToggleChip`, 순매출 내림 기본정렬) + **결제 트랜잭션 목록**(결제일·학생·과정·수단(`payMethodLabel`)·금액·순액·상태 배지(결제완료/부분환불/환불)·영수증 링크, 검색+상태 필터) + **CSV 내보내기**(클라 Blob, BOM으로 엑셀 한글 대응). **부가세(VAT 10%) 내역**: 저장 결제금액=부가세 포함 합계라 **공급가액=`round(합계/1.1)`·부가세=합계−공급가액**을 역산(`src/lib/vat.ts` `vatSupply`/`vatAmount`/`vatBreakdown`, 통화무관)해 KPI·분류표·거래목록·CSV 전반에 표시(순매출=공급가액+부가세 불변식; 로더가 **행별 반올림 후 합산**해 감사 정합). **PG 수수료**: 카드 등 **PG 경유 결제**(`method!=='bank_transfer'`, 무통장=0)에 대해 **순결제액(결제액−환불액) × 결제일 유효 율**(`src/lib/pgfee.ts` `pgFeeRateAt`/`pgFeeOf`/`isPgPayment`, 적용일 이력 `pg_fee_schedules`, 율=부가세 포함 총 청구율)을 로더가 행별 계산(`RevenueRow.pgFeeRate`·`krwPgFee`) → KPI 카드·분류표·거래목록·CSV에 표시(**순매출·공급가액·부가세 정의는 불변**=표시만). 이력 편집은 `/admin/centers` "PG 수수료 설정"(`PgFeeHistoryEditor`, `ExchangeRateHistoryEditor` 단일값 클론; 액션 `addPgFeeSchedule`/`updatePgFeeSchedule`/`deletePgFeeSchedule`, `requireAdmin`+service_role). 환불 등 결제 액션은 여기 없음(수강신청 탭에서 수행). **로더 추출**: payments+enrollment+센터역산+환율 조립은 **`loadRevenueRows(admin)`(`src/lib/revenue.ts`, server-only, 매출이익과 공용)**로 이관(`RevenueRow`에 `teacherId`·행별 사전환산 `krwAmount`/`krwCancelledAmount`·**부가세 역산 `krwSupply`/`krwVat`/`krwCancelledSupply`/`krwCancelledVat`** 포함 — 결제일 기준 날짜별 환율). **매출이익**: `profit/page.tsx`(server)가 `loadRevenueRows`+`loadSettlementRows`+`center_settlements`(확정 원장) 로드→`<ProfitManager>`(client) — **매출이익 = 공급가액(매출÷1.1) − 정산 − PG 수수료**(`RevenueRow.krwPgFee` 합, KPI·분류표·차트 툴팁 반영; 징수 부가세는 납부 대상이라 이익 베이스에서 제외·정산은 부가세 없는 인건비, **이익률=매출이익÷공급가액**; `ProfitManager`가 `RevenueRow`의 부가세 필드 그대로 소비=로더·page·스키마 무변경). 월간/년간 토글(주간 없음=정산 월 단위)·테스트 포함 토글. KPI 7종(매출(부가세 포함)·공급가액·부가세·**PG 수수료**·정산·매출이익·이익률) + **공급가액 vs 정산 그룹 막대 차트** + 분류 표(센터별/과정별/강사별, 병합; 공급가액·부가세·정산·매출이익·이익률 컬럼). **정산 확정 전 '예상치'/확정 후 '확정' 배지**(정산·매출이익 카드): 정산을 **(센터,월) 버킷별로 블렌딩** — 확정 레코드(`center_settlements`) 있으면 실지급액(`total_krw`), 없으면 추정치(Σ `krwPerSession`); 전 버킷 확정+센터 미지정 없음이면 '확정'(초록)·아니면 '예상치'(앰버). 분류 표는 추정 원가 유지(과정·강사별은 센터 확정액 분해 불가, tfoot도 추정 합=표 내부 정합, KPI와 차이는 안내 문구). 마이그레이션 불필요. **수강신청 관리**: `enrollments/page.tsx`(server)가 전체 `enrollments`(**`teacher_id` 포함**, `created_at desc`) service_role 조회→`<EnrollmentsManager>`(client, `src/components/admin/`) — 상태 필터 탭(전체/신청/결제대기/결제완료/승인/거절/취소/**환불**)(**환불=DB 상태 아님, 파생**: `status='취소' && payment.status∈{cancelled,partial_cancelled}` — 환불 건을 `취소`에서 분리해 앰버 "환불" 배지·별도 필터·카운트로 표시. 학생 마이페이지="환불됨"·강사="Refunded" 동일 파생, 공통 배지색 `bg-[#FFF4E5] text-[#B45309]`. 강사 화면은 payments RLS 미허용이라 page가 `createAdminClient()`로 환불 id 조회)+검색(학생·강사·과정)+**정렬 테이블**(`SortHeader` 학생/강사/**센터**(강사 현재 소속 센터 역산 `teacher_id→profiles.center_id→centers.name`, 미지정 폴백·검색 포함)/시작일/신청일; **시작일 열 아래 `summarizeSlots(slots)` 요일·시작시각 요약**) + **prop→state 동기화**(`useEffect(()=>setRows(enrollments),[enrollments])` — 테스트 신청 생성 등 `router.refresh()` 후 목록 즉시 반영) + **시간충돌 표시**(같은 `teacher_id` 진행중 신청끼리 슬롯 겹치면 `overlappingIds`로 "시간 겹침" 칩+좌측 앰버 액센트, 전체 rows 기준 계산이라 필터 무관). **행 클릭→`EnrollmentDetailModal`**(커스텀 오버레이 패널, **주간 일정**(`summarizeSlots`)·**수업 일정**(시작~종료=`lessonEndDate(start,slots,total_sessions??24)`)·**수업 횟수**(`total_sessions??24회`)·**센터**·전화·**수강료**·사유 표시 — `priceLabel`은 page가 `price_krw != null ? formatPrice(price_krw) : getCourse(course).price`로 파생(+`priceKrw` 숫자)해 `AdminEnrollment`에 실어 전달, 학생 결제대기 패널과 동일 유효가격). **입금 확인(결제 1단계)**: 모달에서 `결제대기` 행에 **"결제 확인"** 버튼+`AlertDialog`(입금 대조용 **"입금 확인 금액: {priceLabel}"** 표기)→`confirmPayment(id)`(`actions.ts`, 상태 가드→`결제완료`+학생 SMS+강사 결제확정 알림 메일). **개입=강제 취소**: 모달에서 `신청`/`승인`/`결제대기` 행에 사유 입력+`AlertDialog`→`adminCancelEnrollment(id,note)`(`requireAdmin()`→service_role, 상태 가드→`취소`+`teacher_note="[관리자] …"`, 학생 SMS + **센터 매니저 알림**(event `cancelled`, `cancelledBy:"admin"`) best-effort, `/admin/enrollments`·`/teacher`·`/mypage` revalidate). **환불(카드 결제)**: page가 `payments`(enrollment_id별 최신) 병합해 `AdminEnrollment.payment{paymentId,status,amount,cancelledAmount}` 전달 → 모달에서 `결제완료`+`payment.status='paid'`면 **"환불" 버튼**(사유 필수+환불 금액(비우면 전액·부분 가능)+`AlertDialog`)→`refundPayment(id,{reason,amount?})`(`actions.ts`, `requireAdmin`→payments 조회(`method` 포함)→**무통장(`method='bank_transfer'`∥`payment_id`가 `bank-` 접두)이면 `cancelPortonePayment` 건너뜀**(PG 취소 불가=실제 환불은 계좌 수동 송금, 모달에 "계좌로 직접 송금" 안내)·카드면 PortOne 취소→`refundEnrollmentPayment`=수강 취소·미래 수업 취소·학생 SMS·강사 메일·`payment_refunded` 이벤트). 환불된 결제는 `환불됨: ₩N (부분)` 배지. `거절`/`취소`는 read-only. **[개발용] 테스트 수강신청 생성**: `/admin/enrollments` 상단 `<TestEnrollmentCreator>`(`src/components/admin/`, props teachers·courses·**students**(후보=가입 회원 중 강사·이메일 미인증 제외, `page`가 `listUsers`+`profiles` merge로 `{email,label}` 구성)·본인 이메일) — 모달에서 **학생 `<select>`**(가입 회원 드롭다운, 라벨 `이름 (이메일)`, 기본=본인 계정; 선택값 이메일을 액션에 전달, `createTestEnrollment`는 기존대로 `findUserIdByEmail` resolve)·**과정(등록 5종 `<select>` + "직접 입력…" 센티넬 `__custom__` 선택 시 한글·영문 과정명 2칸 입력, 커스텀은 `course="custom"` 슬러그+`course_title`/`course_english_title` 스냅샷)**·`EnrollScheduleField`로 요일·시간 선택→**그 시간에 가능한 강사 라이브 검색(`teacherHasAllSlots`, 실 위저드와 동일 패턴, `loadEnrollTeachers` 카드)·택일**·**시작일 자유**·**수업 횟수(1~60)**·**수강료(원, 기본 `COURSE_PRICE_KRW`)** 입력→`createTestEnrollment`(`actions.ts`, `requireAdmin`, **실 동선의 D+3/D+14·폰·영문 가드 우회**, service_role insert: status `신청`·`total_sessions`·**`course_english_title`·`price_krw`**·`is_test=true`, 강사/학생 스냅샷). 이후 승인→결제확인은 일반 흐름과 동일(결제확인 시 클래스 개수=`total_sessions ?? TOTAL_SESSIONS`). 생성분은 `EnrollmentsManager` 목록에 **"테스트" 칩** + 모달 **"테스트 삭제"**(`deleteTestEnrollment`, `is_test=true`만 하드삭제→classes FK cascade 동반 제거). **화상수업 관리(2단계)**: **①상위 `classes/page.tsx`**(server)가 전체 `classes`를 좁은 select로 조회→**`enrollment_id`별 JS 집계**(과정 인스턴스=수강신청 1건당 1행, 클래스 스냅샷으로 과정·학생·기간·진행 카운트 산출(**대표 강사는 `enrollments.teacher_name`(현재 담당, 강사 대체 반영)로 override** — 과거 회차 스냅샷은 옛 강사라 부적합); `upcoming`=**시간 기반** `예정 && kstDateMinToMs(session_date,lessonEndMin(end_min))≥now`(강사/수강생 강의실과 동일 — 수업 종료 시각 지나면 `done`, 날짜 단위 아님)/`done`/`cancelled`/`makeup`)→`<ClassEnrollmentsManager>`(client) — 검색(학생·강사·과정)+상태 필터(전체/진행중(`upcoming>0`)/완료/**환불**)+정렬(과정/학생/강사/시작일)(**환불**=page가 enrollments select에 `status` 추가해 `취소`/`거절`이면 `refunded` 플래그 → 배지 "환불"(앰버)·별도 필터, `진행중`/`완료`에서 제외 — 예정 수업 전무해 `upcoming=0`이면 무조건 "완료"로 오표시되던 문제 교정), 행=과정·학생·강사(**이름 아래 소속 센터 역산**)·기간(`firstDate~lastDate`+**아래 주간 요일·시작시각 요약** `schedule`=**`enrollments.slots`(현재 주간 템플릿, 일정 변경 시 갱신) → `summarizeSlots`** (page가 map 구성 후 enrollment id들의 `slots` 조회해 파생; slots 비면 클래스 session_date 파생 `slotSets` 폴백 — **과거 회차가 옛 스케줄로 오염시키던 문제 방지**)) + **지금 진행 중 라이브 표시**(집계가 `liveStartMs`(가장 이른 미종료 회차 입장 15분 전)·`liveEndMs`(레슨 종료) 파생 → 클라 **1분 틱** `isLive(now)`면 상태 셀 `bg-cta animate-pulse` "● 수업 중" 배지 + **라이브 과정 항상 최상단 안정 정렬**(사용자 컬럼 정렬 위에 고정); 상세 뷰 "진행중" 기준과 동일)·진행바(done/active)+예정·취소·보강 칩, **행 클릭→`/admin/classes/[enrollmentId]`**(router push). **목록/주간 뷰 토글**(`view` state, 세그먼트 pill 기본 목록): **주간 뷰=`<ClassWeekGrid>`**(`src/components/admin/`, client — page가 `classes` per-session 행에서 **진행 예정만**(취소·연기 제외, 보강 포함) `AdminSession[]`(enrollment·과정·강사·학생·`sessionDate`·`start/endMin`·`isMakeup`·**`weekdays`**=과정 주간 요일 요약 "월/수/금") 매핑해 prop 전달, 신규 쿼리 없음; **`weekdays`는 목록 뷰 `schedule`과 동일 소스(`enrollments.slots`)를 `summarizeWeekdays`로 파생**(slots 비면 `slotSets` 폴백)). 학생/강사 `WeekSchedule` 격자 스캐폴딩 차용(월~일 06:00~24:00 `ROW_MINS`·주 네비·주말색·오늘 강조·`mondayOf`)하되 **블록 대신 30분 슬롯 격자** — admin은 여러 수업이 겹쳐 수업당 블록이 안 맞음: `byDaySlot`(요일칼럼×`rowMin`→그 슬롯에 겹치는 세션들, 60분 수업=2칸 점유)로 각 칸에 **수업 개수** 표시(개수별 색 강도 1→`bg-accent-blue-soft`·2→`/30`·≥3→`/50` 흰글씨, **지나간 슬롯**(`now≥kstDateMinToMs(date,min+SLOT_MIN)`)은 배경 `bg-surface`·칸 `bg-rule/60` 회색 음영, 단 **미진행 종료 수업**(세션 종료 && `conductedOverride ?? !!conductedAt`=false)이 하나라도 있는 칸은 붉은색 `bg-brand/15 text-brand`로 강조) → 칸 클릭 **`SlotModal`**(커스텀 오버레이 `z-[110]`/패널 `z-[120]`·Esc·scroll lock, 그 시간 수업 목록=시간·과정(**과정명 옆 주간 요일 `weekdays` "월/수/금"** — 개별 회차 시각은 이미 노출돼 요일만, 시간 제외)·강사·학생·**센터**(강사 소속 센터 역산)·보강 배지·**대체 배지**(`teacher_reassigned_at` 있으면, `bg-cta/10 text-cta`, `AdminSession.teacherReassignedAt`) + **종료 수업엔 진행여부 배지**(`now≥lessonEnd`이면 유효 진행여부 `conductedOverride ?? !!conductedAt`로 초록 "진행됨"/brand "미진행", `ClassesManager` "진행" 컬럼·정산과 동일 기준 — `AdminSession`에 `conductedAt`/`conductedOverride` 포함), **행 클릭→`/admin/classes/[enrollmentId]`** 드릴인) + **수업 일정(`AdminSession.period`=`scheduleDateRange`)·수업 횟수(`totalSessions`)** 표시. **`ClassWeekGrid`는 `readOnly`·`onReassign?` prop으로 센터 매니저 재사용**: `readOnly`면 행 클릭 드릴인 대신(센터 매니저는 admin 상세 접근 불가) `onReassign`+**미래 회차에 "강사 대체" 버튼 인라인**(→`ReassignModal`); `AdminSession`에 `classId`·`teacherId`·`sessionNo`(대체 대상) 포함, admin은 `readOnly`/`onReassign` 미전달=기존 드릴인 유지. ⚠️ per-session 무제한 로드(기존 목록 쿼리 공유, PostgREST ~1000행 cap) — 대량 시 주 단위 날짜 필터가 scale path. ⚠️ JS 집계는 중규모까지 적정 — 대규모 시 **DB 집계 뷰/RPC**로 전환(상위 쿼리를 과정 수만큼만 로드)이 scale path. **②상세 `classes/[enrollmentId]/page.tsx`**(server)가 `eq("enrollment_id")`로 **scoped 조회**(0행=`notFound()`) + **이 enrollment의 `enrollment_events`(감사 로그) `created_at desc` 조회**→`<ClassesManager>`(아래에 읽기 전용 **`<EventTimeline events>`**(`src/components/admin/`, client)로 변경 이력 렌더=생성·승인·결제·연기·강사 대체·일정 변경 등 최신순, 행=KST 시각·이벤트 라벨·`detail` 요약(`summarize()` 이벤트별 매핑; **일정 변경=`oldSlots→newSlots`를 `summarizeSlots`로 "화 12:30 · 목 12:30 · 토 12:30 → 월 12:30 · 수 12:30 · 금 12:30" 표시**, 강사 대체=old→new 강사명 등)·액터 역할/이름; `EVENT_META` 타입→한국어 라벨/색 매핑)(client, `title`/`subtitle`/`backHref` props로 헤더+"← 목록으로"; **`subtitle`은 page가 `currentSlots`(=`enrollments.slots`)에서 파생한 주간 요일 요약(예 "월수금")을 강사~기간 사이에 포함**(slots 비면 정규 수업 날짜 `dowOf` 폴백 — 일정 변경 반영·과거 회차 오염 방지; **강사명·`teacherSlots`(남은 일정 일괄 변경 검증용 가용)는 `enrollments.teacher_id/teacher_name`(현재 담당) 기준** — `first.teacher_id`(과거 회차 스냅샷=옛 강사) 대신, 강사 대체 후 반영)=`강사 … · 센터 …(강사 현재 소속 센터 역산) · 요일 월/수/금 · 기간 … · 총 N회`) — **달력/목록 뷰 토글(달력 기본)**(`ClassAdminCalendar`=`ui/calendar` 월간, 예정/완료/취소 색·오늘·주말·진행 마커 ●진행됨/▲미진행, 날짜 클릭→그날 회차 목록→행 클릭 시 기존 `ClassDetailModal`) + 상태 필터(전체/예정/완료/취소)+검색+정렬 테이블(`SortHeader` 날짜/강사/회차 + 시간·**피드백** 컬럼, **과정·학생 컬럼은 제거**(한 enrollment 스코프라 중복, 헤더 title/subtitle에 표시), **날짜 셀·상세 모달 날짜 필드는 `formatDateKo`로 요일 병기**(`2026-07-04 (토)`, 정렬키는 문자열 날짜 그대로), 시간=`timeRange(start,end)`=시작~**레슨 종료**(`lessonEndMin`=end−5 → :25/:55, 내 강의실 등과 표시 통일; 슬롯 점유·편집 `<select>`는 30분(end) 그대로), `is_makeup`이면 "보강" 칩; **강사 열엔 `teacher_reassigned_at` 있으면 "대체" 칩(cta) — select·`AdminClass`에 `teacher_reassigned_at` 포함, in-modal 강사 대체(`doReassign`) 낙관 갱신도 스탬프**). **상태 배지·필터·카운트는 시간 기반 파생**(`displayStatus(r,now)`: 취소면 취소, `now≥kstDateMinToMs(session_date,lessonEndMin(end_min))`면 **완료**, `now≥`입장가능(`start_min−ENTRY_LEAD_MS`=시작 15분 전)이면 **진행중**(`bg-cta`+`animate-pulse` 필드 배지, 필터 탭 포함, 입장 시간창과 일치), 아니면 예정 — DB `status`엔 완료 없음, 1분 틱; 강의실·상위 목록과 동일 기준. 배지색 예정=파랑·완료=회색·취소=빨강). **피드백 컬럼="보기" 버튼**(`Eye`, 피드백 있으면 활성·없으면 disabled, `stopPropagation`으로 행 클릭과 분리)→`FeedbackModal`(피드백 전용 읽기 모달, 커스텀 오버레이 `z-[110]`/패널 `z-[120]`·Esc·scroll lock, 작성일+전문 `whitespace-pre-wrap`). 모달 편집(일정변경/취소/강사대체)은 DB `status='예정'` 기준이되 **회차 상태별로 노출을 좁힘**(`ClassDetailModal` 파생: `isEnded=displayStatus==="완료"`(레슨 종료 시각 지남)·**`hasStarted=now≥kstDateMinToMs(session_date,start_min)`(실제 시작 시각 지남)**·`isConducted=`conducted_override ?? !!conducted_at``(**유효 진행여부**=정산·마커와 동일 기준; 자동 진행됨이라도 admin이 미진행 override 시 미진행 취급)): **①미시작(미래 예정) = 강사 대체+연기/취소 2종 모두**, **②시작 후(진행중·종료 미진행) = 강사 대체 숨김+"이미 시작된 수업이라…" 안내문, 연기·취소만**(**강사 대체는 `!hasStarted` 게이팅** — 시작 시각 지나면 숨김; 노쇼 사후 처리 연기/취소는 진행중·종료 후에도 가능), **③진행됨(`isConducted`) = 완전 읽기전용**("진행 완료된 수업입니다"). + **과정 전체 종료 시 편집 자체 미노출**(`courseEnded`=ClassesManager가 `loadEndedEnrollmentIds` 기준을 클라 파생: 남은 미래 `예정` 수업(`now<kstDateMinToMs(date,lessonEndMin(end))`)이 하나도 없으면 종료, 단일 enrollment 스코프라 rows 전체가 한 과정). **행 클릭→`ClassDetailModal`**(커스텀 오버레이 `z-[110]`/패널 `z-[120]`): 위 노출 규칙에 따라 액션(미래 예정=2종) — ⓐ**수업 연기/취소**(라디오 3모드+"적용" 버튼, "남은 연기 N/6" 표시)→`adminCancelClass(id,reason)`(`reason`: **`student`**=수강생 요구 연기(보강 O·`cancel_reason='student'`로 남은 연기 차감)·**`company`**=강사/회사 사유 연기(보강 O·미차감)·**`cancel`**=수업 취소(보강 X); 상태 `예정` 가드+**유효 진행여부='진행됨'이면 거부**(`conducted_override ?? conducted_at`, 종료-미진행·미래 예정은 통과)→`취소`+`cancel_reason` 기록+연기 모드만 `createMakeupClass`+강사 이메일·**센터 매니저 알림**(`class_postponed`/`class_cancelled`) best-effort; **6회 한도·1시간 컷오프 하드 미적용**=student 모드 남은 연기 0이어도 경고 후 진행, 차감은 카운트로 자연 반영. `postponedCount`(=학생 연기 수) prop으로 남은 연기 표시) ⓑ**강사 대체**(강사 사정으로 특정 회차만 다른 강사로 교체, 상세 page가 `loadEnrollTeachers()`로 전체 강사 카드 prop 전달→모달이 이 수업 요일·시간에 가용한 강사만 `teacherHasAllSlots` 클라 필터·원 강사 제외해 `<select>` 노출)→`adminReassignClass(id,newTeacherId)`(**공유 코어 `reassignClassCore` `src/lib/reassign.ts`에 위임** — 센터 매니저 `centerReassignClass`와 공용, admin은 센터 제약 없음; 상태 `예정` 가드+**시작 시각 전만**(`now≥kstDateMinToMs(session_date,start_min)`이면 차단 — 이미 시작/종료 수업 대체 방지, 클라 `!hasStarted`와 동일 기준)+새 강사 `role='teacher'`·**주간 가용 검증**+**같은 날짜 새 강사 시간 충돌 차단**→`teacher_id`/`teacher_name`+`teacher_reassigned_at`+**`original_teacher_id`(=덮어쓰기 전 원 강사, `cls.original_teacher_id ?? oldTeacherId` coalesce로 최초값 보존)** update; 시간·학생 불변이라 입장 시 zoom은 `teacher_id`로 실시간 조회돼 새 강사로 라우팅·새 강사 My Classroom 노출/원 강사 제거 — **마이그레이션 불필요**; best-effort 학생 SMS+기존/새 강사 메일 `sendClassReassignToOld/NewTeacher`). 셋 다 `/admin/classes`·`/admin/classes/[enrollmentId]`·`/teacher`·`/mypage` revalidate. `취소` 행은 read-only. **남은 일정 일괄 변경(과정 단위, 담당 강사 유지·요일/시간만)**: 상세 헤더에 **"남은 일정 일괄 변경"** 버튼(미래 `예정` 수업 있을 때만; 상세 page가 `enrollments.slots`(현재 패턴)+담당 강사 `teacher_availability`를 `currentSlots`/`teacherSlots`/`enrollmentId` prop으로 전달)→`RescheduleRemainingModal`(`EnrollScheduleField`로 새 요일·시각(**`initialSlots={currentSlots}`로 현재 주간 스케줄 프리필** — 진입 시 요일·시작시각이 현재값으로 선택돼 시작일만 바꿔 즉시 제출 가능) + 적용 시작일 date input(**KST 내일(D+1)~오늘+7일(D+7)** — 오늘 배치 시 과거 시각·같은 날 중복 혼선 방지 + 과도한 미래 이동 방지, `min`/`max`+`canSubmit` 범위 가드) + 강사 가용 밖이면 경고·제출 차단)→`adminRescheduleRemaining(enrollmentId,newSlots,effectiveDate)`(`actions.ts`, `requireAdmin()`→**적용 시작일 범위 재검증**(`effective <= todayKst()`∥`effective > addDaysStr(todayKst(),7)` 거절)→**미래 `예정` 클래스만** 대상(`session_no` 오름차순, 과거·완료·취소 유지)+강사 주간 가용 `teacherHasAllSlots` 검증→`enumerateLessonSessions(effectiveDate,newSlots,남은수)`로 **1:1 remap**(행 id·회차 유지=진행률 불변, session_date/start/end만 update)+**같은 강사·학생의 다른 enrollment `예정` 클래스와 시간 충돌 차단**+`enrollments.slots`=newSlots 갱신(향후 가용/요약 반영) + **통보 3종 best-effort**: 학생 SMS(한국어 새 일정)·**담당 강사 메일**(`sendRemainingRescheduleToTeacher`, 이전→새 일정)·**센터 매니저 알림**(`remaining_rescheduled`)). 성공 시 `router.refresh()`; ClassesManager는 `useEffect(()=>setRows(classes),[classes])`로 새 prop 동기화. `/admin/classes`·`/admin/classes/[id]`·`/admin/enrollments`·`/teacher`·`/mypage` revalidate. **남은 수업 전체 강사 대체(과정 단위, 강사 중도 하차 — enrollment 이관)**: 상세 헤더 "남은 일정 일괄 변경" 옆 **"남은 수업 전체 강사 대체"** 버튼(미래 `예정` 있을 때만)→`ReassignRemainingModal`(남은 클래스 요일·시각 union(rows 파생)에 가용한 강사만 `teacherHasAllSlots` 필터·원 강사 제외 `<select>` + **적용 시작일 필수(D+1 기본, D+1~D+7)**)→`adminReassignRemaining(enrollmentId,newTeacherId,effectiveDate)`(`actions.ts`, `requireAdmin()`→새 강사 `role='teacher'` 검증→남은 미래 `예정` 클래스(`session_no` asc)→**주간 패턴 `weekly`=`enrollments.slots` 정규화(비면 남은 클래스 union 폴백)**→새 강사 `teacherHasAllSlots(weekly)` 검증→`enumerateLessonSessions(effectiveDate,weekly,남은수)`로 재배치 타깃 산출→**새 강사 OR 학생의 다른 enrollment `예정`과 target 날짜·시간 충돌 차단**→남은 클래스 `teacher_id`/`teacher_name`+`session_date`/`start_min`/`end_min`+`teacher_reassigned_at` update(원자 가드)+**`enrollments.teacher_id`/`teacher_name` 이관(slots 불변)**; 과거·완료·취소는 원 강사 보존; best-effort 학생 SMS+새/기존 강사 메일 `sendCourseReassignTo*`+**센터 매니저 알림**(`remaining_reassigned`, `teacherIds=[old,new]`); `/admin/classes`·`/admin/classes/[id]`·`/admin/enrollments`·`/teacher`·`/mypage` revalidate). **⚠️ 단일 대타 `adminReassignClass`(1회성, enrollment 유지·날짜 불변)와 구분**: 벌크는 enrollment 이관+시작일부터 재배치. **센터 관리**: `centers/page.tsx`(server)가 `from("centers")`(name·sort_order·**price_per_session·price_currency·manager_name**) + `settings` 외화 환율(`FOREIGN_CURRENCIES` 키 `.in()`→`ratesFromSettings`→`Rates`) 조회→`<CentersManager>`(client, `useRouter.refresh()`+낙관 토스트). **추가 폼**(센터명·**센터 매니저(선택)**·통화 `<select>`·단가)+**테이블 목록**(`#`·센터명·**매니저 계정**(`manager_id`→이메일 주 표시, `manager_name` 부 표시; **`manager_id` 없으면 "미지정"**=이름만 있어도 계정 미지정이면 미지정, page가 listUsers `id→email` 맵 파생)·회당 단가·관리, `CurrentTeacherTable` 스타일)+**행 "수정"→`<CenterDetailModal>`**(`src/components/admin/`, 커스텀 오버레이 패널 `z-[110]`/`z-[120]`, Esc·scroll lock; 센터명·**매니저 표시이름**·**매니저 계정 `<select>`**(미지정 sentinel+후보(page가 listUsers+profiles로 조립)=지정 시 그 회원이 '센터 관리' 권한, `centers.manager_id`) 편집(`updateCenter(id,name,managerName,managerId)`는 이름·매니저명·매니저계정만·계정 존재검증; **통화·단가는 공유 `<RateHistoryEditor scope="center">` 적용일 이력 CRUD**), 인라인 편집 폐지)+**환율 설정 카드**(`FOREIGN_CURRENCIES` 반복 → 통화별 **`<ExchangeRateHistoryEditor>`**(적용일 이력 CRUD): 단발 설정 폐지·적용일별 환율 관리. 정산·매출이익은 **수업/결제 날짜의 환율**로 환산돼 환율 변경이 과거 집계에 소급 안 됨. `addExchangeRateSchedule`/`updateExchangeRateSchedule`/`deleteExchangeRateSchedule`(admin, `exchange_rate_schedules` upsert + **`syncFxCache`**=오늘 유효 환율을 `settings.<code>_to_krw`에 동기화 → 기존 `ratesFromSettings` 현재값 소비처 무변경). 구 `updateExchangeRate`는 폐지). **매니저 이름(`manager_name`)은 선택 입력**(nullable, 센터명만 필수), 센터 관리 CRUD로만 관리(타 소비처는 `id,name`만 select). **단가 통화 단일 소스 `src/data/currencies.ts`**: `CURRENCIES`(KRW `₩`/PHP `₱`/**USD `$`**)·`CURRENCY_VALUES`(검증)·`DEFAULT_CURRENCY`·`normalizeCurrency`·`formatPrice(amount,code)`(예 `₩30,000`/`₱1,500`/`$100`)·**`FOREIGN_CURRENCIES`**(원화 환산 가능 외화={code,settingKey,symbol,rateLabel}: PHP→`php_to_krw`·USD→`usd_to_krw`)·**`Rates`**(통화코드→1단위당 원 맵)·**`krwEquivalent(amount,code,rates)`**(외화면 `rates[code]>0`일 때 환산, KRW/미설정이면 null)·**`ratesFromSettings(rows)`**(settings key/value→rates, **현재값 스냅샷**). 외화 단가는 목록·모달·정산에서 `≈ ₩환산` 함께 표시. **⚠️ 환율 = 적용일 이력(`exchange_rate_schedules`)이 진실 소스**, `settings.<code>_to_krw`는 현재값 캐시(`syncFxCache` 동기화). **날짜별 환산은 `src/lib/fx.ts`**(`FxRow`·`fxRateAt(rows,currency,date)`·`toKrwAt`·`krwAtOrNull`·`fxRatesAt`) — 정산(`loadSettlementRows`가 `krwPerSession`)·매출(`loadRevenueRows`가 `krwAmount`) 로더가 **행별 날짜 환율로 사전 환산**해 원화 집계를 다날짜 정확하게(추정치 배지 등은 매출이익 항목). 액션 `addCenter(name,price,currency,managerName)`(초기 단가는 `rate_schedules`에 effective=오늘 1행 생성)/`updateCenter(id,name,managerName)`(이름·매니저만; 단가는 이력 CRUD)/`deleteCenter`/**`addRateSchedule`/`updateRateSchedule`/`deleteRateSchedule`**(정산 단가 적용일 이력 CRUD + `syncRateCache`)/**`addExchangeRateSchedule`/`updateExchangeRateSchedule`/`deleteExchangeRateSchedule(currency|id,rate,effectiveFrom)`**(환율 적용일 이력 CRUD + `syncFxCache`, `FOREIGN_CURRENCIES`·양수·`numeric(12,4)` 검증)(`actions.ts`, `requireAdmin()`+service_role, `revalidateCenterConsumers()`로 `/admin/centers`·`/teacher/apply`·`/teacher`·`/admin/teacher-requests` 갱신). `normalizePrice(raw,currency)`(빈값/음수→null; **USD는 소수 첫째자리 반올림, 그 외 통화는 정수 내림** — 단가 입력 `step`도 USD=0.1·그외=1)·`normalizeText`(매니저명 trim 후 빈값→null). **`centers.price_per_session`은 `numeric(12,2)`**(20260703200612, USD 소수 지원) — numeric은 supabase-js가 문자열로 반환할 수 있어 두 소비처 page(`centers`·`settlements`)에서 `Number()` 강제(null 보존). 센터 삭제 시 FK `on delete set null`로 참조 강사는 자동 None. (구 `availability/` "강사 시간표" 탭은 강사 관리 `TeacherInfoModal`로 통합·제거됨.) **강사 정산 리포트(읽기 전용)**: `settlements/page.tsx`(server)가 **진행 수업만**(`classes` `.or("conducted_override.eq.true,and(conducted_override.is.null,conducted_at.not.is.null)")`=유효 진행여부 `conducted_override ?? conducted_at`)+`.neq("status","취소")` 조회(전용 select=`teacher_id`·`conducted_override` 포함) → 강사 **현재 소속 센터**(`profiles.center_id`→`centers` `manager_name`(표시용); **단가는 `rate_schedules` 이력에서 session_date 기준 역산**) 산정 + `settings` 외화 환율(`ratesFromSettings`→`Rates`, PHP·USD) → enriched row(`{teacherId,teacherName,centerId,centerName,centerManager,course,courseTitle,sessionDate,startMin,studentName,studentEnglishName,isMakeup,pricePerSession,currency}` — `start_min`·학생 스냅샷은 상세 세션 표기용, 정산 전용 select)로 `<SettlementsManager>`(client) 전달(**row 조립은 공유 로더 `loadSettlementRows`**(`src/lib/settlements.ts`) 위임 — admin은 필터 없이 전체, 센터 정산은 teacherIds 스코프. `SettlementsManager` props로 재사용성 노출: `initialGrouping`·`groupings`(단일=토글 숨김)·`enableTeacherDetail`(강사 행→`TeacherSettlementModal` 강사 상세)·`showKrwEquivalent`(false=외화 `≈₩` 생략·합계 통화별); admin은 기본값(3분류·센터 드릴인·`≈₩`))). **센터별 분류 행엔 센터명 아래 "매니저: {이름|미지정}" 보조 표시**(`Group.manager`, 센터별 모드에서만 세팅/노출; 강사별·과정별은 미노출). **센터별 행 클릭→`SettlementDetailModal`**(같은 파일, 커스텀 오버레이 `z-[110]`/패널 `z-[120]`·Esc·scroll lock): 그 센터 소속 **강사별 상세 정산**(강사·진행 수업·지급 예정액+합계) — **모달 자체 주간/월간 토글 + 이전/다음/오늘**(전체 `rows` prop을 자체 기간으로 필터, 메인 화면 기간과 독립). 집계는 메인·모달 공용 **`aggregate(rows,keyOf,rates)`** 헬퍼(수업 수·통화별 합·원화 합·단가 미설정 수). 분류를 강사별/과정별로 바꾸면 모달 닫힘. **모달 강사 행 클릭→같은 모달이 그 강사 상세 뷰로 전환**(`selectedTeacher` state, 상단 "← 뒤로"): **과정별 그룹(소계=`AmountCell`)+그 아래 날짜별 세션**(`fmtSessionDate`=`M/D (요일)`+**시작시각 `fmtTime(startMin)`**·`isMakeup`이면 "·보강"·**수강생명(`studentName (studentEnglishName)`)**·단건 금액 `SessionAmount`)+하단 강사 합계. 기간(주간/월간·이전/다음)은 목록/상세 두 뷰 공유, 상세는 `rows`를 센터·강사·기간으로 필터해 재구성(서버 무변경). 지급 대상=**conducted(강사 입장+피드백)**, 학생 출석 무관·취소 제외·보강 포함. **단가=`rate_schedules` 적용일 이력을 수업 날짜(session_date) 기준 역산**(`src/lib/rates.ts` `effectiveRate`: 강사 개별(scope=teacher, price!=null) > 소속 센터(scope=center); teacher `price=null` 행=그 날짜부터 센터 단가로 복귀. `centers.price_per_session`·`profiles.custom_price_*`는 오늘 기준 현재값 캐시라 정산은 미참조; 이력이 진실 소스. `SettlementRow.isCustomRate`로 강사별·상세 뷰에 "개별 단가" 배지; 외화 PHP·USD는 `krwEquivalent(_, _, rates)`로 `≈₩` 병기·합산). UI: **분류 토글(센터별(기본)/강사별/과정별)** × **기간 토글(일간/주간/월간/**년간**, 주 시작 월요일)** + 이전/다음 이동·"오늘", conducted 수업 수·지급 예정액 집계 테이블(`SortHeader`)+합계 행. 단가 미설정(센터 미지정/단가 없음) 수업은 수업 수엔 포함·금액 제외+경고 배지. 날짜 계산은 클라 인라인 TZ-safe 헬퍼(`makeup.ts`/`booking.ts`는 server-only라 재사용 불가). `classes`엔 center/price 스냅샷 없어 **현재 소속 센터 기준**(강사 센터 변경 시 과거 정산 소급 변동 수용). 마이그레이션 없음(단 `conducted_override` 컬럼 참조). `SettlementRow`에 **`krwPerSession`**(수업 진행일 환율로 사전환산, 로더 계산)·`isTest` 포함. **강사 상세 모달 과정별/날짜별 토글**(`SettlementDetailModal`/`TeacherSettlementModal` → `buildTeacherDetail(mode)`, 기본 **날짜별**): 과정별=과정 카드→날짜순 세션, 날짜별=날짜 카드→시간순(과정명) 세션. **월 정산금액 확정(센터×월, `settlements/page.tsx` 하단 `<MonthlySettlementSection>`, admin 전용)**: 정산은 센터 단위(각 센터가 선 정산받아 소속 강사 지급). 자체 월 네비(기본=이번 달) + 센터별 목록(진행 수업·기본 정산액·상태 칩 미확정(빨강)/확정(연녹)/지급완료(진녹)) → 클릭 시 `<SettlementFinalizeModal>`(센터). 확정 시 **서버 재계산 스냅샷**(`confirmCenterSettlement` `centerId·periodMonth` → `computeCenterMonthlyBase`(그 센터 rows·월 필터)·조정 항목 `fxRateAt`(월말) KRW 스냅샷·`total_krw=base_krw+Σ조정 or override`) → `center_settlements` upsert. 조정 항목=라벨·금액·**항목별 통화**(수수료 등, KRW 환산 합). 상태 2단계 확정→지급완료(`markSettlementPaid` 송금일)·`updateCenterSettlement`·`reopenSettlement`·`unconfirmCenterSettlement`. **마감월(월말<오늘KST)만 확정** — **테스트용 env `ALLOW_EARLY_SETTLEMENT_CONFIRM=true`면 당월도 허용**(서버 가드+UI 완화, page가 읽어 `allowEarly` prop 전달). **conducted 자동판정 보정=admin 수동 override(`classes.conducted_override` boolean, 3-state: null=자동(`conducted_at` 기준)/true=강제 진행됨/false=강제 미진행)**: `ClassesManager` 상세 모달 "진행 여부 수정" 세그먼트(자동/진행됨/미진행, **종료된 수업만**=`displayStatus==="완료" && status!=="취소"`, 취소/대체 읽기전용 여부와 독립)→`adminSetClassConducted(id,override)`(`actions.ts`, `requireAdmin`→취소 아님·**종료 후**(`now≥lessonEnd`) 가드→`conducted_override` update, `/admin/classes`·`/admin/settlements`·`/teacher`·`/mypage` revalidate). **유효 진행여부 `override ?? !!conducted_at`가 정산·admin `conductInfo`·강사 `conductMark`(○/△) 3곳 공통** — override=true면 정산 포함·강사 ○, false면 제외·마커 없음. `conducted_at`(sticky 자동 신호)은 불변, override만 위에 얹음. 각 server page가 **`createAdminClient()`(service_role, RLS 우회)**로 데이터 조회 → client 매니저(`src/components/admin/{EnrollmentsManager,ClassEnrollmentsManager,ClassesManager,MembersManager,TeacherRequestsManager,CentersManager,SettlementsManager,YoutubeManager}`)가 검색·필터·CRUD. 회원은 `admin.auth.admin.listUsers` + `profiles.role` merge. 쓰기는 `src/app/admin/actions.ts`(`"use server"`) — **모든 액션 첫 줄 `requireAdmin()`(세션 클라로 isAdmin 재확인, **반환=admin user id∥null**=감사 로그 actor_id용, `requireTeacher`와 동일 규약; `if (!(await requireAdmin()))` 불리언 문맥 불변) 후 service_role 쓰기**. 유튜브 `add/update/setVisibility/delete`(`revalidatePath("/admin/youtube")`+`"/"`로 랜딩 갱신). **회원 role 직접전환은 폐지** — `MembersManager`는 **role 읽기 전용 배지**(`ROLE_BADGE`에 admin/teacher/**friender**(`bg-cta/10 text-cta`)/**friender_plus**(`bg-cta text-white`)/student, 미등재 role은 student 스타일 폴백; 표시는 role 원문이나 `ROLE_LABEL` 맵으로 `friender_plus`→"friender plus") + **이메일 미인증 시 "미인증" 칩**(`page`가 `listUsers`의 `email_confirmed_at`→`email_confirmed:boolean`로 파생 전달, 미인증 행만 `bg-brand/10 text-brand` 칩)만 표시. **role 변경은 강사·프렌더 신청 flow로만**(프렌더는 `docs/friender.md` 참조): `approveTeacherApplication`(**Postgres RPC `approve_teacher_application(p_app_id)`(security definer) 호출로 원자 처리** — 상태가 '신청'일 때만, role=teacher(`profiles.role`+`auth.users.app_metadata` 동기)+신청서 내용으로 profiles 채움(필수 first/last/bio 항상, 선택 experience·phone·zoom_url·avatar_url·nationality·gender·center_id은 `coalesce(신청서,기존)`)+status '승인'을 **단일 트랜잭션**으로. RPC 반환코드 `ok`/`not_found`/`not_pending`(재처리 차단=프로필 덮어쓰기 사고 방지)/`is_admin` 매핑. 성공 시 **지원자에 승인 알림 메일** best-effort, `revalidatePath("/teacher","layout")`)·`rejectTeacherApplication(id,note)`(**상태 가드: '신청'만**, `.eq("status","신청").select()` 0행=이미 처리, 성공 시 **지원자에 거절 알림 메일**(사유 포함) best-effort)·**`deleteTeacher(uid)`**(회수(→student)는 orphan/불일치 문제로 **폐지** → **계정 전체 삭제**: admin 보호 후 `auth.admin.deleteUser`→FK cascade로 profiles·teacher_applications·teacher_availability·reading_progress 제거, 아바타 Storage 파일은 cascade 비대상이라 `storage.from("avatars").list/remove`로 명시 정리, best-effort). `teacher-requests/page.tsx`는 `teacher_applications`(신청 우선 정렬)+`listUsers` 이메일 보강+현재 강사(`profiles.role='teacher'`, **프로필 필드 first/last_name·avatar_url·zoom_url·bio·experience·phone·nationality·gender·center_id 함께 select**(center_id는 `centers` 조회한 `centerNameById`로 이름 resolve해 `centerName` 전달) + 해당 강사들 `teacher_availability` 일괄 조회·`{day,min}[]` 그룹핑) 조회→`TeacherRequestsManager`(지원 목록=상태 탭·확장 카드, **승인/거절 버튼은 '신청' 행만**(처리완료는 read-only)·거절 사유 필수·승인 시 "현재 강사" 낙관적 추가(슬롯=[])·**toast**(alert 아님). **"현재 강사" 목록은 `<CurrentTeacherTable>`**(`src/components/admin/`, client, 정렬 가능 테이블 — 컬럼 이름·센터·국적·성별, `SortHeader` 토글 asc/desc, 빈 값 후순위·`localeCompare("ko")`, 정렬값은 원본 raw 기준이라 국기 이모지·라벨 미영향) — 행마다 **수업 보기**(→`<TeacherClassesModal>`, "수업 목록" 열)·**정보 보기**(→`<TeacherInfoModal>`)·**강사 삭제**(비가역 경고 AlertDialog, 삭제 시 강사·지원행 낙관적 제거)). **강사별 진행 중인 수업(과정) 열람 `<TeacherClassesModal>`**(`src/components/admin/`, client, `TeacherInfoModal` 스켈레톤 복사=오버레이 `z-[110]`/패널 `z-[120]`·Esc·scroll lock·닫기 포커스, `teacher:CurrentTeacher|null` null이면 미렌더): 헤더=강사명+**"진행 중인 수업" 배지**(accent-blue), 본문=`teacher.classes` 과정 카드 목록(과정명·학생명(`한글명 (영문명)`)·상태 배지(승인/결제 대기/결제 완료)·**주간 일정(`summarizeSlots(_,true," / ")` 요일 `/` 구분)·수업 일정(`scheduleDateRange` 시작~종료)·수업 횟수(`TeacherClassItem.totalSessions`=enrollment `total_sessions ?? 24`)**·**결제완료**면 `done/total회 · 다음 {formatDateKo(nextDate)}`(nextDate 없으면 "예정 수업 없음")·**승인/결제대기**(아직 classes 없음)면 `시작 예정 {startDate}`), 비면 "진행 중인 수업이 없습니다."(단 대체 회차만 있으면 아래 섹션만 노출). + **하단 「대체 수업」 섹션(회차 단위)**: 1회성 강사 대체로 얽힌 **앞으로의** 회차(`lessonEndMin` 기준 미종료·취소 제외)를 `대타`(covering=`classes.teacher_id`=본인·`original_teacher_id`≠본인, cta 배지)/`대체됨`(away=`original_teacher_id`=본인·현재 담당은 대타, 회색·dim=read-only)로 표시(날짜·시간 `fmtTime(start)~fmtTime(lessonEndMin(end))`·과정·학생·상대 강사·보강 칩). 파생=**공유 `buildCoverSessions`**(`teacher-directory.ts`, `classroom.ts` `coveringForOther`/`reassignedAway`와 동일 규칙). **`CurrentTeacher.classes`(`TeacherClassItem[]`)는 page가 조립**: `teacher-requests/page.tsx`가 이미 로드하는 enrollment(`.in("status",["승인","결제대기","결제완료"])`−`loadEndedEnrollmentIds` 종료분 제외, `bookedSlots`와 동일 `rowsByTeacher` 재사용, select에 `course/course_title/start_date` 추가) + **classes 집계 쿼리**(`.in("teacher_id",teacherIds)`, enrollment_id별 `total`(취소·보강 제외 회차)·`done`(`kstDateMinToMs(session_date,lessonEndMin(end_min))<now`)·`nextDate`/`nextMin`(미종료 중 최이른))로 강사별 `TeacherClassItem[]`(nextDate→startDate 정렬) 파생. 범위=승인+결제대기+결제완료(미종료)만(신청·거절·취소·종료분 제외). 진행 판정은 서버 렌더 `Date.now()` 스냅샷(라이브 틱 없음). **"현재 강사" 섹션 위에 `<TeacherAvailabilityFinder>`**(`src/components/admin/`, client, props `teachers`+`onView=setInfoTarget`): 원하는 요일(다중 토글)+**수업 횟수(1~8회, 1회=30분)**+시작 시각(`<select>`, 06:00~24:00 30분)으로 **종료=시작+횟수×30분 자동 계산**(읽기전용 표시, 종료≤24:00 되게 시작 옵션 클램프) → **해당 범위 전체가 비어 있는 강사** 필터(이미 로드된 `teacher.slots` 클라 필터, 추가 쿼리 0 — 매칭=**선택 모든 요일 AND** × **범위 내 모든 30분 슬롯 존재**). 결과 행 "정보 보기"는 동일 `setInfoTarget`로 `<TeacherInfoModal>` 재사용. **`<TeacherInfoModal>`**(`src/components/admin/`, client): base-nova `AlertDialog`는 폭이 `sm:max-w-sm`로 고정돼 넓은 콘텐츠에 부적합 → **`AvailabilityModal` 식 커스텀 패널**(오버레이 `z-[110]`+패널 `z-[120]` `w-[min(92vw,720px)]` `max-h-[90vh]` flex-col, 본문만 `overflow-auto`, a11y: Esc·body scroll lock·닫기 버튼 포커스·오버레이 클릭 닫기, 하단 고정 "닫기" 버튼). 내용: **프로필 사진**(avatar_url 있으면 80px `next/image`, 없으면 이름/이메일 **이니셜 폴백** 박스)+이름·이메일 → `dl`(이메일·전화·국적·성별·**센터(편집: `<select>`+dirty 시 저장→`updateTeacherCenter`, 낙관 `onCenterUpdated`)·적용 단가(개별 단가 적용일 이력=공유 `<RateHistoryEditor scope="teacher" allowRevert>` — 현재 적용 요약+이력 목록(수정/삭제)+추가 폼(적용일 date+통화+금액, "센터 단가 사용"=price=null 행); 없으면 센터 단가 폴백(`fallbackLabel`=선택 센터 현재 단가); page가 `schedulesByTeacher`+`rates` 전달)**·Bio·경력·Zoom URL) → "주간 가능 시간" `<AvailabilityGrid readOnly>`(가용 셀 `bg-accent-blue/40`) + 헤더 우측 **"PDF로 내보내기"** 버튼(`openTimetablePrint` — 강사 가용을 A4 세로 인쇄창으로 열어 `window.print()`). `CurrentTeacher` 타입에 phone·nationality·gender·centerName·bio·experience·zoomUrl·avatarUrl·slots 포함. **승인 알림/거절 알림 메일은 `src/lib/mailer.ts`의 `sendTeacherApprovalNotification`/`sendTeacherRejectionNotification`(지원자 대상, 링크는 액션에서 `getOrigin` 생성).** **랜딩 유튜브 섹션은 `youtube_videos`(is_visible=true) 조회 → 비면 mock `VIDEOS` fallback.** **강사 시간표 열람**은 강사 관리 `TeacherInfoModal`(현재 강사 행 "정보 보기")의 `<AvailabilityGrid readOnly>`로 제공(별도 admin 페이지 없음).
